# Постановки задач для `/replicate` — все 8 проектов

Готовые формулировки. Копировать целиком, включая блок `## GROWTH MECHANICS CONSTRAINTS` —
именно он заставляет пайплайн заложить рост в требования, а не «вспомнить о нём потом».

> Основание: [`/research/GROWTH-MECHANICS-REQUIREMENTS.md`](../research/GROWTH-MECHANICS-REQUIREMENTS.md)

## Анатомия хорошего запроса

`/replicate` принимает свободный текст и передаёт его в Phase 1 (`sparc-prd-mini`) вместе с блоками
контекста. Пайплайн уже несёт **Architecture Constraints** (Distributed Monolith, Docker Compose,
VPS, MCP) — их дублировать не нужно. А вот growth-блока в нём нет, его подаём мы.

Рабочая постановка состоит из шести частей:

| # | Часть | Что будет без неё |
|---|---|---|
| 1 | **Что копируем** — продукт + одна фраза сути | Пайплайн придумает свой продукт |
| 2 | **MVP-scope** — что входит и **что НЕ входит** | Сгенерирует ТЗ на три месяца |
| 3 | **Основной growth loop** — ровно один | «Виральность вообще», размазанная по фичам |
| 4 | **4 обязательных FR-GROWTH** | Growth останется прозой в PRD и не дойдёт до кода |
| 5 | **Non-negotiable ограничения** | Построим то, что нельзя показывать |
| 6 | **Метрика недели** — название **и целевое число** | Неделя не измерима |
| 7 | **Требование ≥3 вариантов CJM** | Пайплайн выберет путь молча, без развилки |

**Правило пункта 2:** список «что НЕ входит» важнее списка «что входит». Без него Phase 1
раздувает scope, а Phase 2 честно валидирует раздутый scope как валидный.

---

## Универсальный блок — вставлять в КАЖДУЮ постановку

```
## GROWTH MECHANICS CONSTRAINTS (обязательно, наравне с Architecture Constraints)

Главный результат проекта — рост (аудитория + выручка), а не работающий прототип.
Механики роста закладываются в Specification.md как FR, а не описываются прозой в PRD.

Основной growth loop: <ОДИН из: badge | content-driven | incentivized referral | sales | network effect>
Вторичная тактика: <максимум одна, без нового кода>

Обязательный минимум требований — 4 FR в Specification.md, формат FR-GROWTH-NNN:

FR-GROWTH-001  One-click share В МОМЕНТ ПОЛУЧЕНИЯ ЦЕННОСТИ (не на онбординге).
               Момент = <конкретный момент для этого продукта>.
FR-GROWTH-002  Партнёрская атрибуция ДО ПЛАТНОЙ КОНВЕРСИИ, не до регистрации.
               Cookie + промокод как независимый fallback (Safari ITP гасит JS-cookie за 7 дней).
FR-GROWTH-003  Attribution badge на бесплатном тарифе; снятие — платная опция.
               Решается на этапе Specification как часть тарифной архитектуры, не хардкодится потом.
FR-GROWTH-004  Персональные трекируемые коды для партнёров с аудиторией.
               Без кода на партнёра нельзя посчитать CAC по когорте.

Каждый FR-GROWTH обязан иметь три Gherkin-сценария с тегами:
  @FR-GROWTH-NNN @growth @happy-path   — с числовым порогом
  @FR-GROWTH-NNN @growth @edge-case
  @FR-GROWTH-NNN @growth @security     — anti-fraud, ОБЯЗАТЕЛЕН

Метрики, инструментированные с первого релиза: i (invites per user) и conv% — раздельно.
Без них K-factor нельзя посчитать никогда.

НЕ закладывать в план недели: retention D7/D30, CAC payback, любой процент на выборке n < 30.
За неделю это не измеряется — считать их значит обманывать себя.

Anti-fraud минимум в @security-сценариях: self-referral, дубликаты, накрутка.
Любое действие ОТ ИМЕНИ пользователя (инвайт, пост, рассылка) — только по явному отдельному согласию.

Метрика недели: <название> — целевое значение за неделю: <число>
(без числа метрика неизмерима: «больше нуля» — не цель)

ОБЯЗАТЕЛЬНО на первом этапе — не менее 3 вариантов CJM (Customer Journey Map).
Каждый вариант описывается по общему каркасу и ОБЯЗАН быть привязан к growth-цели:

| Поле варианта | Требование |
|---|---|
| Aha Moment | конкретное событие, а не «понял ценность» |
| Entry Hook | какой JTBD-сегмент и какая его боль |
| Onboarding → Core Loop → Paywall | последовательность экранов |
| **Growth loop варианта** | ОДИН из: badge / content-driven / incentivized referral / sales / network effect |
| **Как ложатся FR-GROWTH-001..004** | момент ценности для share-CTA, где живёт badge, где партнёрский код |
| **Микро-тренды** | 2-4 актуальных тренда, на которые опирается вариант, со ссылкой на источник |
| Главный риск | честно, включая «этот вариант не даёт виральности» |

Варианты должны РАЗЛИЧАТЬСЯ по growth loop, а не только по интерфейсу.
Три варианта с одним и тем же loop — это один вариант в трёх обёртках.
Итог первого этапа: сравнительная таблица вариантов + ОСТАНОВКА и ожидание выбора.
```

### Обязательный чекпоинт: выбор CJM делает человек

Пайплайн **не имеет права выбрать вариант сам** и пойти дальше. После показа трёх вариантов
он останавливается и ждёт одно из трёх:

| Ответ | Что происходит |
|---|---|
| «вариант B» | Фиксируется как `{CHOSEN_CJM}`, дальше всё строится на нём |
| «Aha от A, paywall от C, growth loop от B» | Собирается гибрид из блоков; фиксируется как отдельный вариант с обоснованием |
| «выбери наиболее оптимальный CJM сам» | **Только по этой явной команде** пайплайн выбирает сам и обязан written-обосновать выбор |

Молчаливый выбор варианта — дефект прогона. Если в постановке не сказано «выбери сам»,
а пайплайн пошёл дальше без ответа человека — это тот же «тихий пропуск», что и отсутствие
growth-FR: результат выглядит законченным, но развилка, определяющая весь продукт,
была пройдена без решения.

### Отвергнутые варианты остаются в отчётности — обязательно

Выбор одного варианта **не удаляет** остальные. Все три (а при гибриде — четыре, включая
собранный) сохраняются в `docs/CJM_Variants.md` целиком, с полным описанием и growth-привязкой.

| Что сохраняется | Зачем |
|---|---|
| Все варианты в исходном виде | Отвергнутый вариант — не мусор, а протестированная гипотеза |
| Пометка, какой выбран и почему | Через месяц никто не вспомнит логику решения |
| Причина отказа по каждому отвергнутому | Если выбранный loop не даст сигнала, возвращаемся сюда, а не начинаем заново |
| Growth loop каждого варианта | Видно, какие механики роста мы сознательно НЕ стали пробовать |

Удаление отвергнутых вариантов из документации — потеря главного актива первого этапа.
Провалившийся выбранный loop без сохранённых альтернатив означает повтор всего анализа.

**Проверка после первого этапа:**

```bash
grep -c '^## Variant\|^| \*\*Aha Moment\*\*' docs/CJM_Variants.md   # варианты вообще есть
grep -n 'CHOSEN_CJM\|Зафиксированный выбор' docs/*.md                   # выбор записан
grep -c 'Отвергнут\|Не выбран\|Причина отказа' docs/CJM_Variants.md    # альтернативы сохранены
```

---

---

## Готовые постановки по проектам

Восемь готовых текстов — в отдельном файле, чтобы каждый файл оставался под лимитом 500 строк:

➡️ **[`REPLICATE-PROMPTS-PROJECTS.md`](REPLICATE-PROMPTS-PROJECTS.md)**

| # | Проект | Основной loop |
|---|---|---|
| 01 | Senja-клон · отзывы и «Стена любви» | badge |
| 02 | Умный QR для отзывов | sales ⚠️ не виральный |
| 03 | Rewardful-клон · партнёрка | incentivized referral |
| 04 | Cal AI-клон · калории по фото | content-driven |
| 05 | Opus Clip-клон · нарезка клипов | content-driven |
| 06 | Chatbase-клон · RAG-бот | badge |
| 07 | Instantly-клон · cold email | network effect |
| 08 | Interior AI-клон · редизайн комнаты | content-driven |

---

## Как вести себя на чекпоинтах

Пайплайн останавливается после каждой фазы и ждёт «ок». Не проставлять «ок» рефлекторно.

| Чекпоинт | Что проверить перед «ок» |
|---|---|
| **Phase 0** | Discovery про НУЖНЫЙ продукт? Спорные факты помечены? |
| **Phase 1** | Есть FR-GROWTH-001..004 в `Specification.md`? Нет — возврат на доработку, а не «допишем потом». Scope не вышел за «не входит»? |
| **Phase 2** | Вердикт validator'а: 🔴 = возврат на Phase 1. У каждого FR-GROWTH есть `@security`-сценарий? |
| **Phase 3** | Тулкит создан, pre-shipped артефакты не перезаписаны |

**Быстрая проверка после Phase 1** — этим же ловится «тихий пропуск»:

```bash
cd projects/<проект>
grep -c 'FR-GROWTH' docs/Specification.md          # должно быть >= 4
grep -c '@security' docs/test-scenarios.md          # >= число FR-GROWTH
```

Если первая команда вернула 0 — growth-блок не дошёл до требований. Это не повод идти дальше.

---

## Частые ошибки в постановке

| Ошибка | Чем кончается |
|---|---|
| Нет списка «что НЕ входит» | ТЗ на три месяца, честно провалидированное как корректное |
| Два-три growth loop сразу | Ни один не доведён, за неделю нет сигнала |
| «Сделай виральным» без момента ценности | Share-кнопка в футере, которую никто не нажмёт |
| Growth прозой в PRD, а не как FR | Не дойдёт до кода: валидатор скорит FR, а не абзацы |
| Retention/CAC в метриках недели | Провал по метрике, которая за неделю не измеряется |
| «Как у оригинала» вместо своего решения | Часть цифр оригиналов не подтверждена — копируем механику, не числа |
