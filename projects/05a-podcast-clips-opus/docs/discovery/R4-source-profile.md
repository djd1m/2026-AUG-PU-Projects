# R4-source-profile — Phase 0.5 Source Product Profile (Opus Clip)

RUN_ID: 20260923T173212Z-replicate-05a-475b
WORK_UNIT_ID: R4-source-profile

## Что сделано

1. **robots.txt.** `curl -A "p-replicator-path-capture/1 (+recon; robots-respecting)" https://www.opus.pro/robots.txt`
   → `User-agent: * / Allow: / / Disallow: /*?* / Disallow: /cdn-cgi/ / Disallow: /agent/ads-library/_hub_ /
   Disallow: /agent/ads-library$` — обход публичного сайта РАЗРЕШЁН (кроме query-строк и `/agent/ads-library`,
   которых мы не трогали). Прочитан ДО обхода >1 страницы, как требует правило.

2. **Ось «путь».** Playwright взят с машины (память `playwright-and-tmux-on-this-machine`):
   `PLAYWRIGHT_MODULE=/home/dz-projects-2026/genai-pulse-discovery/node_modules/playwright
   node .claude/hooks/capture-source-path.cjs https://www.opus.pro --max-pages 5 --delay-ms 1500`
   → **код возврата 0**. 5 экранов: `/` → `/pricing` → `/contact` → `/business` → `/api` (обход шёл по
   верхней навигации маркетингового сайта; экран логина не встретился, вглубь приложения `app.opus.pro`
   инструмент не заходил — сайт не предложил перехода за аутентификацию с этих 5 страниц). Строки
   FR-LOOK-001…006 вставлены в таблицу-семя как ЧЕРНОВИК. Доказательства — `.p-replicator/source-path-capture/`
   (capture.json, step-0N.png, step-0N.aria.txt; каталог закрыт своим `.gitignore`, README.txt помечает
   материал как чужой — не коммитится).

3. **Ось «облик».** Тот же прогон посчитал ПОПУТНО вычисленные стили и семантические роли для КАЖДОГО из
   5 экранов (`capture.json` → `steps[i].regularities`), в том числе для лендинга и `/pricing`, которые нужны
   по постановке. Отдельного вызова `clone-website` не делал — пакет `@dzhechkov/skills-website-cloner` в
   этом проекте не установлен, и постановка НЕ требовала его устанавливать; собственный Playwright-инструмент
   `capture-source-path.cjs` уже снимает закономерности (шаг сетки, шкала кеглей, начертания, радиусы,
   брейкпоинты, CSS-переменные, число CTA/полей) БЕЗ значений для копирования и без имён классов/переменных.
   Дополнил роли ARIA числом кнопок/ссылок/таблиц/слайдеров через `grep` по `step-0N.aria.txt` (роли: nav,
   main, form, table, row/rowheader/columnheader, slider, spinbutton, switch, combobox — на `/pricing`
   обнаружена таблица сравнения планов + калькулятор объёма; на `/` — блок из 16 `blockquote`-отзывов).
   Выпущено 7 строк FR-LOOK-007…013, ось «облик», статус ЧЕРНОВИК, уверенность 5/5 (прямое измерение).

4. **Сторонний разбор приложения (`app.opus.pro`).** Приложение за входом, аутентификация запрещена
   правилом — WebSearch + WebFetch трёх датированных независимых обзоров:
   - ScaleReach (опубл. 2026-05-01, обновлена 2026-07-27) — путь загрузка→клипы описан только формой,
     точных шагов интерфейса не называет; честно записано как НЕ ИЗМЕРЕНО в прозе, не выдумано числом.
   - help.opus.pro (первичный источник, не сторонний — использован для сверки, не как строка-гипотеза):
     Virality Score 0–99, сортировка по убыванию, 4 фактора (Hook/Flow/Value/Trend).
   - BIGVU (опубл. 2026-04-12) и Marc Andrews (опубл. 2026-04-25, обновлена «сентябрь 2026») — ОБА
     подтверждают вшитый водяной знак на бесплатном экспорте (совпадение → рост уверенности), но
     РАСХОДЯТСЯ по кредитам (60 vs 90), сроку жизни проекта (3 дня vs ~7 дней) и видимости Virality Score
     на free (недоступна vs доступна). Расхождение оставлено НЕРАЗРЕШЁННЫМ и записано явно с обеими датами
     — живой прогон для разрешения запрещён (создание аккаунта = аутентификация).
   Этот материал НЕ оформлен строками `FR-LOOK-nnn`: шапка профиля `Происхождение: прокликано` — общая
   для всей таблицы-семени, а `check-look-origin.cjs` требует ОДНОГО значения заголовка на весь набор строк
   (смешать `прокликано`/ЧЕРНОВИК и `сторонний-разбор`/ГИПОТЕЗА в одной таблице-семени он не пропускает).
   Записан отдельным прозаическим разделом «Что говорят о приложении» — честно, но НЕ как обязательство
   с номером; если Фаза 1 решит его промотировать, это отдельное решение с отдельной таблицей.

5. **Файл `docs/source-product-profile.md`** написан по формату `replicate.md`/`replicate-pipeline.md`:
   шапка (Источник/Статус съёмки/Причина/ось «путь»/Происхождение), область доказательств, измеренные
   закономерности (таблица landing vs pricing), раздел о приложении (ГИПОТЕЗА, вне таблицы-семени),
   таблица `🎨 Look Requirements Seed` (13 строк: 001–006 путь, 007–013 облик), ворота фазы.

6. **Ворота.** `node .claude/hooks/check-look-trace.cjs projects/05a-podcast-clips-opus`:
   ```
   ⚠️  проверка НЕ выполнена: нет файла docs/Specification.md
       без спецификации не с чем сверять — это не «всё прослежено»
   ===EXIT=2===
   ```
   Ожидаемый результат до Фазы 1 (профиль написан и парсится — таблица-семя прошла разбор без ошибок,
   упёрлось именно в отсутствие `Specification.md`, а не в дефект профиля). Код `2` не означает
   «всё в порядке» — только что Фаза 1 ещё не промотировала строки.

## Ограничения (честно, не скрыто)

- Инструмент `capture-source-path.cjs` следовал только верхней навигации маркетингового сайта; путь
  внутрь продукта (регистрация → загрузка видео → монтаж → экспорт) браузером НЕ снят — за входом.
- Число шагов онбординга приложения НЕ ИЗМЕРЕНО ни браузером, ни сторонним разбором (источники не
  детализируют).
- Три факта бесплатного тарифа (кредиты/срок жизни/видимость Virality Score) остаются противоречивыми
  между двумя независимыми обзорами и не разрешены — записано как открытое расхождение, а не выбрано
  произвольно по свежести даты.
- `clone-website` (пакет `@dzhechkov/skills-website-cloner`) не вызывался — не установлен в проекте;
  вместо него использован входящий в p-replicator инструмент `capture-source-path.cjs`, который и
  выполнил всю съёмку (путь + попутные вычисленные стили). Это не `НЕ ИЗМЕРЕНО (no-browser-mcp)`:
  облик СНЯТ другим легитимным инструментом того же пакета.

Status: completed
