# Квитанция — refinement-completion

RUN_ID=20260923T173212Z-replicate-05a-475b · WORK_UNIT_ID=refinement-completion

## Файлы (владение по dispatch-plan.md)

- projects/05a-podcast-clips-opus/docs/Refinement.md — создан
- projects/05a-podcast-clips-opus/docs/Completion.md — создан
- projects/05a-podcast-clips-opus/docs/long-job-contract.md — создан
- projects/05a-podcast-clips-opus/docs/model-cost-contract.md — создан
- projects/05a-podcast-clips-opus/docs/webhook-contract.md — создан
- projects/05a-podcast-clips-opus/docs/embed-contract.md — создан

Ничего кроме перечисленного не редактировалось. Pseudocode.md/Architecture.md не читались.
projects/05-podcast-clips-opus не читался.

## Прогон четырёх ворот (из корня репозитория, 2026-09-23)

```
$ node .claude/hooks/check-job-contract.cjs projects/05a-podcast-clips-opus
✅ все 3 состояния различимы и проверены по идентификатору `job_id`
   ручка: выдаётся ДО начала работы · повтор продолжает: запись-в-хранилище · потолок 150 мин против окна 60 с
exit=0

$ node .claude/hooks/check-model-cost.cjs projects/05a-podcast-clips-opus
✅ 2 вызов(ов) названы, у каждого предел на пользователя и в сутки — числа, достижение предела отказывает
   Из них запускаются посторонними: 2 (из них без входа: 0)
exit=0

$ node .claude/hooks/check-webhook-contract.cjs projects/05a-podcast-clips-opus
⚠️  проверка НЕ выполнена: контракт говорит «Входящие вебхуки: нет» — чужие системы в продукт не звонят
    это законный ответ, а не нарушение; проверять нечего, поэтому не 0 и не 1
exit=2   ← ЗАКОННЫЙ ответ: Specification.md §1 объявляет «вебхуков нет» (fake-door на неделе).
          v1-план (интерфейс PaymentProvider, дедупликация payment_event, HMAC/IP-allowlist,
          порядок по версии события) описан в самом контракте, раздел «v1 (будущее)».

$ node .claude/hooks/check-embed-contract.cjs projects/05a-podcast-clips-opus
⚠️  проверка НЕ выполнена: контракт говорит «Встраиваемый виджет: нет» — продукт не грузится на чужие страницы
    это законный ответ, а не нарушение; проверять нечего, поэтому не 0 и не 1
exit=2   ← ЗАКОННЫЙ ответ: Specification.md §1 объявляет «не встраивается».
```

Все четыре кода осмысленны: `0`/`0`/`2`/`2`, оба `2` — задокументированные легитимные «нет» по
самому тексту проверок (exit-код 2 явно перечисляет «входящих вебхуков нет» /
«продукт не встраивается» как один из своих двух законных случаев, не как ошибку).

## Промежуточное падение и исправление (для прозрачности)

Первый прогон `check-job-contract.cjs` дал `exit=1`: в таблице состояний строка «выполняется»
содержала неэкранированный `|` внутри перечисления `transcribing \| selecting \| rendering` —
наивный построчный парсер ворот делит markdown-таблицу по `|` без учёта экранирования, поэтому
`\|` сдвинул все последующие столбцы, и колонка «Доказательство» перестала содержать `job_id`.
Починено заменой на `` `transcribing`/`selecting`/`rendering` `` без символа `|`. Второй прогон —
`exit=0`.

## Вопросы к автору Specification / канону

1. **Расхождение имени таблицы.** `Specification.md` FR-clips-10 п.6 называет таблицу расхода
   `usage_attempt`; `canon.md` §4 называет `spend_ledger` (попытки) и `quota_counter` (резерв по
   суткам/скоупу). По правилу канона (§12 — «при расхождении действует канон») контракт
   `model-cost-contract.md` использует имена канона и явно фиксирует это расхождение отдельным
   разделом («Расхождение со Specification, зафиксированное здесь») — передаю автору Specification
   для правки текста FR-clips-10 п.6 либо для добавления строки в canon §12.
2. **Числовые допущения, не зафиксированные ни в Specification, ни в каноне**, введены в
   `long-job-contract.md` как явно обоснованный расчёт (не решение владельца): «Предельное время
   задачи» = 150 мин (расчёт худшего случая по частям конвейера + запас) и «Таймаут посредника» =
   60 с (консервативная оценка отсутствия явного `reverse_proxy` таймаута у Caddy). Оба числа не
   меняют поведение кода (порог не проверяется кодом, только декларацией ворот), но если у
   владельца/архитектора есть более точное число — заменить в файле напрямую, координатора не
   уведомлять отдельно.
3. **`Продолжение при повторе`** в `long-job-contract.md` заполнено значением
   `запись-в-хранилище` (а не `идемпотентный-ключ`), потому что поле ворот однозначное, а в
   Specification задействованы ОБА механизма на разных стадиях (создание vs повтор после отказа).
   Обоснование выбора — в самом файле, раздел «Как продолжение переживает три отказа». Если
   Pseudocode/Architecture выберут другую формулировку механизма — сверить с этим разделом, а не
   менять только заголовок.

Status: completed

## Правка после амендмента канона (координатор, 2026-09-23, после первой сдачи)

Перечитал `docs/canon.md` после уведомления координатора (§4/§5 `account.role`, §7 `/admin/*` +
роль `operator`, §9 — 17 событий, §12 новые строки). Свёл шесть файлов с обновлённым каноном:

- `usage_attempt` → `spend_ledger`/`quota_counter`: уже было закрыто в `model-cost-contract.md`
  разделом «Расхождение со Specification — закрыто» на момент правки — без изменений.
- `landing_visited(source=direct)`, `email_verified`, перекрытие кусков 5 с, «нет speaker →
  субтитры без подписи, задача продолжается», отдельный `job_id` на `POST …/complete` (202) — во
  всех шести файлах уже были согласованы с этими значениями ДО амендмента (взяты напрямую из
  ADR.md, который канон и подтвердил), правок не потребовалось.
- **Реальная правка:** `ops spend today` как CLI-адрес расхода — снят из
  `model-cost-contract.md` (`**Где виден расход:**`) и `Completion.md` (§3 «Наблюдаемость», §7
  «Operations») и заменён на единственный адрес `/admin/spend` (роль `operator`); добавлено явное
  примечание, что CLI `ops` канон сузил до `ops grant-operator <email>`.
- **Мелкая правка для полноты:** `long-job-contract.md` — в список детерминированных `jobId`
  BullMQ добавлен `{job_id}:stt:prepare` (новая строка canon §3, подготовительный этап до
  нарезки на куски).

Все четыре ворот перепрогнаны после правки — коды не изменились: `check-job-contract.cjs` → 0,
`check-model-cost.cjs` → 0, `check-webhook-contract.cjs` → 2 (законно), `check-embed-contract.cjs`
→ 2 (законно).

Status: completed

## Итерация исправлений 1 (validation-report.md, val-trace.md — VT-17, VT-23; + обязательные тесты)

Прочитан `docs/validation-report.md` и `docs/validation/val-trace.md` (+ выборочно `val-adversarial.md`
для точных формулировок VA-01/VA-07/VA-12, на которые ссылается сводка). Правки — только в
файлах владения (`Refinement.md`, `Completion.md`, `long-job-contract.md`; `model-cost-contract.md`
не менялся, `webhook-contract.md`/`embed-contract.md` не затронуты этой итерацией).

### VT-23 — `long-job-contract.md`, «Предельное время задачи»

- Переименована рамка: было «худший случай 150 мин», стало «оценка типичной длительности, НЕ
  гарантированный потолок» — с явной оговоркой, что очередь рендера общая на всех авторов и
  ожидание в ней не ограничено этим контрактом (срок жизни задачи как принудительный TTL **не
  введён** — по прямой рекомендации отчёта).
- Пересчёт с реальными таймаутами: подготовка ~10 мин `[НЕ ИЗМЕРЕНО]` + STT 40 партий × (90 с ×
  3 попытки BullMQ) = 180 мин + LLM 120 с × 3 попытки = 6 мин + рендер (без чужой очереди) 50 мин +
  аренда/переустановка до 15 мин = 261 мин, округлено до **270 мин**.
- Маршрут `GET /api/clips?job_id=` (не в каноне §7) заменён на явное указание, что список клипов
  встроен в `JobView` того же `GET /api/jobs/{job_id}`.
- Ссылка на «AC-clips-8 (0 клипов)» заменена на `Refinement.md` EC-6 — AC-clips-8 про запрет
  подставных моментов, не про пустой список.
- `jobId` BullMQ переведён с разделителя `:` на `.` по канону (правка VT-01, `bullmq@5.81.5
  job.js:1075`): `{job_id}.stt.prepare`, `{job_id}.stt.{chunk_idx}`, `{job_id}.llm`,
  `{clip_id}.render`.

### VT-17 — числа и имена не из канона

- `Refinement.md` §2.1: длительность фрагмента `15–90 с` → `20–90 с` (канон и Specification).
- `Refinement.md` EC-6, §2.2: `moments: []` → `fragments: []` (имя поля по схеме).
- `Refinement.md` §2.3: cookie `first_ref` → `pref` (canon §7, TTL 60 дней ADR-015 п.3).
- `Refinement.md` EC-4 и §2.4: конкурентный тест перенаправлен с `complete` (потолок не резервирует)
  на `POST /api/videos` (лимит 3 загрузок/сутки) и отдельно на допуск STT (резерв секунд) — два
  разных теста вместо одного неверно нацеленного.
- `Completion.md` §5 (проба STT дня 1): список кандидатов расширен с 3 до всех 5 из ADR-001
  (добавлены `meta/muse-voice-transcribe-1.0`, `x-ai/grok-stt-1.0`).
- Мимоходом (правка предыдущего амендмента канона, не отдельная находка): `Completion.md` §2.3
  дополнен упоминанием нового `GET /api/health` (canon §7) как healthcheck-маршрута `web`.

### Добавленные обязательные тесты (`Refinement.md` §2.4, пункты 5–6)

- **VA-01** (был единственным blocker'ом отчёта): после отказа `failed/quota_user`/`quota_global`
  повтор в те же сутки должен давать **0 новых вызовов STT** и не растить `spend_ledger` — допуск
  проверяется заново перед постановкой оставшихся кусков, а не считается «оплаченным» без факта
  допуска. Добавлен мутационный страж в §2.5: убрать повторную проверку допуска → тест обязан
  покраснеть.
- **Браузерный E2E загрузки с ETag** (VA-07/VT-05): реальный `fetch`/`XHR` из браузера, не `curl`;
  проверяет, что `ExposeHeaders: ETag` в CORS бакета действительно даёт скрипту доступ к заголовку
  — без него `CompleteMultipartUpload` получает пустые ETag и не завершает ни одну загрузку.
  Добавлена отдельной строкой в таблицу E2E §2.3.
- **Перебор паролей** (VT-07/VA-12/VS-03): 50 параллельных неверных попыток на один email → не
  больше 10 дошло до `argon2.verify`, атомарный `INCR` до сравнения пароля, а не «прочитать-потом-
  инкремент».
- Конкурентный резерв — уже существовавший тест (было EC-4/§2.4.1), передвинут и переориентирован
  на канон-корректные маршруты (см. VT-17 выше), не добавлен новым, а исправлен.

### Не трогал (по прямому указанию команды)

VA-03/VT-03 (потолок LLM на задачу — деньги, решает владелец), VA-05 (место знака — решает
владелец), VA-09/VT-... (оценка объёма недели в `Completion.md` §1 DoD — ждёт решения владельца
про объём; не переписывал, что и было явно запрещено). VT-02 (оспорил алгоритм допуска STT против
EC-2) не мой файл — это расхождение чинят Pseudocode/Specification/ADR-006, приводя алгоритм к
уже верному EC-2 («ровно 120 мин → Принято» отражает намерение OWN-05A-007), а не наоборот.

### Прогон ворот после правок

```
check-job-contract.cjs   → 0
check-model-cost.cjs     → 0
check-webhook-contract.cjs → 2 (законно, без изменений в этой итерации)
check-embed-contract.cjs   → 2 (законно, без изменений в этой итерации)
```

Status: completed

## Дополнение к итерации 1 — решения владельца OWN-05A-012…014

Перечитан `docs/canon.md` (новый хеш) и `docs/decisions-owner.md` (OWN-05A-012…014), а также
соответствующие разделы `ADR.md` (ADR-006, ADR-009, ADR-010, ADR-013) и `Specification.md`,
которые их уже отразили.

### OWN-05A-012 — потолки LLM

- `model-cost-contract.md`: `LIMIT_LLM_USER_KOP_DAY` в таблице ворот 3000 → **5000**;
  предел на задачу в прозе 1000 → **2400** коп. (2 попытки **в сутки**, не «2 попытки» без
  единицы). Добавлен явный числовой вывод: 3 загрузки × 24 ₽ = 72 ₽ > 50 ₽/сутки — третья
  загрузка может честно отказать на допуске, это ожидаемо, а не дефект.
- Добавлен раздел «Порядок проверки — дешёвая до дорогой»: выполнимость резерва LLM проверяется
  на допуске STT, ДО оплаты транскрипции (закрывает VA-03 = VT-03 = В-10, единственный вторым
  раундом ещё не закрытый high/blocker-класса риск из моих файлов).
- `Refinement.md` §2.4 пункт 7 (новый обязательный тест: 120-мин выпуск с достаточным бюджетом →
  проходит; выпуск с недостаточным бюджетом → отказ ДО STT, `spend_ledger` без строки `stt`).
  §2.5 — мутационный страж на эту проверку. §3 — риск закрыт.

### OWN-05A-013 — место знака

- `Refinement.md` §2.4 пункт 8 (новый обязательный тест: знак переживает `crop=1080:608:0:420` —
  снятие чёрных полей — и остаётся читаемым OCR). §2.5 — мутационный страж. §3 — риск закрыт
  (был VA-05).
- `Completion.md` DoD п.8 дополнен явной отсылкой к этому тесту.

### OWN-05A-014 — объём первой недели

- `Completion.md` §1 разделён на **1.1 DoD первой недели (ядро)** и **1.2 DoD второй очереди**
  (сброс пароля, `/admin/partners`, `/admin/spend`, fake-door — не блокируют приёмку ядра).
- Добавлен **§1.3 Честная оценка по дням**: 10 дней сборки ядра (день 0 — инфраструктура/домен/
  DNS/Resend/Cloud.ru, дни 1–9 — конвейер, тесты, замер, развёртывание) + отдельно 7 календарных
  дней беты для метрики недели (не сокращаются параллельной работой). Итого ≈ 17 дней от старта
  до числа метрики — применение вывода VA-09 к уменьшенному (ядро-только) объёму, а не к полному
  16–20-дневному.
- **Возврат адреса расхода к CLI.** Важное следствие: OWN-05A-014 переносит `/admin/spend` и
  `/admin/partners` во вторую очередь, а первая неделя пользуется CLI `ops spend-today` /
  `ops partner-add` (canon §7). Это ОТМЕНЯЕТ мою правку из предыдущего раунда («Где виден
  расход» → только веб-страница `/admin/spend`) — тогда это было верно по канону на тот момент,
  сейчас канон снова изменился, и я вернул `model-cost-contract.md`, `Completion.md` §3 и §7
  (Operations) к `ops spend-today` для первой недели, явно назвав дату перехода на веб-страницу.
  Это не колебание с моей стороны — оба раунда правок были верны каждый для своей версии канона;
  фиксирую здесь, чтобы это было видно, а не выглядело недосмотром.
- `Completion.md` §7 (Operations), §8 (что переносится дальше — теперь два разных горизонта:
  решённая 2-я очередь и открытый v1) обновлены соответственно.

### Прогон ворот

```
check-job-contract.cjs     → 0
check-model-cost.cjs       → 0  (LLM-предел 5000/2400 вместо 3000/1000 — гейт всё ещё проходит,
                                  персональный ≤ суточный: 5000 ≤ 30000)
check-webhook-contract.cjs → 2 (законно, без изменений)
check-embed-contract.cjs   → 2 (законно, без изменений)
```

Status: completed

## Решения владельца 012–014 (подтверждение, повторный запрос координатора)

Координатор перепроверил grep'ом и не нашёл OWN-05A-012…014 в файлах — на момент того grep правки
уже были внесены предыдущим сообщением («Дополнение к итерации 1 — решения владельца
OWN-05A-012…014» выше в этом же файле); видимо, гонка по времени между записью и проверкой.
Подтверждаю точным grep на текущий момент:

```
$ grep -n "OWN-05A-012\|OWN-05A-013\|OWN-05A-014" docs/model-cost-contract.md docs/Refinement.md docs/Completion.md | wc -l
24
```

Дополнительно усилен тест §2.4.7 по точной формулировке координатора: добавлен ТРЕТИЙ прогон
«120-мин выпуск проходит с одним повтором LLM» — резерв на допуске оценивается по ХУДШЕМУ случаю
(обе попытки `LIMIT_LLM_ATTEMPTS_JOB=2`, не одна), поэтому видео с достаточным остатком проходит
даже если первая попытка LLM вернула невалидную схему и потребовался повтор. Мутационная строка
§2.5 переномерована (третий прогон, не второй). Координаты знака `x=70, y=444` для 16:9 — уже
были точно названы в §2.4.8 предыдущим сообщением, подтверждаю совпадение с запросом координатора.

Прогон ворот не изменился: `check-job-contract.cjs` → 0, `check-model-cost.cjs` → 0,
`check-webhook-contract.cjs` → 2 (законно), `check-embed-contract.cjs` → 2 (законно).

Status: completed
