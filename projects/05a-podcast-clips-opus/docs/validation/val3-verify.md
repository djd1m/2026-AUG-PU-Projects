# Точечная проверка итерации 3 — val3-verify

**RUN_ID:** 20260923T173212Z-replicate-05a-475b · **WORK_UNIT_ID:** val3-verify · **Ревизия:** `d424ea3`
(рабочее дерево `projects/05a-podcast-clips-opus/docs` чистое на момент чтения, `git status --short .` пуст).
**Проверяющий:** Claude Opus 5.5 (Anthropic), установка скептическая; чужие документы не правились.
`projects/05-podcast-clips-opus/` не читался.

**Ворота слоя 1, прогнанные для опоры:** `check-model-cost.cjs` → 0, `check-job-contract.cjs` → 0,
`check-look-trace.cjs` → 0. Это проверка ДЕКЛАРАЦИЙ, по сути выводов ниже они ничего не доказывают.
Сверка сценариев: `grep "Scenario: SC-"` → 68 в `Specification.md` и 68 в `test-scenarios.md`; `diff` отсортированных
строк `Scenario/Given/When/Then/And` двух файлов — пустой.

## 1. Находки high/blocker итерации 2 и блокер VA-01

| Находка | Статус | Цитата (текущие документы) |
|---|---|---|
| VA-01 (blocker, итер. 1) | **закрыта по сути** | Pseudocode «Транскрипция куска» шаг 1: «**Ворота допуска (VA-01):** IF `c.attempt_count = 0` AND отметки допуска задачи нет THEN `admit_stt(job_id)`; отказ → RETURN»; шаг 2 берёт отметку `FOR SHARE`, без неё возвращается к воротам; «Допуск STT» шаг 5 `release_stt_admission`; «Повтор задачи» шаг 5 полагается на ворота шага 1. Уникальность отметки при любом `day` — частичный индекс `quota_counter_stt_admission` (Data Structures). Мутационный страж в Refinement §2.5 указывает на шаг 1 «Транскрипции куска», не на «Повтор» (VA2-25 учтён) |
| VT2-01 (high) | **закрыта по сути**, остаток — V3-08 | Одно правило в 6 документах: Spec FR-clips-10 (блок формулы), Pseudocode «Допуск STT» 2a, ADR-006 п.3 («оценка больше `LIMIT_LLM_KOP_JOB` (→ `failed/quota_user`) или больше остатка автора… либо остатка сервиса»), model-cost-contract (условия 1–3), Refinement 2.4.7 («одна попытка, не умножается на `LIMIT_LLM_ATTEMPTS_JOB`»), Architecture:257 («оценка LLM по длительности ≤ LIMIT_LLM_KOP_JOB и ≤ остатков»). Формулы «остаток ≥ `LIMIT_LLM_KOP_JOB`» и «2 × est» не найдены нигде (`grep -i "обе попытки\|худш"` — только в тексте «было») |
| VT2-02 (high) = VA2-01 (high) | **закрыта по сути** | Pseudocode «Метрики недели» шаг 3: «`authors_confirmed ← min(accounts, channels)` … выводятся все три числа; `goal_met ← clips_confirmed ≥ 5 AND authors_confirmed ≥ 3`»; второго присваивания `COUNT(DISTINCT account_id)` нет; тест «3 аккаунта × 1 `channel_key` × 5 клипов → `authors_confirmed = 1`, `goal_met = false`» с мутацией. Spec FR-GROWTH-005 п.4 — «по **меньшему** из двух чисел». Остаток — V3-18 (low) |
| VA2-02 (high) | **закрыта по месту знака; частично по проверке** (V3-12) | OWN-05A-015 перенесено: Pseudocode «Раскладка» шаг 3 «Знак по центру, у нижней кромки картинки видео, над субтитрами», `wm_x ← clamp(video_x + (vw − pw) / 2, …)`; Spec FR-GROWTH-003 п.4; ADR-009/010; Refinement 2.4.8 — две обрезки `crop=1080:608:0:420` и `crop=608:608:236:420`. Обрезка «растянуть на весь экран» (`crop=342:608:369:420`, случай (а) самой находки) не тестируется нигде |
| VA2-03 (high) | **закрыта в алгоритме**; фикстура противоречит OWN-05A-015 (V3-01) | Pseudocode «Подготовка» шаг 4: `display_dims(probe)` — SAR и `rotation` из `side_data_list`/`tags.rotate`, при `rot ∈ {90, 270}` поменять `w` и `h`; «Раскладка» INPUT и «Рендер» шаг 5 берут `display_dims`. Но фикстура там же: «MOV 1920×1080 с `rotate=90` → … знак в (70, 200)» |
| VA2-04 (high) | **закрыта по сути**, остатки (V3-11) | Константа `LLM_FIELD_MAX_CHARS` (50/80/80/90/90) с расчётом «10 фрагментов ≈ 2 580 ≤ 3 000»; «Выбор фрагментов» шаг 7: «IF `finish_reason == 'length'` THEN … `finish_failed('selection_failed')` сразу, БЕЗ повтора BullMQ»; стартовая проверка 5a гарантирует две попытки на 120 мин (≈ 1 170 × 2 = 2 340 ≤ 2 400) |
| VA2-13 (medium) | **закрыта решением владельца OWN-05A-016**, остаток (V3-13) | Spec FR-clips-16 п.1–7; Pseudocode «Допуск STT» шаг 3 (`is_newbie`, пул `stt_sec_newbie`, отметка для возврата); canon §6 `LIMIT_STT_NEWBIE_*`; Refinement 2.4.9–2.4.11 и три мутации |
| VA2-24 (medium) | **закрыта** (арифметика — V3-14, low) | Completion §1.3: «**Итого ядро: 13–17 рабочих дней одного агента**»; «20–24 календарных дня»; день 0 с набором беты параллельно; день 8 разнесён |
| VS2-01 (high) | **закрыта** | `test-scenarios.md` шапка: «все 68 сценариев из Specification.md … тексты — дословно»; 68 = 68, `diff` шагов пуст |
| VS2-02 (high) | **закрыта** | `test-scenarios.md:532-535`: «Given оператор завёл 3 партнёров командой ops partner-add … When оператор открывает /admin/metrics»; `/admin/partners` в файле нет |

## 2. Решения владельца OWN-05A-012…016 по документам

| Решение / число | Где совпадает | Где расходится |
|---|---|---|
| OWN-05A-012: 2 400 / 5 000 / 30 000 коп. | canon §6, Spec FR-clips-10 и §10, Pseudocode, ADR-006, model-cost-contract, Refinement, PRD:217/381, Architecture-compose (`:?`) | **canon §12 строка FR-clips-10**: «LLM **30 ₽ на автора**, 300 ₽ на сервис» (V3-06) |
| 9 потолков канона §6 | Spec:857, Pseudocode «Проверка конфигурации» шаг 5, model-cost-contract:194, ADR-006 (9 переменных в 8 строках), Architecture-compose `x-limits` + `web`/`worker-ai` | **Completion §2.4**: «16 наименований + **7 потолков**»; **Final_Summary**: «**7 потолков** числами» (V3-05) |
| OWN-05A-015: центр x = 540, низ 1 004 (16:9/4:3), низ 1 084 (вертикальный) | Spec FR-GROWTH-003 п.4 и SC-US-008-1 (16:9), Pseudocode «Раскладка» шаг 3, Refinement 2.4.8, PRD:389/422, Architecture:371, ADR-009 | **ADR-010 п.2**: формула `min(video_y + vh, 1100) − 24` и «для вертикального … на **y = 1076**» (V3-02); **Pseudocode «Подготовка» шаг 4**: «знак в (**70, 200**)» (V3-01); **C4:130**: «знак по центру … ; знак полупрозрачный **в углу** полосы видео» (V3-03); **Completion §1.1 п.8 и день 5** ссылаются на отменённое OWN-05A-013 (V3-03) |
| OWN-05A-016: 1 800 с, пул 45 000 с, `beta_at`, `ops beta-add` | canon §4/§5/§6/§7, Spec FR-clips-16 и AC-clips-30, Pseudocode, ADR-006, model-cost-contract, Refinement 2.4.9–11, PRD:227/385, Architecture:310 (750 мин = 45 000 с), Completion (день «до старта беты»), test-scenarios SC-US-002-7…10, Architecture-compose `x-limits` | model-cost-contract, раздел «Именование хранилища — не решено»: «Pseudocode и Specification этого решения ещё не отразили» — устарело (V3-09) |
| OWN-05A-014: состав первой недели и второй очереди | PRD «Очереди поставки», Spec (пометки «2-я очередь»), canon §7, ADR:9-10, Architecture:9-10/62/68, Pseudocode (партнёры/расход CLI), test-scenarios `@queue-2` | **Сброс пароля:** OWN-05A-014 — «Во вторую очередь: сброс пароля»; Completion §1.2 п.1 — «В первой неделе (ядро) у забывшего пароль автора нет способа восстановить доступ вообще»; а PRD:398, Spec FR-clips-1 п.5 и SC-US-001-8 (без `@queue-2`), canon §7, Pseudocode «Операции над аккаунтами» шаг 5, ADR:389 — `ops reset-link` **в первой неделе** (V3-04). **Fake-door:** Spec FR-clips-9 п.4 «На неделе … экран плана — fake-door» без пометки «2-я очередь»; Final_Summary «на неделе fake-door» (V3-16, V3-05) |

## 3. Формула выполнимости LLM и правило секунд STT (canon §11)

| Документ | Формула допуска LLM (est_kop против трёх пределов) | Стартовая проверка `llm_reserve_kop(7200 × LLM_EST_CHARS_PER_SEC) × LIMIT_LLM_ATTEMPTS_JOB ≤ LIMIT_LLM_KOP_JOB` | Секунды STT: персональные по записи, глобальные по провайдеру |
|---|---|---|---|
| Specification FR-clips-10 | ✅ дословно (блок кода) | ✅ | ✅ «Аудио в потолках STT» |
| Pseudocode | ✅ «Допуск STT» 2a | ✅ «Проверка конфигурации» 5a | ✅ «Допуск STT» шаг 1; но повтор куска списывает `provider_sec` и с персонального (V3-10) |
| ADR-006 | ✅ по смыслу (проза) | ❌ нет | ✅ п.2 |
| model-cost-contract | ✅ условия 1–3 | ❌ нет | ❌ «STT — **минута аудио, округление куска вверх** … длительность куска» (V3-09) |
| Refinement | ✅ 2.4.7 | ❌ нет ни теста, ни мутации (V3-08) | правила нет (тестов на разницу 7 200 / секунд провайдера нет) |

## 4. Регрессии (выборка из закрытых в итерации 2)

| Находка итерации 1 | Проверено | Итог |
|---|---|---|
| VT-01 `jobId` с `:` | `grep jobId` по Pseudocode/canon/Architecture — двоеточий нет | не сломано |
| VT-06 нормализация ссылок | константа `PLATFORM_KEEP_QUERY`, «Возврат ссылки» шаг 5 | не сломано |
| VA-11 `429` за CGNAT | «Партнёрская ссылка» шаг 1: «Превышение не даёт 429» → `302 /` | не сломано |
| VA-26 экранирование ASS | «Субтитры» шаг 1: `\{`→`｛`, `\}`→`｝` | не сломано |
| VT-12 переменные потолков в compose | `x-limits` содержит новые `LIMIT_STT_NEWBIE_*` для `web` и `worker-ai` | не сломано; но у `worker-ai` нет новой обязательной `LLM_EST_CHARS_PER_SEC` (V3-07) |
| VA-18 отметка допуска | частичный уникальный индекс «при любом `day`» | не сломано |

## 5. Новые находки V3

| ID | Severity | Файл:раздел | Цитата | Как чинить | Чей файл |
|---|---|---|---|---|---|
| V3-01 | medium | Pseudocode «Подготовка задачи», шаг 4 (`display_dims`) | «Фикстура: MOV 1920×1080 с `rotate=90` → выход без боковых полей, кадр 1080×1920, знак в (70, 200).» — координаты угла из отменённого OWN-05A-013 | Заменить на «центр плашки x = 540 ±2, нижний край y = 1 084 ±2» (как «Раскладка» шаг 3). Сейчас исполнитель напишет две несовместимые проверки для одного вертикального клипа, и красную «починит» переносом знака в угол | pseudocode |
| V3-02 | medium | ADR.md ADR-010 п.2 | «`y = min(video_y + vh, 1100) − 24 − ph` … для вертикального исходника … — на y = 1076» против Spec FR-GROWTH-003 п.4 `min(video_y + video_h − 24, 1100 − 16)` = 1 084 | Привести формулу ADR-010 к Spec (1 084); Refinement 2.4.8 ссылается на «Specification FR-GROWTH-003 п.4 / ADR-010 п.2» как на одну формулу | adr |
| V3-04 | medium | Completion §1.2 п.1 против PRD:398, Spec FR-clips-1 п.5, SC-US-001-8, canon §7, Pseudocode «Операции» шаг 5, ADR:389; decisions-owner OWN-05A-014 | Completion: «В первой неделе (ядро) у забывшего пароль автора нет способа восстановить доступ вообще»; PRD: «сброс пароля оператором через `ops reset-link`» в 1-й очереди; OWN-05A-014: «Во вторую очередь: сброс пароля» | Решить одно: либо записать решение владельца (например OWN-05A-017 «`ops reset-link` в первой неделе», источник VA2-22), либо убрать `ops reset-link` из первой недели везде. Затем привести Completion §1.2 п.1 и таблицу дней §1.3 (где нет ни `ops reset-link`, ни `ops beta-add`, ни квоты новичка) | координатор/owner; refinement-completion |
| V3-05 | medium | Completion §1.1 п.1, §2.4; Final_Summary | «Все **28** AC» (canon §10: «Всего 30 AC»); «16 наименований + **7** потолков» (canon §6: 9); Final_Summary: «7 потолков», «15 FR, 8 NFR, 24 AC», «на неделе fake-door» | Completion — 30 AC и 9 потолков (§2.4 — это список, по которому оператор выставляет окружение). Final_Summary пересобрать после закрытия Phase 2 или пометить «на момент 1c5051e» | refinement-completion; координатор |
| V3-06 | medium | canon.md §12, строка FR-clips-10 | «LLM **30 ₽ на автора**, 300 ₽ на сервис» при §6 `LIMIT_LLM_USER_KOP_DAY` = `5000` (OWN-05A-012) | «LLM 50 ₽ на автора, 24 ₽ на задачу, 300 ₽ на сервис; OWN-05A-012». Противоречие внутри самого канона | координатор |
| V3-07 | medium | Architecture-compose.md, сервис `worker-ai` | `environment:` содержит `FX_USD_RUB_KOP`, `LIMIT_LLM_*`, но не `LLM_EST_CHARS_PER_SEC`; canon §6: «`LLM_EST_CHARS_PER_SEC` \| worker-ai \| без дефолта» | Добавить `LLM_EST_CHARS_PER_SEC: ${LLM_EST_CHARS_PER_SEC:?}`. Иначе стек по эскизу не стартует (отказ громкий: «Проверка конфигурации» шаг 3.1) | adr-architect |
| V3-08 | medium | ADR-006 п.3; model-cost-contract; Refinement §2.4/§2.5 | Вторая половина canon §11 («при старте `worker-ai`: `llm_reserve_kop(7200 × LLM_EST_CHARS_PER_SEC) × LIMIT_LLM_ATTEMPTS_JOB ≤ LIMIT_LLM_KOP_JOB`, иначе не стартует») есть только в Spec и Pseudocode 5a. Pseudocode 2a прямо перекладывает на неё гарантию AC-clips-8: «Один повтор AC-clips-8 гарантирует проверка при старте … а не этот шаг» | Перенести правило в ADR-006 п.3 и контракт; в Refinement — тест «`LLM_EST_CHARS_PER_SEC`, при котором 2 × резерв > 2 400 → `worker-ai` не стартует» и мутацию «убрать 5a → тест красный». Сейчас свойство, на котором держится AC-clips-8, не проверяется ничем | adr, refinement-completion |
| V3-09 | medium | model-cost-contract.md: «Единицы измерения и резерв»; «Именование хранилища — не решено» | «STT \| минута аудио, округление куска вверх \| длительность куска, зарезервированная до вызова»; «Pseudocode и Specification этого решения ещё не отразили» | Строку STT — по canon §11: секунда; персональные по `ceil(Σ unique_ms / 1000)`, глобальные по `Σ ceil(chunk.duration_ms / 1000)`. Раздел об именовании заменить ссылкой на canon §5 `stt_sec_newbie` и Pseudocode «Допуск STT» шаг 3 | refinement-completion (владелец контракта) |
| V3-10 | medium | Pseudocode «Транскрипция куска» шаг 3 против canon §11 и Spec FR-clips-10 | «IF `n ≥ 2` THEN `r ← reserve([{account,…,'stt_sec', provider_sec(k), ulim,'quota_user'}, …])`; отказ → … `finish_failed(r.fail_reason)`». Допуск уже списал с персонального ровно `ceil(Σ unique_ms/1000)`, то есть для выпуска 120 мин весь `LIMIT_STT_USER_SEC_DAY` = 7 200 (для новичка с записью 30 мин — все 1 800). Первый же сетевой повтор любого куска (5xx/429/таймаут — ожидаемый случай, ADR-001) даёт `failed/quota_user` посреди задачи, после оплаты большей части STT. Нигде (Spec, Refinement, PRD) это поведение не названо. Кроме того, повтор списывает с персонального секунды провайдера (с перекрытием 5 с), а canon §11 говорит, что персональные — «по длительности ЗАПИСИ … без перекрытий» | Решение (Spec + canon §11): либо повторы кусков идут только в глобальный счётчик (деньги платим мы, а предел числа повторов уже задаёт `MAX_JOB_ATTEMPTS`), либо при допуске резервируется запас на повторы, либо поведение «на пределе повтор куска отказывает» записывается в Spec как ожидаемое, с текстом для автора и тестом «120 мин + один повтор куска». Сейчас это неявное следствие | spec (решение), pseudocode, canon |
| V3-11 | medium | Pseudocode «Выбор фрагментов», шаг 7; константа `LLM_MAX_OUTPUT_TOKENS`; «Проба STT дня 1»; Completion §1.3 день 11; Refinement | (а) Шаг 7 начинается с «Разбор: `JSON.parse` + проверка схемы кодом», а проверка `finish_reason == 'length'` стоит после. Обрезанный JSON бросает исключение разбора → «бросить `SchemaInvalid` → повтор BullMQ» с тем же `max_tokens`, то есть ровно то, что исправление запрещает. (б) Константа: «выход 10 фрагментов замеряется на пробе дня 1 по `usage.completion_tokens`», но алгоритм «Проба STT дня 1» зовёт только STT, шага LLM в нём нет; Completion ставит «замер токенов LLM» на день 11, после LLM-воркера (день 4); VA2-04 требовала замер до дня 4. (в) Ни теста, ни мутации на `finish_reason = 'length'` в Refinement нет | (а) Проверять `finish_reason` ДО `JSON.parse` и написать это порядком шагов. (б) Добавить в пробу дня 1 (или день 4) один вызов LLM на реальном транскрипте с записью `completion_tokens`; согласовать день в Completion. (в) Тест: ответ с `finish_reason = 'length'` и обрезанным JSON → `selection_failed`, 1 попытка в `spend_ledger`, повтора BullMQ нет | pseudocode, refinement-completion |
| V3-12 | medium | Refinement §2.4.8; ADR-009 «Как проверить» | Две обрезки: `crop=1080:608:0:420` и `crop=608:608:236:420`. Нет обрезки «растянуть на весь экран» 9:16 (`crop=342:608:369:420`), то есть случая (а) из VA2-02, названного самым частым. Плашка при кегле 44–52 — примерно 290–345 px (Refinement: «≈ 300 px … `[ПРЕДЛОЖЕНИЕ]`»), окно — 342 px: запас от 0 до ~25 px с каждой стороны, при 52 px плашка может не влезть | Добавить третий прогон `crop=342:608:369:420` с OCR ≥ 9/10, а в «Раскладку» шаг 2 — условие `pw ≤ 342 − 2 × WM_INSET` для 16:9 (или потолок кегля). Мутация «вернуть угол» при этой обрезке краснеет гарантированно | refinement-completion, pseudocode |
| V3-13 | medium | Spec FR-clips-16, вступление и п.3; Refinement §3, строка VA2-13 («закрыто OWN-05A-016») | «Так вторая половина пула **всегда** остаётся авторам старше суток и бета-авторам»; «Так один человек, заведший десятки аккаунтов, не выбирает суточный потолок сервиса». Через 24 ч аккаунты перестают быть новичками (п.6): 13 аккаунтов, заведённых за час с одного IP (VA2-13), на следующие сутки получают по 7 200 с, а 13 × 7 200 = 93 600 > 90 000. Квота новичка откладывает атаку на сутки и не отменяет её | Записать как принятый остаточный риск (PRD §7, Refinement §3) и убрать «всегда»; либо, если владелец решит иначе, — мера против «отлежавшихся» аккаунтов (например, полная квота только после первой подтверждённой публикации или по `ops beta-add`) | spec; owner |
| V3-14 | low | Completion §1.3 | «`16–20 − (2…3) + 1 = 14…18` дней; беру диапазон **13–17 дней**» | 16 − 3 + 1 = 14, 20 − 2 + 1 = 19, значит 14…19. Выбранный диапазон ниже собственной арифметики без объяснения: либо обосновать, либо писать 14–19 | refinement-completion |
| V3-15 | low | Spec SC-US-008-1 против Pseudocode «Раскладка» шаг 3 | Pseudocode: «Тест SC-US-008-1: … 1 004 ±2 px для 16:9 и 4:3, 1 084 ±2 px для вертикального»; сам сценарий проверяет только «у исходника 16:9 … нижний край y=1004» | Добавить в SC-US-008-1 шаги для 4:3 и вертикального (1 084) | spec |
| V3-16 | medium | Spec FR-clips-9 п.4; canon §7 (таблица маршрутов); SC-US-010-1 | Spec: «На неделе способа стать `paid` у автора нет: оплаты нет, экран плана — fake-door» — без пометки «2-я очередь»; canon §7: `GET /plans`, `POST /api/fakedoor` без пометки очереди (у `/admin/partners` и `/admin/spend` она есть); SC-US-010-1 первой недели проверяет «клики fake-door» когорты | Пометить «2-я очередь (OWN-05A-014)» в FR-clips-9 п.4 и в строках canon §7; в SC-US-010-1 — «клики fake-door (со 2-й очереди, до неё 0)» | spec; координатор (canon) |
| V3-03 | low | C4_Diagrams:130; Completion §1.1 п.8, §1.3 день 5 | C4: «знак по центру у нижней кромки полосы видео; … знак полупрозрачный **в углу** полосы видео» — одно описание противоречит самому себе; Completion: «(OWN-05A-013, `Refinement.md` §2.4.8)», «знак внутри полезной полосы OWN-05A-013» | Убрать «в углу» из C4; в Completion сослаться на OWN-05A-015 | adr-architect; refinement-completion |
| V3-18 | low | Refinement §2.5; Completion §1.1 п.2 | Pseudocode «Метрики недели» объявляет мутацию «считать авторов по `account_id` → тест красный», но в таблице 12 стражей Refinement §2.5 её нет | Добавить 13-ю строку и поправить «12 стражей» в Completion | refinement-completion |

**Итого новых:** blocker 0 · high 0 · medium 13 (V3-01, 02, 04–13, 16) · low 4 (V3-03, 14, 15, 18).

## 6. Вердикт

**ЗАКРЫТО**: 0 blocker, 0 high открытых. Все high итерации 2 (VT2-01, VT2-02 = VA2-01, VA2-02, VA2-03, VA2-04,
VS2-01, VS2-02) и блокер VA-01 закрыты по сути в алгоритме и в Specification. VA2-13 и VA2-24 тоже закрыты.

Условие честности вердикта: он опирается только на порог «нет high и blocker». Из 13 medium выше четыре лежат на пути
денег или знака и вводят исполнителя в заблуждение, если он читает документ буквально: V3-01 (фикстура с углом),
V3-10 (повтор куска на пределе), V3-11 (порядок `finish_reason` / `JSON.parse`), V3-06 (30 ₽ в каноне). Их
разумно закрыть до нарезки работы Phase 3. V3-04 (сброс пароля в первой неделе) требует решения владельца, а не
правки текста.

Status: completed
