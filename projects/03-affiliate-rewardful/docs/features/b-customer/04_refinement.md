# Refinement — b-customer

Each AC in specification includes its positive/negative state transition. Additional adversarial paths: absent/invalid session, cross-tenant IDs, wrong role, expired/revoked grant, duplicate keys with different bodies, request size and malformed input, SQL/XSS injection, stale source and unavailable database.

No in-transaction external calls; database failures rollback without committing idempotency claim. All projections scoped before serialization. UI supports loading/empty/error/denied/retry and390px/1440px keyboard use; buttons restore on failures. Historical transfer/credit facts never rewritten by refund. F1 uses explicit fixture clock and independent run IDs; no real payment capability.

Algorithm details and explicit limits: [runtime contract](../../runtime-contract.md). Late-event, registry allocations, invoice unknown, cancel/handoff requirements remain shared even when this UI exposes a smaller subset.
