# Публичные стенды N3

A–D: `https://n3-{a,b,c,d}.212.192.0.33.sslip.io/`. Каталог и инструкции: `/demos/index.html` на A, `/demo.html` на каждом варианте. Временные имена sslip.io разрешаются в212.192.0.33; Caddy получил валидные сертификаты Let's Encrypt. SSH не нужен.

На этом VPS существующий ai-hub-tls-proxy владеет80/443. Фрагмент Caddyfile в этой папке добавлен к `/home/dz-projects-2026/dz-harness-hub/.dz/deploy/ai-hub/Caddyfile`; существующие маршруты сохранены байт-в-байт. Резервная копия: соседний `Caddyfile.before-n3-20260909T083804Z`. Frontend Compose сохраняет подключение к talk-ai-public и n3-frontend. Caddy использует полные уникальные имена контейнеров. API не присоединён к talk-ai-public и опубликован только на127.0.0.1:13030. PostgreSQL не имеет hostports и находится только в internal n3-database вместе с API.

При повторном деплое сначала проверять порты и итоговый Compose. Конфигурацию Caddy валидировать до reload, не добавлять дублирующие блоки. При этой публикации существующий file bind mount продолжал видеть прежний inode: reload стандартного пути сообщил unchanged. Проверенный кандидат скопирован в `/tmp/Caddyfile.n3` внутри прокси и загружен оттуда без перезапуска остальных сайтов. Канонический файл хоста обновлён; независимый новый read-only bind mount подтвердил его SHA256 a0766011ce8e7599ac1422a60261943ae7303918360da9008e3e293e8c1146de. Новое монтирование/пересоздание прокси использует сохранённый файл. До пересоздания для reload проверять SHA файла внутри контейнера, а не только на хосте.

Проверки: `node scripts/run-public-e2e.mjs` (локальный Firefox WebDriver), `node scripts/check-deployment.mjs`, `node scripts/build.mjs --all`. Origin-политика централизована в shared/contracts/deployment.mjs. Смена доменов требует обновления этой матрицы, Caddy и ссылок, повторной проверки реального B iframe и D→A handoff.

F1 остаётся синтетическим демо. Публичный веб-доступ не означает подключённые провайдеры, реальные выплаты или production identity.
