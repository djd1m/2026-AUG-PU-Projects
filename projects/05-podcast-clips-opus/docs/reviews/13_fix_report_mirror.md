# RV-5 / RV-6 — отчёт исправления

| Находка | Исправление и результат |
|---|---|
| RV-5 | `select.ts`: запись исхода в журнал и `failSelection` защищены отдельными `try/catch`; обе предпринимаются, ошибки записи диагностируются, наружу возвращается исходный объект ошибки. Запись `attempt` до поставщика сохранена. Проверены отказ журнала до вызова, после вызова, отказ БД и одновременный отказ обоих хранилищ. |
| RV-6 | `render-media.test.ts`: на заведомо белой фикстуре медиана яркости области белого префикса должна быть `< 140`. Белый текст не может затемнить белый фон: потемнение доказывает подложку (ожидается около 102 при opacity 0,60). Выбран этот вариант, поскольку он использует существующий кадр без второго рендера. Прежний относительный контраст ≥ 4,5:1 сохранён на белом, тёмно-синем и чёрном фоне. |

Мутации выполняются в отдельной временной копии: `node scripts/test-mirror-mutations.mjs`.
Квитанция: `tests/artifacts/mirror/mutations/results.json`, `total: 3`, `partial: false`.

| Страж | Внедрённый дефект | Красное | Восстановленный код |
|---|---|---|---|
| RV-5: отказ журнала | Снова бросить ошибку outcome-записи вместо исходной причины | exit 1, 1 failed | exit 0, 1 passed |
| RV-5: отказ БД | Удалить попытку записи outcome в журнал | exit 1, 1 failed | exit 0, 1 passed |
| RV-6: пиксели | Удалить drawbox подложки, сохранив текст и код | exit 1: белая failed (`255 < 140` неверно), тёмно-синяя и чёрная passed | exit 0: все 3 фикстуры passed |

Отдельный новый PostgreSQL-тест в `tests/selection.integration.test.ts` проверяет немедленные
`video.status=failed`, `job_attempt.status=failed` и `finished_at` при отказе журнала.
В этом окружении он пропущен; проверка вызова закрытия в юнит-тесте не выдается за проверку SQL.

Проверки на снимке `tests/artifacts/mirror/source-snapshot.json`:

- Целевые `npm test -- tests/selection-order.test.ts tests/render-media.test.ts`: 14 passed, exit 0.
- `npm test`: 432 passed, 140 skipped; 52 набора passed, 16 skipped; exit 0.
- Все три мутации red→green: exit 0; журналы и JSON результатов в `tests/artifacts/mirror/mutations/`.
- `npm run build`, `npm run typecheck`, `npm run lint`, `git diff --check`: успешно.
- Рендер проверен настоящим локальным ffmpeg 4.4.2; прогон контейнерного ffmpeg 7 не выполнен.
- Docker API: `permission denied` на `/var/run/docker.sock`. Compose-интеграция PostgreSQL/Redis/MinIO
  недоступна; пропуски не считаются успешной проверкой. Контейнеры не запускались.
- Независимое Anthropic-ревью (OWN-002): один запрос `sonnet`, effort high, без инструментов/hooks,
  лимит 60 с; ответ не получен, процесс завершён по таймауту (exit 124). Actual model неизвестна.
  Ревью не выполнено и не подменено самопроверкой автора.

Исправления и локальные доказательства готовы. Полная приёмка остаётся незавершённой:
нужны интеграционный прогон и независимое Anthropic-ревью при доступном окружении.
`.claude/`, origin, DNS, развёртывание и длина короткого кода не изменены; в `docs/` создан только этот отчёт.

Телеметрия: RUN_ID `20260923T161957Z-fix-mirror`, профиль `compact-quality-first-v2`.
Исходная ревизия `63c9a9df474e6e0d436d96c0ab6f5c78bd1d5b9d`; исходное дерево чистое.
ROUTE: механически S (3 исходных файла, exit 0); содержательно M — исправление порядка обработки отказов.
PLAN/VALIDATE: постановка Anthropic передана владельцем; повторный IMPLEMENT ROUTE сохраняет M и границы.
Один проход реализации; бюджет 20 минут, без повторов реализации и смены модели.
План расширен только тестом фактических статусов PostgreSQL и воспроизводимым раннером требуемых мутаций.
E2E preflight: `not_applicable` — локальные проверки, развёрнутый пользовательский сценарий не проверялся.
Первое измерение: 2026-09-23T16:19:57Z; конец: 2026-09-23T16:26:40.336546+00:00.
Измерено 403.3 с; начальное чтение правил до первого измерения не учтено, это нижняя граница.
Исполнитель: текущий Codex (GPT-6 по описанию среды); точные actual model/effort хост не экспортировал (`null`).
Запрошенная модель ревью — Anthropic `sonnet`; actual `null`, ответа нет, fallback не выполнялся.
Active time, токены и стоимость: `null`, счётчики и интервалы ожидания недоступны; экономия пока не установлена.
Начальная запись и границы стадий велись в этом отчёте; итоговый паспорт/события и доказательства:
`tests/artifacts/mirror/run.json`, `events.jsonl`, `source-snapshot.json` и журналы рядом.
Размещение вне `docs/telemetry` соблюдает ограничение постановки. Семидневное наблюдение pending.

Status: failed
