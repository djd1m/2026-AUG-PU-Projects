# RV-1…RV-4 — исправления единиц и стражей

Базовая ревизия: `46b4a37a0dcf38b6bdaac453677124a5b38c2334`. Снимок изменённых исходников: `47f6f5c684028048cf1034edc48e5a1857d8ea54ed5a20fffad96e1480c42e65`;
хеши — `tests/artifacts/units-and-guards/source-sha256.json`.

| Находка | Исправление и результат |
|---|---|
| RV-1 | `Completion.md`: секунды STT, потолок 36 000 с = 600 мин, сигнал > 32 400 с; суммировать только `stage=stt`, `phase=attempt` за московские сутки. Выбраны секунды, чтобы сравнивать с журналом без неявного пересчёта. Logging исправлен. `Architecture.md` отделяет минуты квоты в `job_attempt.unit_count` от секунд попыток журнала с перекрытиями/повторами и требует пересчёта с учётом округления. Страж сверяет документацию, тип события и писатель. |
| RV-2 | Выбран предпочтительный вариант: ошибка старта предлагает сократить `N5_PUBLIC_ORIGIN` либо уменьшить `N5_SHORT_CODE_LENGTH`; сохраняет формат, ширину и причину отказа. Подсказка доступна в момент отказа; алгоритм допуска и значения конфигурации не менялись. |
| RV-3 | `results.json` теперь объект с `requested`, `total`, `partial`, `results`. Любой непустой фильтр даёт `partial: true`; пустой `requested: []` означает весь список. Тест реального раннера со стабом только дочернего Vitest проверяет полный запуск и последующую перезапись частичным в том же каталоге. Дополнительно реально выполнены все 13 мутаций и отдельно одна. |
| RV-4 | Вместо абсолютного тёмного порога — отношение яркостей 95-го и 10-го процентилей в каждой области метки после перевода в линейную яркость, минимум 4,5:1. Реальный ffmpeg прошёл на исходном тёмном `0x224466`, белом и чёрном фонах. Мутация старого порога падает именно на белом. |

Формулировка для `.claude/rules/coding-style.md` (владелец вносит сам): «Каждая попытка платного вызова пишет строку в `model-spend.jsonl`: `video_id`, стадия, единица (секунды STT — `unit=seconds`, вызовы LLM — `unit=calls`), количество, `phase`, результат; суммируются только строки `phase=attempt`, строки `phase=outcome` повторно не суммируются.»

`.claude/`, `N5_PUBLIC_ORIGIN`, DNS, развёртывание и длина кода не изменены.

## Мутационные квитанции

Команда: `N5_MUTATION_OUTPUT=tests/artifacts/units-and-guards/mutations node scripts/test-units-guards-mutations.mjs`.
Мутации применяются в изолированной временной копии. Ниже обе строки каждого сценария;
подробные JSON/TXT — в `tests/artifacts/units-and-guards/mutations/`.

| Сценарий | Состояние | Exit | Результат |
|---|---|---:|---|
| RV-1-monitor | Красное: вернуть «Минуты STT» в мониторинг | 1 | 1 failed |
| RV-1-monitor | Зелёное: восстановить секунды | 0 | 1 passed |
| RV-1-writer | Красное: вернуть округлённые минуты в писатель | 1 | 1 failed |
| RV-1-writer | Зелёное: восстановить секунды | 0 | 1 passed |
| RV-2 | Красное: убрать подсказку об обеих переменных | 1 | 2 failed |
| RV-2 | Зелёное: восстановить подсказку | 0 | 2 passed |
| RV-3 | Красное: убрать поле `partial` | 1 | 1 failed |
| RV-3 | Зелёное: восстановить поле | 0 | 1 passed |
| RV-4 | Красное: вернуть абсолютный порог `< 50` | 1 | белая failed; тёмная и чёрная passed |
| RV-4 | Зелёное: восстановить контраст | 0 | белая, тёмная и чёрная: 3 passed |

Исходный раннер: `N5_MUTATION_OUTPUT=tests/artifacts/units-and-guards/render-full node scripts/test-render-mutations.mjs` — exit 0, 13/13 red→green, `requested: []`, `total: 13`, `partial: false`.
Частичный: `N5_MUTATION_OUTPUT=tests/artifacts/units-and-guards/render-partial node scripts/test-render-mutations.mjs default-six` — exit 0, 1/13 red→green, `requested: ["default-six"]`, `total: 13`, `partial: true`.

## Проверки и ограничения приёмки

- Целевые тесты: 24 passed, exit 0 (`focused.txt`).
- `npm test`: 430 passed, 139 skipped, 52 набора passed и 16 skipped, exit 0 (`all-tests.txt`). Пропущенные интеграционные проверки НЕ объявляются пройденными.
- `npm run lint`, `npm run typecheck`, `npm run build`: exit 0; отдельные журналы в каталоге доказательств.
- `git diff --check`: exit 0.
- Docker API: permission denied; полный compose-прогон с PostgreSQL/Redis/MinIO недоступен. `check-env-wiring.sh` и `check-ports.cjs`: exit 2, compose не сконфигурирован (`N5_DB_APP_PASSWORD` отсутствует). Окружение не изменялось, контейнеры не запускались.
- Независимое Anthropic-ревью по `CLAUDE.md`, OWN-002: первая попытка CLI `--bare` — `Not logged in`; повтор с обычным чтением существующих credentials — `EAI_AGAIN`, API недоступен, exit 1, ответа модели нет. Инструменты и hooks отключены; лимит попытки 180 с. Ревью не выполнено и не заменено самопроверкой автора.
- E2E на развёрнутом стенде не выполнялось; companion preflight — `not_applicable` для локальных тестов. Структура work-record проверена, это не приёмка продукта.

Итог: исправления четырёх находок и их мутационные доказательства готовы. Полная приёмка не завершена:
нужно выполнить пропущенную интеграцию и независимое Anthropic-ревью при доступном окружении.
Поэтому финальный статус отчёта — failed, хотя целевые локальные проверки зелёные.

## Телеметрия

RUN_ID: `20260923T152753Z-units-and-guards`; профиль `compact-quality-first-v2`; содержательный тир M
(формат квитанции), механический роутер S, exit 0. План — данная владельцем постановка Anthropic.
Исполнитель — текущий Codex; точный actual model/effort хост не предоставил, поля `null`.
Для независимого ревью запрошен Anthropic `sonnet`, effort high; actual `null`, вызов API не состоялся;
fallback на другую модель не выполнялся. Нулевая usage двух неуспешных CLI-попыток не является
нулевым расходом всей работы: usage и стоимость координатора неизвестны.

Измерено 503 с от начала записи до этой фиксации; начальное чтение инструкций началось раньше,
его точная длительность недоступна, поэтому это нижняя граница полного времени. Active = `null`;
токены/стоимость всего прогона = `null`; экономия пока не установлена. Один повтор ревью,
эскалаций модели нет. Семидневное наблюдение pending.

Телеметрия и доказательства: `tests/artifacts/units-and-guards/run.json`, `events.jsonl`,
`work-record.json`, журналы тестов и мутаций. Размещены вне `docs/telemetry`, чтобы соблюсти явное
ограничение постановки на изменяемые файлы в `docs/`.

Status: failed

## Дополнение integration owner (23.09.2026)

Прогон на настоящей инфраструктуре профиля `test`: **569 passed (569)**, 68 файлов.

Первый прогон дал 1 красный — `tests/units-and-guards.test.ts` не смог прочитать
`docs/Completion.md`: каталог `docs` отсечён из образа. Это **второй за день** случай одного вида:
утром так же падал страж И-2 на `.env.example`. Страж падает громко, и это правильнее тихого
пропуска, но проверить он при этом не может НИЧЕГО.

Исправлено точечным исключением в `.dockerignore` с пометкой для будущих авторов: добавляя страж,
читающий документ, добавь туда и строку. Оба случая вскрылись только полным прогоном в Docker —
локальный `npm test` их не видит, потому что читает рабочее дерево.

RV-4 стоит отметить отдельно: абсолютный порог тёмных пикселей заменён на **отношение яркостей**
95-го и 10-го процентилей внутри области метки, не ниже 4,5:1. Это утверждение о МЕТКЕ, а не о цвете
фона; проверено настоящим ffmpeg на тёмном `0x224466`, белом и чёрном фоне, и мутация прежнего
порога падает именно на белом — то есть ровно там, где старый страж молчал.
