# Исправление RM-002…RM-006

RUN_ID: 20260923T102213Z-money-fixes
Исходная ревизия: `46c3275edd302f3ff63eb00a204b31414bc6c59d` (чистое дерево).
Профиль: `compact-quality-first-v2`; исполнитель: текущая сессия Codex (GPT-6 по
инструкции хоста; точный model ID/effort и счётчики провайдера недоступны).
Usage, cost, active_wall_ms: `null`; причина — хост не предоставил измерений.
Телеметрия встроена в этот отчёт: постановка разрешает в docs только контракт и отчёт.

## Журнал стадий

- 10:22:13Z ROUTE/PLAN: механический тир S (exit 0); содержательный XL — учёт денег
  и общий ресурс двух контейнеров. Применимое разрешение — конкретный план в
  `08_fix_brief_money.md` и указание пользователя выполнить его. Повторное согласование
  того же плана не требуется. AC: RM-003 общий файл + мутация тома; RM-004 равенство
  авторизаций и списаний + мутация read/write; RM-005 секунды; RM-006 исход до БД;
  RM-002 честное описание существующих шести scope.
- VALIDATE/IMPLEMENT: границы повторно сверены с брифом; новых scope, миграций,
  внешних вызовов, изменений .claude, origin, DNS и развёртывания нет в плане.
  Требования: бриф и ревью 01; архитектура: существующие runtime, compose,
  Architecture.md (наблюдаемость), model-cost-contract.md. Один исполнитель;
  бюджет реализации/проверок 25 минут, остановка расширения объёма после обязательных
  проверок. Прогноз принятого результата: insufficient_data.
- Подготовка проверок: Docker API недоступен (`permission denied`); проверяю наличие
  локального PostgreSQL 16 для настоящего конкурентного теста. E2E пока не запускался.
- Cross-family REVIEW (OWN-002): Anthropic отсутствует среди доступных исполнителей;
  независимую проверку Anthropic не подменяю самопроверкой.

- 10:29Z CHECK: локальные целевые 20 тестов прошли; RM-003 мутация раздельных томов
  дала красное, восстановление — зелёное. Локальный PostgreSQL заблокирован:
  `runuser: cannot set groups: Operation not permitted`. RM-004 не выполнен.
  Typecheck нашёл четыре обращения к необязательным группам regex в новом тесте;
  исправлена типизация, требуется повтор затронутой проверки.
- REVIEW: выделяю read-only независимый просмотр текущего diff (до 5 минут),
  requested `gpt-5.6-sol/high` по политике моделей; только находки в разрешённой области,
  без правок и повторного исследования репозитория. Это дополнительная проверка OpenAI,
  не замена отсутствующего Anthropic по OWN-002. Usage и подтверждённый actual пока `null`.

## Результат по находкам

| Находка | Вердикт и доказательство |
|---|---|
| RM-003 | Исправлено в исходниках: общий том `model-spend:/work/spend`, оба runtime передают один путь `/work/spend/model-spend.jsonl`. Страж `tests/model-spend.test.ts` запускает настоящие runtime, обработчики STT/LLM и файловые writers с подменёнными внешними зависимостями; именованные тома из compose моделируются symlink. После двух попыток оба пути читают один файл с обеими стадиями. Мутация возвращает раздельные тома и краснеет. Docker-контейнеры в этом прогоне не запускались; развёртывание не выполнено по границам брифа. |
| RM-004 | **Не закрыто проверками.** В `tests/transcription.integration.test.ts` добавлена первая конкурентная отправка при свободной квоте: отдельное соединение блокирует `video`, тест дожидается обеих транзакций в `pg_stat_activity`, затем проверяет равенство авторизаций и списанных минут в обоих scope, включая первичное списание probe. Мутационный скрипт восстанавливает чтение `j.stt_calls` из старого joined snapshot и запись без CAS. PostgreSQL недоступен; фактического красного/зелёного нет. |
| RM-005 | Исправлено: STT `unit=seconds`, `quantity=chunk.durationSeconds`. Тесты настоящего обработчика и границ чанков: **300 → 302 с (5,0333 мин, +0,67 % к квоте 5 мин); 5400 → 5460 с (91 мин, +1,11 % к квоте 90 мин)**. Возврат округления в минуты краснит оба сценария. Контракт объясняет перекрытия, округление квоты, повторы и суммирование только `phase=attempt`; сверка с реальным счётом не заявлена. |
| RM-006 | Исправлено: запись `phase=outcome` перед `failSelection`. Тест отказа БД подтверждает порядок `quota → attempt → call → outcome → db-down`; обратный порядок даёт красное. |
| RM-002 | Исправлено в контракте: ложное обещание ключа `ip_prefix` снято; названы шесть фактических scope и возможность одному актору выбрать глобальный бюджет пачкой аккаунтов. Схема и квоты не изменены. |

Вопрос владельцу по RM-002: вводим ли отдельный суточный потолок по `ip_prefix` (седьмой scope; с какими лимитами и поведением для общего IP), или принимаем риск, что один актор через несколько бесплатных аккаунтов исчерпает глобальные 600 минут / 20 вызовов за сутки?

## Мутации

Команда: `node scripts/test-money-mutations.mjs`; можно выбрать находку аргументом,
например `node scripts/test-money-mutations.mjs RM-004`. Скрипт работает в временной копии,
проверяет baseline, конкретные assertion failures и прохождение восстановленного теста;
skip/ошибка инфраструктуры не принимаются за успешную мутацию.
Общий запуск вернул **exit 1**, потому что RM-004 не выполнен.

```text
RM-003: раздельные тома → red exit 1, 1 assertion failed.
RM-003: общий том восстановлен → green exit 0, 1 passed.
RM-004: чтение-потом-запись → NOT EXECUTED, red exit null (нет PostgreSQL).
RM-004: исправный код → NOT EXECUTED, green exit null (нет PostgreSQL).
RM-005: округлённые минуты → red exit 1, 2 assertions failed.
RM-005: секунды восстановлены → green exit 0, 2 passed.
RM-006: БД раньше исхода → red exit 1, 1 assertion failed.
RM-006: исход раньше БД восстановлен → green exit 0, 1 passed.
```

Доказательства: `tests/artifacts/money-mutations-kgLjzv/results.json` и соседние логи;
повтор RM-003 после исправления типизации теста:
`tests/artifacts/money-mutations-fyfxXM/results.json` (снова red 1 / green 0).
Мутанты сохранены отдельно; рабочий `packages/db/src/transcription.ts` не изменялся.

## Проверки и границы доказательства

- `npm test`: **402 passed, 138 skipped**, 50 файлов passed / 16 skipped, 127,87 с.
  Это не полный интеграционный PASS. Нет `DATABASE_URL`, `REDIS_URL`, `S3_ENDPOINT`.
- `npm run typecheck`: **exit 0** после исправления групп regex в новом тесте.
- `npm run lint`: правила ошибок не нашли. `git diff --check`: **exit 0**.
- `npm run build`: production-сборка завершена без ошибок, маршруты Next.js построены.
  Логи тестов, build и typecheck: `tests/artifacts/money-fixes-final/`.
  У первого запуска build и полного npm test оболочка завершалась `tail`, поэтому
  отдельный exit npm не сохранён; вывод сборки и итог Vitest сохранены. Не выдаём
  exit оболочки за измеренный exit внутренней команды.
- `docker compose --project-directory . config --no-interpolate --format json`: **exit 0**,
  нормализованный compose показывает обоим воркерам один source `model-spend` и target
  `/work/spend`. Это чтение конфигурации, а не проверка работающих контейнеров.
- `node ../../.claude/hooks/check-model-cost.cjs .`: **exit 0** — только декларация контракта.
- E2E/preflight: **blocked** для полного контейнерного прогона — Docker API `permission denied`;
  попытка одноразового локального PostgreSQL 16 остановилась на запрете смены пользователя
  (`runuser: cannot set groups: Operation not permitted`). Сервисы не запущены,
  E2E-pass не заявлен. Платных вызовов и изменений развёртывания не было.

Для завершения нужен доступный изолированный PostgreSQL 16 с `DATABASE_URL` на БД `*_test`:
запустить `node scripts/test-money-mutations.mjs RM-004`, затем полный набор на тестовых
PostgreSQL/Redis/MinIO. Межсемейное ревью Anthropic также остаётся невыполненным.
Это перечисление недостающих доказательств, не разрешение на изменение боевого стенда.

## Завершение и телеметрия

- 10:33:11Z REVIEW/HANDOFF: независимый read-only агент `money_review`
  не нашёл конкретных дефектов в пределах брифа; подтвердил ограничения доказательства.
  Дополнительный целевой прогон ревьюера: 4 passed, 16 skipped по фильтру.
  Requested `gpt-5.6-sol/high`, запуск подтверждён инструментом collaboration;
  отдельные метаданные actual model/effort и usage инструмент не вернул (`null`).
  Это OpenAI-review; Anthropic-review не выполнено и не объявлено пройденным.
- SHA-256-манифест конечных исходников и стражей:
  `tests/artifacts/money-fixes-final/source-sha256.json`, digest `1c9bcc219107208fff510497be862c3711d5e7137e4ccf209792bf3befc722dc`.
  Сборка относится к конечному product-коду; после неё изменялись только тест/скрипт/отчёт.
- Измеренный интервал от первой отметки времени 10:22:13Z до 10:33:11Z:
  **659 с**. Чтение инструкций до первой отметки не измерено,
  поэтому это нижняя граница полной длительности, а не полный time-to-acceptance.
  `active_wall_ms`, `agent_work_ms`, tokens, cached/reasoning tokens, cost: `null`;
  хост не предоставил usage и разделение ожиданий. Экономия не установлена.
- Одна реализация, одно независимое дополнительное ревью; исправлена конкретная ошибка
  типизации, после неё повторены typecheck и мутация RM-003. Модель координатора
  не переключалась; глобальные настройки не изменялись.
- Итог: исправления подготовлены, полная приёмка заблокирована недостающими проверками.

Status: failed

## Дополнение integration owner (23.09.2026)

Codex не имел доступа к PostgreSQL и честно оставил RM-004 невыполненным. Выполнено мной на
настоящей инфраструктуре профиля `test` (PostgreSQL 16, Redis 7, MinIO), где база есть.

| Проверка | Результат |
|---|---|
| Полный прогон `docker compose --profile test run --rm --build test` | **540 passed (540)**, 66 файлов |
| Мутация RM-004 внутри контейнера | `red={"exit":1,"verified":true}; green={"exit":0,"verified":true}` |

Команда мутации: `docker compose --project-directory . --profile test run --rm --build
--entrypoint sh test -c "node scripts/test-money-mutations.mjs RM-004"`.

Тем самым RM-004 закрыт: конкурентный тест первой отправки чанка при свободной квоте КРАСНЕЕТ при
возврате чтения-потом-записи и зеленеет на исправном коде. Строка отчёта «Не закрыто проверками»
относилась к песочнице Codex, а не к состоянию кода.

RM-001, RC-001 и RC-002 подтверждены тем же прогоном: пометка «подтверждение ожидается» снята.

Остаётся ровно один открытый вопрос — RM-002, седьмой ключ квоты по `ip_prefix`. Это решение
владельца, и до него в силе честно записанный в контракте риск: один актор способен выбрать
глобальный суточный бюджет, зарегистрировав несколько бесплатных аккаунтов.
