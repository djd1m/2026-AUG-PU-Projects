# Дополнение к спецификации — требования, возникшие при реализации

`docs/Specification.md` заморожена по контрольной сумме
`sha256:3ac09f3dcccc354f292ac0ed0452de0e7f14f8bafd68b6f8e54354e303407532`: её правка потребовала бы
перепривязки ревизии в `docs/test-scenarios.md`. Поэтому требования, появившиеся после первого живого
прогона 23.09.2026, записаны здесь. Для реализации и приёмки этот файл **равноправен** спецификации;
где он уточняет замороженный пункт, это сказано прямо.

Каждое требование ниже появилось из наблюдения на настоящей записи, а не из замысла. Источник назван.

---

## FR-RENDER-004 · Кадр показывает человека, а не пространство между людьми

**Уточняет** FR-RENDER-001 («кроп 9:16»). Решение — ADR-009.

**Источник:** владелец на живых клипах 23.09.2026: «видеоряд ни о чём, в некоторых случаях просто
стена: сидят 2 человека, а показан не кто-то один из них, а пространство посередине».

```gherkin
Scenario: кадр следует за лицом
  Given запись с участниками, которых камера показывает по очереди
  When рендерится клип
  Then в каждом кадре клипа присутствует лицо участника
  And при смене плана камеры вырез переезжает на показанного участника не позже чем через 1,5 с

Scenario: два этажа — только если на обоих люди
  Given в клипе два участника
  When один из них присутствует меньше чем в 90 % замеров
  Then два этажа не строятся
  And показывается крупным планом тот, кто чаще в кадре

Scenario: без информации о лицах этажей нет
  Given детектор лиц недоступен или лиц не нашёл
  Then применяется центральный кроп
  And рендер клипа не прерывается
```

**Приёмка:** прогон детектора по готовым клипам даёт ноль кадров без лица (измерено: 384 с, семь
клипов, ноль).

## FR-RENDER-005 · Субтитры читаются и не мигают

**Уточняет** FR-RENDER-001 («вшитые субтитры»).

**Источник:** владелец 23.09.2026: «моргает транскрипт», «увеличь кегль и обводку».

```gherkin
Scenario: подпись не мигает между словами
  Given между словами пауза короче 1 с
  Then подпись остаётся на экране, переезжает только подсветка активного слова

Scenario: подпись не висит после речи
  Given речь закончилась
  Then подпись исчезает не позже чем через 1 с после последнего слова

Scenario: строка помещается в безопасную область
  Given строка из 24 знаков
  Then её ширина не больше 972 px при кегле 86
```

Числа: кегль 86, обводка 5, тень 1, перенос 24 знака, не больше двух строк, подъём от низа 500.
Перенос **измерен** отрисовкой (36,2 px на знак), а не выведен.

## FR-RENDER-006 · Английские названия пишутся латиницей

**Источник:** владелец 23.09.2026: «плохо, особенно когда английские слова обозначаются русскими
буквами»; в записи «Велоклауд» и «Виллоклауд» — одно и то же название.

```gherkin
Scenario: термин из словаря
  Given распознаватель вернул «Велоклауд»
  Then в субтитрах показано «VeloCloud»
  And сохранённая расшифровка не изменена
  And замена записана в журнал событием glossary_applied
```

Словарь курируемый: встроенный + `N5_GLOSSARY`. Автоматическая транслитерация **не делается**:
замена — правка сказанного человеком. Подсказка словаря модели распознавания проверена и у
исполнителя Together не работает.

## FR-TRANSCRIBE-003 · Погрешность таймкодов не отвергает запись

**Уточняет** ADR-003. Решение — ADR-010.

**Источник:** запись на 88 минут отвергнута трижды подряд строгими проверками таймкодов.

```gherkin
Scenario: слово начинается раньше предыдущего
  Given откат начала слова на 0,26 с
  Then начало поджимается к предыдущему слову, расшифровка принимается

Scenario: сегмент длиннее куска на доли микросекунд
  Given конец сегмента превышает длительность на 0,0000205 с
  Then конец поджимается к длительности, расшифровка принимается

Scenario: таймкодов нет
  Then расшифровка отвергается, как требует ADR-003
```

## FR-SELECT-003 · Негодный кандидат отбрасывается один

**Уточняет** FR-SELECT-001 (выбор 3–8 фрагментов).

**Источник:** запись на 88 минут: модель нашла семь годных фрагментов, один негодный отбросил все,
и владелец видел «самодостаточных фрагментов не найдено».

```gherkin
Scenario: один из восьми вне диапазона
  Given восемь кандидатов, один с длиной 76 с
  Then сохраняются семь
  And база повторно проверяет набор и не принимает неотсеянный
```

## FR-SELECT-004 · Модели уходят сегменты, а не каждое слово

**Источник:** замер 23.09.2026 — весь список слов 88-минутной записи весил 1128 КБ (≈ 385 тысяч
токенов), сегменты — 115 КБ. Модель вернула пустой список.

**Требование:** сообщение модели выбора фрагментов содержит язык и сегменты, но не слова. Границы
выбранных фрагментов двигаются к ближайшему слову нашим кодом (требование ADR-003 сохраняется).
Измеримо: на расшифровке в 12 916 слов сообщение меньше 200 000 байт (измерено 153 412).

## FR-RESULT-004 · Кнопка повтора не гаснет по прогнозу

**Источник:** владелец 23.09.2026: после подъёма потолка кнопка «Повторить» оставалась серой до
полуночи — экран гасил её по прогнозу обновления квоты, не заглядывая в счётчик.

**Требование:** кнопка доступна всегда, когда повтор законен; решение о квоте принимает сервер и
отвечает отказом с причиной, если места нет.

## NFR-CFG-001 · Непригодная конфигурация валит старт каждого платящего процесса

**Уточняет** V2-R06 / DEC-A-010. Находки SL-003, SL-007.

**Требование:** адрес, не помещающийся в метку, валит старт `web`, `worker-stt`, `worker-llm` и
`worker-video` — всех, кто тратит деньги, а не только тех, кому адрес нужен функционально. Иначе
платящие стадии разбирают очередь и оплачивают клипы, которые нельзя отрендерить.

## NFR-PERF-001 · Измерение

Требование «часовая запись за 20 минут p95» впервые измерено 23.09.2026: 88,4 минуты обработаны за
12 мин 39 с, в пересчёте на час ≈ 8,6 минуты. Одна точка; p95 не установлен.
`docs/measurements/2026-09-23-first-long-recording.md`.

## FR-RENDER-007 · Фоновая музыка по выбору при загрузке

При загрузке можно отметить «Добавить фоновую музыку» (по умолчанию выключено). Тогда каждый клип
записи получает подложку из закрытого каталога CC0-треков на 18 LU ниже громкости речи ЭТОГО клипа
(полоса 300–3400 Гц), без приглушения под речью. Трек из каталога 11 CC0-треков выбирается по
номеру клипа в записи (`(index − 1) mod 11`), поэтому клипы одной записи звучат по-разному. Сбой замера даёт клип без музыки, а не отказ.
Выключенная галочка оставляет рендер побайтово прежним. ADR-011, OWN-009.

## FR-SELECT-005 · Таймаут выделения по измерению длинной записи

Вызов выделения ограничен **240 с** (было 120 с). Измерено на записи 88 мин: 1:45 (23.09) и > 2:00
(24.09, оборвано). Верхняя граница — окно молчания 5 минут из `long-job-contract.md`: дольше экран
прогресса покажет «нет ответа» при живом вызове.

## FR-RENDER-008 · Финальный акцент (пэк-шот)

Если в клип смешана фоновая музыка (FR-RENDER-007), на последних 0,8 с звучит CC0-удар на
`STINGER_MARGIN_LU` ниже громкости речи (полная полоса, пиковый потолок −3 dBTP относительно пика
сэмпла) и кадр вспыхивает без текста; метка остаётся читаемой. Длительность клипа не меняется.
Сбой — клип с музыкой без пэк-шота. ADR-012.

## FR-RENDER-009 · Заголовок в начале клипа

При загрузке отмечено «Заголовок в начале клипа» (по умолчанию да) — каждый клип первые 2,5 с
показывает свой заголовок крупно (до 3 строк) в верхней трети кадра на полупрозрачной подложке.
Заголовок не является цитатой и кавычек не имеет. Сбой разметки — клип без заголовка. ADR-013.

## FR-PARTNER-004 · Блокировка кода считает людей и снимается оператором

Код блокируется, когда 50 РАЗЛИЧНЫХ аккаунтов применили его с одной подсети за 10 минут. Оператор
снимает блокировку командой с причиной; партнёр видит в кабинете дату и причину. ADR-014.

## FR-PARTNER-005 · Атрибуция переживает удаление партнёра

Удаление аккаунта-партнёра не удаляет атрибуции приглашённых им людей, а обезличивает их (статус
`partner_deleted`, без ссылки на партнёра); такой аккаунт нельзя привлечь повторно. ADR-014.

## FR-RENDER-010 · Убрать паузы

При загрузке отмечено «Убрать паузы» (по умолчанию да) — из каждого клипа вырезаются паузы тишины
0,3–2,0 с, на стыке остаётся 0,05 с; слова не вырезаются, речь не ускоряется. Клип уплотняется, только
если экономия ≥ 3 % и результат не короче 20 с. Карточка показывает фактическую длину файла. ADR-015.

## FR-RESULT-005 · Выбор и смена музыки у готового клипа

На карточке клипа — выбор «Авто / 9 треков / Без музыки» с прослушиванием 20-секундного фрагмента.
Смена пересобирает только этот клип; пока идёт пересборка, прежний файл доступен. Не больше 20 смен
в сутки на аккаунт; выбор того же звучащего трека лимит не тратит. ADR-016.

## FR-RESULT-006 · Призыв к действию в конце клипа

Ведущий задаёт, что сделать зрителю после клипа: «Смотреть полный выпуск», «Подписаться», «Перейти по
ссылке» или «Без призыва» (по умолчанию) и ссылку `https://…` для первых трёх. Задаётся в форме загрузки
и меняется на экране записи. На странице клипа по короткой ссылке `/c/{code}` главная кнопка — призыв
автора, под ней домен назначения и пометка «ссылка автора клипа»; «Сделать свои клипы» — вторая кнопка.
Ссылка ведёт прямо на адрес автора, не через наш домен. Непригодный адрес (не `https`, с логином/паролем,
с пробелами, длиннее 2048) — отказ с причиной. Сервер по адресу не обращается. **Статус 25.09.2026:
часть 27a выполнена** (выбор, хранение, `/c/`). ADR-017, OWN-018.

**Дополнение 25.09.2026 (часть 27b, код; выкатки нет).** В последние **2,5 с** клипа (не раньше конца заголовка)
над меткой появляется короткая надпись вида призыва: «Полный выпуск — по ссылке», «Подписывайтесь — ссылка
ниже», «Подробнее — по ссылке». Адрес в кадр не вшивается: путь зрителя — метка → `/c/{code}` → кнопка. Надпись
занимает полосу между субтитрами и меткой (на 1080×1920 — y 1438–1561), кегль 72 → 48; если не помещается —
клип собирается без надписи с записью в журнал, наложения на субтитры и метку нет. При «Без призыва» — ничего.
Смена ВИДА у готовой записи пересобирает все её готовые клипы: по одной пересборке на клип из суточного лимита
(20), списание одним действием; если какой-то клип записи ещё собирается (в том числе меняется музыка) — отказ
`409` целиком; если остатка меньше числа клипов — отказ `429` целиком, призыв не меняется. Смена только ссылки —
без пересборки. Отказ пересборки отдельного клипа после постановки оставляет у него прежний файл (со старой
надписью) и квоту не возвращает — известное ограничение (ADR-017).

## NFR-UI-001 · Мобильная вёрстка принимается прибором

Интерфейс считается mobile-friendly, когда `scripts/check-responsive.sh` возвращает `0` на стенде: WebKit
(iPhone) и Chromium (Android), ширины 320–1440, маршруты `/`, `/c/{code}`, `/g/{code}`, кабинет и экран
записи. Правила: нет горизонтального скролла; основные цели ≥ 44×44, прочие ≥ 24×24; axe без
serious/critical (с `target-size`); поля ввода ≥ 16 px; текст растёт при увеличении до 200 % без скролла.
Методика — навык `responsive-ui`; решения — OWN-016. Базовая линия 25.09.2026 —
`docs/measurements/2026-09-25-responsive-baseline.md`.

**Дополнение 25.09.2026 (фича 24 `mobile-audit-fixes`).** Правило R9: основное действие виден без
прокрутки на 390×844, 375×667, 360×740 — кнопка входа на `/`, «Сделать свои клипы» на `/c/{code}`.
Статус: **выполнено** — прибор по стенду `https://clipmkr.ru` дал код 0 (97 проходов, WebKit+Chromium),
замер — `docs/measurements/2026-09-25-responsive-baseline.md`, раздел «После фичи 24». Композицию
десктопа прибор не проверяет — приёмка дополнительно по скриншотам `width-1440`.

**Дополнение 25.09.2026 (фича 25 `dark-theme`).** FR-LOOK-008 **воплощено**: тёмная тема основная (по
умолчанию для всех), светлая — явным выбором (cookie `n5_theme`). Прибор проверяет обе темы: код 0,
194 прохода по стенду. Значения палитры свои (закономерности источника, не его цвета).

**Дополнение 25.09.2026 (фича 26 `clip-card`).** FR-RESULT-002 **выполнено полностью**: на карточке три
действия «Скачать», «Ссылка», «Гостю». FR-GROWTH-001 (призыв поделиться на первой карточке в первом экране)
**не выполнено**: на телефоне панель первой карточки на 330–500 px ниже первого экрана (замер
`docs/features/clip-card/screens/`); переносится на шаг 6 (перекомпоновка экрана записи).

**Дополнение 26.09.2026 (фича 29 `progress-ribbon`, код; выкатки нет — черновик исполнителя, ждёт ревью).**
FR-GROWTH-001 — **выполнено в браузерном наборе по настоящей разметке, по стенду НЕ проверено**: на 390×844, 375×667,
360×740 (WebKit и Chromium, обе темы) панель «Скачать · Ссылка · Гостю» первой карточки готовой записи кончается на
681 / 616 / 639 px — в первом экране (было 330–500 px ниже). Средства: лента стадий в готовом состоянии одной
строкой, `video_id` под «Подробнее», h1 1,5rem, межсекционные зазоры 16 px, кадр карточки `max-height:32svh`.
Статус записи — лента стадий «Загрузка → Расшифровка → Выбор → Монтаж» с тремя состояниями задачи и «нет ответа»
(`docs/long-job-contract.md`, дополнение). R9 прибора добавлен для экрана записи (прогон со входом). Ограничение:
R9 по стенду проверяет только готовое состояние; «идёт» и «отказ» — браузерным набором по разметке.

## FR-LOOK-007 и FR-LOOK-011 · Первый экран лендинга с демо-клипом

**Дополнение 26.09.2026 (фича 28 `landing-demo`, код; выкатки нет — черновик исполнителя, ждёт ревью).**
На первом экране лендинга — готовый вертикальный клип из настоящей записи 88 мин (витрина `CTDUUG`, выбор владельца
25.09): видео 9:16 с кнопкой воспроизведения, без автозвука и без загрузки до нажатия, подпись «88 мин разговора → 7
клипов» и ссылка «Открыть клип» на `/c/CTDUUG` (там призыв автора). На телефоне порядок: заголовок → демо → кнопка
«Попробовать бесплатно» → форма входа ниже (кнопка прокручивает к форме и ставит фокус в поле почты); на ширине
от 600 px демо в левой колонке под текстом, форма справа, кнопки нет. На 390×844, 375×667 и 360×740 без прокрутки
видны постер ЦЕЛИКОМ и кнопка (видео шириной `clamp(7rem, 38vw, 9.5rem)`; на 375×667 — 143×253 px, кнопка кончается
на 542 px из 667 — замер браузерного набора по настоящей разметке, не стенда). Текст владельца (надзаголовок, H1)
не изменён. Файл витрины отдаётся ТОЛЬКО для клипов закрытого набора в коде (ADR-018).

- **FR-LOOK-011 — воплощено частично.** Есть: надзаголовок (на ≥ 600; на телефоне скрыт, как и до фичи) → H1 →
  пример «длинная запись → короткие клипы» с длительностью (демо с подписью «88 мин разговора → 7 клипов»).
  Нет: «H1 в одну строку с числами» (текст владельца не менялся) и «одна строка действия ссылка + призыв +
  загрузить файл» — см. FR-LOOK-007.
- **FR-LOOK-007 — принятая формулировка расходится с реализацией, и это записано, а не скрыто.** Принято: «на
  первом экране поле „ссылка на видео“ и кнопка „загрузить файл“, регистрация — следующий экран». Реализовано:
  первый экран — демо и кнопка «Попробовать бесплатно», ведущая к форме входа/регистрации; загрузка — после входа
  (кабинет). Причины: (1) загрузка по ссылке VK/Rutube (FR-INGEST-003) `UNCONFIRMED` и в продукте не сделана —
  поле «ссылка на видео» было бы обещанием, которого продукт не выполняет; (2) приём файла до входа потребовал бы
  анонимной загрузки многогигабайтного файла без квоты аккаунта (квота `user_uploads` списывается ДО выдачи
  ссылок, ADR-002) — новая денежная и ресурсная поверхность, которую фича не открывает; (3) выбор владельца
  (OWN-016) для этого шага — «лендинг с демо-клипом», то есть показать результат до регистрации, а не действие.
  Сохранена суть источника «ценность — до регистрации»: результат (клип) виден до входа; действие — после.
- **Воронка — контролируемое изменение.** До фичи форма входа была в первом экране телефона (0 касаний до поля
  почты), теперь — ниже, до поля почты +1 касание «Попробовать бесплатно». Ставка — показанная ценность окупает
  касание; конверсию «визит → регистрация» наблюдать после выкатки.
- R9 прибора для `/` проверяет ОБА: кнопку `.landing-cta` и видео `.landing-demo video`
  (`FIRST_SCREEN_ACTIONS.selector` — строка или список).
