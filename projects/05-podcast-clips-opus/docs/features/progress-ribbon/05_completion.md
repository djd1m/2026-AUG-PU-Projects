# Фича 29 `progress-ribbon` — завершение (шаг 6 плана mobile-ui)

Постановка → проверка плана Sonnet 5 (`01_validate.md`, READY WITH FIXES, 12 правок: замер бюджета первого экрана
реальным рендером, порог 360 px, стадия отказа) → код Opus 5.5 (`07_code_report.md`) → ревью Sonnet 5
(`08_review.md`, APPROVE WITH FIXES: регресс отступов экрана ошибки кабинета) → правка координатора (плоская
разметка `dashboard/error.tsx`) → проверки → выкатка → прибор.

## Доказательства
| Проверка | Результат |
|---|---|
| Набор в образе (после правки) | `Test Files 109 passed (109)`, `Tests 975 passed (975)` |
| Браузерные тесты прибора | `Tests 134 passed (134)` |
| Мутации (молчание = «идёт», стадии, R9 экрана записи, storageState, контраст) | 8/8 красные → зелёные |
| Прибор по стенду, обе темы | **код 0**, 206 проходов (R9 экрана записи со входом), 0 нестабильных |
| Первый экран экрана записи, 375×667 | `screens/stand-iphone-se-dark.png`: «Клипы готовы · 4 из 4», первая карточка, панель «Скачать · Ссылка · Гостю» |
| Низ панели первой карточки | 681/844, 616/667, 639/740 px (было на 330–500 px ниже экрана) |

**FR-GROWTH-001 — выполнено** для готового состояния.

## Чего фича НЕ доказывает
- R9 на стенде проверяет только готовое состояние записи (фикстура `done`); вид идущей обработки — тестами разметки.
- Настоящий iPhone с панелями Safari человеком не проверен (эмуляция WebKit).
- `video_id` спрятан под «Подробнее» на всех ширинах, а не только на узком (наблюдение ревью, оставлено).
