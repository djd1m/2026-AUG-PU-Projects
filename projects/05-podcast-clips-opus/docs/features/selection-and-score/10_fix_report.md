# Фича 5 — исправления FS-001 и FS-002, раунд 2

Исправления по утверждённой постановке `09_fix_brief_codex.md` внесены; локальные ворота пройдены. Интеграционная приёмка остаётся integration owner, как предписано brief. Отклонённых требований нет.

## Учёт прогона

- RUN_ID: `20260922T201506Z-selection-fix-r2-166e0546`.
- Исходная ревизия: `166e05466879a17c2449ba561a66c2a3dddd098d`; дерево было чистым.
- Профиль: `compact-quality-first-v2`; содержательный риск XL (квота платных вызовов), план и разрешение — FS-001/FS-002 в brief. Механический роутер по шести указанным файлам: S, exit 0; это нижняя граница.
- Исполнитель: текущая сессия Codex (GPT-6 по метаданным роли); точный runtime model ID и effort: `null`, хост их не предоставил. Другие модели, fallback и делегирование не запускались. Независимое Anthropic-ревью остаётся integration owner.
- Телеметрия хранится в этом отчёте: brief разрешает менять из `docs/` только его, поэтому отдельные `run.json`, `events.jsonl` и work-record не создаются.
- Первая измеренная отметка: `2026-09-22T20:15:06Z`. Время предшествующего чтения: `null` (учёт начат поздно); полный elapsed/active: `null`. Токены, стоимость и billing basis: `null`, счётчики недоступны. Экономия не установлена.
- ROUTE/PLAN/VALIDATE: прочитаны brief, правила проекта, FR-SELECT-001 и архитектурная граница worker-llm. Перед IMPLEMENT scope подтверждён: атомарный захват, семантика честного количества, соответствующие тесты. Канон, миграции, зависимости и инфраструктура вне изменений.
- E2E preflight: `not_applicable`; Docker и интеграционные прогоны по brief находятся у integration owner.
- IMPLEMENT завершён; CHECK начат. Требуемые ворота: build, lint, typecheck, весь доступный npm test. Исходный конкурентный тест сохранён без изменений.
- CHECK, 20:16:01 UTC: до реализации обновлённый `selection.test.ts` дал 2 failed / 17 passed (exit 1): старая схема и частичное принятие ответа.
- CHECK, 20:17:19 UTC: целевые тесты дали 28 passed / 10 skipped (exit 0); интеграционная БД не настроена.
- CHECK, 20:17:40–20:17:43 UTC: две мутации FS-002 красные; мутация FS-001 пропущена. Все временные изменения восстановлены через `finally`.
- Привязка к исходникам: SHA256 `git diff -- apps packages tests` = `0b0eca68d289a22f560f48af7fe0023b981997323f7b9f16d5ef2f49ee6281b2`.
- Неизменность исходного конкурентного теста проверена побайтово относительно HEAD; SHA256 тела = `ca8c7d691771492a480e540b4f26ec8dbabac1e7dc87cdf4cbc1d8a5cbfa3367`. Первая команда сравнения ошиблась путём от корня Git; повтор с `HEAD:./tests/...` успешен.
- CHECK/INTEGRATE завершены `2026-09-22T20:19:58Z`. Измеренный интервал от первой отметки — 292 с (4 мин 52 с); он НЕ включает предшествующее чтение. Полная длительность до принятого результата неизвестна: приёмка owner ещё не выполнена.

## FS-001 — исправлено в коде, интеграционное подтверждение ожидается

`authorizeSelection` захватывает попытку условным `UPDATE job_attempt SET llm_dispatched=true … AND llm_dispatched=false RETURNING unit_count`. Пустой результат сразу останавливает дубль; квота проверяется только после успешного захвата и в той же транзакции. Проверка актуального фенса и блокировка `video` сохранены; порядок блокировок остаётся `video → job_attempt → quota`. Резерв `unit_count` читается из результата захвата, а не из потенциально устаревшего JOIN. Повтор, оплаченный `video.retry`, не списывается заново. Двухоператорная форма квоты не менялась, платный вызов остаётся вне транзакции.

Исходный тест `duplicate workers dispatch once, then accept once with stale-result fencing` не изменён.

Мутация (обе строки):

- Удалено `AND llm_dispatched=false`; `npm test -- tests/selection.integration.test.ts -t 'duplicate workers dispatch once'`: exit 0, **10 skipped**, красное НЕ получено — нет `DATABASE_URL`.
- Условие восстановлено; целевой интеграционный файл: **10 skipped**, зелёное НЕ доказано. Integration owner должен повторить неизменный конкурентный тест на PostgreSQL 16 с условием и без него.

## FS-002 — исправлено

В схеме `minItems=1`, `maxItems=8`; подсказка сохраняет цель 3–8 и явно разрешает вернуть меньше трёх без добора слабыми. Наш валидатор принимает честные 1/2, проверяет каждый кандидат до и после привязки к словам и отвергает весь ответ, если любой кандидат нарушает диапазоны или объяснения. Возвращённый пустой результат обрабатывается существующим отказом `no_fragments`; пустой успешный экран не создаётся. Сортировка, устранение пересечений и потолок восемь сохранены.

Проверки покрывают один честный фрагмент, два честных фрагмента и три кандидата с одним непригодным. Добавлены интеграционные проверки сохранения `clips_total=1` и отсутствия клипов/ссылок при невалидном ответе; здесь они пропущены вместе с остальными тестами БД. Проверяемые диапазоны не объявляются доказательством смысловой самодостаточности живого ответа модели.

Мутации:

- Возврат фильтрации плохих кандидатов вместо отказа всему ответу: `npm test -- tests/selection.test.ts -t 'never salvages|adapter path rejects'` → **2 failed**, exit 1.
- Возврат `minItems=3`: `npm test -- tests/selection.test.ts -t 'schema request'` → **1 failed**, exit 1.
- После восстановления: результат полного прогона указан ниже.

## Ворота и передача

Команда `npm run build && npm run lint && npm run typecheck && npm test` завершилась с **exit 0** на указанном снимке исходников:

| Проверка | Результат |
|---|---|
| build | Все workspace из build-скрипта, включая Next.js production build, успешно |
| lint | Статические правила: ошибок нет |
| typecheck | Workspace и тесты успешно |
| npm test | **186 passed, 79 skipped**, 24 файла passed, 9 skipped; длительность Vitest 55,01 с |
| Восстановление FS-002 | `selection.test.ts`: **21 passed**, обе ранее красные мутации восстановлены |
| Восстановление FS-001 | `selection.integration.test.ts`: **10 skipped**; это не доказательство устранения гонки |
| git diff --check | exit 0 |

Docker в этой среде недоступен по условиям brief; контейнеры не запускались. Не проверены конкурентный захват на настоящем PostgreSQL, красное/зелёное FS-001, сохранение честного числа клипов и остальные 79 интеграционных сценариев. Не выполнялись живые платные вызовы, E2E на стенде, независимое Anthropic-ревью. Исполнение Codex завершено; полная приёмка фичи не заявляется.

Для integration owner: после штатного preflight выполнить `docker compose --project-directory . --profile test run --rm --build test`. Затем временно убрать ровно `AND llm_dispatched=false` из захвата в `packages/db/src/selection.ts`, пересобрать тестовый образ и запустить неизменный тест `duplicate workers dispatch once, then accept once with stale-result fencing`: ожидается красное. Восстановить условие, снова пересобрать образ и подтвердить зелёное. Обе фактические строки результата ещё предстоит получить.

Из `docs/` изменён только этот отчёт. Коммит и публикация не выполнялись.

Status: completed
