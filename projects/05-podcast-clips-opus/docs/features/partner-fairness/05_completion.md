# partner-fairness — квитанция завершения

**Статус:** done · 24.09.2026 · решение владельца (OWN-011, RT-002 и RT-009) · ADR-014 · FR-PARTNER-004/005

| Часть | Коммит |
|---|---|
| Постановка; проверка плана READY WITH FIXES (скрипт не попал бы в образ; тест «различные аккаунты» не различал; разные часы БД и приложения) | `8babff9`, `ae8d42d` |
| Код (Codex `gpt-6-astra` medium, ≈195 тыс. токенов); ревью Anthropic — APPROVE | `a416718` |

**Проверки.** Полный набор в образе: **697 / 697**, 87 файлов (интеграционные — на настоящем
PostgreSQL). Мутации в образе без приёмочного режима: партнёрские 21 — все красные с дефектом и
зелёные после (у `explicit-replaces-cookie` красная фаза — исключение 409, а не AssertionError,
поэтому строгий критерий скрипта даёт итоговый код 1; мутация старая, из фичи 10, её интеграционная
часть в образе раньше не запускалась); удаления аккаунта 7 — все красные/зелёные, итоговый код 0.
Снята мёртвая мутация `guest-expiry` (её опорной строки и теста давно нет). На стенде: миграция 016
применена, ограничения на месте; скрипт разблокировки в контейнере `web` отвечает кодом 1 на
несуществующий код.

**Воспроизведение.** Миграция 016 — штатным `migrate.js`; команда оператора — `REPRODUCE.md` шаг 8а.

**Чего это НЕ доказывает.** Реальных партнёров и всплесков на стенде не было; разблокировка
проверена тестами и запуском на несуществующем коде, не на настоящей блокировке.
