# teaser-headline — квитанция завершения

**Статус:** done · 24.09.2026 · автономный режим (A-2409-04/05) · FR-RENDER-009 · ADR-013 ·
**ждёт подтверждения владельца на слух/глаз**

| Часть | Коммит |
|---|---|
| Постановка; проверка плана READY WITH FIXES (text_align требует ffmpeg ≥ 6.1; два теста не могли покраснеть; ошибка глифа превращалась в отказ клипа) | `8c0748e`, `b9b93ae` |
| Код (Codex `gpt-6-astra` medium, ≈150 тыс. токенов); ревью Anthropic — APPROVE WITH FIXES | `e4d06ba` |
| Правки: исчезновение из констант (мутация окна в образе ВЫЖИЛА до правки), NOT_EXECUTED → код 2 (≈62 тыс. токенов) | `1328a03` |

**Проверки.** Полный набор в образе (ffmpeg 8.1): **684 / 684**, 86 файлов; медиа-тест приманки
в образе выполнен (приёмочный режим валит прогон на любом пропуске). Мутация `window-pixels` в
образе: red exit 1 / green exit 0. Миграция 015 применена на стенде; эталоны `baseline.json` и
`music-only.json` не изменены.

**Воспроизведение.** Миграция 015 — штатным `migrate.js`; новых переменных и процедур нет.
Уже загруженной записи: `UPDATE video SET teaser=true` + перерендер.

**Чего это НЕ доказывает.** Удержание зрителя не измеряется; владелец заголовок ещё не видел.
