# music-bed — правки после REVIEW (Anthropic) и полного прогона в образе

Полный набор в тестовом контейнере (ffmpeg 8.1): **630/631**. Ревью: APPROVE WITH FIXES.
Сквозной музыкальный тест в образе ЗЕЛЁНЫЙ — формат `ebur128` 8.1 разбирается (находка ревью 1 закрыта прогоном).

Исправить, ничего сверх этого:

1. **Упавший тест `tests/music.test.ts` «off arguments equal main fixture byte for byte».** Нормализация
   `replaceAll(process.cwd(), '<ROOT>')` в контейнере (`cwd=/app`) задевает `/apps/…` и даёт
   `<ROOT><ROOT>s/worker/…`. Нормализовать только префикс пути: `process.cwd() + '/'` → `'<ROOT>/'`.
   Эталон `tests/fixtures/music-bed/baseline.json` НЕ менять. Проверить такие же нормализации в
   остальных новых тестах.
2. **Медиа-тест ветки с видеовходом** в `tests/music-media.test.ts`: исходник `testsrc`+`sine` в mp4,
   `renderClip({music:true})` на настоящем ffmpeg → событие `music_mix`, `music !== null`,
   длительность как у клипа, есть и видео-, и аудиопоток 1080×1920.
3. **Округлять `gain_db` до 0,1 дБ** в `prepareMusic` ДО использования в фильтре и в контракте
   (шум плавающей точки не должен попадать в хеш). Тест.
4. **Таймаут замера 2 мин** (`MUSIC_MEASURE_TIMEOUT_MS = 120_000`); таймаут замера → `music_skipped`,
   `reason:"measure_failed"`; отмена по внешнему `signal` по-прежнему пробрасывается. Тест.
5. **`Uploader.tsx`:** без записи Resume для выбранного файла галочка сохраняет текущее значение, а не
   сбрасывается в `false`. Тест.

Прогон: `npx vitest run` по затронутым файлам; мутации для 3 и 4 обеими строками. Не коммить.
Дописать в `docs/features/music-bed/07_code_report.md` раздел «Правки после REVIEW». Последняя строка
отчёта — ровно `Status: completed` либо `Status: failed` (Docker тебе недоступен — это не повод для
`failed`: полный прогон делает координатор).
