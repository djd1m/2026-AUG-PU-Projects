# SL-001 / SL-002 — отчёт исправления

Код исправлен; полная приёмка не завершена из-за недоступных интеграционных проверок и независимого ревью.

- **SL-001:** генератор, Compose и `.env.example` используют длину **6** по умолчанию. Явное значение `10` поддерживается. Чтение сохраняет множество **{6, 10}**, регистр нормализуется; 5 и 11 отвергаются с 404. Существующие записи не изменялись.
- **Правка канона §7:** в `docs/canon.md` добавлена отдельная строка «Длина короткого кода клипа»: 6 по умолчанию, алфавит из 32 знаков, совместимость с 10. В исходном §7 отдельного числа длины не оказалось — заменять несуществующую строку не стал. Порог размера шрифта не изменён.
- **SL-002:** двухстрочная геометрия удалена. Одна строка `КлипМейкер · <настроенный домен>/c/`, затем чёрный код в непрозрачном белом чипе; общий baseline. Порядок: scale/crop → ASS → плита → префикс → чип → код. После метки фильтров нет.
- `N5_PUBLIC_ORIGIN`, DNS и рабочее окружение не менялись. `clipmkr.ru` используется как вход тестов. QR не добавлялся.

## Кегль и геометрия

Измерения для 1080×1920, закреплённого `ClipMakerNarrow-Bold.ttf` и `clipmkr.ru`:

| Параметр | Результат |
|---|---:|
| Кегль | **73 px = 3,802 % высоты** |
| Префикс | **555 px** |
| Самый широкий код `WWWWWW` | **240 px** |
| Чип с отступами 16 + 16 | **272×84 px** |
| Плита с отступами 24 + 24, 16 + 16 | **875×116 px** |
| Запас до строгих 880 / канонических 972 | **5 / 97 px** |
| Типичный `AB3XK9`: плита | **804×116 px** |
| Площадь типичной / худшей плиты | **4,50 / 4,89 % кадра** |
| Положение плиты / чипа | **(54, 1573) / (633, 1589)** |
| Контраст префикса, чёрная подложка alpha 0,60 на белом | **≥ 5,74:1**, консервативный расчёт sRGB |
| Контраст кода в чипе | **21:1** |

Ширина получена суммой округлённых вверх advance каждого глифа; тест выбирает самый широкий символ всего алфавита. При 75 px ширина уже 893 px: страж ловит именно `893 > 880`. 68 px не потребовались. Приведённые 5,74:1 — расчёт, не новое измерение контраста экрана и не заимствованное измерение 5,93:1 из дизайн-квитанции.

Реальный FFmpeg MP4 с субтитрами и аудио прошёл проверку пикселей префикса и чипа, JPEG-превью и размеров кадра; отдельно прошёл рендер из MP3. [Кадр](../../../tests/artifacts/short-link-fix/frame.png) осмотрен: метка одна, текст читается, чип выровнен с префиксом, субтитры выше.

## Стражи и мутации — обе строки

Дефекты внедрялись в изолированной временной копии. JSON-квитанции подтверждают падения тестов, а не только ненулевые коды процесса.

| Страж / внедрённый дефект | Дефект возвращён | Код восстановлен |
|---|---|---|
| Дефолт 6 → 10 | 1 failed, exit 1 | 1 passed, exit 0 |
| Разрешать только 6: старая ссылка 10 → 404 | 1 failed, exit 1 | 1 passed, exit 0 |
| Ширина: 73 → 75 px, плита 893 > 880 | 1 failed, exit 1 | 1 passed, exit 0 |
| Последний слой: метка перенесена перед scale/ASS | 1 failed, exit 1 | 1 passed, exit 0 |
| Контраст: opacity 0,60 → 0,20, контраст 1,61 < 4,5 | 1 failed, exit 1 | 1 passed, exit 0 |
| Безглагольный текст: имя заменено на «Создай клип» | 1 failed, exit 1 | 1 passed, exit 0 |

Также прошли красно-зелёные пары существующих стражей fail-closed тарифа, создания ссылки до рендера, закреплённого fontfile, concurrency=1, резерва диска и условной публикации S3 — всего **12 мутаций**.

Команда: `N5_MUTATION_OUTPUT=tests/artifacts/short-link-fix/mutations-verified node scripts/test-render-mutations.mjs`.
Для ширины после переноса проверки ширины перед проверкой точного кегля повторена только `one-line-width` с выходом в `mutations-width`.
[12 пар](../../../tests/artifacts/short-link-fix/mutations-verified/results.json), [точное падение по ширине](../../../tests/artifacts/short-link-fix/mutations-width/one-line-width-red.json).

## Проверки и ограничения

| Проверка | Результат |
|---|---|
| Целевые unit-тесты | **40 passed** |
| `npm test` — все наборы | **388 passed, 137 skipped**, exit 0; **не полная приёмка** |
| `npm run lint`, `npm run typecheck`, `npm run build` | **exit 0** |
| Реальный FFmpeg | Видео, MP3, ASS, чип, JPEG — прошли в общем наборе |
| `check-env-wiring.sh`, `check-ports.cjs` | **exit 0 на случайном тестовом окружении**; исходный запуск wiring без окружения — exit 2 |
| Проверка конфликтов портов | Скрипт exit 0, но Docker socket недоступен; состояние всех контейнеров не подтверждено |
| Независимое ревью OWN-002 | Запрошен `claude-sonnet-4-6`, лимит 90 с, **exit 124**, ответ пуст; ревью не состоялось |
| Живой CJM / DNS / Cloud.ru / телефон | Не проверялись; `scripts/check-cjm.sh` отсутствует, публикация и домен вне задания |

137 пропусков относятся к PostgreSQL/Redis/S3: переменные тестовых сервисов отсутствуют, доступ к `/var/run/docker.sock` запрещён. Пропуски не названы успехом. SQL, схема, конкурентные операции и ростовые события не изменялись.

Однострочная метка сохраняет отказ при слишком длинном настроенном домене или коде. Со старым длинным origin часть новых рендеров может не поместиться: интеграцию нового бейджа необходимо согласовать со сменой origin владельцем. Чтение уже выданных десятисимвольных ссылок от этого не зависит. Рабочие значения окружения, включая явный `N5_SHORT_CODE_LENGTH=10`, эта правка не переписывает.

## Учёт и передача

- RUN_ID: `20260923T090033Z-short-link-fix`; профиль **compact-quality-first-v2**. Механический ROUTE — S/exit 0, содержательный — **M**: изменение существующего контракта и геометрии; использована утверждённая постановка.
- Исполнитель — Codex, семейство GPT-6 по метаданным хоста. Точный model ID/effort не раскрыт. Запрошенное Anthropic-ревью не даёт сведений о фактически запущенной модели. Переключение модели и независимое ревью не заявляются.
- Телеметрия и доказательства: [run.json](../../../tests/artifacts/short-link-fix/run.json), [events.jsonl](../../../tests/artifacts/short-link-fix/events.jsonl), [манифест исходников](../../../tests/artifacts/short-link-fix/source-manifest.json). Путь вне `docs/telemetry` выбран по ограничению постановки «docs — только свой отчёт».
- Измеренный интервал: **547.3 с**. Начальное чтение инструкций до записи не измерено; это нижняя граница, а не полная длительность задачи. `active_wall_ms`, usage, токены и стоимость — **null**; экономия не установлена.
- Исходная ревизия: `68277f1551c45d7e44bfc8f3901ac41b2e671509`; SHA-256 финального диффа кода/тестов: `5a50cd1f94cd37fab3bb2b3f585ee743834eebf3a5c4a574193abac6290edce7`. Отчёт и доказательства в этот снимок не включены.
- Повторы: один для получения непустых JSON-доказательств мутаций, один ограниченный для проверки падения именно по ширине. Успешные неизменные полный набор и сборка не повторялись.
- Для закрытия приёмки остаются полный интеграционный прогон и независимое Anthropic-ревью. Деплой и смена домена остаются у integration owner. Коммит не создавался.

Status: failed
