# pack-shot — правки после REVIEW (Anthropic: APPROVE WITH FIXES, 0 blocker)

Полный набор в образе (ffmpeg 8.1): **653/653**. Страж уровня на 8.1 зелёный (находка 5 закрыта).
Код уже закоммичен (`ccf5d73`) и развёрнут. Исправить только это:

1. **[medium] Форма вспышки «не раньше звука» не защищена.** `tests/pack-shot.test.ts:38` сравнивает
   `buildFlashFilter` сам с собой; медиа-тест смотрит только `t0−1` и `t0−0,5`. Добавить в медиа-тест
   кадр в `t0 − 0,1` (≈ рендер «только музыка») и кадр около `t0 + 0,4` (ещё светлее базы). Мутация в
   `tests/run-pack-shot-mutations.mjs`: заменить кусочную формулу на симметричную
   `max(0,1-abs(t-t_peak)/0.25)` → красный; обе строки в отчёт.
2. **[medium] `tests/music.test.ts:48-52, 61, 79, 112` зелёные случайно**: пэк-шот пропускается лишь
   потому, что настоящий ffmpeg падает на несуществующем `/tmp/input.wav`. Разрешаю правку: в
   `beforeEach` этого файла мокать `measureFullLoudness` отказом (и сбрасывать кэш сэмпла), с
   комментарием-допущением «эти тесты — про музыку без пэк-шота». Ни одно утверждение не ослаблять.
3. **[low] Контракт без огибающей и формы вспышки.** Экспортировать `FLASH_SHAPE_VERSION` рядом с
   `buildFlashFilter` и использовать его в `workers/render.ts` вместо строки `'v1'`; в `packshot`
   контракта добавить `envelope: STINGER_ENVELOPE`. Тест: смена огибающей меняет хеш.
   **Эталон `tests/fixtures/pack-shot/music-only.json` НЕ менять** (он про `packshot=null`).
4. **[low] Мёртвое поле `speech_lufs`** в `render/music.ts` — удалить, если не читается.
5. **[low] Дробный хвост пика вспышки**: считать `t_peak` как `(t0Ms + 50) / 1000`.

Прогон: `npx vitest run` по затронутым файлам + мутации. Не коммить. Дописать раздел «Правки после
REVIEW» в `07_code_report.md`; последняя строка — `Status: completed` либо `Status: failed` (Docker
недоступен — не повод для `failed`).
