# TR-001 / TR-002 — исправление границы транскрипта

Код исправлен; локальные проверки пройдены. Полная приёмка **не закрыта**: независимое
ревью Anthropic завершилось таймаутом без ответа модели, 134 интеграционных теста не запущены.

## Решение

Выбран третий вариант: подрезка окончания с обязательным журналированием. Допуск не константа:
`max(0, измеренная длительность MP3 − длительность видео)`. MP3 измеряется существующим
`probeFile` после `extractAudio`, до платных вызовов; таймаут ограничен оставшимся временем задачи.
В текущем исходнике `extractAudio` возвращает только путь и паузы, поэтому измерение добавлено
в воркер, а извлечение и чанкинг не менялись.

Сначала каждый чанк проверяется по своей длительности, затем склейка — строго по измеренному MP3.
Только прошедшим проверку словам/сегментам с началом внутри видео подрезается `end` до конца
видео. Затем повторяется строгая проверка по видео; существующая проверка в `acceptTranscript`
сохранена. Слово целиком за видео, неверные таймкоды и превышение измеренной границы отвергаются.
Это закрывает также повторную проверку БД, которую одной заменой аргумента склейки не исправить.

Число из брифа: `237,528 − 237,433 = 0,095 с` — измерение владельца, не новый наш замер.
В регрессионной фикстуре MP3 длится 237,55 с: допуск 0,117 с, окончание на +0,1 с принимается
и подрезается; +5 с отвергается. Изначальный реальный AAC→MP3 тест требовал появления хвоста;
это ошибочное предположение исправлено по брифу 11 (см. дополнение ниже).

## Диагностика

- Отказ поставщика без пригодных слов сохраняет `no_timestamps`.
- Отказ сборки — `TranscriptMergeError`, в БД существующая причина `stt_failed`, в журнале
  `stt_merge_failed`. Enum и миграции не расширяются за границы задания.
- Журнал содержит video_id, fence, вид элемента, индекс слова в склейке и chunk_index,
  начало, конец, проверяемую границу и `excess_seconds`. Слово идентифицируется индексами,
  текст пользовательской речи в служебный журнал не копируется.
- Подрезка пишет `stt_timestamp_clamped` с исходным окончанием, величиной подрезки и измеренной
  длительностью MP3. Успешные ответы провайдера остаются `success` в журнале расхода:
  ошибка сборки является отдельным событием и не переписывает историю платного вызова.
- Ошибка измерения производного MP3 не выходит как `ProbeError` к возврату слота исходного файла.

## Проверки и мутации

До реализации: `npx vitest run tests/transcription-order.test.ts -t TR-00` — **2 failed**:
+0,1 с браковалось, причина +5 с была `no_timestamps`.
Квитанция: [before-red.txt](../../../tests/artifacts/transcription-fix/before-red.txt).

| Внедрённый дефект | Красная строка | Восстановленная строка |
|---|---|---|
| Вернуть видеодлительность в склейку | `video-duration-red.txt`: 1 failed, exit 1 | `video-duration-green.txt`: 1 passed, exit 0 |
| Снять верхнюю границу `end > duration` | `no-audio-bound-red.txt`: 1 failed, exit 1 | `no-audio-bound-green.txt`: 1 passed, exit 0 |
| Вернуть сборке причину `no_timestamps` | `wrong-failure-red.txt`: 1 failed, exit 1 | `wrong-failure-green.txt`: 1 passed, exit 0 |

[Результаты мутаций](../../../tests/artifacts/transcription-fix/mutations.json) содержат заменённые
строки и SHA восстановленных файлов; [скрипт](../../../tests/artifacts/transcription-fix/run-mutations.py)
запускается из корня проекта. Все шесть квитанций лежат рядом.

- `npm test`: **349 passed, 134 skipped**, 44 набора прошли, 16 пропущены; 96,31 с.
- Реальный AAC→MP3, строгая граница сохранения, сегменты, укороченный звук, нечисловая
  длительность и отказ измерения — покрыты тестами.
- `npm run build`, `npm run typecheck`, `npm run lint`, `git diff --check`: exit 0.
  Первый typecheck обнаружил старые декларации shared в dist; после сборки повтор прошёл.
- Три проверки стыка с одноразовыми тестовыми переменными: exit 0. Без них получены 2/2/1
  из-за отсутствующих обязательных переменных; это не проверка готовности живого окружения.
- `claude -p --model sonnet --effort high --tools '' --no-session-persistence --output-format json`:
  exit 1, `Request timed out`, 183060 мс, `modelUsage={}`; независимого вердикта нет.

## Границы доказательств и передача

Файл владельца, платное распознавание, развёрнутый ffmpeg 7, PostgreSQL/Redis/MinIO и живой CJM
этим прогоном не проверены. Docker socket недоступен; попытка поднять отдельный PostgreSQL 16
в /tmp остановилась на запрещённой смене владельца каталога, сервер не запущен. Нужны
интеграционный прогон и обязательное OWN-002 ревью Anthropic на сохранённой ревизии.

Профиль `compact-quality-first-v2`, содержательный тир M (механический минимум S).
Исполнитель — текущая сессия Codex, хост указывает семейство GPT-6; точный model ID/effort
не предоставлен. Запрошенный reviewer — Anthropic `sonnet/high`; фактическая модель не подтверждена.
Измеренный интервал от начала учёта до отчёта: **564.8 с**; начальное чтение инструкций
предшествовало таймеру, полное время неизвестно. Токены и стоимость Codex — `null`; нулевой usage
неудачной попытки Claude относится только к ней и не означает бесплатность всей работы.

Телеметрия размещена в `tests/artifacts/transcription-fix/`, поскольку бриф разрешает в docs
только этот отчёт: [run.json](../../../tests/artifacts/transcription-fix/run.json),
[events.jsonl](../../../tests/artifacts/transcription-fix/events.jsonl),
[work-record.json](../../../tests/artifacts/transcription-fix/work-record.json).
Исходная ревизия: `27c11bd420979f00a2ed0483bcef81350524b139`.
Финальный снимок: `sha256:2d9e1888714ad3c253c1a4f1f5646a73f73bd0c7c32e2925f999260db7c23de7`;
[manifest](../../../tests/artifacts/transcription-fix/source-manifest.json) связывает все шесть файлов.
Чанкинг не изменён. Коммит и развёртывание не выполнялись.

## Дополнение по брифу 11 — детерминированная проверка хвоста

Убрано утверждение `audioDuration > videoDuration`: кодировщик не обязан создавать хвост.
Изменён только `tests/transcription.test.ts`; производственный код и остальные пять файлов
предыдущего снимка сохранены побайтово ([сверка](../../../tests/artifacts/transcription-fix/brief-11/continuity.json)).

- Малый хвост задан данными: видео 2,433 с, измеренная длительность звука в фикстуре
  2,533 с, `end` слова и сегмента 2,533 с. Результат принимается, оба окончания подрезаются
  до видео; проверяются две записи журнала с исходной границей и превышением 0,1 с.
  Эти числа — тестовая модель, а не новый замер и не допуск в производственном коде.
- Окончание на +5 с проходит локальную границу тестового чанка, но отвергается при
  склейке против длительности звука. Подрезок в журнале нет; ADR-003 сохранён.
- При равных длительностях 2,433 с транскрипт принят с неизменными таймкодами,
  включая окончание ровно на границе; журнал подрезок пуст.
- Реальный AAC→MP3 тест сохранён: обе длительности измеряются, а слово заканчивается
  внутри обеих границ. Появление хвоста больше не является условием успеха.

Мутация нового теста — вернуть видеодлительность в проверку склейки:
`words, segments }, audioDuration)` → `words, segments }, duration)`.
[Красный прогон](../../../tests/artifacts/transcription-fix/brief-11/mutation-red.txt):
**1 failed, exit 1**; [после восстановления](../../../tests/artifacts/transcription-fix/brief-11/mutation-green.txt):
**1 passed, exit 0**. Исходник восстановлен, SHA совпадает с предыдущим снимком.

Проверки этой попытки: целевые **7 passed**; `npm test` — **352 passed, 134 skipped**,
44 набора прошли, 16 пропущены, 95,04 с. `npm run build`, `npm run lint`,
повторный `npm run typecheck` и `git diff --check` — exit 0.
Первый typecheck упал из-за сохранённой копии теста с расширением `.ts` среди артефактов;
копия переименована в `.txt`, повтор прошёл без изменения исходников.
[Команды и коды](../../../tests/artifacts/transcription-fix/brief-11/checks.json).

Полная приёмка по правилам проекта остаётся незакрытой: OWN-002 требует независимого
Anthropic review, но попытка `sonnet/high` остановлена после 60 с без вердикта
([квитанция](../../../tests/artifacts/transcription-fix/brief-11/review-result.json)).
Интеграционные тесты здесь пропущены; `check-env-wiring.sh` — exit 2,
`check-port-conflicts.sh` — exit 1: compose-конфигурация не прочитана.
`scripts/check-cjm.sh` отсутствует; живой CJM и развёртывание не выполнялись.
Три функциональных сценария брифа выполнены, финальный статус ниже относится ко всем
обязательным воротам, а не к наличию оставшегося дефекта теста.

Профиль `compact-quality-first-v2`, тир этой правки **S**, один исполнитель Codex
(хост указывает GPT-6; точные model ID и effort недоступны). Запрошен reviewer
Anthropic `sonnet/high`, фактическая модель не подтверждена. Измеренный интервал
возобновлённой попытки до отчёта — **328.7 с**; начальное чтение инструкций
до таймера в него не входит, полная длительность неизвестна. Токены и стоимость — `null`.
Сохранён RUN_ID `20260923T062900Z-transcription-fix`, добавлены события попытки `brief-11`:
[run.json](../../../tests/artifacts/transcription-fix/run.json),
[events.jsonl](../../../tests/artifacts/transcription-fix/events.jsonl).
Новый снимок: `sha256:c5269d745a31df5a3cb41b4d2dffec2e98c45bdc6ca0db90b73801bb029ef595`,
[manifest](../../../tests/artifacts/transcription-fix/brief-11/source-manifest.json).

Status: failed
