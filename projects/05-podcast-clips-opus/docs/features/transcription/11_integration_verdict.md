# Интеграционная проверка фичи 4 `transcription` — вердикт integration owner

**Прогон:** профиль `test`, изолированный стек `n5-clipmaker-test`, с `--build`. 22.09.2026.

`Test Files 30 passed (30)` · `Tests 227 passed (227)` · `EXIT=0`

## Что доказано

- 227 тестов зелёные на настоящих PostgreSQL 16, Redis 7 и MinIO. Фича 4 не сломала фичи 1–3:
  195 тестов предыдущего прогона входят в эти 227.
- **Первый прогон упал ПРАВИЛЬНО:** compose отказался стартовать без `OPENROUTER_API_KEY`
  с сообщением, называющим переменную и последствие. Это сработал fail-closed, а не дефект —
  и это единственное доказательство того, что страж умеет падать.

## Чего НЕ доказывает

- **Живой вызов поставщика не выполнялся.** Codex прогнал только детерминированный фейк и честно
  это записал. Часовой файл, живая дикция, фоновая музыка, чанкинг ≤ 25 МБ на настоящем материале —
  не проверены.
- **Ревью фичи 4 не проводилось.** Код написан семейством OpenAI; по OWN-002 его обязано проверить
  семейство Anthropic. Зелёный прогон — утверждение о тестах, которые написал сам автор кода.
- Совместимость Cloud.ru не проверялась (DEC-A-005).

Status: completed
