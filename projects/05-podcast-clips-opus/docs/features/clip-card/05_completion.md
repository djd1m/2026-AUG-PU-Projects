# Фича 26 `clip-card` — завершение

Шаг 4 плана `docs/plans/2026-09-25-mobile-ui.md`. Постановка `00_brief.md` → проверка плана Sonnet 5
(`01_validate.md`, READY WITH FIXES, 9 правок) → код Opus 5.5 (`07_code_report.md`) → ревью Sonnet 5
(`08_review.md`, APPROVE WITH FIXES) → правки координатора (WCAG 2.5.3: `aria-label` «Ссылка на клип —
скопировать»; удалён мёртвый CSS `.score`) → проверки → стенд. Кросс-семейной проверки нет (OWN-017).

## Что сделано
- Плашка оценки на кадре (`.score-badge`, `aria-hidden`, контраст 14,76:1 тёмная / 7,56:1 светлая); текст
  «Оценка N из 99» остаётся в карточке.
- Строка трёх компонентов + `<details>` «Почему такая оценка» с объяснениями (закрыт по умолчанию).
- Панель `.clip-actions` «↓ Скачать · Ссылка · Гостю» сразу под кадром, цели ≥ 44 px; события
  `download`/`link_copy` без изменений.
- «Гостю»: чистая функция `addGuestPreselect`, проп `preselect {clip_id, nonce}` у `GuestPacks`, прокрутка
  (без плавности при `prefers-reduced-motion`) и фокус в «Имя гостя»; `guest.create` карточка не вызывает (ADR-008).
- Контейнерные запросы по ширине карточки.

## Доказательства
| Проверка | Результат |
|---|---|
| Набор в образе (после правок координатора) | `Test Files 101 passed (101)`, `Tests 857 passed (857)` |
| Браузерные тесты прибора | `Tests 50 passed (50)` |
| Мутация А: `addGuestPreselect` не отмечает клип | `1 failed | 15 passed` → `16 passed` |
| Мутация Б: эффект `GuestPacks` без вызова | `1 failed | 15 passed` → `16 passed` (страж по исходнику — jsdom нет) |
| Прибор по стенду, обе темы | **код 0**, 194 прохода, 0 нарушений |

## Чего фича НЕ доказывает / хвосты
- **FR-GROWTH-001 не выполнен:** на 390×844 первая карточка начинается на 634 px, панель — на 1178–1225 px;
  на 375×667 и 360×740 — на 330–500 px ниже первого экрана (`screens/`). Лечится перекомпоновкой экрана
  записи — шаг 6.
- Клик «Гостю» в живом браузере не симулирован (нет jsdom) — поведение проверено чистой функцией и по исходнику.
- Прибор не заходит на экран записи правилом R9 (первый экран) — только общие правила R1–R8.
