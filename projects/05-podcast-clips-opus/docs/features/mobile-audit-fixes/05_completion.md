# Фича 24 `mobile-audit-fixes` — завершение

Шаг 2 плана `docs/plans/2026-09-25-mobile-ui.md` (NFR-UI-001, OWN-016). Цикл: постановка
(`00_brief_codex.md`) → проверка плана Anthropic (`01_validate.md`, READY WITH FIXES, 12 правок) →
код Codex gpt-6-astra medium (`07_code_report.md`) → ревью Anthropic (`08_review.md`, APPROVE WITH FIXES)
→ второй круг Codex (`08_review_fixes_codex.md`) → проверки координатора → выкатка → прибор по стенду.

## Что изменено
- Типографика в `rem`, заголовки `clamp` с достижимым максимумом (`globals.css`, SSR-CSS `/c/` и `/g/`).
- Цели ≥ 44 px: шапка, «← Все записи», `select` музыки (`appearance:none`), «Послушать треки», ▶/■,
  ссылки `/c/`, `/g/`; чекбоксы — строка-подпись, не растягиваются (`GuestPacks.tsx` без инлайн-стиля).
- Поля и `select` ≥ 16 px; `--muted` `#56615a`; рамки ≥ 3:1; отключённый `select` выглядит отключённым.
- Первый экран: форма входа сразу под заголовком на узком экране; `.cta` над кадром на `/c/` (A-2509-01).
- Порядок: кабинет — загрузка первой, остатки компактно; экран записи — остатки после клипов.
- Удаление аккаунта — `.danger`; вертикальный ритм между разделами; `viewportFit: 'cover'` + safe-area;
  `prefers-reduced-motion`.
- Склонение — `apps/web/src/lib/plural-ru.ts` + `tests/plural-ru.test.ts` («ваш 1 момент», «ваши 2 момента»).
- Прибор: правило **R9** (`FIRST_SCREEN_ACTIONS`, сценарии `first-screen-WxH` в обоих движках, до
  `domRules`), фикстуры `tests/fixtures/responsive/r9-*.html`.

## Доказательства
| Проверка | Результат |
|---|---|
| Набор в образе | `Test Files 99 passed (99)`, `Tests 809 passed (809)` |
| Браузерные тесты прибора (контейнер Playwright) | `Tests 44 passed (44)` |
| Мутация R9: убрать `rect.bottom <= innerHeight` | `4 failed | 40 passed` → восстановлено `44 passed` |
| Мутация R9: «действие не найдено» → `[]` | `8 failed` → восстановлено `44 passed` |
| Прибор по стенду `https://clipmkr.ru` | **код 0**, 97 проходов, 0 нарушений (было: 18 R2, 7 R5, 2 R4, R8 на всех публичных) |
| Десктоп 1440 и 375×667 | скриншоты просмотрены координатором после второго круга |

## Найдено по ходу (в документы)
- Codex включил `isMobile` для всех браузерных фикстур — в Chromium это масштабирует страницу без
  `meta viewport`, и тесты R1/R8 перестали видеть дефект (2 красных). Координатор вернул обычный
  контекст, `isMobile` — только для R9.
- Первый круг дал код 0 при испорченном десктопе → в навык и `REPRODUCE.md`: код 0 необходим, но не
  достаточен, смотреть скриншоты 1440.
- Codex сократил текст лендинга ради первого экрана — откачено (A-2509-02).

## Чего фича НЕ доказывает
- Встроенные браузеры Telegram/VK — не проверялись (остаток OWN-016, в конце плана).
- Живой телефон — только эмуляция WebKit/Chromium.
- Предупреждения axe moderate (субтитры `<video>`, уникальность landmark, h1 в кабинете) — не отказ, остаются.
- Тёмная тема, карточка клипа, лендинг с демо, лента стадий — шаги 3–6.
