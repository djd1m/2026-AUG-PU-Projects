# pause-compaction — правки после REVIEW и полного прогона в образе

Полный набор в образе (ffmpeg 8.1): **723/724**. Ревью Anthropic: APPROVE WITH FIXES, 0 blocker.

1. **[high, найдено прогоном в образе] `tests/compaction-media.test.ts` «real ASS pixels appear at
   remapped word time»** падает в образе: `expected 0 to be greater than 1000`. Причина — тест
   рисует `ass=` БЕЗ `fontsdir`; на хосте находится системный шрифт, в образе системных шрифтов нет,
   libass не рисует ничего. Передавать `fontsdir='<SUBTITLE_FONTS>'` так же, как продукт
   (`buildFilterChain`), через `escapeFFmpegPath`. Утверждения не ослаблять.
2. **[low→fix] `workers/render.ts:410-424`**: `catch` вокруг анализа ловит и ошибки БД
   (`saveLoudnessMedian`), и временный сбой Postgres навсегда записывает план из одного куска.
   Откатываться на один кусок ТОЛЬКО при `FFmpegError` и ошибках пустой/непригодной огибающей;
   остальное пробрасывать (в `retryRender`). Тест: ошибка БД при записи медианы → план НЕ записан,
   попытка падает ретраибельно.
3. **[low→fix] `ffmpeg.ts:59-61`**: громкость РЕЧИ для музыки и пэк-шота мерить на исходном
   диапазоне `[start, end]` (длина источника), а длина трека, `atrim`/`afade` и `t0` — по D'. Тест.
4. **[low→fix] `workers/render.ts:408`**: `cut_plan` из БД проверять схемой (массив пар конечных
   чисел, возрастание, внутри `[start, end]`, ≥ 1 кусок); непригодный → понятная ошибка
   `Непригодный cut_plan` (не NaN в фильтре). Тест.
5. **[low, записать]** Несимметричный запас тишины у стыка (правый может быть ~25 мс, перекрестие
   гасит до ~55 мс начала слова) — записать в отчёт как известное свойство K ± 10 мс; не менять.
6. **[info]** Замеры детектора лиц внутри паузы сворачиваются в одну точку — проверь, что
   `planFollow`/`getFollowFilter` корректно обрабатывают сегменты нулевой длины (тест на сегменте
   длины 0).

Прогон: `npx vitest run` по затронутым файлам. Не коммить. Раздел «Правки после REVIEW» в
`07_code_report.md`; последняя строка — `Status: completed` либо `Status: failed`.
