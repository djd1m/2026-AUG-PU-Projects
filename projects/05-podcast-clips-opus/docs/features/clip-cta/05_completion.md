# Фича 27 `clip-cta` — завершение части 27a

OWN-018, ADR-017, FR-RESULT-006, A-2509-04. Цикл: постановка → проверка плана Sonnet 5 (`01_validate.md`, READY WITH
FIXES, разбиение 27a/27b) → код Opus 5.5 (`07_code_report.md`) → ревью Sonnet 5 (`08_review.md`, **APPROVE**) →
выкатка (миграция 020 на стенде) → прибор → правки координатора (фон `html`, перепроверка контраста в приборе).

## Доказательства
| Проверка | Результат |
|---|---|
| Набор в образе | `Test Files 103 passed (103)`, `Tests 904 passed (904)` |
| Браузерные тесты прибора | `Tests 74 passed (74)` |
| Мутации фичи (протокол, экранирование `/c/`, CHECK миграции ×2, набор, `SQL_ENUMS`, владелец, идемпотентность, `.cta`, порядок разбора…) | 12/12 красные → зелёные (`07_code_report.md`) |
| Мутация прибора «всегда понижать контраст до предупреждения» | `4 failed | 70 passed` → `74 passed` |
| Прибор по стенду, обе темы | **код 0**, 194 прохода; 1 предупреждение «нестабильный замер» (WebKit iPhone 13, `#upload-cta-kind`) |
| `/c/CTDUUG` | `<a class="cta" href="https://www.youtube.com/watch?v=ukZyNkgqVho" rel="noopener noreferrer nofollow" target="_blank">Смотреть полный выпуск →` |

## Найдено по ходу
- axe в мобильном WebKit считал фон страницы белым (фон был только у `body`) — 180 ложных нарушений → фон и у `html`.
- Мобильный WebKit изредка (≈ 1 из 6 проходов) отдаёт устаревший computed style у `<select>` в форме загрузки —
  axe видит «чёрный на чёрном», скриншот того же прохода — светлый текст. Простой пересчёт раскладки не лечит,
  лечит изменение таблицы стилей → прибор делает «толчок» и повторный замер; исчезнувшая находка — предупреждение
  с причиной. Первая гипотеза координатора («WebKit отбрасывает `background` с `var(url())`») НЕ подтвердилась —
  комментарий в коде исправлен.

## Чего 27a НЕ доказывает / хвосты
- Надписи в кадре и пересборки нет (27b). Модерации адресов нет: вредный https-сайт автора будет показан с доменом.
- Встроенные браузеры VK/Telegram не проверены.

# Часть 27b — надпись в кадре и пакетная пересборка (код; ревью и выкатки ещё нет)

Постановка `00b_brief.md`, правки 2–7 и 10 из `01_validate.md`. Исполнитель Opus 5.5, квитанция кода —
`07b_code_report.md` (итоги проверок и мутаций дословно). Ревью Sonnet 5 (OWN-017) и выкатка — за координатором.

## Чего 27b НЕ доказывает
- Надпись не смотрел человек на телефоне: пиксели проверены разностью кадров в образе (ffmpeg 8.1), не глазом.
- Живая пересборка на стенде не выполнялась: воркер и S3 в тестах — заглушки/MinIO; механизм публикации — ADR-016.
- Частичный отказ пересборки после постановки оставляет у клипа старую надпись (принято, ADR-017 п. 13).
