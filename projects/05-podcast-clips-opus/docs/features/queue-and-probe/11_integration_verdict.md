# Интеграционная проверка фичи 3 `queue-and-probe` — вердикт integration owner

**Прогон:** `docker compose --project-directory . --env-file <test.env> --profile test run --rm --build test`
в изолированном стеке `n5-clipmaker-test`, 22.09.2026. `--build` обязателен — без него `run` берёт
ранее собранный образ и молча проверяет старый код (наблюдено в этот же день на раунде 3 фичи 2).

## Итог

`Test Files 26 passed (26)` · `Tests 195 passed (195)` · `EXIT=0`

Отказов нет. Фича написана Codex в изолированном worktree параллельно раунду 3 фичи 2; слияние в
основную ветку прошло без конфликтов, включая общие манифесты.

## Что этот прогон ДОКАЗАЛ

- 195 тестов зелёные на настоящих PostgreSQL 16 и Redis 7 — то есть фича 3 не сломала ни фичу 1,
  ни фичу 2. Это главное, ради чего гоняются ВСЕ наборы, а не только свой.
- Код фич 1 и 2 после слияния работает в прежнем объёме: 153 теста предыдущего прогона входят в эти 195.

## Чего он НЕ доказывает

- **Ревью фичи 3 не проводилось.** Код написан семейством OpenAI; по режиму cross-family review
  (OWN-002) его обязано проверить семейство Anthropic, и эта проверка ещё не выполнена. Зелёный
  прогон — утверждение о тестах, которые написал сам автор кода, а не независимая оценка.
- Конкурентный прогон «два воркера на одном фенсе» присутствует в наборе, но его поведение под
  настоящей параллельной доставкой BullMQ отдельно не наблюдалось.
- Совместимость с Cloud.ru не проверялась (DEC-A-005).
- Ни один живой файл через конвейер не проходил: стадия транскрипции — фича 4.

Status: completed
