# clip-music-choice — квитанция завершения

**Статус:** done · 25.09.2026 · решение владельца OWN-015 · ADR-016 · FR-RESULT-005

| Часть | Коммит |
|---|---|
| Постановка; проверка плана **NOT READY**: механизм рендера привязан к `status='rendering'` — пересборка не запустилась бы или лишила бы файла; неудачная пересборка портила клип; сторож её не видел; новая переменная роняла stt/llm | `5281b5d`, `bed9fde` |
| Код (Codex `gpt-6-astra` medium, ≈204 тыс. токенов); ревью Anthropic **REJECT**: очистка по сроку вызывала удаление с префиксом, который отвергает настоящая проверка — в тестах стояла заглушка | `388742a` |
| Правки ревью и теста, падавшего в образе (нет `bash`) (≈145 тыс.); опора мутации `free-expiry` перенесена координатором | `388742a` |

**Проверки.** Полный набор в образе **786 / 786**, 97 файлов. Мутации в образе: выбора музыки — все
red/green (итог 0), удаления — 7/7. На стенде: миграция 018, превью `/music-previews/*.mp3` → 200.

**Воспроизведение.** Новая обязательная переменная `N5_LIMIT_USER_RERENDERS` (шаг 3 `REPRODUCE.md`),
миграция 018 — штатным `migrate.js`. Превью пересобираются `scripts/build-music-previews.mjs`.

**Чего это НЕ доказывает.** Смену на стенде владелец ещё не пробовал; подписанные ссылки владельца на
старую версию перестают работать сразу после публикации новой (≤ 900 с) — принято.
