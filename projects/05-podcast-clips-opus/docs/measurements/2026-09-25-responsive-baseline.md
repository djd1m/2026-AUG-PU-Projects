# Базовая линия мобильной вёрстки — 25.09.2026

Прибор `scripts/check-responsive.sh` (контейнер `mcr.microsoft.com/playwright:v1.60.0-noble`), стенд
`https://clipmkr.ru`, тестовые данные `scripts/seed-ui-fixture.mjs`. 85 проходов страниц: WebKit (iPhone 13,
iPhone SE, ширины) и Chromium (Pixel 7, ширины 320–1440); маршруты `/`, `/c/{code}`, `/g/{code}`,
`/dashboard`, экран записи. Ошибок выполнения 0. Код возврата **1** — нарушения есть (ожидаемо до шага 2).

| Правило | Уникальных элементов (error) | Где |
|---|---|---|
| R1 горизонтальный скролл | **0** | — |
| R2 основная цель < 44×44 | 18 | логотип/«Выйти» в шапке; `select` музыки (19 px в Chromium) и `summary` «Слушать треки» в карточках; ссылки на `/c/` и `/g/`; поле в форме гостя 36 px |
| R4 контраст (serious) | 2 | блок лимитов кабинета |
| R5 шрифт поля < 16 px | 7 | 3 поля входа (14 px), `select` музыки (13,3 px) |
| R8 текст не растёт при 200 % | все публичные страницы | весь CSS в px |

Предупреждения: R4 moderate/`incomplete` 255 (в т.ч. `video-caption` на `/g/`), R7 5 (px в `clamp`, `100vh`).
После ревью прибора ложные R3 (328 — кнопки в ЗАКРЫТОМ `details`) устранены.

**Вес образов.** `playwright` 1.60.0 + `playwright-core` + `@axe-core/playwright` ≈ 32 МБ пакетов в
`devDependencies`; браузеры при `npm ci` не скачиваются (у пакета нет install-скрипта). Рантайм-образы
получают devDependencies и раньше (`npm ci` без `--omit=dev`) — отдельная задача, не этого шага.
