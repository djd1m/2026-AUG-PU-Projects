# Прогон `REPRODUCE.md` с нуля — 24.09.2026

**Зачем.** Единственное настоящее доказательство, что проект поднимается по документам, а не по
памяти того, кто его строил. Выполнено в автономном режиме (план `docs/plans/2026-09-24-12h-autonomous.md`,
решение A-2409-03).

**Условия.** Та же машина, отдельный стек `N5_COMPOSE_PROJECT=n5-repro`: своя сеть, свои
PostgreSQL/Redis/MinIO, прокси только на `127.0.0.1:4182`. Код — **свежий `git clone` с GitHub**
(`e151fe6`), не рабочая копия. Ключ OpenRouter — тот же. Файл — запись владельца на 237 с (`.mov`,
91 МБ). После прогона стек, тома, сеть и образы `n5-repro-*` удалены.

## По шагам

| Шаг | Результат | Расхождение с документом |
|---|---|---|
| 2. Код | 16 миграций, последняя `015_teaser.sql` | нет |
| 3. Конфигурация | `check-env-complete.sh` → 0 (20 обязательных); `compose config` → ok; в `.env.example` есть каждая переменная документа | нет |
| 4. Тесты | **684 / 684**, 86 файлов, в изолированном стеке; база стенда не тронута | нет |
| 5. Первый запуск | миграции → 0; бакет создан; **`web` не стал здоровым** | **да:** `S3_PUBLIC_ENDPOINT=http://minio:9000` отвергнут («нужен URL с протоколом https»). Документ говорил «внешний адрес», но не называл требования `https` — исправлено |
| 5. Проверка | `/health` 200; хранилища наружу не опубликованы; прокси — только петля | `/c/ZZZZZZ` без `X-Forwarded-For` → **503** (fail-closed), с заголовком → 404. Добавлено объяснение |
| 6. Домен | `preflight.js` → 0 для `https://repro.test` | нет |
| 8. Приёмка | загрузка по API: 9 частей, `complete` → 202; `transcribing` → `selecting` → `rendering` → **`done`, 3 клипа за ≈ 4 мин** | диапазон `gain_db` −30…+12 неверен: измерено −35,7 и −35,5 (тихая речь −34,6, громкий трек −16,9). Исправлено на «не выше +12» |

## Что видно в клипах

- Музыка и пэк-шот: `music_mix` и `packshot_mix` на клипах; треки разные (громкость трека −16,9 и
  −15,2 LUFS на разных клипах).
- Заголовок: кадр в 1,0 с — две строки по центру вверху на подложке; 2,4 с — полупрозрачный;
  3,5 с — нет. Метка `repro.test/c/<код>` читается на всех трёх.
- Кадрирование: `mode: center`, «самое частое лицо только в 0,01–0,02 замеров» — запись снята
  телефоном с экрана ноутбука, лиц нет; центр законен.

## Наблюдение без объяснения

Контейнер `n5-repro-worker-video-1` пересоздан в 17:04:14 UTC посреди рендера (создан позже
остальных, `RestartCount=0`). Этим прогоном он не трогался; в журнале событий Docker в то же время —
чужие проверки контейнеров на машине. Причина НЕ установлена. Следствие: журнал первого клипа
пропал вместе со старым контейнером (событий 2 из 3), а задания второго и третьего клипа новый
контейнер подхватил из очереди — все три `done`. Непреднамеренная проверка того, что очередь
переживает замену обработчика.

## Чего прогон не доказывает

- Другая машина и другая ОС — не проверены: прогон на той же машине, с тем же кэшем слоёв Docker.
- Шаг 7 (выход наружу, TLS) — не выполнялся: наружу ничего не публиковалось.
- Cloud.ru — не проверен (MinIO).
