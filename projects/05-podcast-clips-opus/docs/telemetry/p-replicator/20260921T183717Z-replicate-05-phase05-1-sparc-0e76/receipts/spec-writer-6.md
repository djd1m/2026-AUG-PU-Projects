# Квитанция — spec-writer, раунд исправлений 2 (перевалидация)

**WORK_UNIT_ID:** spec-writer-attempt-6
**RUN_ID:** 20260921T183717Z-replicate-05-phase05-1-sparc-0e76
**Модель:** `claude-opus-5[1m]` (Opus 5, 1M context) · **Дата:** 2026-09-21
**Предыдущие квитанции:** attempt-1…5 (`spec-writer.md`, `spec-writer-2.md` … `spec-writer-5.md`)
**Основание:** `docs/validation-revalidation.md`, находки V3-R08 и V3-R10 (обе low).

## Что изменено (только `docs/Specification.md`)

1. **V3-R08 — FR-AUTH-001.** «Полный набор сценариев области AUTH (4 сценария, из них 2
   `@security`)» → «(8 сценариев, из них 3 `@security`)». Числа не назначены на глаз, а СЧИТАНЫ по
   самому разделу:

   ```
   awk '/^## Область AUTH/{f=1} f' docs/test-scenarios.md | grep -c '^Scenario:'  → 8
   awk '/^## Область AUTH/{f=1} f' docs/test-scenarios.md | grep -c '@security'   → 3
   ```

   Совпадает со сводной таблицей самого `test-scenarios.md` («Область AUTH (без US) | 8»). Вариант
   «сослаться без числа» отвергнут: число проверяемо командой, а ссылка без него не ловит
   расхождение при следующем расхождении — именно так и возникла эта находка.
2. **V3-R10 — FR-LIMIT-003.** В список «Без возврата» добавлен `refused_user_llm`; теперь там все
   четыре отказа по потолкам: `refused_user_minutes`, `refused_user_llm`,
   `refused_global_minutes`, `refused_global_llm`. Причина подтверждена по соседним документам, а
   не додумана: `Pseudocode.md` §Data Structures держит её в ЗАКРЫТОМ наборе из 16, шаг 5
   `ReadVideoProgress` раскладывает её в действие «повторить», `Refinement.md` несёт для неё
   отдельный edge-case. Изобретения новой причины не потребовалось.

## Проверка устаревших чисел (пункт 3 брифа)

Искал в `Specification.md` остатки прежних редакций — не найдено ни одного:

```
grep -n 'пять scope\|(пять)\|60 минут\|60 мин\|9 путей\|девять путей\|14 процедур\|
        четырнадцать процедур\|четыре разных текста\|9 публичных\|ровно 9\|ровно 14\|
        четыре scope\|четырёх scope'   → пусто
```

Действующие числа документа: `quota_counter.scope` — шесть, минуты на аккаунт — 90, тексты отказа
по потолкам — пять (шестой `scope` `user_upload_refunds` своего текста не имеет, это записано в
FR-LIMIT-003 при attempt-5). Маршрутов и процедур `Specification.md` числом не называет вовсе — она
ссылается на канон §5 и на конкретные имена (`POST /api/auth/register`, `POST /api/auth/login`,
`guest.revoke`), поэтому устареть этим числам здесь негде.

## Коды стражей

```
grep -c '^### FR-'                            → 34
grep -c '^### NFR-'                           → 6
grep -o 'FR-LOOK-0[0-9][0-9]' | sort -u | wc  → 15
node ../../.claude/hooks/check-look-trace.cjs .     → 0
node ../../.claude/hooks/check-metric-source.cjs .  → 0
node ../../.claude/hooks/check-canon.cjs .          → 0
node ../../.claude/hooks/check-file-ownership.cjs . → 0
```

## Файлы

| Файл | Строк | sha256 |
|---|---|---|
| `docs/Specification.md` | 898 | `3ac09f3dcccc354f292ac0ed0452de0e7f14f8bafd68b6f8e54354e303407532` |
| `docs/PRD.md` | 291 | `3129d633a65b5b43dd315d7c3b516c6534ec1bd5216e01a19cc79edfb10f475c` (не изменялся) |

Число строк не изменилось: обе правки внутристрочные.

## Что осталось непроверенным

1. **Число «8 сценариев, из них 3 `@security`» устареет молча, если `test-scenarios.md` пополнят.**
   Детерминированной проверки на это соответствие в пакете нет, и я её не заводил: это правка
   одной строки, а не мой файл. Если раздел AUTH будет расти, счёт стоит либо пересчитывать в том
   же раунде, либо снять число из ссылки — но тогда исчезнет и сигнал о расхождении.
2. **Полноту списка «Без возврата» я сверял с набором причин, а не с алгоритмом возврата.** Что
   `algo-writer` действительно НЕ возвращает слот на всех четырёх `refused_*`, видно только из
   `Pseudocode.md`; требование теперь это предписывает, совпадение не проверял.
3. **Остальные пункты квитанций attempt-3…5 в силе:** шестой `scope` в `CHECK` и физической модели,
   транзакционная связка возврата со счётчиком `user_upload_refunds`, проектные пороги `i ≥ 4`,
   `conv% ≥ 1 %`, «расхождение ≤ 10 %».

Status: completed
