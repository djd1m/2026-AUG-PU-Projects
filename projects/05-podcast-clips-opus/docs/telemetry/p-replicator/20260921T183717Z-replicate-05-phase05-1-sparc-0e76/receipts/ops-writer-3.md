# Квитанция — ops-writer-3 (Phase 2 раунд исправлений)

**WORK_UNIT_ID:** ops-writer-attempt-3
**RUN_ID:** 20260921T183717Z-replicate-05-phase05-1-sparc-0e76
**Модель:** Sonnet 5 (claude-sonnet-5) — подтверждено system-reminder текущей сессии.

## Что прочитано заново (целиком)
docs/canon.md (разморожен DEC-A-010, §4/5/6/7 изменены) · docs/decisions-autonomous.md
(DEC-A-007…014 целиком) · docs/Architecture.md (Component Breakdown, External Dependencies, Data
Architecture, Reconciliation with Pseudocode) · docs/ADR.md (ADR-001 п.7, ADR-002 п.2/4) ·
docs/Specification.md (FR-INGEST-002, FR-LIMIT-001…003, FR-AUTH-001, FR-RENDER-002 `@security`,
новые SC-US-003-3/005-3/005-4, §5 US-003/US-005/US-011) · docs/validation-coherence.md — 976 строк,
целиком; построчно разобраны V2-R01, R02, R03, R06, R08, R11, R12, R14, R15, R16, R20, R21, R22,
R26, L1 (владелец каждой находки сверен по итоговой таблице строк 935–959).

## Правки docs/Refinement.md
1. Трассировка: добавлены SC-US-003-3, SC-US-005-3, SC-US-005-4; заголовок и вступление «Test
   Cases» — 32 → 35. `comm -23` между Specification.md и Refinement.md пуст (два оставшихся
   совпадения `SC-US-012-`/`SC-US-013-` — ложные срабатывания regex на прозе `SC-US-012-*`/
   `SC-US-013-*` в Specification.md §5, не реальные пропущенные идентификаторы — проверено вручную).
2. Новая таблица «Тесты «сначала красное» на исправленные блокеры Phase 2» — 11 строк (а)–(к),
   каждая со слоем и внедряемым дефектом, покрывает все перечисленные координатором находки
   (V2-R01, R02, R03, R06, R08→нет, R11→нет как отдельная строка ops-writer, R12, R14, R15, R16,
   R20, R21) — R11 не тестируется здесь отдельным пунктом: это canon/Architecture правка (владелец
   arch-writer/координатор), Refinement уже отражает её следствие в Edge Cases (probe в worker-stt).
3. Edge Cases Matrix: 4 строки исправлены (звуковая дорожка, 1:59, 90:01, ffprobe-таймаут — все
   теперь называют probe в `worker-stt` ПОСЛЕ скачивания, ДО списания минут, и возврат слота
   `user_uploads` по DEC-A-014); 3 новые строки добавлены («два слишком длинных файла подряд»,
   `N5_PUBLIC_ORIGIN` не задана, третий вызов `user_llm` за сутки); «Redis недоступен при постановке»
   уже существовала с первого раунда — не дублирована. Итого 39 → **43 строки**, счётчик в тексте
   обновлён.
4. Числа: `60` → `90` минут везде, где это была граница FR-LIMIT-001 (Обязательные конкурентные
   прогоны п.1, Gherkin `@upload-to-clips`); квота — «4 ключа» → «5 ключей» (`user_llm`) в Testing
   Strategy и в списке конкурентных прогонов; добавлен п.5 «Уникальность `guest_opened`» в тот же
   список (V2-R12).

## Правки docs/Completion.md
5. **V2-R08.** Monitoring: явно названо, что реальный канал ОДИН — Telegram владельцу, и он обязан
   получить строку инвентаря; `PagerDuty`/`Slack`/`Email` — обязательные строки шаблона, не
   подключённые каналы. Отдельный абзац: доставка ФАЙЛА (FR-RESULT-003) и доставка СООБЩЕНИЯ —
   разные строки инвентаря; FR-RESULT-003 `UNCONFIRMED`, в неделю не входит. То же самое продублировано
   короче в «Что живо на конец недели».
6. **V2-R26.** Новый пункт Pre-Deployment: сверка по Admin API требует организационного аккаунта и
   `ANTHROPIC_ADMIN_KEY` У ОПЕРАТОРА (не у сервисов compose, не в каноне §6); без него — только
   ручное измерение, названное ручным.
7. Pre-Deployment: `N5_PUBLIC_ORIGIN` добавлена в список обязательных переменных без дефолта (у
   `web` и `worker-video`), с отдельным пояснением цены ошибки (метка в пикселях). Lifecycle
   `Expiration` переписан: ТОЛЬКО `clips/free/`, `clips/paid/` не трогается (V2-R24). «Четыре
   потолка» → «пять потолков» (`user_llm`), «ЧЕТЫРЬМЯ» → «ПЯТЬЮ» прогонами. Handoff: «39 edge cases»
   → «43 edge cases» (V2-L1, было доказанное расхождение 39 против фактических 40 строк на тот
   момент — теперь оба числа синхронизированы на актуальном 43).

## Правки docs/Final_Summary.md
8. Technical Approach: добавлено предложение о `worker-stt`/probe (DEC-A-011) и о `N5_PUBLIC_ORIGIN`
   без дефолта. Risks & Mitigations: добавлена строка «отказ по длительности приходит после
   скачивания воркером, слот `user_uploads` возвращается при отказе по свойствам файла и не
   возвращается при отказе по потолку» (DEC-A-011/014) — новая строка, а не замена: строки «два
   длинных файла» в Final_Summary до этого раунда не было физически (искал `grep -n` — пусто),
   формулировка координатора добавлена как есть. Число 90 «на аккаунт в сутки» уже отражено в
   Timeline (ссылкой на Specification §7, не пересказывается числом) и в Success Metrics (не
   тронуты, как предписано). Упоминание «шестидесяти минут» в Target Users НЕ правилось: это
   описание типичной ДЛИТЕЛЬНОСТИ ЗАПИСИ персоны Сергея («час в неделю»), а не суточная квота
   FR-LIMIT-001 — правка исказила бы персону, а не поправила число.

## Самопроверка (коды возврата и числа)
- `wc -l docs/Refinement.md` → 304 (≤ 500); `docs/Completion.md` → 166 (≤ 300);
  `docs/Final_Summary.md` → 164 (≤ 200).
- `comm -23` SC-US Specification↔Refinement → пусто (кроме двух ложных срабатываний regex на
  прозе, не реальных идентификаторов).
- `grep -c 'Response time p99\|Error rate\|CPU usage' docs/Completion.md` → 3.
- `node ../../.claude/hooks/check-metric-source.cjs .` → exit 0.
- `node ../../.claude/hooks/check-canon.cjs .` → exit 0 (43 параллельных пишущих единиц — счётчик
  канона, не связан с 43 строками Edge Cases Matrix, совпадение случайное).
- `node ../../.claude/hooks/check-file-ownership.cjs .` → exit 0.
- Отдельная сверка: таблица Success Metrics в Final_Summary.md побайтово совпадает со Specification
  §8 (`diff` двух извлечённых блоков таблицы → пусто) — подтверждает, что правка 8 не тронула её.

## Что осталось вне области этого раунда (честно)
- V2-R02/R03/R11/R12/R13 — правка САМОГО Pseudocode/Architecture (SQL, ключи, колонки) — владелец
  algo-writer/arch-writer, не ops-writer; здесь только тесты, доказывающие исправленное поведение.
- V2-R04/R05/R07 (маршруты `register`/`revoke`, контракт входа, модель Sonnet 5) — уже отражены в
  Specification.md/canon.md/Architecture.md чужими единицами; Refinement/Completion эти детали не
  описывали напрямую и правки не потребовали (проверено grep'ом — расхождений не найдено).
- V2-R18/R19/R23/R25/L2/L3 (medium/low, не входят в явный список координатора) — не трогались.

Status: completed
