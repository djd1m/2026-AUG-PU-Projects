# Квитанция — spec-writer

**WORK_UNIT_ID:** spec-writer-attempt-1
**RUN_ID:** 20260921T183717Z-replicate-05-phase05-1-sparc-0e76
**Модель:** `claude-opus-5[1m]` (Opus 5, 1M context) — совпадает с назначением стадии 1 в
`dispatch-plan.md`. Уровень усилия изнутри исполнения не наблюдаем и потому не подтверждается.
**Дата:** 2026-09-21

## Что прочитано

`docs/canon.md` (целиком) · `docs/CJM_Variants.md` (целиком, включая решение владельца и вариант D) ·
`docs/source-product-profile.md` (целиком, таблица-семя FR-LOOK-001…015) · `docs/decisions-owner.md` ·
`docs/model-cost-contract.md` · `docs/long-job-contract.md` · `docs/dispatch-plan.md` ·
`start/REPLICATE-PROMPTS-PROJECTS.md` §05 · `start/REPLICATE-PROMPTS.md` строки 50–135 ·
`.claude/skills/sparc-prd-mini/SKILL.md` (Document Role Map Contract, машинные ключи, SYNTHESIS) ·
`.claude/skills/sparc-prd-mini/templates/prd.md` · `projects/04-calorie-vision-cal-ai/docs/Specification.md`
и `docs/PRD.md` (форма) · исходники стражей `check-look-trace.cjs` и `check-metric-source.cjs`
(чтобы писать под разбор, а не под догадку) · `docs/Architecture.md` раздел `## External Dependencies`
(см. расхождение 1).

Референс январского клона `scratchpad/opus-clone/docs/Specification.md` прочитан списком историй
US-01…US-14; автопостинг, brand kit, команда, BYOK и модели стороннего облака из него НЕ взяты.

## Что написано

| Файл | Строк | Содержимое |
|---|---|---|
| `docs/Specification.md` | 774 | §1 контракт · §2 29 FR · §3 5 FR-GROWTH с 15 Gherkin · §4 15 FR-LOOK · §5 14 US и 32 SC · §6 6 NFR · §7 Feature Matrix · §8 Success Metrics · §9 ответы на семена |
| `docs/PRD.md` | 287 | обзор, проблема, 4 персоны, JTBD, ценностное предложение, рамки, CJM D, механики роста, «что не копируем», переиспользование клона, Success Metrics, риски, зависимости, решения владельца |

Счёт идентификаторов: FR — 29 (порядок канона §3 сохранён посимвольно), FR-GROWTH — 5,
NFR — 6, FR-LOOK — 15 (12 принято, 3 отклонено с причиной), US — 14, SC-US — 32,
Gherkin-сценариев growth — 15 (по три на требование), `@security` — 5.
Дублей машинных ключей нет: каждый FR/NFR объявлен ровно одним заголовком уровня `###`.

## Самопроверка (коды и числа)

```
grep -c '^### FR-'                      → 34   (29 FR + 5 GROWTH, ожидалось 34)
grep -c '^### NFR-'                     → 6    (ожидалось 6)
grep -o 'FR-LOOK-0[0-9][0-9]' | sort -u → 15   (ожидалось 15)
grep -c '@security'                     → 5    (ожидалось ≥ 5)
node .claude/hooks/check-look-trace.cjs .    → 0   «все 15 обязательств прослежены либо отклонены с причиной»
node .claude/hooks/check-metric-source.cjs . → 0   «10 метрик: журнал 3, БД 5, внешний API 1, вручную 1»
node .claude/hooks/check-canon.cjs .         → 0
node .claude/hooks/check-file-ownership.cjs . → 0
```

## Расхождения с каноном и входами

1. **`docs/Architecture.md` и `docs/ADR.md` УЖЕ СУЩЕСТВОВАЛИ к моменту работы стадии 1** (отметки
   времени 19:29–19:33). `dispatch-plan.md` сводит стадии 2a/2b ПОСЛЕ стадии 1, на замороженную
   Specification. Фактический порядок обратный, и координатору это стоит знать: часть решений
   архитектуры принята до того, как требования были написаны. Мои файлы я не правил чужими руками
   и чужих файлов не трогал; владение соблюдено.
   Следствие для метрики: строка «сверка расхода» названа методом `/v1/organizations/cost_report`,
   ПОТОМУ ЧТО именно он уже стоит в инвентаре `## External Dependencies` (строка «Anthropic Usage &
   Cost Admin API»). Бриф предлагал придумать токен и передать его arch-writer — вместо этого я
   подстроился под уже существующий инвентарь, и страж отвечает 0. **Если arch-writer будет
   переписывать инвентарь, строка с этим методом обязана в нём остаться**, иначе
   `check-metric-source` немедленно даст 1.
2. **Половина расхода не покрыта программным отчётом.** Инвентарь подтверждает отчёт о стоимости
   только у поставщика выделения фрагментов; у поставщика транскрипции подтверждённой строки нет.
   Метрика названа одним методом, а вторая половина объявлена ручной сверкой открытием консоли —
   прямо в §8 и в PRD. Ручное измерение под вывеской инструментованного не заявляется.
3. **`CJM_Variants.md` называет 20 минут записи на пользователя в сутки, канон §7 — 60.** Побеждает
   канон (60), как предписано брифом. Там же в CJM осталась строка постановки «файлы — том Docker»,
   пересмотренная OWN-004; в требованиях использовано объектное хранилище.
4. **Персонального потолка вызовов LLM канон §7 не называет** — только суточные 20 на всех.
   `model-cost-contract.md` называет 2 на пользователя. Противоречия нет: 2 вызова на пользователя
   получаются из 2 загрузок в сутки (FR-LIMIT-003, канон §7), по одному вызову выделения на запись.
   FR-LIMIT-002 фиксирует глобальные 600 минут и 20 вызовов, FR-LIMIT-003 — 2 загрузки.
5. **FR-AUTH-002 переписан механизм-нейтрально.** Бриф в этом пункте ничего не предписывал, но
   исходная формулировка «подпись по сырым байтам постоянным сравнением» противоречила бы уже
   написанному `Architecture.md`, где вход Telegram сделан через Telegram Login (OIDC, JWT по JWKS),
   а старый HMAC-виджет архивирован. Требование теперь фиксирует ПОРЯДОК («подпись проверяется до
   того, как из данных что-либо прочитано и применено») и оставляет механизм архитектуре.

## Отклонения от брифа

1. **Success Metrics — 10 строк, бриф называл 6–8.** Перечисленное в брифе содержание даёт ровно
   десять: две цифры метрики недели раздельно (клипы и аккаунты), гостевая половина, i, conv%, p95,
   отказы по потолку, расход в сутки, сверка расхода, читаемость метки. Ужать можно было бы только
   склеив величины, которые бриф требует держать раздельно. Выбрано полное покрытие, диапазон
   нарушен сознательно.
2. **`docs/Specification.md` — 774 строки при цели ≤ 700.** Превышение 10,6 %. Источник роста: 29 FR
   по 3–8 строк с числами, 15 Gherkin-сценариев growth и 32 SC под 14 историями. Сокращать пришлось
   бы за счёт наблюдаемого поведения или числа сценариев; ни то, ни другое бриф сокращать не
   разрешал. Цель была объявлена как «цель», а не как потолок.
3. **FR-GROWTH-005 сначала получил четыре сценария** (два `@security` — накрутка переходов и
   самопереход владельца). Приведено к трём слиянием в один security-сценарий, как требует бриф
   («по ТРИ Gherkin-сценария каждому»).
4. **PRD — 287 строк при цели ≤ 350**, в рамках.

## Что осталось непроверенным

1. **Пороги `i ≥ 4` и `conv% ≥ 1 %` — мои, а не из канона.** Канон §7 даёт только две цифры недели
   (40 клипов от 10 аккаунтов; 30 % отправлено, 50 % открыто). Бриф требовал вести i и conv%
   раздельно, но числа не назвал, а метрика без числа неизмерима. Обе величины объявлены
   проверяемыми гипотезами прямо в §8; они нуждаются в подтверждении владельцем или в Phase 2.
2. **Порог «расхождение ≤ 10 %»** для сверки расхода — тоже проектный, из соседнего проекта 04.
3. **Соответствие требований алгоритмам не проверялось** — это стадия 2a (`algo-writer`) и ворота
   `REQUIREMENT:` в `Pseudocode.md`. Ни один FR не доказан реализуемым этим документом.
4. **Согласованность с `ADR.md`** прочитана только через `## External Dependencies` файла
   `Architecture.md`; остальные разделы обоих документов я не сверял с требованиями построчно.
5. **`check-growth-trace.cjs` и `check-handoff-manifest.cjs` не запускались** — по канону §2 они
   ожидаемо отвечают `2` (Phase 0 не выполнялась, вход `--from-docs`); запуск ничего бы не доказал.
6. **Страж `check-look-origin.cjs` не запускался.** Строки профиля несут статус `ЧЕРНОВИК`, а не
   `ГИПОТЕЗА`, поэтому промоушен `check-look-trace` не блокировал; вопрос происхождения строк —
   отдельная проверка и отдельная ответственность.

Status: completed
