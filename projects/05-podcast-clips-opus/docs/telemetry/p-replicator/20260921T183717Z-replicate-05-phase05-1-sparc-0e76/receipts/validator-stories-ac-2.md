# Квитанция — validator-stories-ac, раунд исправлений 2

WORK_UNIT_ID: validator-stories-ac-attempt-2
RUN_ID: 20260921T183717Z-replicate-05-phase05-1-sparc-0e76
Модель: Claude Sonnet 5 (заявлено средой выполнения).
Находка: V3-R03 (high, из `receipts/revalidator.md`). Правился ТОЛЬКО `docs/test-scenarios.md`;
`docs/validation-stories.md` не трогался, как предписано.

## Что прочитано

- `docs/canon.md` целиком (180 строк, разморожен на Phase 2, DEC-A-010) — 6-й scope `user_llm`,
  90 мин на аккаунт, 10 путей/15 процедур, измерение длительности в `worker-stt`.
- `docs/decisions-autonomous.md` DEC-A-010…016 целиком.
- `docs/Specification.md` (898 строк, ревизия `sha256:870424e1…`) — FR-INGEST-001/002, FR-RENDER-003,
  FR-LIMIT-001/002/003 (с их встроенными Gherkin-блоками), FR-AUTH-001 (со встроенным каноническим
  security-сценарием, строки 386–401), US-003/US-005/US-011/US-012/US-013 целиком, включая новые
  `SC-US-003-3`, `SC-US-005-3`, `SC-US-005-4`.
- `docs/telemetry/.../receipts/revalidator.md` (grep по `test-scenarios`/`V3-R`/`V2-R`) — сверил
  формулировку V3-R03 с оригиналом координатора и убедился, что других находок в файле,
  закреплённых за этой единицей, нет (V3-R01/02/04…11 принадлежат другим писателям).

## Что исправлено

1. **Ревизия.** `sha256:2831e1c6…` → `sha256:870424e1b63ee49a03b5cfab1643129f24007bddf5fb908497a3b0b4969db118`
   (текущая `docs/Specification.md`, 898 строк).
2. **Три новых критерия:**
   - `SC-US-003-3` — тег добавлен на уже существовавший сценарий «ноль пригодных фрагментов»;
     текст сценария переписан дословно под формулировку Specification.md (причина `no_fragments`
     вместо прежней прозы).
   - `SC-US-005-3` — новый сценарий: скачивание клипа free после 3 суток хранения → 404 с текстом,
     а не битый/пустой файл.
   - `SC-US-005-4` — новый сценарий: клип в `rendering` не блокирует скачивание готового соседа.
3. **`SC-US-011-1` переписан под 90 + 30** (было 60 + 30, что при новом потолке 90 не давало бы
   отказа: 60 + 30 = 90 ≤ 90). Добавлено упоминание измерения длительности в `worker-stt` после
   скачивания (DEC-A-011), чтобы сценарий не противоречил FR-INGEST-002/FR-LIMIT-001.
4. **Устаревшие числа/списки заменены по всему файлу:**
   - 60 → 90 минут на аккаунт в сутки (US-011-1); проверено `grep -n '\b60\b'` — пусто после правки.
   - Добавлен персональный потолок `user_llm` (2 вызова/сутки) как отдельный `error-handling`
     сценарий в US-011 — раньше файл знал только про `global_llm`, шестой `scope` канона не был
     представлен ни одним сценарием.
   - Вход и регистрация — почта и пароль (не одноразовая ссылка); проверено, что файл этого
     никогда не утверждал — правка не потребовалась содержательно, только явно назван маршрут
     `POST /api/auth/register` в шапке Feature-блока AUTH.
   - Измерение длительности — нигде в файле не было ложного утверждения про `upload/complete`;
     проверено `grep -n 'ffprobe\|upload/complete\|длительност'` — единственное совпадение не
     касалось механизма измерения.
   - 9 путей / 14 процедур — в файле явно не перечислялись, правка не требовалась; новый маршрут
     `POST /api/auth/register` и новая процедура `guest.revoke` упомянуты по месту (AUTH-раздел
     маршрут уже называл его до этой правки в happy-path Then; `guest.revoke` уже был в SC-US-006-3
     косвенно как «отозвать» — оставлено без изменений, так как raunda-2 инструкция не выявила там
     дефекта числом).
5. **Две новые механики DEC-A-014/015** — добавлен сценарий в блок US-001 (`@FR-LIMIT-003
   @edge-case`), дословно отражающий встроенный Gherkin-блок Specification.md (строки 359–369):
   возврат слота `user_uploads` при отказе по СВОЙСТВАМ файла и потолок 2 возврата/сутки
   (`user_upload_refunds`).
6. **Раздел «Область AUTH» приведён в соответствие с Specification.md.** FR-AUTH-001 теперь сама
   цитирует один канонический security-сценарий (строки 391–401) как «постоянный критерий приёмки»
   и объявляет число «8 сценариев, 3 `@security`» для этой области. Мои два прежних раздельных
   security-сценария (энумерация отдельно, лимит частоты отдельно) заменены на ОДИН, скопированный
   СЛОВО В СЛОВО из Specification.md (сверено построчно после правки — расхождений нет), и добавлен
   новый `@security`-сценарий «пароль не появляется ни в одном логе и ни в одном ответе», чтобы
   сохранить заявленное число 8/3.

## Итоговые числа (после правки, засвидетельствовано командами)

- Всего сценариев: 86 (growth 15 дословно + US-блоки 63 + область AUTH 8; было 82).
- По типам: `@happy-path` 26 · `@error-handling` 33 · `@edge-case` 22 · `@security` 14.
- Уникальных `SC-US-*` в `test-scenarios.md`: 35 из 35 реальных в `Specification.md` (два лишних
  совпадения `grep` с обеих сторон — литеральные `SC-US-012-*`/`SC-US-013-*` в прозе про
  сохранение идентификаторов, не настоящие критерии; известный артефакт regex ещё с раунда 1).

## Самопроверка

```bash
$ grep -o 'SC-US-[0-9]*-[0-9]*' docs/Specification.md | sort -u > /tmp/a
$ grep -o 'SC-US-[0-9]*-[0-9]*' docs/test-scenarios.md | sort -u > /tmp/b
$ comm -23 /tmp/a /tmp/b
SC-US-012-
SC-US-013-
```
Обе строки — regex-артефакт из прозы «`SC-US-012-*`/`SC-US-013-*`» (US-012/US-013, абзац про
DEC-A-013), не настоящие идентификаторы критериев; ни один РЕАЛЬНЫЙ `SC-US-nnn-k` не пропущен.
Тот же артефакт наблюдался и фиксировался как некритичный в раунде 1.

```bash
$ grep -c '@security' docs/test-scenarios.md
14
```
14 ≥ 5 (число FR-GROWTH) — выполнено.

## Что осталось вне периметра этой правки

V3-R01 (`model-cost-contract.md`), V3-R02/R04/R05/R06/R07/R09/R10 (`Refinement.md`, `Pseudocode.md`,
`Solution_Strategy.md`) и V3-R11 (манифест квитанций `arch-writer`) закреплены за другими единицами
и в `test-scenarios.md` не отражались и не отражаются — файл этой правки их не касается вовсе.

Status: completed
