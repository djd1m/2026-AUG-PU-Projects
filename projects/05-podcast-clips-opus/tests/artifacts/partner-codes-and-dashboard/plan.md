Owner brief authorizes implementation of the existing ADR-007/specification. Scope: code.apply, atomic burst block, self referral, partner.dashboard with five counters and mobile UI. Excludes features 11/12 and new routes/events. One implementation pass, focused corrections, full regression and mutation checks. Attempt budget: 30 minutes before reassessment. Exact API model/usage unavailable; system GPT-6. Cross-family review requested via Claude CLI, not substituted by author. PostgreSQL Docker blocked; local startup blocked by unavailable unprivileged execution.
