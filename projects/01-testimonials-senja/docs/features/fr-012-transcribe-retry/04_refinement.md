# FR-012 · Краевые случаи

| Случай | Ожидание | Почему |
|---|---|---|
| Первый `SttApiError` | `pending`, счётчик 1, срок ≈ +1 мин | FR-012.1 |
| Второй | `pending`, счётчик 2, срок ≈ +2 мин | FR-012.3 — задержка растёт |
| Третий | `failed`, счётчик 3 | FR-012.2 |
| Строка со сроком в будущем — единственная в очереди | `empty` | NFR-012.2 |
| Строка со сроком в будущем + другая готовая | берётся готовая | NFR-012.3 |
| Срок наступил | строка снова выбирается | AC-012.5 |
| Успех после двух неудач | один `transcript`, `completed`, счётчик 2 сохранён | NFR-012.1, NFR-012.5 |
| Не-`SttApiError`, попытка 1 | `ROLLBACK`; отдельной транзакцией счётчик 1 и срок; статус остаётся `pending`; исключение пробрасывается | NFR-012.4, NFR-012.7 |
| Не-`SttApiError`, попытка 3 | `failed` — «мы перестали пытаться», а не «провайдер отверг» | NFR-012.4 |
| Ядовитая строка + другая готовая | после планирования срока берётся другая | NFR-012.3 |
| **Медленный** клиент: пауза дольше задержки, затем `SttApiError` | записанный срок **больше `clock_timestamp()` после возврата** | **NFR-012.6** |
| БД недоступна на шаге учёта попытки | учёт не удался, исходное исключение НЕ потеряно | NFR-012.7 |
| `ROLLBACK` сам упал | соединение возвращается с ошибкой и уничтожается, а не отравляет пул | **NFR-012.8** |
| Два прогона подряд на одном пуле после не-`SttApiError` | второй выполняется нормально | AC-012.14 |
| Текстовый отзыв (`video_object_key IS NULL`) | не выбирается | регрессия, уже покрыта |

## Тест-стратегия

**Слой 1, поведение.** Все девять строк выше — на живой БД, с фиксированными числами,
прибитыми в тесте отдельно от кода.

**Слой 1, страж по исходнику** (AC-012.9): условие выборки содержит проверку
`transcript_next_attempt_at`. Поведенческий тест проверяет один прогон; страж — что
проверка вообще есть в запросе, а не выполнена после выборки.

**Испытание на способность падать.** Для каждого стража — внедрить дефект, убедиться
в красном, восстановить. Конкретно:

| Внедрить | Обязан упасть |
|---|---|
| Убрать условие срока из `SELECT` | NFR-012.2 (AC-012.4) |
| `attempts >= MAX` → `>` | AC-012.2 |
| `clock_timestamp()` → `now()` в `UPDATE` | **AC-012.11 и AC-012.13** — проверено прогоном. Критерий переписывался дважды: сравнение с моментом после возврата не ловило (ревизия 2), сравнение с `T_begin + BASE` тоже не ловило, потому что транзакция открывается ПОЗЖЕ `T_begin`. Ловит третья редакция: различает формулы длительность самого вызова |
| `SELECT clock_timestamp()` отдельным запросом до `ROLLBACK` | **AC-012.14** — соединение возвращается в пул отравленным |
| `release(err)` → `release()` при неудавшемся `ROLLBACK` | **AC-012.14** |
| Убрать учёт попытки в ветке не-`SttApiError` | AC-012.8, AC-012.12 |

Последняя строка — суть находки B-2: критерий, зеленеющий при двух взаимоисключающих
поведениях, не является критерием.
