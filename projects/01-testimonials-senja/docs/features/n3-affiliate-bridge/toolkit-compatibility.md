The shared root toolkit remains authoritative. No local toolkit copies or second orchestrator are required. Run the N3 source-bound wrapper:

`node projects/03-affiliate-rewardful/scripts/check-feature-catalog.mjs projects/01-testimonials-senja docs/features/n3-affiliate-bridge`

It stages byte-identical selected feature documents, passes root regular role-map sources explicitly, hashes inputs and preserves the existing pinned p-replicator1.13.2 compatibility wrapper. New-feature planning gates pass6/6. This is not whole-project PASS: historical P1 catalog still has28 traceability gaps and11 inconclusive report/scenario contours, preserved in N3 run evidence/p1-legacy-gates.txt. Negative fixtures prove exit1 on broken traceability/scenarios and exit2 on invalid input. Temporary root-vendor mirrors created during diagnosis were removed after replacing them with explicit role-map arguments.
