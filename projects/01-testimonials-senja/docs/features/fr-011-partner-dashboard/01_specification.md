# FR-011 · Спецификация: партнёрский кабинет

> **Ревизия 2.** Первая редакция получила 🔴 — 3 блокера, 6 высоких, 10 средних.
> Самый едкий: документ **назвал** класс «проверка изоляции молчит, когда своим объявлен
> чужой» и тут же **повторил его вариацию** — ни один из пятнадцати критериев не краснел,
> если в cookie положить публичный код вместо секретного токена. Разбор — `validation-report.md`.

## Проблема

`getPartnerCohortDashboard` написана (`lib/partner.ts:132`), покрыта тестами и **не вызывается
ниоткуда**. Роадмап прямо фиксирует причину: функция «предполагается вызываемой из админки
владельца продукта, способ аутентификации партнёра не описан». Партнёр не видит ни своей
когорты, ни своих начислений — он узнаёт о них, только спросив владельца.

## Отступление от роадмапа: адрес `/partner/[code]` использовать НЕЛЬЗЯ

Роадмап предлагает файл `apps/web/src/app/partner/[code]/page.tsx`, то есть адрес,
параметризованный партнёрским кодом. **Это раскрыло бы заработок партнёра любому, кто видел
его реферальную ссылку.**

Партнёрский код — значение **публичное по назначению**, и это не предположение, а два
работающих пути в коде:

| Путь | Где | Кто видит код |
|---|---|---|
| Промокод, который вводит пользователь при регистрации | `referral.ts:33-38` | каждый приглашённый |
| Метка в реферальной ссылке, кладущаяся в cookie | `referral.ts:42-44` | каждый перешедший по ссылке |

Партнёр распространяет код по своей аудитории — в этом весь смысл. Сделать его же ключом к
странице с суммами начислений означает опубликовать эти суммы вместе с ссылкой.

Поэтому вводится **отдельный секрет**, не совпадающий с кодом: `dashboard_token`.

## Почему секрет не в адресе, а в cookie

Простейшая форма — `/partner/d/<token>` — плоха тремя утечками, каждая из которых
наблюдалась в реальных продуктах:

1. **`Referer`.** Любой внешний ресурс на странице (шрифт, картинка, счётчик) получает
   полный адрес в заголовке `Referer`.
2. **История и синхронизация браузера.** Адрес попадает в историю, а с ней — в облачную
   синхронизацию и в автодополнение на чужом устройстве.
3. **Журналы прокси.** Caddy пишет путь запроса; секрет оседает в логах, к которым доступ
   шире, чем к самим начислениям.

Ни одна из трёх не касается тела POST-запроса. Поэтому: партнёр открывает `/partner`, вводит
токен в форму, ответ ставит **httpOnly-cookie**, дашборд читает её. Та же дисциплина, что у
сессий владельца, и по той же причине.

## Функциональные требования

| # | Требование |
|---|---|
| FR-011.1 | У партнёрского кода есть отдельный секрет `dashboard_token`, не совпадающий с самим кодом |
| FR-011.2 | Токен хранится **хешем**; исходное значение возвращается один раз при выдаче |
| FR-011.6 **[v2]** | Утраченный токен **ротируется в той же строке**, а не перевыпуском кода | Перевыпуск создаёт НОВУЮ строку `partner_codes` с новым `code`, а все начисления привязаны к `partner_code_id` старой (`partner.ts:146,153`). Партнёр увидел бы нули вместо своей истории, а прежние реферальные ссылки перестали бы быть его ссылками. Первая редакция предлагала именно перевыпуск — в двух принимаемых рисках сразу |
| FR-011.3 | `POST /api/partner/session` принимает токен и ставит httpOnly-cookie, ограниченную путём `/partner` |
| FR-011.4 | `/partner/dashboard` показывает когорту и начисления **того** партнёра, чей токен предъявлен |
| FR-011.5 | Отозванный партнёрский код (`status = 'revoked'`) доступа к кабинету не даёт |

## Нефункциональные требования `@security`

| # | Требование | Почему несущее |
|---|---|---|
| NFR-011.1 **[v2]** | Партнёр определяется **только** предъявленным токеном. Дальше по стеку передаётся `partner_code_id`, полученный из сверки, — **не `code`** | Тот же класс, что закрыт в FR-010 (NFR-010.7). Первая редакция утверждала, что ошибка «взяли партнёра из адреса» невозможна по сигнатуре — неверно: последний вызов был `getPartnerCohortDashboard(c, partner.code)`, то есть по коду, публичному значению. Граница модуля кончалась на один вызов раньше, чем нужно |
| NFR-011.2 | Все запросы к `partner_codes`, `referral_attributions`, `commissions` фильтруют по `partner_code_id` **явно** | RLS на этих таблицах **нет вовсе**, и грантов роли `app_authenticated` тоже нет (`007_rls.sql:66-72`) — путь идёт под `app_service`, то есть без единой подстраховки. Забытый фильтр показывает чужие деньги |
| NFR-011.3 | Ответ на неизвестный, отозванный и синтаксически негодный токен **неразличим** | Различимость превращает форму в оракул: «такой партнёр есть, но код отозван» — уже утечка |
| NFR-011.4 **[v3]** | Проверка и запись счётчика **атомарны** относительно параллельных попыток с того же адреса, и добросовестный партнёр при этом **не получает отказ** — он получает очередь, измеряемую микросекундами. Требование сформулировано свойством, без выбора реализации | Три редакции подряд ошибались здесь по-разному, и это стоит записать целиком. **Ред. 1:** лок по IP — воспроизводила дефект FR-009 (`login.ts:16-18`: «лок по IP наказывал бы всех за одним NAT»): два партнёра одной компании, отправившие форму одновременно, получали бы `429`. **Ред. 2:** лок по паре `(IP, хеш токена)` — убрала ложный отказ и вместе с ним **атомарность счётчика**, ради которой лок и вводился: разные токены дают разные локи, и 40 одновременных попыток проходят все сорок. **Ред. 3** формулирует свойство и не выбирает механизм в требовании. Реализация — в псевдокоде: счётчик живёт в **отдельной короткой транзакции** с ждущим локом по адресу, а сверка токена — в следующей. Ожидание длится один `COUNT` плюс один `INSERT`, микросекунды, а не argon2, — поэтому пул оно не держит, и запрет FR-009 на ждущий лок сюда не переносится: там ожидание длилось 50 мс на argon2 |
| NFR-011.5 | Токен не попадает ни в адрес, ни в тело ответа, ни в журнал | См. три утечки выше |
| NFR-011.6 | Предел размера тела — общий с остальными маршрутами, одной реализацией | Маршрут неаутентифицирован по определению: он и есть вход |

## Критерии приёмки

| # | Критерий | Падает при |
|---|---|---|
| AC-011.1 | Токен партнёра A открывает кабинет и показывает **числа A** | не сверять токен с БД вовсе |
| AC-011.2 | **Межпартнёрская изоляция:** в кабинете A нет ни одного числа партнёра B — ни регистраций, ни конверсий, ни сумм. Проверяется на данных, где у B они заведомо ДРУГИЕ и ненулевые | убрать `where partner_code_id = $1` из любого из трёх запросов |
| AC-011.3 | Подстановка чужого `code` в адрес, поле формы или заголовок не меняет показанное | взять партнёра из параметра адреса |
| AC-011.4 | Страж по исходнику: страница и маршрут не читают идентификатор партнёра ниоткуда, кроме cookie-токена | то же |
| AC-011.5 | Неизвестный токен, отозванный код и мусорная строка дают **один и тот же** ответ: код, тело, и отсутствие cookie | вернуть «код отозван» отдельным текстом |
| AC-011.6 | Отозванный код: кабинет закрывается, даже если cookie была выдана ДО отзыва | проверять статус только при входе, а не при каждом показе |
| AC-011.7 | Страж по исходнику: в `partner_codes` не пишется исходный токен — только `dashboard_token_hash` | сохранить токен как есть |
| AC-011.8 | Выданный токен возвращается вызывающему **ровно один раз**, при создании; повторно его получить нельзя | добавить чтение токена |
| AC-011.9 | Токена нет ни в адресе, ни в теле ответа дашборда | передать токен в query-параметре |
| AC-011.10 | Cookie httpOnly, ограничена путём `/partner` | снять httpOnly |
| AC-011.11 | Порог попыток достигается **настоящими** предъявлениями; превышение → `429` тем же литералом, что у входа | убрать `rateLimit.exceeded` |
| AC-011.12 | Одно неверное предъявление увеличивает счётчик по IP ровно на 1 | убрать `rateLimit.record` |
| AC-011.13 | Тело больше предела → `413`; маршрут в списке стража предела тела | убрать `readBodyAtMost` |
| AC-011.14 | `conversion_rate` при нуле регистраций — `null`, а не `0`: «нет данных» и «0 %» разные ответы | вернуть `0` |
| AC-011.15 **[v2]** | Сравнение идёт **по хешу, а не по секрету**: в SQL уходит `sha256(токен)`, сам токен в условие запроса не попадает. Прежняя формулировка обещала «константное по времени сравнение по индексу» — поиск по btree константным по времени **не является** (сравнение текста прекращается на первом различающемся байте). Защищает здесь другое свойство: утечка тайминга не даёт восстановить токен, потому что нужен прообраз sha256 | сравнивать открытые строки |
| AC-011.16 **[v2]** | **Подделанная cookie, содержащая публичный код или `id` партнёра, доступа НЕ даёт.** Единственное значение, открывающее кабинет, — то, чей `sha256` совпал с `dashboard_token_hash` | положить в cookie `code` или `id` вместо токена — вариант, проходивший ВСЕ пятнадцать критериев первой редакции |
| AC-011.17 **[v2]** | Выданный `dashboard_token` не равен `code` и не выводится из него | `dashboard_token = code` |
| AC-011.18 **[v2]** | Два одновременных предъявления **разных верных** токенов с одного адреса: оба получают `200`. Последовательный тест на этом дефекте зеленеет всегда | вернуть лок по одному `keyIp` |
| AC-011.19 **[v3]** | `issuePartnerCode` сохраняет **шесть** свойств: сигнатуру `(client, partnerName, options)`; возврат `id`; запись в `audit_log`; поля `commission_rate` и `owner_account_id`; цикл подбора кода; **и исчерпание попыток бросает исключение, а не возвращает пустое значение**. Шестое было пропущено в ревизии 2, а её собственный diff превращал явный отказ в тихий: цикл `for` после десяти коллизий просто заканчивался, и вызывающий получал `undefined.id` | переписать функцию заново — так первая редакция ломала 10 вызовов и терял self-referral |
| AC-011.20 **[v3]** | Страж по исходнику: запросы к `partner_codes`, `referral_attributions`, `commissions` встречаются только в `lib/partner.ts`, `lib/partner-auth.ts` и `lib/referral.ts` | дописать запрос в страницу или маршрут |
| — | *Формулировка ревизии 2 была КРАСНА на верном коде: `referral.ts` содержит семь таких запросов, и это правильное для них место — атрибуция живёт там. Страж, который нельзя выполнить, снимают целиком, а вместе с ним исчезает настоящая проверка.* | — | дописать запрос прямо в страницу |
| AC-011.21 **[v2]** | Страж по исходнику: ни `console.*`, ни запись в `audit_log` на пути аутентификации партнёра не принимают переменную токена | залогировать токен при отладке |
| AC-011.23 **[v3]** | N одновременных предъявлений **разных неверных** токенов с одного адреса при пороге 30: до сверки доходит **не более 30**. Последовательный тест этого не ловит | развести ключ лока и ключ счётчика |
| AC-011.24 **[v3]** | После ротации **прежний** токен доступа не даёт, и уже выданная по нему cookie тоже | обновлять хеш, не проверяя его при каждом показе |
| AC-011.25 **[v3]** | Код, выданный `issuePartnerCode` без явной ставки, получает ставку **по умолчанию**, а не `NULL` | подставлять `?? null` — явный NULL отменяет дефолт колонки |
| AC-011.22 **[v2]** | Ротация токена **не меняет `code`**: `dashboard_token_hash` обновляется в ТОЙ ЖЕ строке | перевыпускать код — это разрезало бы когорту партнёра надвое |

## Как токен попадает партнёру — честно

`issuePartnerCode` **не вызывается из продакшн-кода**: административного маршрута в проекте нет
вовсе (`grep` даёт только объявление и тесты). FR-011 его не добавляет — это отдельная фича с
собственной аутентификацией владельца.

Пока его нет, выдача выполняется владельцем **вручную**, скриптом на сервере, а токен
передаётся партнёру тем же каналом, которым сейчас передаётся сам код. Это записано здесь, а
не умолчано, потому что «показывается один раз при выдаче» без ответа на вопрос «кому и куда»
— требование без реализации.

## Вне объёма

Самостоятельная регистрация партнёров, вывод средств, история начислений по датам,
уведомления. Ставка комиссии остаётся `[GAP]` — она уже помечена в схеме (`004_growth.sql:50`)
и решается владельцем, а не этой фичей.
