# FR-010 · Готовность

> **Ревизия 3.** Добавлен AC-010.25; у .15, .22 и .24 расширен список мутаций.
>
> **Ревизия 2.** Таблица трассировки ревизии 1 состояла из одной строки
> «AC-010.1 … AC-010.13 | — | —». По собственному правилу документа это блокер: диапазон
> в одной ячейке и есть способ не заметить пропущенный критерий. Здесь — строка на критерий.

- [ ] Все AC-010.x закрыты тестами — **перечнем ниже**, а не утверждением
- [ ] `npm test` зелёный целиком, `npm run typecheck` — 0
- [ ] Каждая строка таблицы мутаций (`04_refinement.md`, M1–M17) внедрена, прогнана,
      результат записан в столбец «испытан»
- [x] Выкачено на стенд: главная и /login отдают 200, `POST /api/auth/password` без сессии — 401 верным телом
- [x] Роадмап: `FR-010` → `done`

## Соответствие критерий → тест → испытание

Пустая ячейка = критерий не закрыт, и это блокер, а не примечание.

| Критерий | Тест | Испытан внедрением дефекта |
|---|---|---|
| AC-010.1 | `password-change` · «верный текущий → ok, вход НОВЫМ паролем работает» | — |
| AC-010.2 | `password-change` · «вход СТАРЫМ паролем после смены → отказ» | — |
| AC-010.3 | `password-change` · «cookie, КОТОРОЙ аутентифицирован запрос, мертва» | **M5 ✅ красное** |
| AC-010.4 | там же, третье утверждение | **M5 ✅ красное** |
| AC-010.5 | `password-change` · «сессии и пароль ДРУГОГО аккаунта не тронуты» | **M1 ✅ красное** |
| AC-010.6 | там же, второе утверждение | **M2 ✅ красное** |
| AC-010.7 | `password-change` · «пароль прежний, сессии живы» + `route` · «401 тем же телом» | — |
| AC-010.8 | `password-change` · границы + `route` · «короче 8 / длиннее 200 → 400» | — |
| AC-010.9 | `route` · «новый пароль равен текущему → 400» | — |
| AC-010.10 | `password-change` · «5 неверных подряд → следующая даёт too_many» | **M9 ✅ красное** |
| AC-010.11 | `route` · «тело больше предела → 413» | — |
| AC-010.12 | `password-change` · «индуцированный сбой на отзыве откатывает смену пароля» | механизм индукции сработал (`CHECK … NOT VALID`) |
| AC-010.13 | `password-change` · два стража, по обоим файлам | **M1, M2 ✅ красное** |
| AC-010.14 | `password-change` · «вторая сессия того же аккаунта тоже мертва» | **M5 ✅ красное** |
| AC-010.15 | `password-change` · «+1 в паре И +1 в IP, считая ПО SCOPE» | **M8b ✅, M8c ✅ красное** |
| AC-010.16 | `password-change` · «исчерпанная пара НЕ мешает с другого адреса» | **M10 ✅ красное** |
| AC-010.17 | `password-change` · «lib не знает про HTTP» + «маршрут берёт из currentAccountId()» | **M11 ✅ красное** |
| AC-010.18 | `route` · «владелец A с account_id жертвы B меняет только СВОЙ пароль» | **M11 ✅ красное** |
| AC-010.19 | `password-change` · «занятый advisory-лок даёт busy, а не too_many» | **M15 ✅ красное** |
| AC-010.20 | `password-change` · «классы системы и autocomplete на обоих полях» | — |
| AC-010.21 | `password-change` · «route вызывает readBodyAtMost» + список в `login.test.ts` | — |
| AC-010.22 | `password-change-argon2` · «неверный текущий: hash 0, verify 1» | страж порядка по исходнику |
| AC-010.23 | `password-change` · «лок двухаргументный, с namespace и таймаутом» | **M14 ✅ красное** |
| AC-010.24 | `password-change` · «hashKey объявлен ровно в одном файле проекта» | **M16 ✅ красное** |
| AC-010.26 **[ревью]** | `password-change` · «N одновременных смен с РАЗНЫХ адресов: ровно одна ok» | **снять `for update` ✅ красное** (5 успешных из 5 — гонка возвращается) |
| AC-010.27 **[ревью]** | `password-change` · «30 неверных с одного адреса по разным аккаунтам → 429» | **M9b ✅ красное** |
| AC-010.28 **[ревью]** | `password-change` · «больше 10 успешных смен подряд не проходят» | **H-1a, H-1b ✅ красное** |
| AC-010.29 **[ревью]** | `password-change` · «поток вора не мешает владельцу сменить пароль» | **вернуть лок по accountId ✅ красное** |
| AC-010.30 **[ревью]** | `password-change` · «литерал TOO_MANY объявлен один раз» | — |
| AC-010.31 **[ревью]** | `password-change` · «в транзакции есть statement_timeout» | — |
| AC-010.25 | `password-change-argon2` · «исчерпанный лимит: hash 0, verify 0» | **M13b ✅ красное** — ровно дефект ревизии 2 |

**Прогон мутаций: 12 из 12 дали красное.** Базовая линия зелёная, каждая мутация внесена в
исходник, тест прогнан, исходник восстановлен. Ни один страж не остался неиспытанным.

45 тестов фичи (31 + 10 + 4), полный набор проекта — 634 зелёных, `typecheck` 0.

**Два прохода brutal-honesty-review.** Первый: 2 блокера, 3 высоких, 5 средних, 4 низких.
Второй: 1 блокер, 3 высоких, 5 средних, 2 низких — и блокер был **новым, внесённым починкой
первого**. Оба раза это один и тот же класс: правку проверяли на её сценарии и не проверяли на
разделяемом ресурсе, который она создала.

### Найдено при прогоне и исправлено

**Тест лимита зеленел ложно и упал бы «через раз» позже.** Ключ грубого счётчика строится
только по адресу, порог 30, окно ЧАС, а `rate_limit_events` между прогонами не чистится.
Генератор адресов нумеровал их от нуля при каждом старте набора — после шести прогонов
подряд один и тот же адрес набирал 30 записей, и ПЕРВАЯ попытка возвращала `too_many`
вместо `unauthorized`. Воспроизведено: прогоны 1–2 зелёные, 3–5 красные. Исправлено
ключом, уникальным между прогонами; подтверждено шестью прогонами подряд.

Класс тот же, что лечит сама фича: **разделяемый ресурс, который не проверял никто.**
Изоляцию тестов писали внутри прогона, а счётчик разделяется МЕЖДУ прогонами.

## Принимаемые риски

**1. Восстановления забытого пароля по-прежнему нет.** Фича помогает тому, кто пароль
помнит. Забывший теряет доступ, как и раньше: нужна почта, которой в MVP нет.

**2. Список активных сессий не показывается.** Владелец не видит, сколько устройств вошло,
и не может отозвать одно. Отзыв — всё или ничего.

**3. Отзыв не мгновенен для уже открытой страницы.** Проверка сессии идёт при запросе к
серверу; открытая вкладка с отрисованным кабинетом продолжает показывать данные до
следующего обращения. Это свойство серверной проверки, а не дефект отзыва.

**4. Потерянный ответ = требуется повторный вход.** Транзакция коммитится ДО отправки ответа
(`tenant.ts:33`). Если ответ не доедет — разрыв связи, закрытая вкладка, сбой прокси — пароль
уже сменён и все сессии, включая текущую, мертвы, а новая cookie потеряна. Владелец войдёт
заново **новым** паролем. При «отзыве прочих» он остался бы внутри: это прямая цена
отступления от роадмапа, и она названа здесь, а не умолчана. Обратного пути — «cookie выдана,
изменений нет» — не существует: `COMMIT` стоит до возврата значения, и при его отказе до
строки с cookie исполнение не доходит.

**5. Различимость «нет сессии» и «неверный пароль» по времени сохраняется.** Коды и тела
ответов совпадают (NFR-010.4), но путь «нет сессии» возвращается до argon2, а «неверный
пароль» его считает. Заглушечный хеш, как на входе, здесь **не вводится осознанно**: сигнал,
который он скрывал бы, — «сессия жива» — вор получает и без него, любым запросом к дашборду
той же cookie. Мера, не закрывающая ничего, но удерживающая соединение пула на 100 мс, хуже
её отсутствия.

**6. Перебор с ротацией IP парным ключом не закрыт.** Пара «аккаунт + IP» защищает владельца
от блокировки вором — это её задача (NFR-010.3). Атакующего, меняющего адрес, она замедляет
лишь грубым счётчиком по IP (30/час на адрес). Полностью закрыть это ключом по одному
аккаунту нельзя: именно такой ключ и даёт вору кнопку «запереть владельца». Размен сделан в
пользу владельца сознательно; настоящая мера против перебора здесь — argon2id, а не счётчик.

**7. Счётчик попыток технически удаляем.** `007_rls.sql:79` выдаёт `app_authenticated` право
`delete on rate_limit_events`, RLS на этой таблице нет. Сегодня ни один путь кода не вызывает
`rateLimit.revoke` из дашборда, поэтому это не эксплуатируемо — но FR-010 первая фича, чья
**безопасность** зависит от этого счётчика, и предположение «строку никто не удалит»
зафиксировано явно, а не оставлено подразумеваемым.
