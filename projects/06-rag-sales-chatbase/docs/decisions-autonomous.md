# N6 — решения, принятые без владельца

Режим: владелец спит, конвейер идёт автономно (память «Автономия, когда владелец ушёл»). Здесь
каждое решение, которое по регламенту принимает человек, но было принято агентом, — с причиной и
условием отмены. Необратимого ничего не делалось: нет коммитов, нет внешних действий, нет денег.

Исполнитель: Claude Opus 5.5 (агент Phase 0 / 0.5). OpenAI/Codex как исполнители не вызывались
(OWN-017: только Anthropic с 25.09.2026).

| ID | Дата | Решение | Почему | Как отменить |
|---|---|---|---|---|
| A-N6-001 | 2026-09-25 | **Вариант CJM НЕ выбран, а РЕКОМЕНДОВАН**: гибрид «A-ядро (badge) + демо-страница из C + партнёрский минимум из B». В `CJM_Variants.md` он помечен `РЕКОМЕНДАЦИЯ (ожидает владельца)`, а не `CHOSEN_CJM` | `start/REPLICATE-PROMPTS.md`: «Пайплайн НЕ имеет права выбрать вариант сам»; команды «выбери сам» в постановке нет. Прототип построен на рекомендованном пути, чтобы владельцу было что смотреть утром; переключатель показывает все три | Ответ владельца «вариант X» / гибрид — переписать строку выбора и шапку прототипа |
| A-N6-002 | 2026-09-25 | Файлы Phase 0 лежат так: бриф — `docs/product-discovery-brief.md` (его читает `check-handoff-manifest.cjs`), варианты и прототип — `docs/discovery/` (по поручению координатора) | Бриф обязан лежать по пути `replicate.md`, иначе страж манифеста ответит `2`. `CJM_Variants.md` в `docs/discovery/`, а не в `docs/` — отклонение от grep-проверки методологии (`docs/CJM_Variants.md`); проверку запускать с путём `docs/discovery/` | Перенести `CJM_Variants.md` в `docs/`, если владелец хочет единообразия с N4/N5 |
| A-N6-003 | 2026-09-25 | Рабочее имя клона в прототипе — **«Суфлёр»** | Нужно имя для бейджа «Работает на Суфлёре» в прототипе. Доступность имени и домена НЕ проверялась | Любое имя владельца — замена строкой |
| A-N6-004 | 2026-09-25 | Тарифная сетка в брифе — **гипотеза**, не цифры Chatbase: Free с бейджем → «Без бейджа» → «Студия» | Постановка: «свою тарифную сетку строим сами, не копируем цифры». Цифры Chatbase сняты (2026-09-25) только как ориентир | Phase 1 фиксирует сетку в Specification |
| A-N6-005 | 2026-09-25 | Метрика недели: **15 виджетов на внешних доменах**, где виджет получил ≥1 вопрос посетителя | Постановка задала название без числа; методология требует число. 15 — гипотеза от охвата сети владельца и 3–5 студий-партнёров, бенчмарка нет | Владелец ставит своё число |
| A-N6-006 | 2026-09-25 | Для оси «путь» Phase 0.5 обход пошёл по `--follow "Pricing\|Customers\|Enterprise\|Retail & E-commerce"` | Первый прогон по заметности выбрал «Start free trial» → `/auth/signin`, закрытый robots.txt; инструмент законно остановился (код 1). Внутренний путь (`/dashboard`, `/onboarding`) закрыт robots.txt и регистрацией — туда не шли | — |
| A-N6-007 | 2026-09-25 | Модель эмбеддингов из постановки (`text-embedding-3-small`) **оставлена**, но вынесена в открытый вопрос №1: API OpenAI не обслуживает Россию (проверено 2026-09-25) | Менять модель — решение владельца, а не discovery. Факт о недоступности — в брифе с источником | Владелец выбирает маршрут (VPS вне РФ / OpenRouter / отечественная модель) в Phase 1 |
| A-N6-008 | 2026-09-25 | **Координатор: вариант H принят ВРЕМЕННО как `CHOSEN_CJM` для продолжения конвейера** (Phase 1–3) | Владелец явно поручил вести N6 автономно до утра («делай всё в автоматическом режиме»); без выбора Phase 1 не стартует, и ночь пропадает. Выбор помечен «временный», остальные варианты описаны. Необратимого нет: Phase 1–3 — документы | Владелец выбирает A / B / C / свой гибрид → Phase 1 перезапускается с новым выбором (документы, не код) |
| A-N6-009 | 2026-09-25 | **Эмбеддинги — через тот же шлюз OpenRouter, что у N5**, модель `openai/text-embedding-3-small` (1536); доступность эмбеддингов у шлюза проверяет Phase 1 и записывает с датой. Если шлюз эмбеддингов не отдаёт — запасной путь назвать в ADR (отечественная модель с 1024–1536 измерений), не выбирать молча | Один шлюз и один ключ уже работают у N5 из РФ-контура; прямой API OpenAI Россию не обслуживает. 152-ФЗ: текст вопросов уходит поставщику — то же ограничение, что у N5 (OWN-006), записать в Specification | Владелец выбирает маршрут |
| A-N6-010 | 2026-09-25 | **Модель ответов бота — Anthropic через OpenRouter** (дешёвая: Haiku 4.5; обоснование ценой — в Phase 1). OWN-017 («только Anthropic») касается агентов разработки, но и продуктовая модель выбрана из того же семейства | N5 уже ходит к Anthropic через OpenRouter — готовый адаптер и потолки расхода | Владелец называет модель |
| A-N6-011 | 2026-09-25 | Оплата в N6 — **спящий код** до своего тестового магазина ЮKassa (как в N5, ADR-005): тарифы видны, «оплатить» ведёт на экран интереса | У магазина один адрес уведомлений (открытый вопрос №4); без отдельного магазина вебхук не проверить | Владелец заводит тестовый магазин |
| A-N6-012 | 2026-09-25 | **Phase 1 выполнена в AUTO агентом; канон имён и чисел (`canon.md`) заморожен агентом до написания документов.** Доступность OpenRouter проверена БЕЗ ключа (HTTP 401 — сеть доступна) и по документации/каталогу поставщика; вызов эмбеддинга с ключом НЕ делался — это трата денег владельца, пусть и доли цента. В инвентаре строка сетевой доступности — `UNCONFIRMED` | Необратимое (деньги) без владельца не делаем; честный `UNCONFIRMED` лучше выдуманного `CONFIRMED` | Владелец разрешает пробу (Completion, шаг 3) — строка становится CONFIRMED или CONTRADICTED |
| A-N6-013 | 2026-09-25 | Числа RAG — **гипотезы до калибровки**: фрагмент 500/600/80 токенов, `top_k = 4`, порог сходства 0.40, 2 хода истории | Бенчмарка на русских сайтах малого бизнеса нет; порог калибруется набором 20 + 20 вопросов (Refinement), критерий — 0 выдумок вне базы | Итог калибровки меняет число в каноне §7 новой редакцией |
| A-N6-014 | 2026-09-25 | Предпросмотр — **макет страницы, не скриншот** (ADR-007); ответ **без стриминга** (ADR-006) | Скриншот требует браузера в воркере и новой SSRF-поверхности; стрим показал бы непроверенный текст до проверки цитат | Владелец просит скриншот — v1 |
| A-N6-015 | 2026-09-25 | Планы `nobadge`/`studio` в неделю назначает **оператор командой** пилотным партнёрам; атрибуция конвертируется в этот момент | Оплата спящая (A-N6-011), а FR-GROWTH-004 (студии) нужен в неделю | Появляется свой магазин ЮKassa — план назначает оплата |
| A-N6-016 | 2026-09-25 | Сырой PDF — в томе воркера и удаляется после индексации (ADR-018), объектного хранилища нет | Меньше внешних зависимостей и ключей; источнику ответа достаточно имени файла и страницы | Владелец хочет «скачать исходник» из плашки — S3 из N5 |
| A-N6-017 | 2026-09-25 | Потолки платных вызовов (20 / 60 / 3000 ответов в сутки и др., канон §7) — **числа агента**; худшие сутки ≈ $20,9 | Правило model-call-cost требует число, а не намерение; числа подобраны так, чтобы один посетитель не съедал бюджет бота | Владелец меняет числа в `.env` и каноне |
| A-N6-018 | 2026-09-25 | Модель ответов зафиксирована как `anthropic/claude-haiku-4.5` (A-N6-010) с structured outputs; `CLAUDE.md` проекта в Phase 1 НЕ пишется — его генерирует Phase 3 (cc-toolkit-generator) | Каталог OpenRouter подтверждает `structured_outputs` у модели; Phase 3 владеет тулкитом | Владелец называет другую модель — смена `ANSWER_MODEL` и строки инвентаря |
| A-N6-019 | 2026-09-25 | **Проба эмбеддингов с ключом выполнена координатором**: 2026-09-25T21:19Z, с этой VPS, ключом стенда N5 (worker-llm), `POST https://openrouter.ai/api/v1/embeddings`, `openai/text-embedding-3-small`, `dimensions:1536` → **HTTP 200, 1536 измерений, 6 токенов, $0.00000012** | Строка inventory «доступность OpenRouter с этой VPS» была UNCONFIRMED; цена пробы пренебрежимо мала, а без неё Phase 2 не выше 🟡 | Не отменяется (факт); у N6 будет свой ключ |
| A-N6-020 | 2026-09-25 | **Квота предпросмотра разведена по `scope_key`** (закрытие H1/M2 Phase 2): `preview_session` → `<сессия>:create` (1/сутки, `QUOTA_PREVIEW_SESSION_CREATE`) и `<сессия>:answers` (10/сутки, `QUOTA_PREVIEW_SESSION_ANSWERS`); `global_previews` → `previews` (200, `QUOTA_GLOBAL_PREVIEWS`) и `preview_answers` (1000, `QUOTA_GLOBAL_PREVIEW_ANSWERS`). Scope — 10, переменных `QUOTA_*` — 14; число исправлено во всех цитатах N6 | Из двух способов валидатора выбран «развести», а не «убрать списание при создании»: второй терял записанный предел «1 предпросмотр на браузер». Одна переменная для двух чисел `global_previews` — тот же класс дефекта, найден при сквозном поиске | Владелец меняет числа — в `.env` и каноне §7; структура (14 имён) — новой редакцией канона |
| A-N6-021 | 2026-09-25 | **`web` берёт адрес клиента из ЕДИНСТВЕННОГО значения `X-Forwarded-For`** (дверь Caddy заменяет заголовок на `{client_ip}`); цепочка из двух и более адресов — отказ `503`, а не догадка. Параметр N5 `N5_TRUSTED_PROXY_HOPS` не перенесён. Префикс IPv6 — /48 (канон §7), у N5 был /64 | Число доверенных звеньев имеет смысл, когда прокси ДОПИСЫВАЕТ заголовок; дверь N6 его ЗАМЕНЯЕТ (урок N4), и лишняя переменная была бы ещё одним дефолтом | Дверь начнёт дописывать XFF или появится второй прокси внутри стека — вернуть параметр звеньев из N5 |
| A-N6-022 | 2026-09-25 | **`ANSWER_MODEL` и `EMBED_MODEL` принимаются только из закрытого набора кода** (`packages/rag/src/constants.ts`: `anthropic/claude-haiku-4.5`, `openai/text-embedding-3-small`); иное значение валит старт | honest-configuration CFG-I8: окружение выбирает из кода, а не задаёт; другая модель эмбеддингов = другая размерность = миграция (ADR-001) | Владелец выбирает другую модель — строка в наборе кода + канон §7 |
| A-N6-023 | 2026-09-25 | **Фича `foundation` не создаёт `apps/widget` и `packages/queue`** (в описании фичи они перечислены как workspace); закрытый набор `job_attempt.status` = `running · done · failed` (в каноне §4 не задан); добавлена пара потолков `QUOTA_ACCOUNT_EMBED ≤ QUOTA_GLOBAL_EMBED` сверх перечня Pseudocode LoadCeilings п.2 | Пустой workspace ради зелёной сборки — ложь (compose-hygiene правило 7): очередь приходит с `index-job-core` (её expected_files), виджет — с `widget-runtime-and-badge`; статус попытки нужен CHECK'у уже в 001; пара — прямое требование model-call-cost «персональный не больше суточного» | Канон задаст набор статусов попытки — миграция-дополнение; фича очереди/виджета добавляет workspace и строку `COPY` в Dockerfile |
| A-N6-024 | 2026-09-25 | **Фича `quota-and-spend` (тир XL «деньги») реализована без остановки на плане у владельца** — по постановке координатора (автономный режим). Решения внутри: (1) пробы старта (`EmbedProbe`, проба `ANSWER_MODEL`) — вызовы `свой-код`, предел `PROBE_DAILY_LIMIT = 24` на вид в сутки МСК (файл-счётчик в томе `model-spend`), строки добавлены в `model-cost-contract.md`; (2) `N6_SPEND_LOG` обязателен в `loadWebConfig`/`loadWorkerConfig` (абсолютный путь `.jsonl`) — без журнала процесс не стартует; (3) `@n6/db` зависит от `@n6/rag` (ключи квоты читают LoadCeilings, план и сутки МСК) — сборка `rag → db`; (4) основной вход квоты — `consumeQuota` в СВОЕЙ короткой транзакции: вызывающий не держит строки счётчика (включая общую `global_*`) на время вызова модели | (1) без предела `restart: unless-stopped` при сломанном шлюзе оплачивал бы пробу на каждом перезапуске; 10 scope канона — только для чужих вызовов; (2) RecordModelSpend п.2 «нет учёта — нет трат»; (4) shared-resource-verification п.1 | Владелец возвращает XL-чекпойнт — прочитать `docs/features/quota-and-spend/05_completion.md` и откатить по его разделу «Чего не доказывает»; другое число проб — константа кода + строка контракта |
| A-N6-025 | 2026-09-25 | **Фича `index-job-core`: решения внутри ADR-009.** (1) Поле `state` ответа `GET /api/index-jobs/{id}` — `running · done · failed · no_response` (Pseudocode называет их «выполняется · успех · отказ · нет_ответа»; `queued` показывается как `running`); (2) предел задачи 15 мин (канон §7) закрывает задачу причиной `stalled` — закрытый список `failure_reason` не содержит «превышено время», миграция ради одной причины не делалась; (3) сообщение очереди несёт `generation = current_fence` на момент постановки, аренда требует совпадения: `queued` → новая серия, `running` → автоповтор той же серии; серия, исчерпанная доставками без закрытия, — `failed(stalled)`; (4) маршрут `POST /api/bots/{bot_id}/sources` НЕ создан — только каркас БД `createSourceJob` (маршрут требует `CheckAddress` и приём PDF — фичи `crawler`, `pdf-source`); доступ к задаче по cookie предпросмотра — через зависимость, пока `null` (`preview-flow`); (5) до `crawler` воркер закрывает любую задачу `internal` (`noProcessorYet`), а не держит её «выполняется»; (6) бюджет предпросмотра 20 / 40 000 записывается в `index_job.page_budget/embed_budget` при создании, списание `embed_used` — `chunk-embed` (EmbedAndStore п.1) | ADR-009 и long-running-job требуют трёх различимых состояний и фенса; значения по п.1–3 нигде не заданы, выбор — наименьший, не требующий миграции | Канон задаст значения `state` или причину таймаута — миграция-дополнение (CHECK) и правка `indexJobView`/сторожа; маршрут источника появится с `crawler` |
| A-N6-026 | 2026-09-25 | **Фича `crawler`: решения внутри ADR-010.** (1) HTTP — встроенный `node:http(s)` вместо undici (ADR-010): соединение открывается прямо на проверенный IP (`host` = IP, `Host`/SNI = имя), второго DNS-запроса нет; ноль новых зависимостей. (2) HTML — собственный однопроходный разборщик тегов вместо `linkedom` (Architecture «Parsing»): нужны текст, заголовки и ссылки, не DOM. (3) Бюджет страниц — на ИСТОЧНИК по плану (50/300, предпросмотр 20 из задачи), а не остаток на бота по всем источникам: сумму по боту обязан проверять маршрут создания источника (`bot-cabinet`/`source-lifecycle`). (4) Пропуски с причинами пишутся в журнал и счётчиком `source.pages_skipped`, но разбивка по причинам НЕ хранится (строка `page` требует `content_hash`). (5) Бюджет времени обхода 12 мин (сторож закрывает серию через 15), потолок запросов 2 × бюджет + 10, дубль содержимого под другим адресом (www., `/index.html`) — пропуск `duplicate`. (6) Перенаправления вне хоста — `off_site`; IPv6 — разрешающий список 2000::/3 минус служебные подсети; документационные IPv4 (TEST-NET) запрещены | ADR-010 и Architecture называли библиотеки, но не требовали их; свойства (соединение по проверенному IP, обрыв чтения на 2 МБ, без исполнения JS) достижимы без зависимостей, а каждая зависимость — поверхность. Причины пропуска в БД — миграция CHECK/колонки, вне объёма фичи | (1)–(2) вернуть библиотеки — замена модулей `safe-get.ts`/`extract-text.ts` при тех же тестах; (3) остаток по боту — строка в `site-processor.ts`; (4) колонка причин — миграция 002 вместе с экраном итога |
| A-N6-027 | 2026-09-26 | **Фича `pdf-source`: решения внутри FR-SOURCE-003 и ADR-018.** (1) Разбор — `pdfjs-dist` **6.3.289** (Apache-2.0, Mozilla; `npm audit` — 0 новых уязвимостей; CVE-2024-4367 закрыт с 4.2.67, `isEvalSupported: false` выставлен всё равно), а не poppler `pdftotext`: poppler — GPL и C-код с историей ошибок памяти; pdfjs — чистый JS, его назвали Architecture и coding-style. Необязательный нативный `@napi-rs/canvas` удалён из образа и запрещён к загрузке в дочернем процессе. (2) Разбор — в ДОЧЕРНЕМ процессе `node extract-child.mjs` с пустым окружением; пределы исполняет родитель: таймаут 30 с на документ, резидентная память 384 МБ (замер `/proc/<pid>/status`; куча V8 256 МБ не видит буферы распаковки — flate-бомба 400 КБ давала 915 МБ RSS), stdout 16 МБ, извлечённый текст 2 млн символов. (3) Закрытый список `failure_reason` (11 причин, канон §4) НЕ расширен: `pdf_timeout`/`pdf_memory`/лишний текст/101 страница → `too_large`; шифрованный с паролем → `no_text_layer` (средство то же — загрузить текстовую версию); битый (включая одну непрочитанную страницу) → `not_pdf`; сбой самого разборщика → `internal`. Подробный код пишется в журнал воркера. (4) Файл пишется в том под `index_job_id`, выбранным web ДО вставки строки (`createSourceJobTx` принимает `indexJobId`), и ДО коммита: «строка задачи есть ⇒ файл есть»; удаляется в `onSettled` RunIndexJob после `done` И `failed` (не после `stale` и не перед автоповтором) + подметание тома сторожем раз в минуту (файлы завершённых и несуществующих задач старше 10 мин). (5) Предел числа PDF: считаются источники `kind = pdf` со статусом ≠ `failed`; проверка дважды — до приёма тела (дёшево) и в транзакции под `FOR UPDATE` строки бота (атомарно). (6) Маршрут принимает только multipart PDF; JSON `{ url }` → `422` (сайт этим маршрутом — будущая фича кабинета); `Origin` обязателен; тело читается в память с потолком 10 МБ + 64 КиБ. (7) «Повторить» у PDF после завершения даёт `internal`: файл уже удалён по ADR-018 — нужна новая загрузка | Канон задаёт границу файла (10 МБ, 100 страниц, 20 символов / 90 %, 3/10), но не пределы разбора; без них враждебный PDF держит воркер (concurrency 1) и память машины. Расширение закрытого списка причин — правка канона, CHECK и всех цитат (урок H1/M2), вне объёма фичи и решения агента | (1) другой разборщик — замена `extract-child.mjs` при тех же тестах; (2) числа — `apps/worker/src/pdf/limits.ts`; (3) новые причины — миграция CHECK + канон §4 + `enums.ts` + `PDF_FAILURE_REASON`; (6) приём потоком на диск вместо памяти — замена `readBounded`; (7) хранить файл до явного удаления источника — правка ADR-018 владельцем |
