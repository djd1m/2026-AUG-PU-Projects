# N6 — решения, принятые без владельца

Режим: владелец спит, конвейер идёт автономно (память «Автономия, когда владелец ушёл»). Здесь
каждое решение, которое по регламенту принимает человек, но было принято агентом, — с причиной и
условием отмены. Необратимого ничего не делалось: нет коммитов, нет внешних действий, нет денег.

Исполнитель: Claude Opus 5.5 (агент Phase 0 / 0.5). OpenAI/Codex как исполнители не вызывались
(OWN-017: только Anthropic с 25.09.2026).

| ID | Дата | Решение | Почему | Как отменить |
|---|---|---|---|---|
| A-N6-001 | 2026-09-25 | **Вариант CJM НЕ выбран, а РЕКОМЕНДОВАН**: гибрид «A-ядро (badge) + демо-страница из C + партнёрский минимум из B». В `CJM_Variants.md` он помечен `РЕКОМЕНДАЦИЯ (ожидает владельца)`, а не `CHOSEN_CJM` | `start/REPLICATE-PROMPTS.md`: «Пайплайн НЕ имеет права выбрать вариант сам»; команды «выбери сам» в постановке нет. Прототип построен на рекомендованном пути, чтобы владельцу было что смотреть утром; переключатель показывает все три | Ответ владельца «вариант X» / гибрид — переписать строку выбора и шапку прототипа |
| A-N6-002 | 2026-09-25 | Файлы Phase 0 лежат так: бриф — `docs/product-discovery-brief.md` (его читает `check-handoff-manifest.cjs`), варианты и прототип — `docs/discovery/` (по поручению координатора) | Бриф обязан лежать по пути `replicate.md`, иначе страж манифеста ответит `2`. `CJM_Variants.md` в `docs/discovery/`, а не в `docs/` — отклонение от grep-проверки методологии (`docs/CJM_Variants.md`); проверку запускать с путём `docs/discovery/` | Перенести `CJM_Variants.md` в `docs/`, если владелец хочет единообразия с N4/N5 |
| A-N6-003 | 2026-09-25 | Рабочее имя клона в прототипе — **«Суфлёр»** | Нужно имя для бейджа «Работает на Суфлёре» в прототипе. Доступность имени и домена НЕ проверялась | Любое имя владельца — замена строкой |
| A-N6-004 | 2026-09-25 | Тарифная сетка в брифе — **гипотеза**, не цифры Chatbase: Free с бейджем → «Без бейджа» → «Студия» | Постановка: «свою тарифную сетку строим сами, не копируем цифры». Цифры Chatbase сняты (2026-09-25) только как ориентир | Phase 1 фиксирует сетку в Specification |
| A-N6-005 | 2026-09-25 | Метрика недели: **15 виджетов на внешних доменах**, где виджет получил ≥1 вопрос посетителя | Постановка задала название без числа; методология требует число. 15 — гипотеза от охвата сети владельца и 3–5 студий-партнёров, бенчмарка нет | Владелец ставит своё число |
| A-N6-006 | 2026-09-25 | Для оси «путь» Phase 0.5 обход пошёл по `--follow "Pricing\|Customers\|Enterprise\|Retail & E-commerce"` | Первый прогон по заметности выбрал «Start free trial» → `/auth/signin`, закрытый robots.txt; инструмент законно остановился (код 1). Внутренний путь (`/dashboard`, `/onboarding`) закрыт robots.txt и регистрацией — туда не шли | — |
| A-N6-007 | 2026-09-25 | Модель эмбеддингов из постановки (`text-embedding-3-small`) **оставлена**, но вынесена в открытый вопрос №1: API OpenAI не обслуживает Россию (проверено 2026-09-25) | Менять модель — решение владельца, а не discovery. Факт о недоступности — в брифе с источником | Владелец выбирает маршрут (VPS вне РФ / OpenRouter / отечественная модель) в Phase 1 |
| A-N6-008 | 2026-09-25 | **Координатор: вариант H принят ВРЕМЕННО как `CHOSEN_CJM` для продолжения конвейера** (Phase 1–3) | Владелец явно поручил вести N6 автономно до утра («делай всё в автоматическом режиме»); без выбора Phase 1 не стартует, и ночь пропадает. Выбор помечен «временный», остальные варианты описаны. Необратимого нет: Phase 1–3 — документы | Владелец выбирает A / B / C / свой гибрид → Phase 1 перезапускается с новым выбором (документы, не код) |
| A-N6-009 | 2026-09-25 | **Эмбеддинги — через тот же шлюз OpenRouter, что у N5**, модель `openai/text-embedding-3-small` (1536); доступность эмбеддингов у шлюза проверяет Phase 1 и записывает с датой. Если шлюз эмбеддингов не отдаёт — запасной путь назвать в ADR (отечественная модель с 1024–1536 измерений), не выбирать молча | Один шлюз и один ключ уже работают у N5 из РФ-контура; прямой API OpenAI Россию не обслуживает. 152-ФЗ: текст вопросов уходит поставщику — то же ограничение, что у N5 (OWN-006), записать в Specification | Владелец выбирает маршрут |
| A-N6-010 | 2026-09-25 | **Модель ответов бота — Anthropic через OpenRouter** (дешёвая: Haiku 4.5; обоснование ценой — в Phase 1). OWN-017 («только Anthropic») касается агентов разработки, но и продуктовая модель выбрана из того же семейства | N5 уже ходит к Anthropic через OpenRouter — готовый адаптер и потолки расхода | Владелец называет модель |
| A-N6-011 | 2026-09-25 | Оплата в N6 — **спящий код** до своего тестового магазина ЮKassa (как в N5, ADR-005): тарифы видны, «оплатить» ведёт на экран интереса | У магазина один адрес уведомлений (открытый вопрос №4); без отдельного магазина вебхук не проверить | Владелец заводит тестовый магазин |
| A-N6-012 | 2026-09-25 | **Phase 1 выполнена в AUTO агентом; канон имён и чисел (`canon.md`) заморожен агентом до написания документов.** Доступность OpenRouter проверена БЕЗ ключа (HTTP 401 — сеть доступна) и по документации/каталогу поставщика; вызов эмбеддинга с ключом НЕ делался — это трата денег владельца, пусть и доли цента. В инвентаре строка сетевой доступности — `UNCONFIRMED` | Необратимое (деньги) без владельца не делаем; честный `UNCONFIRMED` лучше выдуманного `CONFIRMED` | Владелец разрешает пробу (Completion, шаг 3) — строка становится CONFIRMED или CONTRADICTED |
| A-N6-013 | 2026-09-25 | Числа RAG — **гипотезы до калибровки**: фрагмент 500/600/80 токенов, `top_k = 4`, порог сходства 0.40, 2 хода истории | Бенчмарка на русских сайтах малого бизнеса нет; порог калибруется набором 20 + 20 вопросов (Refinement), критерий — 0 выдумок вне базы | Итог калибровки меняет число в каноне §7 новой редакцией |
| A-N6-014 | 2026-09-25 | Предпросмотр — **макет страницы, не скриншот** (ADR-007); ответ **без стриминга** (ADR-006) | Скриншот требует браузера в воркере и новой SSRF-поверхности; стрим показал бы непроверенный текст до проверки цитат | Владелец просит скриншот — v1 |
| A-N6-015 | 2026-09-25 | Планы `nobadge`/`studio` в неделю назначает **оператор командой** пилотным партнёрам; атрибуция конвертируется в этот момент | Оплата спящая (A-N6-011), а FR-GROWTH-004 (студии) нужен в неделю | Появляется свой магазин ЮKassa — план назначает оплата |
| A-N6-016 | 2026-09-25 | Сырой PDF — в томе воркера и удаляется после индексации (ADR-018), объектного хранилища нет | Меньше внешних зависимостей и ключей; источнику ответа достаточно имени файла и страницы | Владелец хочет «скачать исходник» из плашки — S3 из N5 |
| A-N6-017 | 2026-09-25 | Потолки платных вызовов (20 / 60 / 3000 ответов в сутки и др., канон §7) — **числа агента**; худшие сутки ≈ $20,9 | Правило model-call-cost требует число, а не намерение; числа подобраны так, чтобы один посетитель не съедал бюджет бота | Владелец меняет числа в `.env` и каноне |
| A-N6-018 | 2026-09-25 | Модель ответов зафиксирована как `anthropic/claude-haiku-4.5` (A-N6-010) с structured outputs; `CLAUDE.md` проекта в Phase 1 НЕ пишется — его генерирует Phase 3 (cc-toolkit-generator) | Каталог OpenRouter подтверждает `structured_outputs` у модели; Phase 3 владеет тулкитом | Владелец называет другую модель — смена `ANSWER_MODEL` и строки инвентаря |
| A-N6-019 | 2026-09-25 | **Проба эмбеддингов с ключом выполнена координатором**: 2026-09-25T21:19Z, с этой VPS, ключом стенда N5 (worker-llm), `POST https://openrouter.ai/api/v1/embeddings`, `openai/text-embedding-3-small`, `dimensions:1536` → **HTTP 200, 1536 измерений, 6 токенов, $0.00000012** | Строка inventory «доступность OpenRouter с этой VPS» была UNCONFIRMED; цена пробы пренебрежимо мала, а без неё Phase 2 не выше 🟡 | Не отменяется (факт); у N6 будет свой ключ |
| A-N6-020 | 2026-09-25 | **Квота предпросмотра разведена по `scope_key`** (закрытие H1/M2 Phase 2): `preview_session` → `<сессия>:create` (1/сутки, `QUOTA_PREVIEW_SESSION_CREATE`) и `<сессия>:answers` (10/сутки, `QUOTA_PREVIEW_SESSION_ANSWERS`); `global_previews` → `previews` (200, `QUOTA_GLOBAL_PREVIEWS`) и `preview_answers` (1000, `QUOTA_GLOBAL_PREVIEW_ANSWERS`). Scope — 10, переменных `QUOTA_*` — 14; число исправлено во всех цитатах N6 | Из двух способов валидатора выбран «развести», а не «убрать списание при создании»: второй терял записанный предел «1 предпросмотр на браузер». Одна переменная для двух чисел `global_previews` — тот же класс дефекта, найден при сквозном поиске | Владелец меняет числа — в `.env` и каноне §7; структура (14 имён) — новой редакцией канона |
| A-N6-021 | 2026-09-25 | **`web` берёт адрес клиента из ЕДИНСТВЕННОГО значения `X-Forwarded-For`** (дверь Caddy заменяет заголовок на `{client_ip}`); цепочка из двух и более адресов — отказ `503`, а не догадка. Параметр N5 `N5_TRUSTED_PROXY_HOPS` не перенесён. Префикс IPv6 — /48 (канон §7), у N5 был /64 | Число доверенных звеньев имеет смысл, когда прокси ДОПИСЫВАЕТ заголовок; дверь N6 его ЗАМЕНЯЕТ (урок N4), и лишняя переменная была бы ещё одним дефолтом | Дверь начнёт дописывать XFF или появится второй прокси внутри стека — вернуть параметр звеньев из N5 |
| A-N6-022 | 2026-09-25 | **`ANSWER_MODEL` и `EMBED_MODEL` принимаются только из закрытого набора кода** (`packages/rag/src/constants.ts`: `anthropic/claude-haiku-4.5`, `openai/text-embedding-3-small`); иное значение валит старт | honest-configuration CFG-I8: окружение выбирает из кода, а не задаёт; другая модель эмбеддингов = другая размерность = миграция (ADR-001) | Владелец выбирает другую модель — строка в наборе кода + канон §7 |
| A-N6-023 | 2026-09-25 | **Фича `foundation` не создаёт `apps/widget` и `packages/queue`** (в описании фичи они перечислены как workspace); закрытый набор `job_attempt.status` = `running · done · failed` (в каноне §4 не задан); добавлена пара потолков `QUOTA_ACCOUNT_EMBED ≤ QUOTA_GLOBAL_EMBED` сверх перечня Pseudocode LoadCeilings п.2 | Пустой workspace ради зелёной сборки — ложь (compose-hygiene правило 7): очередь приходит с `index-job-core` (её expected_files), виджет — с `widget-runtime-and-badge`; статус попытки нужен CHECK'у уже в 001; пара — прямое требование model-call-cost «персональный не больше суточного» | Канон задаст набор статусов попытки — миграция-дополнение; фича очереди/виджета добавляет workspace и строку `COPY` в Dockerfile |
| A-N6-024 | 2026-09-25 | **Фича `quota-and-spend` (тир XL «деньги») реализована без остановки на плане у владельца** — по постановке координатора (автономный режим). Решения внутри: (1) пробы старта (`EmbedProbe`, проба `ANSWER_MODEL`) — вызовы `свой-код`, предел `PROBE_DAILY_LIMIT = 24` на вид в сутки МСК (файл-счётчик в томе `model-spend`), строки добавлены в `model-cost-contract.md`; (2) `N6_SPEND_LOG` обязателен в `loadWebConfig`/`loadWorkerConfig` (абсолютный путь `.jsonl`) — без журнала процесс не стартует; (3) `@n6/db` зависит от `@n6/rag` (ключи квоты читают LoadCeilings, план и сутки МСК) — сборка `rag → db`; (4) основной вход квоты — `consumeQuota` в СВОЕЙ короткой транзакции: вызывающий не держит строки счётчика (включая общую `global_*`) на время вызова модели | (1) без предела `restart: unless-stopped` при сломанном шлюзе оплачивал бы пробу на каждом перезапуске; 10 scope канона — только для чужих вызовов; (2) RecordModelSpend п.2 «нет учёта — нет трат»; (4) shared-resource-verification п.1 | Владелец возвращает XL-чекпойнт — прочитать `docs/features/quota-and-spend/05_completion.md` и откатить по его разделу «Чего не доказывает»; другое число проб — константа кода + строка контракта |
| A-N6-025 | 2026-09-25 | **Фича `index-job-core`: решения внутри ADR-009.** (1) Поле `state` ответа `GET /api/index-jobs/{id}` — `running · done · failed · no_response` (Pseudocode называет их «выполняется · успех · отказ · нет_ответа»; `queued` показывается как `running`); (2) предел задачи 15 мин (канон §7) закрывает задачу причиной `stalled` — закрытый список `failure_reason` не содержит «превышено время», миграция ради одной причины не делалась; (3) сообщение очереди несёт `generation = current_fence` на момент постановки, аренда требует совпадения: `queued` → новая серия, `running` → автоповтор той же серии; серия, исчерпанная доставками без закрытия, — `failed(stalled)`; (4) маршрут `POST /api/bots/{bot_id}/sources` НЕ создан — только каркас БД `createSourceJob` (маршрут требует `CheckAddress` и приём PDF — фичи `crawler`, `pdf-source`); доступ к задаче по cookie предпросмотра — через зависимость, пока `null` (`preview-flow`); (5) до `crawler` воркер закрывает любую задачу `internal` (`noProcessorYet`), а не держит её «выполняется»; (6) бюджет предпросмотра 20 / 40 000 записывается в `index_job.page_budget/embed_budget` при создании, списание `embed_used` — `chunk-embed` (EmbedAndStore п.1) | ADR-009 и long-running-job требуют трёх различимых состояний и фенса; значения по п.1–3 нигде не заданы, выбор — наименьший, не требующий миграции | Канон задаст значения `state` или причину таймаута — миграция-дополнение (CHECK) и правка `indexJobView`/сторожа; маршрут источника появится с `crawler` |
| A-N6-026 | 2026-09-25 | **Фича `crawler`: решения внутри ADR-010.** (1) HTTP — встроенный `node:http(s)` вместо undici (ADR-010): соединение открывается прямо на проверенный IP (`host` = IP, `Host`/SNI = имя), второго DNS-запроса нет; ноль новых зависимостей. (2) HTML — собственный однопроходный разборщик тегов вместо `linkedom` (Architecture «Parsing»): нужны текст, заголовки и ссылки, не DOM. (3) Бюджет страниц — на ИСТОЧНИК по плану (50/300, предпросмотр 20 из задачи), а не остаток на бота по всем источникам: сумму по боту обязан проверять маршрут создания источника (`bot-cabinet`/`source-lifecycle`). (4) Пропуски с причинами пишутся в журнал и счётчиком `source.pages_skipped`, но разбивка по причинам НЕ хранится (строка `page` требует `content_hash`). (5) Бюджет времени обхода 12 мин (сторож закрывает серию через 15), потолок запросов 2 × бюджет + 10, дубль содержимого под другим адресом (www., `/index.html`) — пропуск `duplicate`. (6) Перенаправления вне хоста — `off_site`; IPv6 — разрешающий список 2000::/3 минус служебные подсети; документационные IPv4 (TEST-NET) запрещены | ADR-010 и Architecture называли библиотеки, но не требовали их; свойства (соединение по проверенному IP, обрыв чтения на 2 МБ, без исполнения JS) достижимы без зависимостей, а каждая зависимость — поверхность. Причины пропуска в БД — миграция CHECK/колонки, вне объёма фичи | (1)–(2) вернуть библиотеки — замена модулей `safe-get.ts`/`extract-text.ts` при тех же тестах; (3) остаток по боту — строка в `site-processor.ts`; (4) колонка причин — миграция 002 вместе с экраном итога |
| A-N6-027 | 2026-09-26 | **Фича `pdf-source`: решения внутри FR-SOURCE-003 и ADR-018.** (1) Разбор — `pdfjs-dist` **6.3.289** (Apache-2.0, Mozilla; `npm audit` — 0 новых уязвимостей; CVE-2024-4367 закрыт с 4.2.67, `isEvalSupported: false` выставлен всё равно), а не poppler `pdftotext`: poppler — GPL и C-код с историей ошибок памяти; pdfjs — чистый JS, его назвали Architecture и coding-style. Необязательный нативный `@napi-rs/canvas` удалён из образа и запрещён к загрузке в дочернем процессе. (2) Разбор — в ДОЧЕРНЕМ процессе `node extract-child.mjs` с пустым окружением; пределы исполняет родитель: таймаут 30 с на документ, резидентная память 384 МБ (замер `/proc/<pid>/status`; куча V8 256 МБ не видит буферы распаковки — flate-бомба 400 КБ давала 915 МБ RSS), stdout 16 МБ, извлечённый текст 2 млн символов. (3) Закрытый список `failure_reason` (11 причин, канон §4) НЕ расширен: `pdf_timeout`/`pdf_memory`/лишний текст/101 страница → `too_large`; шифрованный с паролем → `no_text_layer` (средство то же — загрузить текстовую версию); битый (включая одну непрочитанную страницу) → `not_pdf`; сбой самого разборщика → `internal`. Подробный код пишется в журнал воркера. (4) Файл пишется в том под `index_job_id`, выбранным web ДО вставки строки (`createSourceJobTx` принимает `indexJobId`), и ДО коммита: «строка задачи есть ⇒ файл есть»; удаляется в `onSettled` RunIndexJob после `done` И `failed` (не после `stale` и не перед автоповтором) + подметание тома сторожем раз в минуту (файлы завершённых и несуществующих задач старше 10 мин). (5) Предел числа PDF: считаются источники `kind = pdf` со статусом ≠ `failed`; проверка дважды — до приёма тела (дёшево) и в транзакции под `FOR UPDATE` строки бота (атомарно). (6) Маршрут принимает только multipart PDF; JSON `{ url }` → `422` (сайт этим маршрутом — будущая фича кабинета); `Origin` обязателен; тело читается в память с потолком 10 МБ + 64 КиБ. (7) «Повторить» у PDF после завершения даёт `internal`: файл уже удалён по ADR-018 — нужна новая загрузка | Канон задаёт границу файла (10 МБ, 100 страниц, 20 символов / 90 %, 3/10), но не пределы разбора; без них враждебный PDF держит воркер (concurrency 1) и память машины. Расширение закрытого списка причин — правка канона, CHECK и всех цитат (урок H1/M2), вне объёма фичи и решения агента | (1) другой разборщик — замена `extract-child.mjs` при тех же тестах; (2) числа — `apps/worker/src/pdf/limits.ts`; (3) новые причины — миграция CHECK + канон §4 + `enums.ts` + `PDF_FAILURE_REASON`; (6) приём потоком на диск вместо памяти — замена `readBounded`; (7) хранить файл до явного удаления источника — правка ADR-018 владельцем |
| A-N6-028 | 2026-09-26 | **Фича `chunk-embed`: решения внутри FR-INDEX-001/002 и ADR-001.** (1) **Поиск фрагментов — точный перебор внутри бота** (`packages/db/src/chunks.ts`, `SEARCH_CHUNKS_SQL`: `WITH own AS MATERIALIZED (… WHERE c.bot_id = $1)` → `ORDER BY distance LIMIT k`), а не упорядоченный обход HNSW с фильтром: прогон в образе (pgvector 0.8, `hnsw.ef_search = 40`, `hnsw.iterative_scan = relaxed_order`, планировщику запрещены перебор и сортировка) вернул **0 из 3** своих фрагментов бота A, когда 300 векторов бота B ближе к вопросу — изоляция цела, полнота нет («не знаю» при наличии ответа). Цена перебора измерена: 5001 фрагмент одного бота — 34 мс. Индекс HNSW в схеме ОСТАЁТСЯ (миграции только добавляющие) и поиском не используется; его поддержка стоит записи (замер 2,8 мс на фрагмент при вставке пачками по 50; пачка 500 случайных векторов не уложилась в `statement_timeout` 5 с). Утверждение Architecture «Data Architecture» про `iterative_scan` для больших ботов этим прогоном опровергнуто для проверенной конфигурации. (2) Токены — **оценка кода** (`estimateTokens`: ASCII ≈ 4 символа, кириллица ≈ 2, прочее 1 на токен), а не токенизатор поставщика: намеренно сверху, квота списывается по ней ДО вызова; фактические токены пишутся в журнал (`tokens_actual`). Цель 500 / потолок 600 / перекрытие 80 — в единицах этой оценки. (3) `context_path` = «заголовок страницы › h1 › h2 › h3» (h4–h6 — текст раздела), каждый заголовок ≤ 120 символов; перекрытие не пересекает границу раздела. (4) `chunks_done` — число фрагментов ИСТОЧНИКА (база — счёт в начале попытки, дельта «вставлено − удалено» в транзакции страницы). (5) Перед КАЖДОЙ оплачиваемой попыткой пачки — пульс и фенс задачи в той же короткой транзакции, что квота: опоздавшая попытка не платит; сутки квоты — по `now()` БД. (6) Бот без аккаунта и задача без `embed_budget` — `internal`, а не бесплатные эмбеддинги. (7) Шлюз 4xx/мусор — `embedding_unavailable` без повтора; длина ≠ 1536 — `internal` + строка «СИГНАЛ ОПЕРАТОРУ» в журнале воркера | Постановка требует изоляцию и полноту поиска на pgvector; HNSW с фильтром не дал второго на проверке. Токенизатора cl100k в зависимостях нет (ноль новых зависимостей). Прочее — канон задаёт числа, но не единицу токена и не способ оплаты черновика | (1) вернуть HNSW для ботов крупнее порога — отдельный путь по числу фрагментов бота + тест полноты на той же форме данных; либо снять индекс миграцией 002 (решение владельца); (2) заменить `estimateTokens` токенизатором и сверить с `tokens_actual` журнала; (3) числа — `packages/rag/src/constants.ts` |
| A-N6-029 | 2026-09-26 | **Фича `rag-answer`: решения внутри AnswerQuestion и ADR-003/011.** (1) Ядро `answerQuestion` (`packages/rag/src/answer.ts`) — БЕЗ маршрута: `/w/v1/ask` и предпросмотр подключат `visitor-ask-and-limits` и `preview-flow`; бот приходит АРГУМЕНТОМ ядра (серверное разрешение `loadAnswerBot`), тело посетителя — закрытый набор ключей `question`/`history` (`parseVisitorRequest`: `bot_id` и любой другой ключ — `invalid`). (2) Квота вопроса списывается ОДИН раз и покрывает эмбеддинг вопроса и ответ (контракт стоимости: «на каждый ответ ровно один эмбеддинг вопроса»); повторов нет ни у эмбеддинга вопроса, ни у модели — один ответ = одна попытка; отказ после списания не возвращает. (3) История (2 хода) передаётся ДАННЫМИ внутри сообщения user (`<история>`), а не сообщениями `assistant`: иначе посетитель подделал бы «наш» ход. Угловые скобки в чужом тексте (фрагменты, вопрос, история, имя компании) заменяются на ‹ ›: закрыть разделитель изнутри данных нельзя. (4) JSON-схема ответа ограничивает `citations` метками этого контекста (`enum`), серверная проверка — обязательна всё равно. (5) «Не знаю» из-за сбоя поставщика/таймаута/эмбеддинга пишется в `question_log` как `unknown` БЕЗ текста вопроса (это не «чего нет в материалах», сводке владельца не нужно); порог и `not_found`/выдумка — с текстом на 14 дней. (6) `searchChunks` возвращает `bot_id`; ядро отбрасывает чужой фрагмент второй линией (`ownHit`) и пишет «СИГНАЛ ОПЕРАТОРУ». (7) Цитата — на уровне ОТВЕТА (схема Specification FR-ANSWER-002), не каждого предложения. (8) `RecordWidgetInstall(first_answer)` и `RecordGrowthEvent(first_answer)` (AnswerQuestion п.8) в ядро не вошли — их владеет маршрут посетителя | Роадмап: ядро без маршрута; контракт стоимости считает эмбеддинг вопроса тем же scope; FR-ANSWER-004 требует, чтобы вопрос и история были данными | (1) маршрут приходит своими фичами; (2) повтор модели — `retries` в `meteredCall` + строка контракта (каждая попытка списывается); (3) история ролями чата — правка `prompt.ts` при тех же тестах инъекции; (5) хранить текст и у сбоев — одна строка `answer.ts`; (7) цитаты по предложениям — новая схема ответа + ValidateModelAnswer |
| A-N6-030 | 2026-09-26 | **Остаточный риск RAG вынесен владельцу (ревью rag-answer, HIGH):** барьер «цитата на весь ответ» проверяет форму (цитата есть и ведёт на свой фрагмент), но НЕ соответствие текста фрагменту: модель может процитировать настоящий прайс и дописать «скидка 90 %». Принятие риска — решение владельца, не агента; до решения: (1) калибровка 20+20 вопросов на настоящей модели (0 выдумок) до первого реального клиента; (2) тест «валидная цитата + выдуманный текст» фиксирует пробел явно; (3) история диалога — хранить на сервере или не принимать ходы ассистента от клиента (MEDIUM: поддельный ход усиливает атаку) — в `visitor-ask-and-limits` | Фича закоммичена как ядро, маршрута к посетителю ещё нет — риск не открыт наружу | Владелец: принять риск ADR-003 / потребовать проверку соответствия (NLI или вторую модель) |
| A-N6-031 | 2026-09-26 | **Фича `design-shell`: решения внутри ADR-012 и FR-LOOK-001…014.** (1) Палитра своя: холодная нейтральная база + ОДИН бирюзовый акцент (`#5fd0d8` / `#0b6e76`) только для «готово/включено» и денег; зелёный акцент CJM-прототипа не взят — он неотличим от N5; совпадений с N5 ноль (страж). (2) Onest — `next/font/local` из пакета `@fontsource-variable/onest@5.3.1` (OFL), а не `next/font/google`: сборка без сети и без тихого отката шрифта. (3) H1 на телефоне 2.5rem (≈ 2,5 × body), а не ≈ 3 ×: при 3 × первый экран 375×667 теряет поле адреса, а `vw`-часть ломает зум 200 % (R8); на десктопе 4rem = 4 × body. (4) Переключателя «месяц/год» (FR-LOOK-013) нет: годовых цен в каноне нет. (5) Секция доверия НЕ говорит «данные в РФ» (FR-LOOK-012): вопрос уходит модели за рубеж, 152-ФЗ — открытый вопрос №1; написано как есть. (6) Страничные маршруты `/pricing`, `/login`, `/dashboard` — канон §5 называет только API; форма лендинга ведёт на `/preview` (GET), которого нет до `preview-flow` — до неё отправка даёт 404. (7) `playwright@1.60.0` и `@axe-core/playwright@4.11.0` — НЕ в корне (у N5 в корне): страж ADR-007 (`tests/ssrf.test.ts`) запрещает playwright в корневом манифесте, корневые `node_modules` уезжают в образы; прибор получил свой пакет `scripts/responsive/package.json` (`npm ci --prefix scripts/responsive`, `**/node_modules` вне контекста Docker), тесты находят его псевдонимом `vitest.browser.config.ts` и `paths` в `tests/tsconfig.json`. Первая попытка (в корне) уронила этот страж в образе: 1 failed | 705 passed | Указание владельца «взять готовое» (N5) + FR-LOOK; числа и обещания — только из канона и фактов | (1) другой акцент — два значения в токенах; (3) 3 × — `h1` clamp + перепроверка R8/R9; (4) годовые цены в каноне → переключатель; (5) решение по 152-ФЗ → текст секции; (6) `preview-flow` создаёт `/preview` |
| A-N6-032 | 2026-09-26 | **Фича `preview-flow`: решения внутри FR-PREVIEW-001/002, FR-LIMIT-002, ADR-007/008.** (1) **Сегмент адреса маршрутов — `index_job_id`, а не `preview_token`** (канон §5 и Pseudocode «API Contracts» пишут `{preview_token}`): токен живёт только в HttpOnly-cookie `__Host-n6_preview`, в БД — HMAC; адрес уходит в журналы и Referer, а `index_job_id` доступа не даёт (задача обязана принадлежать боту предпросмотра из cookie, иначе 404). Маршруты: `POST /api/preview`, `GET /api/preview/{index_job_id}`, `POST …/ask`, `POST …/claim`; экран — `/preview/{index_job_id}` (SC-US-001-1). (2) **Сессия браузера — отдельный HttpOnly-cookie `__Host-n6_browser`** (30 дней), ключ квоты `preview_session` — HMAC от него; ответы предпросмотра списываются с сессии, ЗАПИСАННОЙ в строке предпросмотра, а не с текущего cookie — смена cookie не даёт новых 10 ответов на тот же предпросмотр. Сброс cookie даёт новую сессию; предел держат `ip_previews` (3 на /24) и `global_previews`. (3) **Idempotency-Key обязателен у `POST /api/preview`** (у Pseudocode его нет): повтор узнаётся по `(browser_session, idempotency_key)` — миграция `002_preview_flow.sql`, уникальный частичный индекс; повтор отдаёт ту же задачу без списания и ПЕРЕВЫПУСКАЕТ токен (сырой не хранится, а первый ответ с cookie мог потеряться). **Уточнение 26.09 (ревью preview-flow, находка 2; фича bot-cabinet):** ключ узнаёт повтор только от УЖЕ опознанного браузера — того, что прислал cookie `__Host-n6_browser`. Если потерялся самый первый ответ, cookie браузера у клиента ещё нет: сервер выдаёт новую сессию, пара `(сессия, ключ)` не совпадает, и повтор создаёт второй предпросмотр. Так же ведёт себя браузер, не хранящий cookie, и параллельные первые запросы. Ключ защищает от двойного клика и повтора уже опознанного браузера, а не от потери первого ответа; второй случай держат `ip_previews` (3 на /24) и `global_previews` (200 в сутки), цена — в `model-cost-contract.md`. Принято как есть. (4) **История диалога предпросмотра — на сервере** (`preview.history jsonb`, ≤ 2 хода, CHECK), тело вопроса — ровно `{ question }`; `history`, `bot_id` и любой другой ключ → `400 unexpected_field` (carry_over ревью rag-answer, находка 2). История очищается при claim; несохранённый предпросмотр удаляет сторож через 24 ч вместе с ботом (каскад). (5) **CheckAddress перенесён в `packages/rag/src/check-address.ts`** (подпуть `@n6/rag/check-address`, не из index — клиентские компоненты не тянут `node:dns`); `apps/worker/src/crawl/check-address.ts` — реэкспорт; мутации краулера перенацелены на новый путь (регресс 9/9). `web` проверяет адрес ДО квоты, записи и постановки; краулер проверяет снова на каждом соединении (DNS-rebinding). (6) **Подсказки-вопросы (FR-PREVIEW-001) — без модели:** до трёх тем, найденных в тексте фрагментов (цены, доставка, график, запись, гарантия, контакты), затем заголовки страниц; макет — заголовок страницы и первый H1 из `context_path` (ADR-007). (7) **CTA FR-GROWTH-001 — одно действие «Сохранить бота и поставить на сайт»**, второе («Поделиться ссылкой на бота») НЕ выведено: публичной страницы `/b/{slug}` ещё нет (фича `public-page-and-summary`), ссылке некуда вести. `share_cta_shown` пишется один раз на бота (сервер решает, первый ли answered); `share_cta_click` не пишется. (8) **Claim при регистрации И при входе** (Pseudocode — только регистрация): владелец с аккаунтом тоже сохраняет предпросмотр; предел ботов плана (`BOTS_BY_PLAN`: free/nobadge 1, studio 10) — под `FOR UPDATE` строки аккаунта; сбой claim вход не валит (`preview: unavailable`). Истёкший и удалённый сторожем — одинаково «создайте заново». | Постановка координатора (автономный режим); закрыть carry_over rag-answer и SSRF в `web` без ожидания владельца | (1) переименовать сегмент в `[token]` и передавать токен в пути — только вместе с запретом Referer и чисткой журналов прокси; (2)–(4) — правка `preview-handler.ts`/`previews.ts`, миграция добавляющая; (7) вторая кнопка — вместе с `/b/{slug}` |
| A-N6-033 | 2026-09-26 | **Фича `bot-cabinet`: решения внутри FR-BOT-001/002, FR-TARIFF-003, FR-INDEX-003.** (1) **Тестовый чат владельца — маршрут `POST /api/bots/{bot_id}/ask`** (в каноне §5 его нет; FR-BOT-001 требует «Задать вопрос» в кабинете). Бот — только по сессии владельца, тело — ровно `{ question }`: истории нет (каждый вопрос отдельно), `bot_id`/`history` → `400`. Режим ядра `owner` (активный бот). **Квота — три scope: `bot_day_answers`, `bot_month_answers` по плану и `global_answers`** — тот же бюджет ответов бота, что у посетителей (FR-TARIFF-003 «ответов в сутки на бота»); своего scope и переменной нет — канон держит 10 scope и 14 переменных. Вызов в журнале — `answer_owner`. **В `question_log` вопросы владельца не пишутся**: сводка FR-BOT-004 — про посетителей, а текст вопроса по 152-ФЗ хранится минимально. Строки контракта стоимости — «ответ-владельцу-haiku» и «эмбеддинг-вопроса-владельца». (2) **«Повторить» — `POST /api/sources/{source_id}/reindex` ТОЛЬКО для отказавшей задачи сайта**: тот же `index_job_id`, фенс +1, `queued`. PDF после отказа удалён с тома (ADR-018) — `409 reupload`, «загрузите заново»; готовый источник — `409 not_failed` до фичи `source-lifecycle` (FR-INDEX-004: переиндексация и удаление источника). **Удаления источника и домена в этой фиче нет**: `DELETE /api/sources/{id}` — `source-lifecycle` по роадмапу, маршрута удаления домена и бота в каноне нет. (3) **Контакт для «не знаю» — закрытый набор форм**: почта, телефон (10–15 цифр), ссылка `https://`; `http://`, `javascript:`, IP, служебные и невидимые символы — `422`. Стереть контакт нельзя (пустое значение — `422`, а не «сохранить пустоту»); из БД он читается fail-closed (`readContact`: старая строка вне форм — как отсутствие). (4) **Домен — IP-адрес отвергается любой, не только частный** (Pseudocode AddAllowedOrigin — только частные): виджет ставится на сайт с именем, а IPv6-литерал схема `allowed_origin` не пропускает всё равно. `localhost`, имя без точки, путь, запрос, учётные данные, наш `N6_PUBLIC_ORIGIN` — отказ; кириллический домен сохраняется в punycode. Предел 20 доменов — под блокировкой строки бота. (5) **Код установки без собранного виджета не выдаётся**: имя бандла — из манифеста сборки `apps/web/public/w/manifest.json` вида `{ "file": "widget.<hex>.js" }` (контракт для фичи `widget-runtime-and-badge`); нет манифеста — экран «виджет ещё не собран», а не тег на несуществующий файл. (6) **Предел ботов плана считает и боты, где аккаунт — студия** (`studio_account_id`, Pseudocode CreateBot п.1); одна функция `lockAccountBots` под `FOR UPDATE` строки аккаунта — у CreateBot и у ClaimPreview. (7) **Предела числа сайт-источников на бота нет** (канон не называет): расход держат `account_embed_tokens` (2 млн/сутки) и бюджет страниц плана на задачу (воркер). (8) Сайт-источник: CheckAddress в `web` ДО записи и постановки, владение — ДО CheckAddress (чужому боту — `404` без DNS-запроса). | Постановка координатора (автономный режим, фича 10) | (1) свой scope/переменная для чата владельца — правка канона §7 (15 переменных) и `ownerAnswerCharges`; история чата — хранить на сервере по образцу `preview.history`; (2) переиндексация готового — фича `source-lifecycle`; (4) разрешить публичный IP — убрать проверку `isIP` в `parseAllowedOrigin` (схема БД всё равно без IPv6); (5) иной формат манифеста — правка `widget-bundle.ts` |
| A-N6-034 | 2026-09-26 | **Фича `widget-runtime-and-badge`: решения внутри FR-WIDGET-001…004, FR-TARIFF-001, FR-GROWTH-003, ADR-004/005/013.** (1) **Маршрут конфигурации — `GET /w/v1/config?bot={public_key}` по канону §5**, а не `GET /w/v1/bots/{public_id}/config` из постановки координатора: канон — источник имён. (2) **Бандл раздаёт маршрут `web` `GET /w/[file]` из `apps/web/widget-bundle/`, а не статика `public/w/`** (меняет путь манифеста из A-N6-033 (5); `widget-bundle.ts` и тест кабинета перенацелены): текущий хэш — `immutable` на год, ЛЮБОЕ другое имя `widget.<hex>.js` — текущий бандл с `max-age=300`. Без этого каждый выпуск молча снимал бы виджет со всех сайтов, где стоит тег со старым хэшем (carry_over bot-cabinet «решить до выпуска»). Строка `immutable` в Caddy снята: дверь перезаписала бы кэш и ответу на старый хэш. API версионировано (`/w/v1`), старый тег с новым кодом совместим. (3) **Origin — только заголовок `Origin`, `Referer` не используется** (постановка: «Origin/Referer»): браузер шлёт Origin на каждый кросс-доменный fetch виджета; Referer лишь открыл бы небраузерным клиентам путь накручивать установки; Pseudocode CheckOrigin п.1 — отказ без Origin. (4) **Предполётный `OPTIONS` разрешается, если origin стоит в списке ХОТЯ БЫ одного бота** (или свой origin): бот в POST — в теле, в предполётном запросе его нет; сам POST сверяется со списком СВОЕГО бота. Индекса по `allowed_origin.origin` нет (≤ 20 строк на бота; при росте — добавляющая миграция). (5) **`visitor_session` генерирует виджет** (UUID v4 из `getRandomValues` в `sessionStorage` хозяина, ключ на бота; без хранилища — в памяти), сервер создаёт строку при первом событии и ПРИВЯЗЫВАЕТ её к боту и origin; чужая пара — `400 invalid_session`. Остаточный риск: посторонний, меняя id, раздувает показы бейджа (метрика i), не деньги — квоты ответов считают по сессии И префиксу IP (фича 12). (6) **Бейдж — внизу окна чата**, не на пузыре; показ отправляется при открытии окна, дедупликация (сессия, сутки МСК) — в БД по `now()`; бот без бейджа — событие `204` без записи. (7) **Стили — только `adoptedStyleSheets`**; запасного `<style>` нет: FR-WIDGET-003 называет запасной путь «`<style>` внутри shadow root с `style-src` нашего origin», но инлайновый `<style>` не покрывается источником, ему нужен `unsafe-inline`/nonce. Браузер без constructable stylesheets (Safari < 16.4) виджета не видит (fail-closed). (8) **Узел-хозяин — свой элемент `<n6-sufler>` в конце `body`**, `:host` с `all: initial` и `!important` (для `!important` побеждает теневой контекст — только так положение 56/16 держится против `* { … !important }` хозяина); `data-theme` тега переносится на узел. (9) **Конфигурация бота без годного контакта (readContact) — 404**, как несуществующего; отказ по origin на конфигурации в `question_log` НЕ пишется — это журнал вопросов, исход `refused_origin` пишет маршрут `/w/v1/ask` (фича 12). Лимит чтений — только дверь (120/мин), событий — ещё и лимит мутаций приложения. (10) **Контракт виджета с фичей 12:** виджет шлёт `POST /w/v1/ask { bot, visitor_session, question }` — БЕЗ истории (carry_over rag-answer: ходы ассистента от клиента не принимать) и ждёт `200 { data: { status: answered, text, source{title,url,excerpt} } \| { status: unknown, text } }`, `429 { error: { message } }`; до фичи 12 маршрута нет — окно отвечает «Не получилось получить ответ. Напишите: <контакт>». (11) Таймаут конфигурации 5 с (донор N1 — 300 мс: сеть посетителя чужого сайта). (12) Вступление окна — «Я бот, отвечаю по материалам сайта.» (FR-ANSWER-005) + приветствие бота; название компании — заголовок окна. | Постановка координатора (автономный режим, фича 11); carry_over bot-cabinet (старый хэш) | (1) переименование маршрута — правка канона §5 и `apps/web/src/app/w/v1/config`; (2) вернуть статику — `public/w/` + `immutable` в Caddy, но тогда хранить старые бандлы в образе; (3) Referer — вторая ветка в `requestOrigin`; (4) предполётный по боту — `?bot=` в адресе POST; (5) серверная выдача сессии в ответе конфигурации; (7) запасной путь — `<link rel=stylesheet>` на наш origin (+ `style-src <origin>` в директивах установки) |
| A-N6-035 | 2026-09-26 | **Фича `visitor-ask-and-limits` (POST `/w/v1/ask`): решения внутри FR-WIDGET-002, FR-LIMIT-001, FR-ANSWER-003, FR-BOT-003, FR-GROWTH-006, ADR-003/004/005/008/013/014.** (1) **Ворота показа ответа посетителю — отметка владельца «Я проверил ответы бота»** (`bot.answers_verified_at`, миграция 003; кнопка в кабинете `POST /api/bots/{id}/verify` с текстом о риске). Без отметки маршрут отвечает `200 { status: unknown, reason: not_verified }` «Бот ещё настраивается и пока не отвечает на вопросы. Напишите: <контакт>» — БЕЗ списания квоты и БЕЗ вызова эмбеддинга и модели (платить за ответ, который не покажут, незачем). Разрешение — строка БД, которую ставит владелец бота, а не переменная окружения (honest-configuration CFG-I8: код и данные владеют списком, окружение только выбирает). Тестовый чат владельца отвечает всегда — он и есть способ проверить. Отметка НЕ снимается при добавлении источника (известное ограничение). (2) **«Один посетитель» = id токена сессии + префикс /24**: токен выдаёт СЕРВЕР в `GET /w/v1/config` — `<uuid v4>.<HMAC-SHA256(SESSION_SECRET, метка домена, uuid, бот, origin, /24)>` (`apps/web/src/server/visitor-token.ts`); виджет хранит его в `sessionStorage` (cookie третьей стороны на чужом сайте ненадёжны) и шлёт в `event` и `ask`; прежний годный токен продолжается через `config?vs=`; токен с другого /24 — `409 session_expired`, виджет берёт новый и повторяет ОДИН раз. UUID, придуманный виджетом (форма фичи 11), больше не принимается нигде — иначе ключ `visitor_answers` выбирал бы клиент. Строка `visitor_session` создаётся лениво (первое событие или вопрос), `config` на каждый показ страницы ничего не пишет. (3) **Бот — в адресе `?bot=`** (как у `config`), тело — ЗАКРЫТЫЙ набор `{ visitor_session, question }`; `history`, `bot`, `bot_id`, любой другой ключ — `422 unexpected_field`. Отличие от Pseudocode «API Contracts» (`history[≤2]` в теле) — сознательное: carry_over ревью rag-answer (ходы ассистента от клиента не принимаются). CheckOrigin — ДО чтения тела; предполётный `OPTIONS /w/v1/ask?bot=` проверяется по списку ЭТОГО бота. (4) **ADR-004 на сервере:** на плане с бейджем вопрос принимается только от сессии с записанным `badge_impression` (`409 badge_required`); виджет ждёт доставки показа перед первым вопросом. Небраузерный клиент может прислать показ сам — это граница механизма (как A-N6-034 п.5), не защита денег. (5) **История посетителя — на сервере** (`visitor_session.history` ≤ 2 хода): читается, только если моложе 30 минут, сторож `worker-index` стирает её и текст вопросов «не знаю» по сроку 14 дней (`sweepVisitorText`, WatchdogTick п.5). Текст ANSWERED-вопроса живёт в истории ≤ 30 минут — это отступление от буквы «текст вопроса хранится только у unknown» (там речь о `question_log`), принятое ради 2 ходов контекста канона; **152-ФЗ — вопрос владельцу** вместе с открытым вопросом №1. (6) Отказ по origin — строка `question_log` `refused_origin` без текста (security.md). (7) Слоты проб старта — `O_CREAT|O_EXCL`, а не «дописать и прочитать» (ревью quota-and-spend M1; прежняя форма под 48 одновременными стартами выдала 23 слота из 24). (8) Файл `apps/web/src/server/record-widget-install.ts` из `expected_files` не создан: `recordWidgetInstall` уже живёт в `packages/db/src/widget.ts` (фича 11), дубль расходился бы молча. | A-N6-030 (валидная цитата не доказывает содержание ответа) не закрыт, а ADR-003 требует решения владельца до открытия маршрута наружу; маршрут при этом обязан работать (установки, метрика недели). Самое строгое, что не ломает продукт, — не показывать непроверенное. Остальное — carry_over ревью rag-answer, quota-and-spend, widget | (1) владелец принимает риск ADR-003 — ворота можно оставить как опцию бота или снять одной строкой в `widget-ask-handler.ts`; либо закрыть A-N6-030 сверкой чисел/NLI и тогда отметку сделать необязательной; (2) токен — `visitor-token.ts`; ротация `SESSION_SECRET` обнуляет все токены (виджеты молча возьмут новые); (3) вернуть `history` в тело — только вместе с серверной проверкой каждого хода; (5) срок истории — константа `VISITOR_HISTORY_TTL_MINUTES` в `packages/db/src/visitor.ts` |
