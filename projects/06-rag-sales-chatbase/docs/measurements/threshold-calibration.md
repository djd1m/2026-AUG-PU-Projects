# Калибровка порога «не знаю» — N6 «Суфлёр»

**Статус измерения:** НЕ ИЗМЕРЕНО
**Причина:** живые вызовы модели ответа и эмбеддингов в фиче `rag-answer` запрещены постановкой (автономный режим, 2026-09-26); набор требует двух реальных ботов (сайт N5 и сайт группы `seed-dogfood`) и двух людей-разметчиков.
**Порог в коде:** `MIN_SIMILARITY = 0.40` (`packages/rag/src/constants.ts`) — гипотеза A-N6-013, НЕ откалиброван.
**Когда выполнить:** до выпуска (Refinement «Набор калибровки»), после появления маршрута ответа и своего ключа OpenRouter N6.

## Протокол (Refinement, testing.md)

1. 20 вопросов «в базе» — URL страницы-ответа записывается ДО прогона.
2. 20 вопросов «вне базы» — правдоподобные, ответа в материалах нет.
3. Для каждого: сходство лучшего фрагмента, исход, текст, цитата.
4. Разметка двумя людьми: `верно` / `выдумка` / `отказ`; расхождение — третье прочтение цитаты.
5. Приёмка: **0 выдумок из 20 «вне базы» И ≥ 16 из 20 «в базе» верно**; шаг порога 0.05; при конфликте — приоритет у нуля выдумок, и это записывается как ограничение.

## Таблица прогона

| № | Набор | Вопрос | URL ответа (до прогона) | Сходство лучшего | Исход | Цитата | Разметчик 1 | Разметчик 2 | Итог |
|---|---|---|---|---|---|---|---|---|---|

(пусто — прогона не было; автотесты `tests/rag-answer.test.ts` проверяют механику порога, а не его величину)
