# N6 «Суфлёр» — бэклог: что осталось сделать

Состояние на 26.09.2026. Roadmap — `.claude/feature-roadmap.json` (17 фич): готовы 1–12, впереди 13–17. У многих фич есть поле `carry_over` — перенесённые находки ревью, обязательные к выполнению в той фиче.
Все решения, принятые без владельца, — `docs/decisions-autonomous.md` (A-N6-001…). Стенда N6 ещё нет.

## 1. Перенос на другой сервер — чего НЕТ в репозитории

| Что | Где сейчас | Что делать |
|---|---|---|
| Одноразовый env для тестового стека (пароли БД/Redis, 14 `QUOTA_*` и прочее) | `/tmp/n6-foundation.env` (вне git, **/tmp может очиститься**) | пересоздать по `.env.example`, пароли `openssl rand -hex 24` |
| Ключ OpenRouter для N6 | своего нет — проба эмбеддингов делалась ключом стенда N5 (A-N6-019) | завести отдельный ключ N6 (свой потолок расхода у поставщика) |
| Данные | стенда нет — переносить нечего | — |
| Зависимости прибора вёрстки | `scripts/responsive/` — отдельный `package.json` вне workspaces (страж ADR-007) | `npm ci --prefix scripts/responsive`; образ `mcr.microsoft.com/playwright:v1.60.0-noble` скачается сам |

## 2. Фичи roadmap, которые осталось сделать (порядок — по зависимостям)

| № | Фича | Главное | Обязательные переносы (`carry_over`) |
|---|---|---|---|
| 12 | `visitor-ask-and-limits` | `POST /w/v1/ask` — вопросы посетителей виджета, пределы на посетителя / сеть / бота / глобально | гонка счётчика проб старта (M1 quota), период квоты от часов БД (M2), история диалога только на сервере, тест «валидная цитата + выдуманный текст», unit-тест origin с точкой/punycode — **сделано 26.09; НЕЗАВИСИМОГО РЕВЬЮ ФИЧИ 12 ЕЩЁ НЕ БЫЛО** (провести первым делом после переезда); не проверены в браузере кнопка «Я проверил ответы бота» и баннер лимита; отметка «проверено» не снимается при добавлении источника |
| 13 | `public-page-and-summary` | публичная демо-страница бота `/b/<slug>` (вторичная тактика роста), сводка вопросов | баннер исчерпания месячного лимита называет расход чатом владельца |
| 14 | `tariffs-and-interest` | тарифы 0 / 990 / 4 900 ₽, экран интереса (оплата спящая) | «цены предварительные» — рядом с цифрами |
| 15 | `partner-and-studio` | партнёры-веб-студии: код студии, несколько ботов, передача бота клиенту | предел ботов клиента при передаче — под той же блокировкой |
| 16 | `source-lifecycle` | переиндексация, удаление источника | предел попыток загрузки PDF (удержание общего воркера), тест CL+TE через Caddy, бюджет токенов на источник, длинная транзакция огромной страницы, NFR-SCALE-001 (1 млн фрагментов), константа `OWNED` в PDF-ветке |
| 17 | `account-erasure` | удаление аккаунта и данных | — |

После фич: **стенд** (docker compose up на VPS, домен, TLS), прогон прибора вёрстки и `check-embed-contract` /
`check-job-contract` по ВЫДАННОМУ адресу (сейчас законно `2 — not-deployed`), сквозной путь CJM H.

## 3. Ждут решения владельца

1. **Риск ответов бота (A-N6-030) — главный.** Проверка цитат не сверяет текст ответа с фрагментом: модель может
   процитировать настоящий прайс и дописать выдумку. Принять после калибровки 20+20 вопросов на живой модели
   (0 выдумок) или проверять соответствие второй моделью. До решения — ответ показывается посетителю только у
   бота, помеченного владельцем «проверен» (A-N6-035, фича 12). Предложение исполнителя: (а) каждое число, ₽, % и срок из ответа обязаны встречаться в процитированном фрагменте, иначе «не знаю»; (б) непроверенному боту показывать только фрагмент-источник.
2. **Вариант CJM** — временно принят гибрид H (A-N6-008).
3. **152-ФЗ** — вопросы посетителей уходят модели за рубеж; секция доверия НЕ обещает «данные в РФ».
4. **Модель ответов** — Haiku 4.5 через OpenRouter (A-N6-010); эмбеддинги `openai/text-embedding-3-small`
   через OpenRouter (проверено пробой 200/1536, A-N6-019).
5. **Цены и метрика недели** — гипотезы: 0 / 990 / 4 900 ₽, 15 виджетов на внешних доменах (A-N6-004/005).
6. **Тестовый магазин ЮKassa для N6** — у магазина один адрес уведомлений; до него оплата спящая (A-N6-011).
7. **Чат владельца делит лимит ответов с посетителями** (A-N6-033).
8. **Рабочее имя «Суфлёр»** — доступность имени и домена не проверялась (A-N6-003).
9. **Отказ от HNSW-поиска** в пользу точного перебора внутри бота (A-N6-028) — пересмотреть при росте ботов.

## 4. Не проверено

- Живые модели (ответы, эмбеддинги в работе) — только фейки в тестах; порог сходства 0,40 не откалиброван.
- Стек целиком (`docker compose up`), healthcheck'и и порядок старта; XFF через настоящий Caddy.
- Виджет на настоящем чужом сайте после развёртывания; Safari < 16.4 (фолбэк закрывается fail-closed).
- Независимое кросс-семейное ревью: весь код N6 написан Opus и проверен Sonnet (OWN-017, OpenAI не используется).
