// Пределы РАЗБОРА PDF (worker-index). Числа границы файла (10 МБ, 100 страниц, 20 символов, 90 %,
// 3/10 PDF по плану) — канон §7, живут в @n6/rag/constants (общие с web). Здесь — числа агента
// (A-N6-027): канон их не задаёт, а разбор враждебного файла без них не ограничен ничем.
export const PDF_EXTRACT_TIMEOUT_MS = 30_000;          // весь разбор документа; зависший разбор — отказ, а не ожидание
export const PDF_CHILD_HEAP_MB = 256;                  // --max-old-space-size дочернего процесса (куча V8)
export const PDF_CHILD_MAX_RSS_BYTES = 384 * 1024 * 1024; // резидентная память целиком: буферы распаковки живут ВНЕ кучи V8
export const PDF_RSS_POLL_MS = 25;                     // частота замера памяти дочернего процесса
export const PDF_MAX_TEXT_CHARS = 2_000_000;           // извлечённый текст документа целиком (≈ 20 000 на страницу × 100)
export const PDF_MAX_OUTPUT_BYTES = 16 * 1024 * 1024;  // stdout дочернего процесса; больше — обрыв и отказ
export const PDF_MAX_STDERR_BYTES = 4096;              // stderr читается только для распознавания нехватки памяти
