# BEGIN N4 TARELKA PUBLIC WEB
# Демо-стенд «Тарелка» — фото-трекер калорий (projects/04-calorie-vision-cal-ai).
# Временное имя sslip.io: разрешается в 212.192.0.33 без A-записи, как у N3.
#
# Проксируется СОБСТВЕННАЯ дверь проекта (его Caddy), а не `web` напрямую: на двери
# живёт ограничение частоты по каноническим числам (30 мутаций и 120 чтений в минуту
# на адрес) и маршрутизация `/api/*` и `/health` в `api`. Мимо двери обошлось бы и то,
# и другое.
#
# Дверь берёт адрес клиента из X-Forwarded-For ТОЛЬКО от этой сети (она объявлена
# доверенной в её Caddyfile). Без этого предел «на адрес» стал бы пределом «на всех»:
# все запросы приходят с адреса этого прокси.
n4.212.192.0.33.sslip.io {
	encode gzip zstd
	header X-Robots-Tag "noindex, nofollow"
	reverse_proxy n4-tarelka-proxy-1:80
}
# END N4 TARELKA PUBLIC WEB
