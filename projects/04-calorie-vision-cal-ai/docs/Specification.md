# N4 «Тарелка» — Specification

**Версия:** 0.1 · **Дата:** 2026-09-12 · **CJM:** E — гибрид «путь A + блок D»
(«код блогера и кабинет партнёра»), зафиксирован владельцем 2026-09-12
([`CJM_Variants.md`](CJM_Variants.md), раздел «Решение владельца»).
**Канон имён:** [`canon.md`](canon.md) — заморожен 2026-09-12; идентификаторы, области, сущности,
маршруты и числа берутся оттуда и здесь не переизобретаются.

## 1. Контракт спецификации

Документ определяет НАБЛЮДАЕМОЕ поведение продукта недели: что пользователь видит, что система
принимает и чем отвечает при отказе. Он НЕ определяет физическую схему БД, схемы запросов и ответов,
алгоритмы и топологию контейнеров — это `Architecture.md` и `Pseudocode.md`.

Единицы и соглашения, действующие во всём документе: масса — граммы (целое), энергия — килокалории
(целое), макронутриенты — граммы с одним знаком после запятой. Календарные сутки, границы дня,
стрик и суточные потолки считаются в `Europe/Moscow`. Идентификаторы сущностей — UUID. Статус
`recognition.status` принимает ровно четыре значения `queued | done | failed | refused`; статус
`attribution.status` — ровно три `pending | activated | rejected`; иные значения не создаются и при
чтении трактуются как самое строгое (`failed`, `rejected` соответственно).

Тарифов в неделе два: `free` и отсутствующий `paid`. Всё, что не равно ровно `paid`, считается `free`
и требует бейджа — это правило fail-closed, а не настройка. Подписки, оплаты и выплаты партнёрам в
неделю не входят (PD-PRICE-001): их место занимает экран интереса (FR-LIMIT-003).

Бренд продукта-источника упоминается только как референс облика в
[`source-product-profile.md`](source-product-profile.md) и не появляется в интерфейсе, текстах и
данных продукта. Процент точности распознавания не заявляется нигде и никогда (PD-INSIGHT-001).

## 2. Функциональные требования

### FR-CAPTURE-001
Первый экран продукта — видоискатель камеры; съёмка доступна анонимно, без регистрации и без
анкеты (PD-CORE-002, FR-LOOK-006). Под видоискателем ровно одна строка режимов: «съёмка» и
«галерея»; штрихкод, ручной ввод и голос в неделю не входят. Готовый к съёмке видоискатель
показывается не позже 1 с после открытия (NFR-PERF-002). Первое обращение создаёт анонимную сессию
устройства (FR-AUTH-001).

### FR-CAPTURE-002
Принимаются JPEG, PNG и HEIC размером не больше 12 МБ и не меньше 320×320 px; клиент уменьшает
длинную сторону до 1536 px перед отправкой. Непригодный формат или размер даёт отказ с названной
причиной и кнопкой повторной съёмки, и такая попытка НЕ списывается с квоты (FR-LIMIT-001), потому
что вызова модели не было. Оригинал кладётся в приватный бакет и удаляется через 30 дней
(NFR-SEC-001); публичной ссылки на фото не существует.

### FR-RECOGNIZE-001
Модель получает изображение и возвращает три вещи: список ингредиентов, оценку массы каждого в
граммах и до трёх кандидатов записей базы на ингредиент. Модель НЕ возвращает калорийность и БЖУ —
эти числа берутся только из базы (FR-SOURCE-001). Результат сохраняется как `recognition` со
статусом из канона. Кадр без распознаваемой еды завершается статусом `refused` с подсказкой
«еда не распознана, снимите тарелку целиком» и не создаёт записи дневника.

### FR-RECOGNIZE-002
Каждое распознавание несёт уверенность в диапазоне 0..1. При уверенности ниже 0,6 выполняется ровно
одна повторная попытка более сильной моделью (PD-ARCH-001); обе попытки считаются в потолках
(FR-LIMIT-001, FR-LIMIT-002), потому что счёт идёт по ПОПЫТКАМ, а не по успехам. Если после
эскалации уверенность всё ещё ниже 0,6, показывается экран «уточните блюдо» с ручным поиском по
базе — тихого усреднения и молчаливого выбора первого кандидата не происходит.

### FR-SOURCE-001
Каждое число дневника вычисляется как `(масса_г / 100) × значение_на_100г` из записи `food_item`.
Оценка калорийности, полученная от модели, не попадает в дневник ни при каких условиях и служит
только материалом для сверки (FR-CORRECT-003). Если подходящей записи базы нет, позиция помечается
«нет в базе» и исключается из итога с видимой пометкой; ноль вместо неизвестного числа не
подставляется.

### FR-SOURCE-002
Под каждым числом видно, откуда оно взято: имя базы (USDA FoodData Central), идентификатор записи,
её название, использованная порция в граммах и дата импорта снимка базы (PD-CORE-001, PD-DATA-001).
Тап по строке источника открывает карточку записи с полным набором значений на 100 г. У
продукта-источника такого экрана нет — это авторское отличие клона (FR-LOOK-012), и оно
обязательно на экране результата, на экране дня и на карточке 9:16.

### FR-SOURCE-003
Русские блюда обслуживает таблица синонимов: 100–300 частых названий, каждое сопоставлено либо одной
записи USDA, либо рецептурной комбинации компонентов с долями (PD-DATA-003). Поиск нечёткий, по
триграммам, и учитывает падежи через нормализованную форму. Комбинация показывает состав и источник
каждого компонента отдельно — «борщ» не становится безымянным числом. Open Food Facts не
подключается до юридической проверки условий ODbL (PD-DATA-002).

### FR-CORRECT-001
Порция правится степпером с шагом 10 г, длинное нажатие даёт шаг 50 г, поле ручного ввода принимает
5–2000 г. Пересчёт всех четырёх чисел выполняется локально и мгновенно, без нового вызова модели и
без списания квоты. Изменённая порция сохраняется в записи дневника с пометкой «порция
скорректирована пользователем» и с исходной оценкой рядом.

### FR-CORRECT-002
Тап по ингредиенту открывает поиск по базе (синонимы и USDA) и позволяет заменить запись; числа
пересчитываются по FR-SOURCE-001. Замена пишется в историю поправок и питает курацию синонимов
(FR-SOURCE-003). Новый вызов модели при замене не выполняется и квота не списывается. Ингредиент
можно удалить из блюда — итог пересчитывается немедленно.

### FR-CORRECT-003
Если оценка калорийности блюда, полученная от модели, отличается от суммы по базе более чем на 15%,
показывается экран расхождения: два числа рядом, у каждого подписан источник, и две кнопки — «взять
из базы» (выбрана по умолчанию) и «уточнить состав». Тихое усреднение запрещено (PD-CORE-001).
Выбор пользователя сохраняется вместе с обоими числами, чтобы расхождения можно было пересчитать
позже.

### FR-DIARY-001
Подтверждённый результат создаёт запись дневника с датой по `Europe/Moscow` и меткой приёма пищи
(завтрак, обед, ужин, перекус — определяется по времени, правится одним тапом). Экран дня
показывает список записей, итог по ккал и трём макронутриентам и разбивку по приёмам. Записи
редактируются и удаляются, итог пересчитывается сразу. Дневник виден только владельцу — публичной
ссылки на день в неделе нет.

### FR-DIARY-002
Стрик — число последовательных суток, в которых есть хотя бы одна запись. Стрик мягкий: один
пропущенный день его не обнуляет и помечается как «заморожен», два пропущенных подряд обнуляют.
Считается по `Europe/Moscow` и показывается на экране дня нейтральной формулировкой без давления и
без предупреждений о потере.

### FR-SHARE-001
После первого успешного распознавания карточка 1080×1920 уже готова, без единого действия
пользователя: фото, название блюда, четыре числа, строка источника и бейдж «распознано в Тарелке»
(FR-GROWTH-003, FR-LOOK-007). Вес, цель, итог дня и любые другие данные о здоровье на карточку не
попадают (FR-GROWTH-006). Карточка доступна по адресу `/c/{card_id}` с непредсказуемым
идентификатором; удаление карточки владельцем немедленно закрывает публичный доступ.

### FR-PARTNER-001
Код блогера — читаемая строка 4–12 символов из A-Z и 0-9, регистр при вводе не важен, выдаётся
вручную оператором и принадлежит ровно одному партнёру. Код применяется по deeplink или вводится
руками после первого результата. Явный действительный код сильнее cookie и параметра ссылки;
явный НЕдействительный код — ошибка ввода, и система НЕ подставляет вместо него cookie
(FR-GROWTH-002). Применение создаёт атрибуцию со статусом `pending`.

### FR-PARTNER-002
Кабинет партнёра показывает по его собственному коду четыре счётчика: переходы, установки,
активации (пользователь с хотя бы одним успешным распознаванием) и шеринги карточек, в окнах
«день», «неделя», «всё время». Партнёр видит только свои данные; чужой код в ответе не появляется
даже при прямом запросе. Денег, ставок и реестра выплат в кабинете нет — их в неделе не существует
(PD-PRICE-001).

### FR-PARTNER-003
Более 50 применений одного кода с одного IP-адреса за 10 минут блокируют код: дальнейшие применения
получают статус `rejected`, а событие пишется в аудит с кодом, усечённым префиксом адреса и
временем. Партнёр не может применить собственный код. Разблокировка выполняется оператором вручную
и тоже пишется в аудит. Пороги взяты из проекта N1 как отправная точка и пересчитываются после
первой недели реального трафика.

### FR-LIMIT-001
Потолок на пользователя — 10 попыток распознавания в сутки по `Europe/Moscow`. Для анонима ключом
служат ОДНОВРЕМЕННО сессия устройства и усечённый IP-префикс: исчерпание любого из двух ключей даёт
отказ, иначе смена одного из них обнуляет защиту. Счёт ведётся по попыткам: эскалация к сильной
модели, таймаут и отказ провайдера списываются наравне с успехом, потому что провайдер берёт деньги
за попытку (PD-COST-001). Достижение потолка — ОТКАЗ с экраном лимита, не переход на модель
подешевле и не очередь.

### FR-LIMIT-002
Суточный потолок системы — 3000 попыток. При его достижении новые сканы получают отказ с экраном
«на сегодня лимит исчерпан», уже начатые распознавания доводятся до конца. Текущий расход за сутки
и остаток потолка видны оператору на служебной странице расхода (NFR-OPS-001). Не заданный в
конфигурации потолок валит запуск сервиса с сообщением, называющим ненастроенный вызов: пустое
значение переменной не означает «ограничений нет».

### FR-LIMIT-003
Экран лимита предлагает оставить почту или контакт в Telegram для «Pro скоро» и явно сообщает, что
оплаты сейчас нет. Запись интереса сохраняется с датой, источником (лимит пользователя или суточный)
и кодом партнёра, если он был применён. Экран показывается не чаще одного раза в сутки на
пользователя. Цена, тариф и любая платёжная форма на нём отсутствуют — практики продукта-источника,
наказанные магазином приложений, не воспроизводятся (PD-INSIGHT-002).

### FR-AUTH-001
Первый запуск создаёт анонимную сессию устройства: непредсказуемый идентификатор в httpOnly-cookie,
достаточный для съёмки, получения результата и просмотра карточки. Дневник анонима хранится 7 суток
и при входе переносится в аккаунт целиком. IP-адрес хранится только усечённым префиксом и только
для квоты и anti-fraud; для аналитики он не используется.

### FR-AUTH-002
Вход выполняется через Telegram Mini App: подпись `initData` проверяется ПЕРВОЙ, до разбора
полезной нагрузки, по сырым байтам, сравнением постоянного времени, и `auth_date` не старше 24
часов. Не прошедшие проверку данные дают отказ 401, аккаунт при этом не создаётся и сессия не
выдаётся. Успешный вход связывает аккаунт с сессией устройства и переносит анонимный дневник.

### FR-AUTH-003
До первой записи в дневник пользователь даёт явное согласие на обработку данных о питании как
специальной категории персональных данных (PD-LEGAL-001); отказ оставляет продукт работоспособным —
съёмка и результат доступны, дневник не ведётся. Согласие версионировано: сохраняются текст, версия
и время. Удаление данных по запросу из настроек удаляет аккаунт, дневник, карточки и фото не позже
72 часов, и отзыв согласия немедленно закрывает все публичные карточки.

## 3. Growth-требования

Промоушен таблицы-семени брифа ([`product-discovery-brief.md`](product-discovery-brief.md), M5).
Идентификаторы не переименовываются. Каждое требование несёт три сценария: happy-path с числовым
порогом, edge-case и security.

### FR-GROWTH-001
Призыв поделиться и готовая карточка 9:16 показываются ровно на экране ПЕРВОГО успешного
распознавания, а не в онбординге и не на экране лимита. Карточка собрана заранее: пользователь
нажимает «поделиться» и получает файл, а не запускает генерацию. Данные о здоровье, кроме состава
самого блюда, на карточку не попадают.

```gherkin
@FR-GROWTH-001 @growth @happy-path
Scenario: карточка готова в момент первого результата
  Given анонимный пользователь получил первое успешное распознавание
  When открывается экран результата
  Then призыв «поделиться» виден без прокрутки, а файл карточки отдаётся не позже 2 с после нажатия

@FR-GROWTH-001 @growth @edge-case
Scenario: распознавание не состоялось — делиться нечем
  Given распознавание завершилось статусом refused
  When открывается экран результата
  Then призыв «поделиться» не показывается и карточка не создаётся

@FR-GROWTH-001 @growth @security
Scenario: чужая карточка по прямому адресу
  Given карточка удалена владельцем
  When её адрес /c/{card_id} открывают по сохранённой ссылке
  Then возвращается 404 без содержимого карточки и без данных дневника
```

### FR-GROWTH-002
Атрибуция к блогеру хранится до платной конверсии, которой в неделе нет, и потому остаётся в статусе
`pending`; активацией считается первое успешное распознавание. Явный действительный код сильнее
cookie и deeplink. Явный недействительный код не откатывается к cookie — это правило поведения,
перенесённое из N1 (PD-REUSE-001), а не оптимизация.

```gherkin
@FR-GROWTH-002 @growth @happy-path
Scenario: явный код сильнее cookie
  Given в браузере сохранён cookie-источник с кодом OLD
  When пользователь вводит действительный код NEW и получает первое распознавание
  Then атрибуция создана ровно одна, с кодом NEW и статусом pending

@FR-GROWTH-002 @growth @edge-case
Scenario: недействительный явный код не откатывается к cookie
  Given в браузере сохранён cookie-источник с кодом OLD
  When пользователь вводит несуществующий код ZZZZ
  Then показана ошибка ввода, атрибуция по OLD не создаётся и ни один код не засчитан

@FR-GROWTH-002 @growth @security
Scenario: самореферал партнёра
  Given партнёр вошёл в свой аккаунт
  When он применяет собственный код
  Then атрибуция получает статус rejected и попадает в аудит
```

### FR-GROWTH-003
Бейдж «распознано в Тарелке» присутствует на каждой карточке и на каждой публичной странице
бесплатного тарифа. Снятие бейджа — будущая платная опция, в неделю не входит; кнопки снятия в
интерфейсе нет. Решение о бейдже принимается по правилу fail-closed: всё, что не равно ровно `paid`,
требует бейджа.

```gherkin
@FR-GROWTH-003 @growth @happy-path
Scenario: бейдж на карточке бесплатного тарифа
  Given у пользователя тариф free
  When он открывает готовую карточку 9:16
  Then бейдж виден на карточке, занимает не более 8% высоты и не перекрывает числа

@FR-GROWTH-003 @growth @edge-case
Scenario: тариф неизвестен
  Given значение тарифа отсутствует или не распознано
  When собирается карточка
  Then бейдж ставится как при free, и отсутствие значения не читается как «платный»

@FR-GROWTH-003 @growth @security
Scenario: подделка тарифа на клиенте
  Given клиент отправляет запрос с полем tariff=paid
  When сервер собирает карточку
  Then тариф читается только из базы, и бейдж остаётся на месте
```

### FR-GROWTH-004
У каждого человека с аудиторией есть персональный трекируемый код, и кабинет партнёра показывает
когортную воронку «переходы → установки → активации → шеринги». Без кода стоимость привлечения по
когорте не считается ничем. Выплаты, ставки и денежные суммы в неделю не входят — в кабинете живут
только счётчики.

```gherkin
@FR-GROWTH-004 @growth @happy-path
Scenario: воронка когорты видна партнёру
  Given по коду партнёра было 100 переходов, 40 установок, 24 активации и 6 шерингов
  When партнёр открывает кабинет за неделю
  Then показаны ровно эти четыре числа с подписью окна и временем последнего обновления

@FR-GROWTH-004 @growth @edge-case
Scenario: код без единой установки
  Given по коду партнёра не было ни одного перехода
  When партнёр открывает кабинет
  Then показаны нули с пометкой «данных нет», а не пустой экран и не прочерки

@FR-GROWTH-004 @growth @security
Scenario: запрос чужой когорты
  Given партнёр А запрашивает дашборд с кодом партнёра Б
  When запрос доходит до сервера
  Then возвращается 403, и счётчики чужой когорты не раскрываются
```

### FR-GROWTH-005
План недели содержит поимённый seeding-план: группы первых 20–50 шерящих, причина, по которой каждая
группа будет делиться, канал и бюджет (PD-GROWTH-005). «Надежда на вирусность» планом не является:
собственной виральности у продукта нет по построению, петля стартует только от посева. Каждой
группе выдаётся свой код, иначе когорту невозможно отличить от органики.

```gherkin
@FR-GROWTH-005 @growth @happy-path
Scenario: план недели поимённый
  Given наступил старт недели измерения
  When открывается план посева
  Then в нём не менее 4 групп, суммарно 20–50 человек, у каждой группы названы причина, канал и код

@FR-GROWTH-005 @growth @edge-case
Scenario: канал без собственного кода
  Given размещение выпущено без персонального кода
  When считается стоимость привлечения по когорте
  Then когорта помечается «не измерена», и её установки не приписываются другому каналу

@FR-GROWTH-005 @growth @security
Scenario: оплаченное размещение без маркировки
  Given блогер получил вознаграждение за публикацию
  When размещение готовится к выходу
  Then публикация не выпускается без идентификатора маркировки рекламы, и этот факт фиксируется
```

### FR-GROWTH-006
Любая публикация или передача записей дневника требует явного согласия на обработку данных о питании
как специальной категории персональных данных; по умолчанию всё закрыто. Отзыв согласия немедленно
закрывает публичные карточки. В неделе публичной страницы дня нет, но правило действует на карточку
и на любой будущий дайджест.

```gherkin
@FR-GROWTH-006 @growth @happy-path
Scenario: дневник не ведётся без согласия
  Given пользователь не дал согласие на обработку данных о питании
  When он подтверждает результат распознавания
  Then запись дневника не создаётся, показан экран согласия, и съёмка остаётся доступной

@FR-GROWTH-006 @growth @edge-case
Scenario: отзыв согласия при живых карточках
  Given у пользователя опубликованы 3 карточки
  When он отзывает согласие
  Then все 3 адреса перестают отдавать содержимое не позже 60 с, а данные удаляются в срок 72 ч

@FR-GROWTH-006 @growth @security
Scenario: передача наружу без согласия
  Given согласие не дано
  When внутренний вызов пытается передать записи дневника во внешний канал
  Then вызов получает отказ и попадает в аудит с причиной «нет согласия»
```

### FR-GROWTH-007
Показатель i (карточек на активированного) и показатель conv% (установок на карточку) считаются и
показываются РАЗДЕЛЬНО с первого релиза. Их произведение в план не закладывается: отраслевого
ориентира для этой ниши не нашлось, и перемножение двух гипотез дало бы третью, выглядящую как
измерение (PD-METRIC-001).

```gherkin
@FR-GROWTH-007 @growth @happy-path
Scenario: два показателя считаются отдельно
  Given за неделю 30 активированных пользователей создали 18 карточек, давших 9 установок
  When открывается панель роста
  Then i показан как 0,60 и conv% как 50%, каждый со своим числом наблюдений

@FR-GROWTH-007 @growth @edge-case
Scenario: наблюдений меньше порога
  Given активированных пользователей за период 12, порог 30
  When открывается панель роста
  Then показано «недостаточно данных (12 из 30)», а не 0% и не пустая полоса

@FR-GROWTH-007 @growth @security
Scenario: переход по карточке не раскрывает автора
  Given зритель открывает публичную карточку
  When засчитывается переход
  Then в счётчик пишется код партнёра и карточка, но не идентификатор и не дневник автора
```

## 4. Look-требования

Промоушен таблицы-семени профиля ([`source-product-profile.md`](source-product-profile.md)).
Строки 001–005 описывают маркетинговый сайт продукта-источника, а не путь внутри приложения; они
принимаются как обязательства «пути публичного входа» либо отклоняются с причиной. Строки 006–012 —
обязательства облика продукта.

| ID | Решение | Требование / причина |
|---|---|---|
| FR-LOOK-001 | Принято | Публичный вход — лендинг: один экран, ноль полей формы до первого действия, три уровня заголовков, единственный призыв «открыть камеру» |
| FR-LOOK-002 | Принято | Второй шаг пути публичного входа — страница вопросов и ответов, достижимая одним действием с лендинга; полей формы 0 |
| FR-LOOK-003 | Отклонён | — причина: третьим шагом у источника идёт блог, а в неделе контентного раздела не создаётся; обязательство вернётся, когда появится контент |
| FR-LOOK-004 | Отклонён | — причина: четвёртым шагом у источника идёт пресс-раздел с формой; у клона нет ни пресс-материалов, ни повода для формы на неделе измерения |
| FR-LOOK-005 | Принято | Последовательность публичного входа — ровно три экрана: лендинг → вопросы и ответы → установка PWA либо открытие Telegram Mini App |
| FR-LOOK-006 | Принято | Первый экран продукта — камера с видоискателем и одной строкой режимов; анкеты до первой съёмки нет (см. FR-CAPTURE-001) |
| FR-LOOK-007 | Принято | Экран результата ровно один: фото с подписями ингредиентов, название крупно, степпер порции, четыре плитки метрик, кнопка исправления рядом с подтверждением |
| FR-LOOK-008 | Принято | Одна гротескная семья на всё, 8–9 ступеней кегля от 12 до 52, H1 ≈ 3,25× body, не более четырёх начертаний; заголовки — утверждения |
| FR-LOOK-009 | Принято | Монохромная база: тёмный текст на белом и офф-белом, ровно один тёмный блок под доказательства, пара кнопок-пилюль «основная тёмная / второстепенная светлая»; акцентный цвет свой, не из источника |
| FR-LOOK-010 | Принято | Первый экран лендинга: чип социального доказательства НАД заголовком и композиция «до → после» из двух экранов продукта; вторая секция — люди с аудиторией |
| FR-LOOK-011 | Принято | Сетка с шагом 4 px, доминанты отступов 16 и 24, один брейкпоинт ≈768 px, на мобильном одна колонка |
| FR-LOOK-012 | Принято | Результат ПОКАЗЫВАЕТ ИСТОЧНИК ЧИСЛА — авторское отличие клона, реализуется требованием FR-SOURCE-002 на всех трёх поверхностях |

## 5. Пользовательские истории

Персоны: **Марина** — худеющая на дефиците (сегмент 1, PD-SEG-001); **Артём** — набирающий белок
(сегмент 2, PD-SEG-002); **Лиза** — блогер-партнёр с каналом на 12 тыс. подписчиков (сегмент 3 и
канал посева, PD-SEG-003).

**US-001:** As Марина, I want сфотографировать обед и сразу увидеть калории и БЖУ, So that учёт
занимает секунды, а не минуты.

[SC-US-001-1] Given Марина открыла продукт впервые и не регистрировалась
When она снимает тарелку и подтверждает кадр
Then результат показан не позже 6 с (p95), без анкеты и без регистрации

[SC-US-001-2] Given на кадре нет еды
When распознавание завершается
Then показан статус «еда не распознана» с подсказкой снять тарелку целиком, запись дневника не создана

**US-002:** As Марина, I want видеть, откуда взято каждое число, So that я могу доверять итогу и
проверить его сама.

[SC-US-002-1] Given результат распознавания получен
When Марина смотрит на число калорий
Then рядом видны имя базы, идентификатор записи, её название и порция в граммах

[SC-US-002-2] Given подходящей записи базы для ингредиента нет
When собирается результат
Then ингредиент помечен «нет в базе» и исключён из итога, ноль вместо него не подставлен

**US-003:** As Марина, I want поправить порцию одним жестом, So that число соответствует тому, что я
действительно съела.

[SC-US-003-1] Given результат показывает порцию 250 г
When Марина трижды нажимает «минус» на степпере
Then порция равна 220 г, все четыре числа пересчитаны мгновенно, новый вызов модели не выполнен

[SC-US-003-2] Given Марина вводит порцию 5000 г вручную
When она подтверждает ввод
Then значение не принимается, показан допустимый диапазон 5–2000 г, прежняя порция сохранена

**US-004:** As Марина, I want видеть, когда оценка и база расходятся, So that продукт не решал за
меня молча.

[SC-US-004-1] Given оценка модели 780 ккал, сумма по базе 640 ккал, расхождение 22%
When открывается экран результата
Then показан экран расхождения с двумя числами и их источниками, по умолчанию выбрано число базы

[SC-US-004-2] Given расхождение составляет 9%
When открывается экран результата
Then экран расхождения не показывается, в дневник идёт число базы

**US-005:** As Марина, I want видеть итог дня, So that я понимаю, укладываюсь ли в дефицит.

[SC-US-005-1] Given за день сделаны 3 записи
When Марина открывает экран дня
Then показаны все три записи, итог по ккал и трём макронутриентам и разбивка по приёмам пищи

[SC-US-005-2] Given Марина удаляет одну запись
When удаление подтверждено
Then итог дня пересчитан немедленно, удалённая запись в списке отсутствует

**US-006:** As Марина, I want мягкий стрик, So that один пропущенный день не обесценивал неделю
работы.

[SC-US-006-1] Given стрик равен 5 дням и вчерашний день пропущен
When Марина делает запись сегодня
Then стрик равен 6 дням, вчерашний день помечен как «заморожен»

[SC-US-006-2] Given пропущены два дня подряд
When Марина делает запись сегодня
Then стрик равен 1, предупреждений о потере и обратного отсчёта не показывается

**US-007:** As Артём, I want увидеть белок в первом же результате и поделиться карточкой, So that я
подтверждаю свою норму и показываю её своим.

[SC-US-007-1] Given Артём получил первый успешный результат
When открывается экран результата
Then четыре плитки показывают ккал, белок, жиры и углеводы, карточка 9:16 готова без его действий

[SC-US-007-2] Given Артём нажимает «поделиться»
When файл карточки собран
Then карточка содержит блюдо, числа, источник и бейдж, и не содержит веса, цели и итога дня

**US-008:** As Артём, I want заменить неверно определённый ингредиент, So that число соответствует
реальному составу.

[SC-US-008-1] Given модель определила «рис белый», а на тарелке гречка
When Артём выбирает «гречка варёная» в поиске по базе
Then числа пересчитаны по новой записи, источник обновлён, вызов модели не выполнен

[SC-US-008-2] Given Артём удаляет ингредиент «соус»
When удаление подтверждено
Then итог блюда пересчитан без этого ингредиента, поправка сохранена в истории

**US-009:** As Марина, I want понимать, что лимит сканов исчерпан, So that я не гадаю, сломался ли
продукт.

[SC-US-009-1] Given за сутки сделано 10 попыток распознавания
When Марина снимает одиннадцатую тарелку
Then показан экран лимита с временем обнуления счётчика по московскому времени, вызов модели не выполнен

[SC-US-009-2] Given показан экран лимита
When Марина оставляет контакт для «Pro скоро»
Then интерес записан с датой и источником, платёжной формы и цены на экране нет

**US-010:** As Лиза, I want раздать своей аудитории персональный код, So that я вижу, сколько людей
пришло именно от меня.

[SC-US-010-1] Given у Лизы есть действительный код LIZA10
When зритель применяет код и получает первое распознавание
Then создана атрибуция со статусом pending и засчитана активация по коду LIZA10

[SC-US-010-2] Given зритель вводит код с опечаткой
When код проверяется
Then показана ошибка ввода, cookie-источник не подставляется, активация никому не засчитана

**US-011:** As Лиза, I want видеть воронку своей когорты, So that я понимаю ценность размещения без
доступа к чужим данным.

[SC-US-011-1] Given по коду Лизы есть переходы, установки, активации и шеринги
When Лиза открывает кабинет партнёра
Then показаны четыре счётчика за выбранное окно и время последнего обновления

[SC-US-011-2] Given Лиза запрашивает кабинет по чужому коду
When запрос доходит до сервера
Then возвращается отказ 403, чужие счётчики не раскрываются

**US-012:** As Марина, I want управлять согласием и удалить свои данные, So that данные о моём
питании остаются моими.

[SC-US-012-1] Given Марина впервые подтверждает результат для дневника
When показывается экран согласия и она отказывается
Then дневник не ведётся, съёмка и результат остаются доступны, отказ зафиксирован с версией текста

[SC-US-012-2] Given Марина запрашивает удаление данных
When запрос подтверждён
Then аккаунт, дневник, карточки и фото удалены не позже 72 ч, публичные карточки закрыты немедленно

**US-013:** As Артём, I want войти через Telegram, So that дневник не пропадёт при смене устройства.

[SC-US-013-1] Given Артём открыл продукт как Telegram Mini App и вёл анонимный дневник
When он подтверждает вход
Then подпись initData проверена до разбора данных, анонимный дневник перенесён в аккаунт целиком

[SC-US-013-2] Given подпись initData подделана
When запрос приходит на вход
Then возвращается 401, аккаунт не создан, сессия не выдана

## 6. Нефункциональные требования

### NFR-PERF-001
Время от подтверждения кадра до показанного результата — не больше 6 с на 95-м процентиле при
исправном провайдере модели. Измеряется по нашему журналу на живом стенде, а не на локальной машине.
Эскалация к сильной модели входит в это время.

### NFR-PERF-002
Готовый к съёмке видоискатель показывается не позже 1 с после открытия продукта на устройстве
среднего класса при соединении 4G. Первый экран не ждёт ни авторизации, ни загрузки базы.

### NFR-SEC-001
Фото хранится не дольше 30 суток и лежит в приватном бакете без публичных ссылок; удаление
выполняется по расписанию и по запросу пользователя. Ссылки на объект выдаются подписанными и
живут не дольше 15 минут.

### NFR-SEC-002
Данные о питании обрабатываются как специальная категория персональных данных: доступ только у
владельца, передача и публикация — исключительно по явному согласию, по умолчанию всё закрыто.
Записи согласий версионированы и хранятся вместе со временем и текстом.

### NFR-SCALE-001
Система выдерживает 3000 распознаваний в сутки и всплеск 10 одновременных распознаваний без
деградации времени ответа сверх NFR-PERF-001. Очередь распознавания не удерживает соединение с
базой во время вызова внешней модели.

### NFR-OPS-001
Расход вызовов модели наблюдаем: число попыток, доля эскалаций, отказы провайдера и оценка затрат
за сутки видны оператору на служебной странице и обновляются не реже раза в 5 минут. Счёт ведётся
по попыткам, а не по успехам.

## 7. Feature Matrix

| Возможность | MVP (неделя) | v1 | v2 |
|---|---|---|---|
| Фото → ингредиенты и порция, число из базы с источником | ✅ | | |
| Экран расхождения модель/база | ✅ | | |
| Степпер порции и замена ингредиента | ✅ | | |
| Дневник дня, итоги, мягкий стрик | ✅ | | |
| Карточка 9:16 с бейджем | ✅ | | |
| Код блогера и кабинет партнёра со счётчиками | ✅ | | |
| Потолки сканов и экран интереса Pro | ✅ | | |
| Анонимная сессия, вход через Telegram, согласия | ✅ | | |
| Подписка и оплата | | ✅ | |
| Публичная страница дня | | ✅ | |
| Реферальные коды пользователей и двусторонний бонус | | ✅ | |
| Выплаты партнёрам и реестр | | ✅ | |
| Кабинет тренера и B2B2C-места | | | ✅ |
| Штрихкоды и импорт упаковок | | | ✅ |
| Голосовой ввод на русском | | | ✅ |
| Интеграции Apple Health / Google Fit | | | ✅ |

## Success Metrics

| Metric | Target | Timeline | Источник значения |
|---|---|---|---|
| Доля активированных, расшеривших ≥1 карточку | 20% при n ≥ 30 активированных | неделя 1 | наша БД |
| Доля установок по коду, дошедших до 1-го распознавания | 60% | неделя 1 | наша БД |
| Время от кадра до результата, p95 | ≤ 6 с | неделя 1 | наш журнал |
| Доля попыток, завершённых отказом по потолку | ≤ 5% | неделя 1 | наш журнал |
| Расход на вызовы модели за сутки | ≤ 1560 ₽/сутки | ежедневно, неделя 1 | наш журнал |
| Сверка расхода с биллингом провайдера | расхождение ≤ 10% | конец недели 1 | внешний API: Anthropic Usage & Cost Admin API |
| Сходимость с базой на ручной сверке блюд | ≥ 24 из 30 блюд в пределах ±20% | конец недели 1 | ручное измерение: сверка 30 блюд по USDA вручную двумя людьми |

Метрики удержания D7/D30 и окупаемость привлечения в неделю не закладываются: горизонт измерения
короче их периода, и число, полученное на такой выборке, было бы формой без содержания.

## 9. Ответы на выходы discovery, закреплённые здесь

Полная таблица по всем идентификаторам манифеста — в `PRD.md` §11. Здесь закреплены выходы,
чьё решение принимает именно спецификация.

| Идентификатор | Где закреплён |
|---|---|
| PD-CORE-001 | FR-SOURCE-002, FR-CORRECT-003 |
| PD-CORE-002 | FR-CAPTURE-001 |
| PD-INSIGHT-001 | §1 контракт, FR-RECOGNIZE-002 |
| PD-INSIGHT-002 | FR-LIMIT-003 |
| PD-COST-001 | FR-LIMIT-001, FR-LIMIT-002 |
| PD-ARCH-001 | FR-RECOGNIZE-002 |
| PD-DATA-002 | FR-SOURCE-003 |
| PD-DATA-003 | FR-SOURCE-003 |
| PD-GROWTH-001 | §3 целиком |
| PD-GROWTH-005 | FR-GROWTH-005 |
| PD-METRIC-001 | FR-GROWTH-007, раздел Success Metrics |
| PD-LEGAL-001 | FR-AUTH-003, FR-GROWTH-006 |
| PD-PRICE-001 | FR-LIMIT-003, FR-PARTNER-002 |
| PD-REUSE-001 | FR-GROWTH-002, FR-PARTNER-003 |
