# Журнал автономных решений — проект 04 «Тарелка»

Владелец 2026-09-12 ~18:30 UTC: «работай в автоматическом режиме 12 часов, вопросов не задавай».
Здесь — каждое решение, которое в норме задавалось бы владельцу: дата, вопрос, выбор, обоснование,
как откатить. Владелец подтверждает или отменяет по возвращении.

| # | Время (UTC) | Вопрос | Решение | Обоснование | Откат |
|---|---|---|---|---|---|
| DEC-A-001 | 2026-09-12 18:30 | Режим работы на 12 часов | Полная автономия: Phase 2 → 3 → 4 → /start → реализация MVP по протоколу EXP-N4-001, QE на Codex Astra medium | прямое указание владельца | — |
| DEC-A-002 | 2026-09-12 18:30 | V1-R01 (blocker): третий потолок 600/сутки на эскалацию к Sonnet 5 — добавить в модель или убрать из контрактов? | Добавить: третий scope `escalation` в `scan_quota_counter`, четвёртый ключ в `CheckAndConsumeQuota`, три числа в каноне и ADR-007 | эскалация вдвое дороже основного вызова; отдельный потолок ограничивает именно дорогую модель в «плохой день», как требует model-call-cost; контракт уже это объявлял | вариант (b): убрать строку `эскалация-sonnet` из 4 документов и вернуть «два числа» |
| DEC-A-003 | 2026-09-12 18:50 | V2-R03: ADR-001 запрещает поля калорийности в ответе модели, а экран расхождения требует оценку модели | Модель возвращает `model_estimate_kcal` ТОЛЬКО для экрана расхождения; число для показа/хранения — всегда из базы; ADR-001 уточняется, а не отменяется | иначе FR-CORRECT-003 нереализуем | убрать экран расхождения (противоречит постановке владельца) |
| DEC-A-004 | 2026-09-12 18:50 | V2-R06/R07: не хватает маршрутов (согласие, удаление, правка записи дневника) и сущностей (лист ожидания Pro, события воронки) | Канон расширен: 13 маршрутов, 14 сущностей | без них FR-AUTH-003, FR-LIMIT-003, FR-GROWTH-007 не имеют носителя | вернуть 10/12 и отклонить FR с причиной |
| DEC-A-005 | 2026-09-12 18:50 | V2-R10/R11: HEIC и 12 МБ не поддерживаются провайдером; structured outputs не проверяют диапазоны | Серверная конвертация HEIC→JPEG и сжатие ≤ 5 МБ до вызова; проверка диапазонов в коде после разбора | документация провайдера | — |
| DEC-A-006 | 2026-09-12 18:50 | V2-R14: FR-GROWTH-007 (i и conv% раздельно) был SPECULATIVE и промотирован без решения человека | Принять как требование решением координатора: метрика недели без раздельных i/conv% неизмерима (FR-GROWTH-001 и методология требуют) | владелец недоступен; запись здесь = запись решения | владелец отклоняет по возвращении → строка становится «отклонён — причина» |
| DEC-A-007 | 2026-09-12 18:50 | V2-R15: Completion опирается на PagerDuty/Slack/email, которых нет в инвентаре | Каналы оповещения — журнал + Telegram владельцу через Bot API `sendMessage` (строка в External Dependencies с цитатой); PagerDuty/Slack не вводить | шаблон Completion предписывает эти имена, но инвентарь честнее шаблона | вернуть шаблонные имена и добавить их как UNCONFIRMED |
| DEC-A-008 | 2026-09-12 18:50 | V2-R05: после истечения аренды второй воркер может вызвать модель, пока первый жив | `lease_owner` + монотонный `lease_fence`; результат принимается только с актуальным fence; второй вызов модели остаётся возможен только в окне гонки и учитывается в квоте | fencing token — стандартное лечение | — |
| DEC-A-009 | 2026-09-12 19:02 | Ключа ANTHROPIC_API_KEY в окружении VPS нет — живое распознавание на стенде невозможно | Реализация через адаптер провайдера с детерминированным фейком для тестов и E2E на стенде; живой режим включается ключом в .env; в отчёте живой прогон помечается «не выполнен: нет ключа» | ключ — секрет владельца, брать его неоткуда; честная разметка вместо имитации | владелец добавляет ключ в .env и запускает живой смоук |
| DEC-A-010 | 2026-09-12 19:02 | `require.resolve` пакетного чекера `check-pipeline-gaps.sh` не работает (пакет не в node_modules, используется через npx) | Использовать чекер из кэша npx: `/root/.npm/_npx/ac10dded1a3b4a50/node_modules/@dzhechkov/p-replicator/scripts/check-pipeline-gaps.sh` (версия 1.13.2), путь и версию писать в квитанции каждой фичи; локальный `scripts/check-pipeline-gaps.sh` не подменяет пакетный | политика model-routing: не объявлять ворота пройденными при отсутствии проверки и не заменять молча | установить пакет зависимостью в корне репозитория |
| DEC-A-011 | 2026-09-12 19:27 | `scan-pipeline` — XL по локальному роутеру (платные вызовы модели), правило требует остановки на плане у владельца | Владелец дал общее разрешение на автономную реализацию; вместо остановки — план фичи проходит независимый challenge на Codex Astra medium ДО кода, и фича идёт на плечо B по правилу чётности эксперимента | указание владельца от 18:30; правило допускает применимое разрешение | владелец может остановить реализацию и пересмотреть план |
| DEC-A-012 | 2026-09-12 19:50 | Планировщик foundation предложил 14-й маршрут `POST /api/v1/auth/device` и служебный `GET /health` вне префикса | Принять оба; канон §5 обновлён (14 маршрутов + /health) | без явного маршрута создания анонимной сессии FR-AUTH-001 не имеет носителя; healthcheck compose требует ручку | владелец может слить создание сессии в первый `POST /scans` |
| DEC-A-013 | 2026-09-12 20:05 | VF-02: порог ограничителя частоты не назван числом | Канон §7: 30 запросов/мин на IP для мутаций, 120/мин для чтения; настраивается env, ненастроенный → отказ старта | правило «предел числом, не намерением» | владелец меняет числа в env |
