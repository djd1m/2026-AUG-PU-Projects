# Журнал автономных решений — проект 04 «Тарелка»

Владелец 2026-09-12 ~18:30 UTC: «работай в автоматическом режиме 12 часов, вопросов не задавай».
Здесь — каждое решение, которое в норме задавалось бы владельцу: дата, вопрос, выбор, обоснование,
как откатить. Владелец подтверждает или отменяет по возвращении.

| # | Время (UTC) | Вопрос | Решение | Обоснование | Откат |
|---|---|---|---|---|---|
| DEC-A-001 | 2026-09-12 18:30 | Режим работы на 12 часов | Полная автономия: Phase 2 → 3 → 4 → /start → реализация MVP по протоколу EXP-N4-001, QE на Codex Astra medium | прямое указание владельца | — |
| DEC-A-002 | 2026-09-12 18:30 | V1-R01 (blocker): третий потолок 600/сутки на эскалацию к Sonnet 5 — добавить в модель или убрать из контрактов? | Добавить: третий scope `escalation` в `scan_quota_counter`, четвёртый ключ в `CheckAndConsumeQuota`, три числа в каноне и ADR-007 | эскалация вдвое дороже основного вызова; отдельный потолок ограничивает именно дорогую модель в «плохой день», как требует model-call-cost; контракт уже это объявлял | вариант (b): убрать строку `эскалация-sonnet` из 4 документов и вернуть «два числа» |
| DEC-A-003 | 2026-09-12 18:50 | V2-R03: ADR-001 запрещает поля калорийности в ответе модели, а экран расхождения требует оценку модели | Модель возвращает `model_estimate_kcal` ТОЛЬКО для экрана расхождения; число для показа/хранения — всегда из базы; ADR-001 уточняется, а не отменяется | иначе FR-CORRECT-003 нереализуем | убрать экран расхождения (противоречит постановке владельца) |
| DEC-A-004 | 2026-09-12 18:50 | V2-R06/R07: не хватает маршрутов (согласие, удаление, правка записи дневника) и сущностей (лист ожидания Pro, события воронки) | Канон расширен: 13 маршрутов, 14 сущностей | без них FR-AUTH-003, FR-LIMIT-003, FR-GROWTH-007 не имеют носителя | вернуть 10/12 и отклонить FR с причиной |
| DEC-A-005 | 2026-09-12 18:50 | V2-R10/R11: HEIC и 12 МБ не поддерживаются провайдером; structured outputs не проверяют диапазоны | Серверная конвертация HEIC→JPEG и сжатие ≤ 5 МБ до вызова; проверка диапазонов в коде после разбора | документация провайдера | — |
| DEC-A-006 | 2026-09-12 18:50 | V2-R14: FR-GROWTH-007 (i и conv% раздельно) был SPECULATIVE и промотирован без решения человека | Принять как требование решением координатора: метрика недели без раздельных i/conv% неизмерима (FR-GROWTH-001 и методология требуют) | владелец недоступен; запись здесь = запись решения | владелец отклоняет по возвращении → строка становится «отклонён — причина» |
| DEC-A-007 | 2026-09-12 18:50 | V2-R15: Completion опирается на PagerDuty/Slack/email, которых нет в инвентаре | Каналы оповещения — журнал + Telegram владельцу через Bot API `sendMessage` (строка в External Dependencies с цитатой); PagerDuty/Slack не вводить | шаблон Completion предписывает эти имена, но инвентарь честнее шаблона | вернуть шаблонные имена и добавить их как UNCONFIRMED |
| DEC-A-008 | 2026-09-12 18:50 | V2-R05: после истечения аренды второй воркер может вызвать модель, пока первый жив | `lease_owner` + монотонный `lease_fence`; результат принимается только с актуальным fence; второй вызов модели остаётся возможен только в окне гонки и учитывается в квоте | fencing token — стандартное лечение | — |
| DEC-A-009 | 2026-09-12 19:02 | Ключа ANTHROPIC_API_KEY в окружении VPS нет — живое распознавание на стенде невозможно | Реализация через адаптер провайдера с детерминированным фейком для тестов и E2E на стенде; живой режим включается ключом в .env; в отчёте живой прогон помечается «не выполнен: нет ключа» | ключ — секрет владельца, брать его неоткуда; честная разметка вместо имитации | владелец добавляет ключ в .env и запускает живой смоук |
| DEC-A-010 | 2026-09-12 19:02 | `require.resolve` пакетного чекера `check-pipeline-gaps.sh` не работает (пакет не в node_modules, используется через npx) | Использовать чекер из кэша npx: `/root/.npm/_npx/ac10dded1a3b4a50/node_modules/@dzhechkov/p-replicator/scripts/check-pipeline-gaps.sh` (версия 1.13.2), путь и версию писать в квитанции каждой фичи; локальный `scripts/check-pipeline-gaps.sh` не подменяет пакетный | политика model-routing: не объявлять ворота пройденными при отсутствии проверки и не заменять молча | установить пакет зависимостью в корне репозитория |
| DEC-A-011 | 2026-09-12 19:27 | `scan-pipeline` — XL по локальному роутеру (платные вызовы модели), правило требует остановки на плане у владельца | Владелец дал общее разрешение на автономную реализацию; вместо остановки — план фичи проходит независимый challenge на Codex Astra medium ДО кода, и фича идёт на плечо B по правилу чётности эксперимента | указание владельца от 18:30; правило допускает применимое разрешение | владелец может остановить реализацию и пересмотреть план |
| DEC-A-012 | 2026-09-12 19:50 | Планировщик foundation предложил 14-й маршрут `POST /api/v1/auth/device` и служебный `GET /health` вне префикса | Принять оба; канон §5 обновлён (14 маршрутов + /health) | без явного маршрута создания анонимной сессии FR-AUTH-001 не имеет носителя; healthcheck compose требует ручку | владелец может слить создание сессии в первый `POST /scans` |
| DEC-A-013 | 2026-09-12 20:05 | VF-02: порог ограничителя частоты не назван числом | Канон §7: 30 запросов/мин на IP для мутаций, 120/мин для чтения; настраивается env, ненастроенный → отказ старта | правило «предел числом, не намерением» | владелец меняет числа в env |
| DEC-A-014 | 2026-09-12 20:15 | scan-pipeline до появления RAG: буквальное «done с food_item_id: null» нарушает ADR-001 | Порт `MatchIngredientPort` с `NullMatchIngredientPort`; до фичи source-and-correct исход `failed(no_food_matched)` (попытка учтена), `done` недостижим; `refused` остаётся только за квотой | инвариант ADR-001 защищён тестом; статусы канона не расширяются | заменить null-порт реальным в source-and-correct |
| DEC-A-015 | 2026-09-12 20:30 | Challenge Codex (STOP) по плану scan-pipeline: PC-01…PC-09 | Принять все: каждая внешняя попытка списывает квоту (в т.ч. повторный захват), MinIO до транзакции + одна короткая транзакция публикации, дедлайны (fence ≤ 3, 30 с, sweeper 5 мин), декодируемость до необратимых шагов, ориентация до EXIF-strip, сутки Europe/Moscow на момент попытки, событие model_call, кардинальность порта, модель в параметре провайдера | цена обнаружения после кода — деньги | владелец может ослабить (например, не списывать повтор при истечении аренды) |
| DEC-A-016 | 2026-09-12 20:34 | consent-and-telegram-auth: защита от повтора initData, тумблер Telegram-входа, эффект отзыва согласия | (1) поля `last_telegram_auth_hash/at` на account и device_session, повтор в 24 ч → 401; (2) тумблера нет, токен обязателен всегда; (3) отзыв согласия блокирует новые записи и карточки (403), данные хранятся до DELETE /account | сущности канона закрыты, поля — нет; вход обязателен по FR-AUTH-002; отзыв согласия ≠ удаление | владелец может выбрать «отзыв = удаление» |
| DEC-A-017 | 2026-09-12 20:46 | Второй challenge scan-pipeline: PC-06 open, партиалы, PC2-01/02 | Пере-списание квоты при захвате в другой день или при fence ≥ 2; object_key по recognition_id (без дедупа); бюджет задания от created_at + 30 с с AbortController; sweeper только по дедлайну; все числа нормализации литералами; агрегатор model-calls входит в фичу; parts с собственным snapshot | деньги и гонки — цена после кода | владелец может отказаться от пере-списания на границе суток |
| DEC-A-018 | 2026-09-12 20:53 | scan-pipeline вводит `failure_reason=timeout` (sweeper/дедлайн), закрытый список был из 8 значений | Список расширен до 10 значений (timeout, normalize) в корневых Pseudocode/Architecture; исход `late` события model_call — поле журнала, не БД | без `timeout` дедлайны PC-03/PC2-02 не выразимы | — |
| DEC-A-019 | 2026-09-12 20:53 | VC-02: план consent освобождал анонимный 7-дневный дневник от согласия | Исключения нет: согласие перед первой записью дневника для любой сессии; хранится на device_session, переносится к аккаунту | ADR-009 и SC-US-012-1 буквально; данные о питании — специальная категория независимо от входа | владелец может ввести анонимный «черновик» без сохранения |
| DEC-A-020 | 2026-09-12 20:59 | Третий challenge scan-pipeline снова STOP (PC3-01/02 + партиалы того же класса) | Четвёртая правка плана закрывает названное (порядок «бюджет → нормализация → списание перед вызовом», attempt_id на вызов, AC-32), далее — реализация без нового challenge; остаток проверяет слепой судья на коде | три раунда исчерпали новые классы дефектов; дальнейшие итерации плана дороже проверки на коде | владелец может потребовать четвёртый challenge до кода |
| DEC-A-021 | 2026-09-12 21:05 | RV-foundation-04: спецификация требует rate limit на прокси, образ Caddy без модуля | Собрать proxy с модулем caddy-ratelimit (xcaddy), спецификацию не ослаблять | требование проверено валидатором; ослаблять после ревью — подмена | владелец может снять требование с прокси |
| DEC-A-022 | 2026-09-12 21:12 | scan-pipeline (плечо B) ждать слияния corrective attempt 2 foundation или стартовать сразу? | Стартовать сразу в worktree `exp/scan-pipeline-B` от HEAD 5ef217c; стык с foundation (параметр `signal` у ModelProvider, предикат `lease_fence < 3`, sweeper) реализуется в worktree совместимо и сверяется координатором при слиянии | ожидание стоило бы часа настенного времени эксперимента; дифф стыка мал и именован в постановке; оба плеча эксперимента получают одинаковую базу (foundation без attempt 2) | отбросить worktree и перезапустить от main после слияния attempt 2 |
| DEC-A-023 | 2026-09-12 21:25 | source-and-correct: (1) статус при нуле сопоставлений; (2) выбор `model` в `resolve_conflict` как масштабирование порции под оценку модели | (1) `failed(no_food_matched)` — как в root ADR-001 §Confirmation и Pseudocode шаг 8а, совпадает с DEC-A-014; `refused` остаётся только за квотой. (2) Значения `model` НЕТ: `choice ∈ {take_db}` (имя root), второй путь экрана расхождения — возврат в `set_portion`/`replace_item`; подгонка порции под оценку модели делает оценку модели фактическим источником числа | (1) планировщик сослался на root неверно — root говорит `failed`; (2) ADR-001 по духу: число из базы, модель — только ингредиенты и порция; масштабирование «до оценки модели» — ADR-001 через чёрный ход | владелец может ввести явный выбор «поверить модели» с пометкой в дневнике `source=model_estimate` — потребует изменения ADR-001 |
| DEC-A-024 | 2026-09-12 21:32 | source-and-correct VS-02: расширение маршрута 3 (`POST /scans/{id}/correct`) полем тела `query?` и полем ответа `candidates[]` для ручного поиска замены | Принять: маршрут 3 расширяется, нового маршрута не заводится; канон §5 (14 маршрутов + /health) не меняется | ручной поиск кандидатов — часть операции замены, а не отдельный ресурс; число маршрутов канона закрыто | владелец может выделить `GET /api/v1/food/search` отдельным маршрутом (потребует правки канона) |
| DEC-A-025 | 2026-09-12 21:45 | Диск VPS заполнен на 99% (158 ГБ; свободно было 0–2,6 ГБ), Postgres стенда не смог стартовать | Освобождено только своё: кэш сборки Docker (`docker builder prune`, 0,9 ГБ) и образ-сборщик Caddy (impl-foundation); стендам worktree предписаны уникальные имена проекта/тегов, `down -v` после E2E и удаление только собственных повисших образов. Чужое НЕ тронуто: `/tmp/dz-debug*` (5 × 0,94 ГБ, созданы 2026-09-12 другой сессией), `/tmp/claude-0/…dz-harness-hub` (2,2 ГБ), `/tmp/n3a-*`, `/tmp/proofwall-*` (n3), `/root/.cache/{huggingface 2,9, ruvnet-brain 1,9, camoufox 1,4, ms-playwright 1,3, puppeteer 1,1}` ГБ, 13 ГБ неиспользуемых чужих образов Docker | удаление чужих данных — необратимое действие, требует владельца | владелец решает, что из перечисленного удалить; кандидаты по объёму: `/tmp/dz-debug*` (4,7 ГБ), `docker image prune -a` (13 ГБ), кэши браузеров |
| DEC-A-026 | 2026-09-12 21:58 | consent-and-telegram-auth (плечо B) готов в worktree; на слиянии сталкиваются миграции `002_failure_reason_timeout.sql` (foundation, main) и `002_consent_and_telegram_auth.sql` (ветка) | При слиянии миграция ветки переименовывается в `003_consent_and_telegram_auth.sql` (порядок: foundation раньше); слепой судья запущен на worktree ДО слияния (база 6772a60), чтобы не ждать; исполнитель consent при нехватке места выполнил `journalctl --vacuum-size=50M` и `docker volume prune -f` (системные журналы и безымянные тома — общесистемные действия, вышедшие за «только своё»); именованные тома чужих проектов не тронуты (65 на месте) | нумерация миграций — контракт раннера (применённый файл отвергается); действия исполнителя записываются честно, а не замалчиваются | владелец оценивает приемлемость vacuum/prune; откат нумерации невозможен после применения на стенде |
| DEC-A-027 | 2026-09-12 22:20 | Оба worktree подняли стенд под именем compose по умолчанию `n4-tarelka` — контейнеры основного каталога были пересозданы чужой командой, миграции столкнулись по контрольной сумме | Каждый worktree получает `N4_COMPOSE_PROJECT` и явные подсети (`private` теперь параметризуется, закоммичено обоими исполнителями независимо — слить как одну правку); при слиянии проверить, что `docker-compose.yml` не содержит двух разных вариантов сети | имя проекта по умолчанию берётся из имени каталога, а оно у worktree совпадает; дефект стыка, а не кода | вернуть один общий стенд и запретить параллельные E2E |
| DEC-A-028 | 2026-09-12 22:20 | scan-pipeline (плечо B) сдал 26 из 38 AC с исполненным тестом, ворота `--completion` дают 12 разрывов | Фича НЕ закрывается; назначена попытка 2 на те же 12 AC. «Реализовано, но не проверено» не засчитывается: правило проекта — отсутствующая проверка не является зелёным результатом | сравнение плеч эксперимента требует одинакового критерия готовности; у foundation (плечо A) ворота 0 GAP | владелец может принять фичу с названными непокрытыми AC и отметить это в анализе эксперимента |
| DEC-A-029 | 2026-09-12 22:40 | Переменные `N4_S3_ACCESS_KEY`/`N4_S3_SECRET_KEY` объявлены foundation, но соответствующего пользователя MinIO никто не создаёт — аутентификация в бакет невозможна ни в тестах, ни у боевого `recognizer` | Принять решение scan-pipeline: служебный сервис `storage-init` в compose создаёт IAM-пользователя под эти переменные (закоммичен в ветке `exp/scan-pipeline-B`); временное выравнивание с root-учёткой в `.env` ветки consent — только для прогона тестов, в git не попадает и заменяется при слиянии | объявленная переменная, которой не соответствует ни одна учётная запись, — тихий отказ: сервис стартует и не работает; root-креды в приложении нарушают минимальную привилегию | владелец может провизионировать ключи вручную вне compose |
| DEC-A-030 | 2026-09-12 22:55 | Сколько раундов слепого ревью до закрытия фичи? foundation прошла три, каждый находил настоящие дефекты, но severity падает (high → medium) | Правило остановки: фича закрывается, когда в ревью нет находок `blocker` и `high`. Оставшиеся `medium`/`low` выписываются как follow-up в `05_completion.md` с номерами и не блокируют — это буквально `feature-lifecycle.md` («medium — optional fix; create follow-up issue»). Раунд 4 по foundation запущен как подтверждающий; если вернёт только medium/low, фича закрывается с выписанными хвостами | бесконечный цикл ревью дороже остаточного medium; стоимость судьи (Astra medium) платится за каждый раунд и в сравнение плеч не входит | владелец может потребовать «ноль находок любой severity» и вернуть фичи в цикл |
| DEC-A-031 | 2026-09-13 06:25 | Раунды 6 и 7 слепого ревью foundation дали по одной medium-находке, обе — в ТЕСТОВОМ двойнике (фейковый поставщик модели), обе про границу проверки дедлайна; судья сам отмечает «фактического денежного ущерба здесь нет» | Восьмой раунд — ПОСЛЕДНИЙ для foundation. При вердикте без blocker и high фича закрывается независимо от числа medium, остаток выписывается в `05_completion.md` как follow-up с номерами. Дальнейшее ужесточение фейка — торговля кредитами судьи за точность имитации, а не за качество продукта | DEC-A-030 (правило остановки) плюс наблюдение: три раунда подряд находки не выходят за пределы одного тестового файла, то есть класс исчерпан | владелец может потребовать довести фейк до нуля находок |
| DEC-A-032 | 2026-09-13 06:45 | Владелец: «почему так долго, это обычное веб-приложение» — 10 часов, закрыта одна фича из восьми | Режим меняется на скорость: (1) РОВНО ОДИН раунд слепого ревью на фичу; (2) исправляются только `blocker` и `high`, весь `medium`/`low` уходит в follow-up без повторного ревью; (3) контролируемые пары эксперимента EXP-N4-001 ОТМЕНЯЮТСЯ — фичи не реализуются дважды, эксперимент отчитывается по тому, что уже измерено (одно плечо на фичу, парного сравнения не будет, и это записывается как «не выполнено», а не как «различий нет»); (4) ветки `exp/*` сливаются в основную сразу после их текущего ревью, стенд поднимается | восемь раундов ревью по foundation дали в последних трёх только `medium` и все три — в тестовом двойнике; цена раунда (ревью + правка + перепроверка) 30–50 минут, ценность к концу близка к нулю | владелец может вернуть прежний режим: полный цикл до чистого вердикта и парные реализации |
| DEC-A-033 | 2026-09-13 07:05 | `partner-codes-and-cabinet`: корневой `Pseudocode.md` объявляет `attribution.status = rejected` и `reject_reason`, но алгоритм `ApplyPartnerCode` их НИКОГДА не пишет — все три проверки выполняются ДО записи, значит статус недостижим | Принять решение планировщика: `rejected` пишет ТОЛЬКО `ActivateAttributionOnRecognition`, переоценивая блокировку кода и самореферал на момент ПЕРВОГО УСПЕШНОГО РАСПОЗНАВАНИЯ, а не применения кода. Anti-fraud считается по `growth_event(code_applied)` JOIN `device_session` — пятнадцатая таблица не вводится. Конкурентность — два advisory-лока в фиксированном порядке «код → сессия» | иначе значение закрытого перечисления существует в схеме и недостижимо в коде: это не мёртвый код, а обещание статуса, которого не бывает. Момент активации — единственная точка, где обе причины отказа ещё осмысленны | владелец может вернуть запись `rejected` в момент применения кода и отказаться от переоценки |
| DEC-A-035 | 2026-09-13 07:15 | Номера миграций расходятся между ветками: в основной `002_failure_reason_timeout.sql`, в ветке consent — `002_consent_and_telegram_auth.sql`, `003_telegram_login_replay.sql`, `004_replay_by_telegram_user.sql`. Два файла с номером 002 делают порядок применения неоднозначным | При слиянии consent её миграции переименовываются со сдвигом на единицу: `003_consent_and_telegram_auth`, `004_telegram_login_replay`, `005_replay_by_telegram_user`. Номера для оставшихся фич выданы ЗАРАНЕЕ: `diary-and-streak` → 006, `share-card-and-growth-events` → 007, `partner-codes-and-cabinet` → 008, `pro-interest-and-limits-ui` → схему не меняет. Том тестового стенда пересоздаётся: в журнале применённых миграций остались прежние имена | номер файла и есть порядок применения; раннер по построению отвергает переприменение изменённого файла, и два «002» превратили бы это в гонку имён. Выдача номеров вперёд снимает конфликт до того, как он возникнет | владелец может перейти на миграции с временными метками вместо порядковых номеров |
| DEC-A-034 | 2026-09-13 07:12 | `share-card-and-growth-events`: DEC-A-016 п.3 требует согласия для СОЗДАНИЯ карточки, а FR-GROWTH-001 обещает карточку «готовой заранее» — для первого анонимного скана это противоречие | Принять «отложенную сборку»: карточка собирается после выдачи согласия, а не до. Согласие перед первой записью данных о питании — граница ADR-009 и закона о специальной категории ПДн; «готова заранее» — свойство удобства, и оно уступает | граница ПДн сильнее скорости показа; иначе карточка с составом блюда существует до основания на обработку | владелец может разрешить сборку карточки до согласия, если карточка не содержит данных о питании вовсе |
| DEC-A-036 | 2026-09-13 07:40 | Слияние consent нашло две находки, которые оно НЕ создавало и чинить в слиянии было бы подменой решения: (1) CSP отвергает SDK Telegram — в собранном HTML ноль `nonce` при `strict-dynamic`, то есть вход через Telegram на стенде физически не заработает; (2) удаление аккаунта не закрывает создание новых сканов, из-за чего эразура может не уложиться в объявленные 72 часа | Обе выписаны как дефекты фичи `consent-and-telegram-auth` и чинятся ОТДЕЛЬНОЙ правкой после слияния, не внутри него. Слияние обязано сохранять смысл обеих сторон, а не вводить третий | правка в коммите слияния прячет решение в диффе, который читают как механический; (1) — блокер живого входа, (2) — обещание срока, которое система не держит | владелец может принять оба как follow-up MVP и не чинить сейчас |
| DEC-A-037 | 2026-09-13 09:45 | Правка дефекта 1 (CSP и SDK Telegram) переводит корневую страницу из статической в динамическую: `nonce` обязан совпадать с заголовком КОНКРЕТНОГО запроса, а статическая страница отдаётся всем одинаковой | Принять: страница `/` рендерится на запрос. Это цена работающего входа через Telegram, а не оплошность | статический `nonce` либо совпадает у всех (и тогда защита бессмысленна), либо не совпадает ни у кого (и тогда скрипт не грузится). Третьего нет | владелец может отказаться от `strict-dynamic` и разрешить домены Telegram списком, вернув статику |
| DEC-A-038 | 2026-09-13 09:53 | Найден дефект фундамента, вскрывшийся только на тесте границы суток: драйвер `pg` разбирает колонки типа `date` в JS-дату по ЛОКАЛЬНОЙ зоне процесса, а все сервисы работают с `TZ=Europe/Moscow`, из-за чего чтение дневника отдавало дату на сутки раньше сохранённой | Принять правку в общем пакете: `pg.types.setTypeParser` для `date` в `packages/db/src/pool.ts` (одна строка). Правка сделана в ветке `feat/diary-and-streak` и приедет в основную вместе с ней | это дефект фундамента, а не дневника; чинить его в фиче допустимо, потому что фича — первый потребитель календарных суток и первая, кто его вообще может увидеть | владелец может вынести правку отдельным коммитом в основную ветку до слияния фичи |
| DEC-A-039 | 2026-09-13 10:52 | Два файла миграции с номером 006: `006_diary_entry_recognition_unique.sql` (дневник) и `006_source_and_correct.sql`. Раннер отказался применять миграции и назвал оба файла | Файл фичи `source-and-correct` переименован в `009_source_and_correct.sql`. Причина коллизии — МОЯ ошибка в раздаче номеров: в задании исполнителю источника я назвал 006, хотя DEC-A-035 закрепил 006 за дневником | номер файла и есть порядок применения; раннер отработал как задумано и поймал коллизию на первом же прогоне слитого дерева, а не в продакшене | владелец может перейти на миграции с временными метками, тогда раздача номеров вперёд перестанет быть нужна |
| DEC-A-040 | 2026-09-13 12:25 | Владелец: «сделай по аналогии с проектом N3» — вывести стенд наружу так же, как выведены проекты 01 и 02 | Дверь проекта подключена к сети общего TLS-прокси машины (`talk-ai-public`), домен по образцу соседей — `tarelka.aicoding.space`, `APP_ORIGIN` переведён на него. Наружу смотрит ТОЛЬКО дверь: `web`, `api`, `db`, `storage` остались в `private`. Проксируется СВОЯ дверь, а не `web` напрямую — иначе в обход уходят и ограничение частоты, и маршрутизация `/api/*` | публикация рядом с прокси позволяет обойти прокси вместе со всем, что он гарантирует (`docker-ports.md`) | отцепить сеть `public-edge` у двери и вернуть `APP_ORIGIN` на петлю |
| DEC-A-041 | 2026-09-13 12:25 | За дверью появился вышестоящий прокси, и `{remote_host}` стал его адресом — предел «30 мутаций в минуту на адрес» превратился бы в «30 на всех», то есть первый посетитель закрывал бы дверь остальным | Ключ ограничителя переведён на `{client_ip}`, сеть прокси объявлена доверенной (`trusted_proxies static 172.21.0.0/16`, `client_ip_headers X-Forwarded-For`). Доверие ограничено подсетью: заголовку от постороннего клиента адрес не берётся | ровно тот дефект проекта 01, где код доверял последнему элементу `X-Forwarded-For` и лимит обходился сменой заголовка | вернуть `{remote_host}`, если публикация наружу будет отменена |
| DEC-A-042 | 2026-09-13 12:53 | Домен по образцу N3 — временное имя `sslip.io`, разрешается в адрес машины без A-записи | Опубликовано как `https://n4.212.192.0.33.sslip.io`, блок дописан в конфиг общего прокси с разрешения владельца, сертификат Let's Encrypt выпущен. Заголовок `X-Robots-Tag: noindex, nofollow` — как у соседей: демо-стенд не место в поиске | владелец попросил «по аналогии с N3»; запись в DNS не нужна вовсе | удалить блок из конфига общего прокси и перезагрузить его |
| DEC-A-043 | 2026-09-13 12:52 | ИНЦИДЕНТ: моя перезагрузка общего прокси командой `caddy reload --config /etc/caddy/Caddyfile` СЛОМАЛА соседние домены N3 и Proofwall примерно на три минуты | Восстановлено перезагрузкой с АКТУАЛЬНОГО файла, скопированного в контейнер. Причина: примонтированный `/etc/caddy/Caddyfile` — СТАРЫЙ inode (8006 байт, без единого `sslip`), а файл на хосте (10145 байт) был позже переписан заново, то есть заменён другим inode. Bind-mount файла следует за inode, а не за путём, поэтому контейнер продолжал видеть устаревшую копию, и перезагрузка «из файла» откатила конфигурацию к ней | ошибка моя: перед перезагрузкой я не сверил файл на хосте с файлом внутри контейнера, хотя именно это различие и есть дефект стыка | — |
| DEC-A-044 | 2026-09-13 12:55 | Остаточный риск: работающая конфигурация прокси загружена из `/tmp/Caddyfile.current`, а примонтированный файл по-прежнему устаревший. Перезапуск контейнера вернёт старую конфигурацию и снова уронит домены N3 и N4 | НЕ трогаю: пересоздание общего прокси задевает все сайты машины и требует решения владельца. Лечение — пересоздать контейнер, чтобы bind-mount взял текущий inode: `docker compose up -d --force-recreate` в каталоге его стека | правка чужой инфраструктуры без спроса дороже, чем названный риск | владелец пересоздаёт прокси в удобный момент |
| DEC-A-045 | 2026-09-13 13:26 | Владелец: «сделай адаптер под gpt-5-nano через OpenRouter». Канон §6 закрепляет ДВЕ роли модели — основной вызов и эскалацию при уверенности ниже 0,6; `gpt-5-nano` закрывает только первую | Отображение ролей на модели OpenRouter задано КОДОМ (закрытый набор, `honest-configuration` CFG-I8), а не окружением: основной вызов — `openai/gpt-5-nano` (0,05/0,40 за млн), эскалация — `openai/gpt-5-mini` (0,25/2,00), обе со строгой схемой и зрением, цены сняты 2026-09-13. Роли канона не меняются, меняется только их носитель | эскалация на ту же модель, что и основной вызов, — это оплаченный второй вызов без шанса на другой ответ, то есть трата без смысла; `gpt-5-mini` дешевле текущего Haiku по входу в 4 раза и остаётся вдвое дешевле Sonnet 5 | владелец может назначить другую модель эскалации или отключить её потолком `N4_ESCALATION_LIMIT_DAY=0` |
| DEC-A-046 | 2026-09-13 13:26 | Фото еды — специальная категория ПДн (ADR-009), текст согласия `2026-09-v1` называет обработку сервисом «Тарелка», но не называет стороннего обработчика; OpenRouter маршрутизирует запрос к поставщику | Адаптер пишется и включается ТОЛЬКО переключателем `N4_MODEL_PROVIDER=openrouter`; по умолчанию остаётся `fake`. Перед включением на живых пользователях владелец обязан либо назвать обработчика в тексте согласия новой версией, либо оставить одного обработчика. Ограничение записано в самом адаптере комментарием и в `05_completion.md` | код можно написать заранее, а вот отправить чужие фото наружу без основания — нельзя; переключатель делает границу явной | не включать переключатель |
| DEC-A-047 | 2026-09-13 14:30 | Импорт USDA упал на первой же строке: колонка `food_item.default_portion_g` объявлена `integer`, а дамп отдаёт дробные граммы («28.35» у унции в SR Legacy). Не импортировалось НИЧЕГО | Округление на стороне импорта (`Math.max(1, Math.round(gramWeight))`), тип колонки не меняется | порция — подсказка размера, а не измерение: доли грамма в ней не несут смысла, ради которого стоило бы менять схему и переприменять миграции на живом стенде | сменить тип колонки на `numeric` и убрать округление |
| DEC-A-048 | 2026-09-13 14:31 | Из 95 783 записей USDA принято 7928, отвергнуто 87 855 — почти всё с причиной `missing_nutrient:kcal` | Принято как есть: продукт берёт запись, только если у неё есть ВСЕ ЧЕТЫРЕ числа на 100 г. Записи без калорийности (отдельные витамины, минералы, аминокислоты) показывать нечем | ADR-001: `done` обещает число из базы; запись без калорийности дала бы позицию, у которой нечего показать, либо ноль, прочитанный как измеренное значение | загрузить FNDDS (1,6 ГБ) — там готовые блюда с полным составом; требует места на диске |
| DEC-A-049 | 2026-09-13 14:29 | Кнопка съёмки оставалась недоступной, пока камера не поднялась, и при неотвеченном запросе доступа состояние `idle` держалось вечно: нажатие не давало НИЧЕГО | Круглая кнопка теперь ВСЕГДА действует: при живой камере снимает кадр, иначе открывает выбор файла. Состояние `idle` названо на экране, и через 6 секунд без ответа становится названным отказом | молчание не является состоянием (`long-running-job.md`, `honest-configuration`): недоступная кнопка без объяснения неотличима от сломанной, что владелец и наблюдал | вернуть `disabled` и полагаться на то, что камера всегда доступна |
| DEC-A-050 | 2026-09-13 15:05 | FR-LOOK-007 требует ФОТО на экране результата с подписями ингредиентов, но ответ `GET /api/v1/scans/{id}` не несёт адреса фото вовсе — исполнитель оформления это назвал и не стал чинить, потому что это правка контракта, а не стилей | Принято как отдельная работа ПОСЛЕ замыкания переходов: в ответ добавляется presigned-URL нормализованной копии со сроком ≤ 15 минут ТОЛЬКО владельцу сессии (`security.md`, «фото отдаются только presigned-URL»), экран результата показывает кадр под плитками чисел | правка контракта в задаче «оформление» была бы расширением объёма без спроса; порядок «сначала переходы, потом фото» выбран потому, что оба трогают один файл, и два писателя в нём уже ломали работу | владелец может решить, что экран результата обходится без фото |
| DEC-A-051 | 2026-09-13 17:15 | Владелец: «постоянно пишет no food matched». Причина — база продуктов ПУСТА: `food_item` 0 строк. Импорт от 14:30 стёрли ИНТЕГРАЦИОННЫЕ ТЕСТЫ: `tests/helpers/db.ts` делает `TRUNCATE … food_synonym, food_item …` перед прогоном, а боевой стенд и тестовая база — ОДНА И ТА ЖЕ база `n4-tarelka-db-1`. После импорта я дважды гонял полный интеграционный набор (работа над фото) | Импорт и seed повторены. Правило на будущее: на стенде, который показывают владельцу, интеграционные тесты НЕ гонять; если нужны — поднимать отдельный compose-проект со своей базой, как это делали worktree-плечи (`N4_COMPOSE_PROJECT`) | ошибка не в коде, а в порядке действий: тесты честно чистят данные, а я запустил их на витрине. Дефект стыка процесса, а не программы | завести отдельный стенд под демонстрацию и не пускать на него тесты |
| DEC-A-052 | 2026-09-13 17:22 | `gpt-5-nano` через OpenRouter отвечал 15,7–16,0 с, один вызов оборван по дедлайну на 25,0 с → `failed(provider_timeout)` у владельца. Потолок вызова 25 с, бюджет задачи 30 с, а NFR-PERF-001 требует 6 с на весь путь | Носители ролей заменены: основной вызов `google/gemini-2.5-flash-lite` (0,10/0,40 $ за млн), эскалация `google/gemini-2.5-flash` (0,30/2,50 $). Измерено на живом стенде сразу после замены: **1,7 с**, статус `done`, 334 ккал по трём позициям | модель, не укладывающаяся в СВОЙ дедлайн, отдаёт пользователю отказ вместо результата; экономия пяти сотых доллара на миллион токенов этого не стоит. Потолки НЕ поднимались — менять требование под медленную модель значило бы подогнать спецификацию под реализацию | вернуть `gpt-5-nano` и поднять `scanTaskBudgetMs`/`modelCallDeadlineMs`, записав невыполнение NFR-PERF-001 |
