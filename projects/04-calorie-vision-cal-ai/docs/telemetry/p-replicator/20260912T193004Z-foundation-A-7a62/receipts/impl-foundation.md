# Квитанция: Phase 3 IMPLEMENT, фича `foundation`, плечо A

RUN_ID: `20260912T193004Z-foundation-A-7a62` · WORK_UNIT_ID: `impl-foundation` · дата: 2026-09-12.
Каталог работы: `projects/04-calorie-vision-cal-ai`. Ветка: `claude/install-npm-packages-n7l3m5`.
Push не выполнялся.

## Файлы

| Категория | Счёт |
|---|---|
| Исходники `apps/*` и `packages/*` (ts, tsx, sql, json, css, png) | 49 |
| Файлы тестов (`*.test.ts`, `*.test.tsx`) | 16 |
| Хелперы тестов (`tests/helpers/`) | 2 |
| Корневые конфигурации и скрипты (`package.json`, два `vitest.*`, `tsconfig*`, `eslint.config.js`, `.editorconfig`, `Caddyfile`, `scripts/check-env-wiring.sh`, `scripts/init-foundation-db.sh`) | 10 |
| Изменённые существующие | 6: `Dockerfile`, `docker-compose.yml`, `.env`, `.env.example`, `04_refinement.md`, `05_completion.md` |

`.env` не коммитился (в `.gitignore`); `node_modules`, `dist`, `.next` — тоже.
Секреты брались только из `.env`; в git уехали имена переменных и безопасные примеры.

## Тесты

| Слой | Файлов | Тестов | Результат |
|---|---|---|---|
| unit (без базы, `npm test`) | 4 | **29** | все зелёные |
| integration + конкурентные (`npm run test:integration` в профиле `test`) | 12 | **33** | все зелёные |
| **Итого** | 16 | **62** | все зелёные |

E2E на поднятом стенде (не vitest, прогон руками, все шаги пройдены): `docker compose --profile app
build` — три образа; `--profile edge up -d` — шесть сервисов, пять `healthy`; `GET /health` через
дверь Caddy по ВЫДАННОМУ адресу `http://127.0.0.1:4180` → `200 {"data":{"status":"ok","db":"ok"}}`;
`POST /api/v1/auth/device` через Caddy → `201` + `Set-Cookie: n4_session=…; HttpOnly; Secure;
SameSite=Lax; Path=/`; корневой маршрут → `200`, видоискатель и две подписи режимов, ОДИН заголовок
CSP с nonce; `docker compose stop db` → `GET /health` = `503 database_unavailable`, процесс `api`
ЖИВ, после возврата базы снова `200`; `docker compose --profile edge down` — **стенд не оставлен**.

Конкурентные прогоны (предмет фичи): 20 одновременных попыток при пределе 10 → ровно 10 `granted`,
`used = 10`, общий счётчик не увеличен отказавшими; 20 одновременных эскалаций при остатке 1 → ровно
один успех; соседняя сессия из другой сети не заблокирована, пик занятых соединений пула ≤ 10 и не
растёт с числом ожидающих; два воркера на одно задание → один захват; 10 воркеров на 3 задания → ни
одного двойного и ни одного потерянного; запись с устаревшим `fence` → ноль строк и
`stale_lease_result`.

## Ворота

| Ворота | Код | Комментарий |
|---|---|---|
| `npm test` | 0 | 29 тестов |
| `npm run test:integration` (профиль `test`) | 0 | 33 теста |
| `npm run lint` | 0 | — |
| `npm run build` | 0 | пять workspace |
| `npm run typecheck` | 0 | — |
| `node ../../.claude/hooks/check-ports.cjs .` | 0 | хранилища наружу не смотрят |
| `bash ../../scripts/check-port-conflicts.sh .` | 0 | прогон ДО `up`; при поднятом стенде честно даёт `1` — порт занят нами же |
| `bash scripts/check-env-wiring.sh .` | 0 | `api` и `recognizer` без потерь; `web` не читает ни одной переменной |
| `check-pipeline-gaps.sh . --completion …` | **2** | см. ниже: по контуру `foundation` — НОЛЬ расхождений |

**Про код `2` у ворот трассировки — прямо, потому что «ворота = 0» было бы неправдой.**
`GAP contour=foundation` — ноль строк; на копии, где оставлен только этот контур, вердикт
`features=1 gaps=0 inconclusive=1`. Код `2` возникает по двум причинам ВНЕ этой фичи:

1. **Дефект вендорного скрипта на контуре ПРОЕКТА.** `NOT-ESTABLISHED contour=project
   role=completion path=./docs/./docs/Completion.md missing or unreadable`: вызывающий код передаёт
   уже собранный `PROJECT_COMPLETION="$DOCS_ROOT/$PROJECT_COMPLETION"`, а `process_completion_contour`
   снова делает `"$directory/$completion_target"` — путь склеен дважды. Файл существует и читается.
   Контуры ФИЧ не задеты. Вендорные файлы не правятся (правило репозитория); кандидат в кузницу.
2. **Планы соседних фич без кода:** `scan-pipeline` (29 критериев) и `consent-and-telegram-auth`
   (20) написаны параллельно другими исполнителями, их тестов ещё не существует.

## Испытание стражей на внедрённом дефекте

Проверка, ни разу не показавшая красное, проверкой не является. Все девять прогонов выполнены.

| Страж | Дефект возвращён | Код восстановлен |
|---|---|---|
| атомарность квоты («прочитать, потом записать») | конкурентный 2 failed \| 1 passed, **последовательный остался ЗЕЛЁНЫМ** | 6 passed |
| аренда: из предиката убран `leased_until` | 2 failed \| 2 passed | 4 passed |
| fencing: запись без `AND lease_fence = $2` | 2 failed \| 2 passed | 4 passed |
| частота: хук `onRequest` → `preHandler` | 1 failed \| 3 passed (пять `400 invalid json` вместо `429`) | 4 passed |
| конфигурация: `?? 10` вместо отказа | 3 failed \| 13 passed | 16 passed |
| гигиена журнала: сырой токен cookie в вызове журналирования | 1 failed \| 4 passed | 6 passed |
| ADR-001: в схему ответа модели возвращено `calories` | 2 failed \| 3 passed | 6 passed |
| `check-env-wiring.sh`: у `api` убрана читаемая `S3_BUCKET` | код `1`, названы сервис и переменная | код `0` |
| `check-env-wiring.sh`: пустой вывод `docker compose config` | код `2`, «проверка НЕ ВЫПОЛНЕНА» | код `0` |

Строка про квоту — главная: последовательный тест ЗЕЛЁН при сломанной атомарности, и это
предъявленное доказательство того, что различает только конкурентный прогон.

## Дефекты, найденные сборкой и живым стендом (пять)

1. `Dockerfile` валил сборку всех трёх образов проверкой `sharp` на HEIF — `sharp` не зависимость
   этой фичи; проверка перенесена туда, где появится сама зависимость.
2. `npm run build --workspaces` идёт в порядке ОБЪЯВЛЕНИЯ: `apps/api` компилировался без
   `packages/shared/dist` — 18 ошибок «Cannot find module». Порядок объявлен явно.
3. Сеть `egress` не создавалась: «all predefined address pools have been fully subnetted» — на
   машине 31 чужая сеть. Подсеть задана явно через `${N4_EGRESS_SUBNET:-10.83.0.0/24}`.
4. `--profile edge` в одиночку не стартовал: `proxy` зависит от `api` и `web`.
5. **Остановка `db` на 10 секунд УБИВАЛА процесс `api`**: событие `error` простаивающего клиента
   `pg.Pool` никто не слушал. Добавлен `pool.on('error', …)` в оба загрузчика и страж по исходнику.

## Отклонения от плана

| Что | Почему |
|---|---|
| Роли создаёт `scripts/init-foundation-db.sh`, а не `001_init.sql` | роль — объект КЛАСТЕРА, её пароль не имеет права лежать в коммитимом SQL. `n4_migrate` без пароля и без права входа: раннер делает `SET ROLE` из-под `n4_admin` |
| Введены `N4_RATE_LIMIT_MUTATE_PER_MIN` и `N4_RATE_LIMIT_READ_PER_MIN` (30/120) | закрытие VF-02: у порога частоты не было ИСТОЧНИКА |
| Введена `N4_EGRESS_SUBNET` | дефект №3 выше; классифицируется как хостовая настройка машины |
| Имя `N4_MODEL_PROVIDER`, а не `N4_MODEL_MODE` | так в `docker-compose.yml`, `.env.example` и `03_architecture.md`; `N4_MODEL_MODE` есть только в тексте ранбука |
| Проба здоровья `api` — HTTP вместо TCP | предусмотрено `03_architecture.md`; TCP говорит «процесс слушает», HTTP — «база отвечает» |
| CSP ставит `apps/web/middleware.ts`, не `Caddyfile` | политика несёт nonce; второй заголовок браузер пересекает и ломает страницу молча |
| Воркер закрывает задание `refused(no_food_matched)` | сопоставления с базой в этой фиче нет, а `done` без ссылки на `food_item` нарушил бы ADR-001 |
| Тест AC-foundation-12 — `.tsx` | файл содержит JSX |
| Правки `04_refinement.md` и `05_completion.md` уехали в чужой коммит `a7a6ad5` | параллельный исполнитель `scan-pipeline` закоммитил рабочее дерево целиком. Содержимое на месте и проверено в `HEAD`; это ровно тот случай, против которого написано правило одного писателя на файл в общем worktree |

## Что НЕ сделано и почему

- Живого вызова модели нет: работает детерминированный фейк, ключа на машине нет (DEC-A-009).
  «Живое распознавание не выполнено: нет ключа» — состояние, а не пропуск.
- NFR-PERF-001 и NFR-PERF-002 НЕ измерены: измерять нечего, продукта на экране нет.
- Публичного адреса у стенда нет: только петля `127.0.0.1:4180`. Сквозной сценарий CJM E — за
  следующими фичами.
- Проверка «в клиентском бандле нет строк `ANTHROPIC_API_KEY`/`TELEGRAM_BOT_TOKEN`» выполнена по
  ИСХОДНИКАМ `apps/web`, а не по собранному бандлу; у `web` нет ни одной такой переменной ни в коде,
  ни в `environment:` сервиса.

## Коммиты (7, без push)

`5196429` каркас монорепо · `51fd6d3` shared и db · `4e488ab` api · `a0b1546` recognizer ·
`dfdbbae` web и Caddyfile · `d1f20cb` тесты · `707d694` страж проброса и правки compose/Dockerfile.
Трейлер `Co-Authored-By: Claude Fable 5.1` во всех.

requested: claude-opus-5; actual: unknown to worker

## Дополнение: правка контракта DEC-A-015 / DEC-A-018 (2026-09-12, после основной поставки)

Задание координатора: (1) порт `recognize(image, schema, opts: { model, deadlineMs })`, фейк
возвращает `model` и уважает `deadlineMs`; (2) предикат выборки `status='queued' AND photo_id IS NOT
NULL AND (leased_until IS NULL OR leased_until < now()) AND lease_fence < 3` плюс уборщик
(`queued` старше 5 минут без захвата, `lease_fence = 3` с истёкшей арендой → `failed('timeout')`).

**Сделано целиком.** Плюс ОДНО следствие, которого в задании не было, но без которого правка вносит
дефект: условная запись результата получила `AND status = 'queued'`. Появился ВТОРОЙ писатель
(уборщик), и без этого условия воркер затирал бы его терминальный статус, воскрешая задание задним
числом. Различение двух случаев «ноль строк» — по НОМЕРУ ЗАХВАТА, а не по статусу: номер больше
своего значит перезахват (`stale_lease_result`), номер равен своему при терминальном статусе значит
уборщик (`swept_as_timeout`), потому что уборщик единственный, кто закрывает строку, не увеличивая
номер.

**Схема:** миграция `002_failure_reason_timeout.sql` расширяет `failure_reason` до десяти значений
(DEC-A-018). Отдельным файлом, а не правкой `001`: применённый файл раннер обязан отвергать, и это
его собственное проверяемое свойство. Вместе с `timeout` добавлено `normalize` — набор из десяти
значений уже объявлен `Architecture.md` и `Pseudocode.md`, а схема, отстающая от документа, который
ею управляет, и есть «два места с одним правилом».

**Прогон после правки**

| Проверка | Код | Было → стало |
|---|---|---|
| `npm test` (unit) | 0 | 29 → **30** |
| `npm run test:integration` (профиль `test`) | 0 | 33 → **41** |
| `npm run typecheck` · `npm run lint` · `npm run build` | 0 | — |
| `bash scripts/check-env-wiring.sh .` | 0 | — |
| `node ../../.claude/hooks/check-ports.cjs .` · `check-port-conflicts.sh` | 0 · 0 | прогон ДО `up` |
| `docker compose --profile app build api recognizer` | 0 | образы пересобраны |
| стенд `--profile app up -d` | 0 | пять сервисов, воркер стартовал с уборщиком в цикле, за 40 с ни одного `tick_failed`; стенд снят |
| `check-pipeline-gaps.sh . --completion` | 2 | `GAP contour=foundation` — по-прежнему **ноль**; код 2 по тем же двум внешним причинам |

**Шесть новых испытаний стражей мутацией** (всего по фиче их стало пятнадцать)

| Страж | Дефект возвращён | Код восстановлен |
|---|---|---|
| незавершённая публикация: убран `photo_id IS NOT NULL` | 1 failed \| 9 passed | 10 passed |
| предел захватов: убран `lease_fence < 3` | 2 failed \| 8 passed | 10 passed |
| гонка с уборщиком: убран `AND status = 'queued'` | 1 failed \| 9 passed (`swept_as_timeout` не появился) | 10 passed |
| уборщик, правило Б: правило вырезано | 2 failed \| 8 passed | 10 passed |
| дедлайн: фейк игнорирует `deadlineMs` | 1 failed \| 5 passed | 6 passed |
| выбор модели: фейк зашивает свою | 1 failed \| 5 passed | 6 passed |

**Документы фичи:** `01` — пять новых критериев `AC-foundation-17…21`; `02` — переписанный предикат,
новый алгоритм `SweepStuckJobs`, новый контракт порта; `03` — размещение и таблица «было → стало»;
`04` — восемь краевых случаев и таблица мутаций; `05` — строки покрытия на новые критерии и раздел
о дополнении контракта.

**Отклонение, требующее внимания координатора:** `opts.model` типизирован union'ом
`'haiku-4.5' | 'sonnet-5'`, а не `string`, как в тексте задания. Это СУЖЕНИЕ: модели — числа канона
§6 (ADR-004), закрытые наборы живут в коде, и `string` принял бы опечатку в идентификаторе модели
как валидное значение. Расширение набора — правка одной строки в `MODEL_IDS`.

**Названный пробел:** пять новых критериев не имеют именованных сценариев в `validation-report.md`
(его владелец — валидатор Phase 2), поэтому ворота `--criterion-scenarios` на них не отвечают.
Это состояние, а не забытый пункт.

**Коммиты дополнения (3):** `070b45e` порт, предикат, уборщик и миграция · `d2381ef` восемь тестов и
шесть мутаций · `3e6e52b` документы фичи. Итого по фиче **11 коммитов**, push не выполнялся.

## Попытка 2 — корректирующая, по вердикту слепого судьи (2026-09-12)

Вердикт `CHANGES_REQUIRED`: четыре находки, три критерия `not met`, одиннадцать `unverifiable`
(судья стенд не поднимал — это граница проверенного им, а не дефекты).

| Находка | Правка | Тест | Мутация |
|---|---|---|---|
| RV-01 high — `request.url` в журнале: прогон `POST /unknown?token=…&ip=…` уводил токен и полный адрес | у неизвестного маршрута ПОСТОЯННЫЙ `route: 'unmatched'` + метод + статус; редактор журнала переведён на ЗАКРЫТЫЙ СПИСОК разрешённых полей (`SERVICE_LOG_FIELDS`) | `tests/integration/log-hygiene.test.ts` (3 утверждения: прогон ревью, поле вне списка, журнал реальной сессии); `device-session` ЗАХВАТЫВАЕТ журнал вместо `sink: () => {}` | вернуть `request.url` → HTTP-тест 1 failed \| 2 passed И страж 1 failed \| 6 passed; восстановлено 3 и 7 passed |
| RV-02 high — регэксп не видел `process.env.X` | точка перед `env` разрешена, добавлена форма `env['X']`; `myenv.FOO` не совпадает | `прямое чтение process.env распознаётся наравне с env.X` — настоящий каталог сервиса: без переменной `1`, с переменной `0` | вернуть прежний класс → 1 failed \| 2 passed; восстановлено 3 passed |
| RV-03 medium — манифест не по `/manifest.json` | переписывание в `next.config.mjs`, ссылка в разметке на адрес критерия | `tests/integration/web-manifest.test.ts` — HTTP к СОБРАННОМУ приложению: статус, тип, поля, отдача иконок, CSP и видоискатель | проверка сама стала HTTP-уровневой |
| RV-04 medium — нет ограничения частоты на двери | `proxy/Dockerfile`: `xcaddy` + `caddy-ratelimit@v0.1.0`, базовые образы по дайджесту, сборка ПРОВЕРЯЕТ модуль в бинаре; `rate_limit` по `{remote_host}`, порог из `N4_PROXY_RATE_PER_MIN` без умолчания | стенд: 130 запросов за минуту с одного адреса → 119 × `200`, затем `429` начиная со 120-го; тело отказа ПУСТОЕ (отказ от Caddy, не от приложения) | автоматического теста нет — назван как незакрытый |

**Дополнение контракта той же попытки:** `signal` стал ОБЯЗАТЕЛЬНЫМ полем
`ModelProvider.recognize`. Фейк прерывается по нему НЕМЕДЛЕННО (не досиживает свой таймер) и
отклоняет промис ошибкой с `name: 'AbortError'` — отдельной от `ModelDeadlineExceeded`: отмена и
исчерпание бюджета разные события, слить их значило бы потерять причину. Воркер создаёт один
`AbortController` на вызов и гасит его таймером дедлайна.

**Прогон попытки 2**

| Проверка | Код | Результат |
|---|---|---|
| `npm test` (unit) | 0 | **30** |
| `npm run test:integration` | 0 | **48** (было 41) |
| `npm run lint` · `typecheck` · `build` | 0 · 0 · 0 | — |
| `bash scripts/check-env-wiring.sh .` | 0 | обе формы чтения распознаются |
| `check-pipeline-gaps.sh . --completion` | 2 | `GAP contour=foundation` — ноль; код 2 по прежним внешним причинам |
| сборка двери + `api`/`web`, стенд `--profile edge up -d` | 0 | шесть сервисов, пять `healthy` |
| `GET /manifest.json` через дверь | 200 | `application/manifest+json` |
| 130 запросов за минуту через дверь | 429 с 120-го | отказ Caddy, тело пустое |
| `docker compose down` | 0 | свой стенд снят; чужой стек `n4-tarelka-consent-b-*` не тронут |

**Помеха, названная как есть:** на машине кончилось место (0 байт, 158 ГБ занято) — Postgres не смог
записать `postmaster.pid`, и прогон встал. Освобождено ТОЛЬКО своё: кэш сборки и образ-сборщик
Caddy, который после сборки не нужен. Чужие образы, тома и каталоги не тронуты: 28 ГБ в `/tmp`
принадлежат другим сессиям, и это не моё решение.

**Осталось незакрытым:** дверной лимит проверяется стендом, автоматического теста на него нет (нужен
поднятый стек — слой E2E, которого у фичи пока нет); пять критериев `AC-foundation-17…21`
по-прежнему без именованных сценариев в `validation-report.md` (артефакт валидатора Phase 2).

**Коммиты попытки 2 (3):** `cf75511` четыре правки и контракт сигнала · `a366a28` восемь тестов и три
мутации · `3a57515` документы. Итого по фиче **15 коммитов**, push не выполнялся.

## Попытка 3 (RV-01…04, уточнения) — 2026-09-12

Четыре находки судьи были закрыты попыткой 2. Здесь закрыты УТОЧНЕНИЯ координатора к ним и две
находки трассировки, которых в ревью не было.

| Находка | Правка | Тест | Мутация |
|---|---|---|---|
| RV-01, состав события | добавлены `request_id` (генерируется ПЕРВЫМ хуком, раньше ограничителя частоты — чтобы `429` тоже находился по ручке), `path` БЕЗ query, `duration_ms`. Усечение запросной строки — ОТДЕЛЬНОЙ строкой кода, чтобы страж «`request.url` не попадает в вызов журналирования» остался действующим | `tests/integration/log-hygiene.test.ts`: `path` равен `/unknown`, идентификатор непустой, длительность числом | страж сработал на собственном авторе: `path` сначала не был внесён в закрытый список и приехал `[redacted]` — то самое поведение, ради которого список вводился |
| RV-02, испытание | регэксп уже исправлен попыткой 2; здесь добавлено испытание НА НАСТОЯЩЕМ файле | — | `S3_BUCKET` убран из `environment` сервиса `api` в настоящем `docker-compose.yml` → **код 1** с названными сервисом и переменной; восстановлено → **0**; пустой конфиг → **2** |
| RV-03, путь манифеста | закрыт попыткой 2 (переписывание + HTTP-тест) | `tests/integration/web-manifest.test.ts` | — |
| RV-04, пороги двери | ДВЕ зоны вместо одной: `@mutations` (не `GET`/`HEAD`/`OPTIONS`) — 30/мин, `@reads` — 120/мин, обе по `{remote_host}`, оба числа из окружения без умолчаний | стенд | — |
| `DUPLICATE foundation pseudocode FR-foundation-6` | уборщик получил СВОЙ ключ `FR-foundation-11`, объявленный в `01`; `LeaseRecognitionJob` остался за `FR-foundation-6` | ворота трассировки | — |
| VF-03 (low) | проза `FR-foundation-6` переписана под DEC-A-015/018: три условия предиката с объяснением каждого, различение двух нулевых записей по номеру захвата, порт с `model`/`deadlineMs`/`signal` | ворота трассировки | — |

**Доказательство обеих зон двери (стенд, 2026-09-12)**

| Прогон через `http://127.0.0.1:4180` | Результат |
|---|---|
| 125 чтений `GET /manifest.json` за минуту с одного адреса | **120 × `200`, затем `429` начиная со 121-го** |
| 35 мутаций `POST /api/v1/auth/device` за минуту с того же адреса | **30 × `201`, затем `429` начиная с 31-го** |
| тело отказа | ПУСТОЕ — отказ от Caddy, не от приложения |
| `GET /health` при исчерпанном окне чтения | `429` — зона чтения общая для читающих путей двери |

Зоны НЕЗАВИСИМЫ: исчерпанное окно чтения не помешало мутациям дойти до своих тридцати. Это и есть
разница между «два порога» и «один порог, названный двумя числами».

**Ворота попытки 3**

| Проверка | Код | Результат |
|---|---|---|
| `npm test` · `npm run test:integration` | 0 · 0 | **30** · **48** |
| `npm run lint` · `npm run typecheck` · `npm run build` | 0 · 0 · 0 | — |
| `bash scripts/check-env-wiring.sh .` | 0 | плюс `1`/`0`/`2` на трёх мутациях |
| `node ../../.claude/hooks/check-ports.cjs .` | 0 | — |
| `bash ../../scripts/check-port-conflicts.sh .` | 0 | прогон ДО `up` |
| `check-pipeline-gaps.sh --traceability` | 0 | **`VERDICT traceability=PASS features=4 gaps=0 inconclusive=0`** — `DUPLICATE` исчез |
| `check-pipeline-gaps.sh --completion` | 2 | `GAP contour=foundation` — **ноль**; код 2 по прежним внешним причинам |
| `docker compose build proxy` | 0 | образ двери собран, наличие модуля проверено НА СБОРКЕ |
| стенд `--profile edge up -d` → проверки → `down` | 0 | свой стенд снят; чужой стек `n4-tarelka-consent-b-*` не тронут |

**Осталось незакрытым (без изменений):** автоматического теста на дверные пороги нет — ему нужен
поднятый стек, то есть слой E2E, которого у фичи пока нет; проверено стендом и названо прямо.
Сценарии для `AC-foundation-17…21` пишет `validator-foundation`, я их не трогал.

**Коммиты попытки 3 (3):** `1bf6c99` ручка запроса и две зоны двери · `b60e1d3` утверждения на состав
события · `9ae9f1c` `FR-foundation-11`, проза `FR-foundation-6` и разделы документов.
Итого по фиче **19 коммитов**, push не выполнялся.

## Попытка 4 (RV-01, RV-02 переревью) — 2026-09-12

Судья на переревью: `CHANGES_REQUIRED`, две находки в моей области (третья ушла валидатору).

| Находка | Правка | Тест | Мутация |
|---|---|---|---|
| RV-01 high — отсечения query недостаточно: СЕГМЕНТЫ пути пишет тот же клиент, и судья воспроизвёл запись токена и адреса запросом `/unknown/<токен>/<адрес>` через настоящий `buildServer` | путь НЕ логируется вовсе: в журнал уходит только ШАБЛОН зарегистрированного маршрута, у неизвестного — метка `unmatched`. Поле `path` убрано из события и из закрытого списка полей. Добавлено событие `request_not_found`: всплеск `404` — сигнал, и молчать о нём значит остаться без него | `чувствительные значения в СЕГМЕНТАХ пути не попадают в журнал`; `известный маршрут пишется ШАБЛОНОМ, а не строкой запроса` | обход ЧЕРЕЗ ПРОМЕЖУТОЧНУЮ ПЕРЕМЕННУЮ → страж **1 failed \| 6 passed**; восстановлено **7 passed** |
| RV-02 medium — тест брал рабочую константу и для числа захватов, и для ожидания: смена 3→4 сдвинула бы тест ВМЕСТЕ с кодом | в сценариях ЛИТЕРАЛЫ спецификации (`[1, 2, 3]`, ожидаемый `lease_fence = 3`), четвёртый захват — отдельное утверждение, рабочая константа сверяется со спецификацией в ЕДИНСТВЕННОМ месте | `рабочая константа равна трём — числу из FR-foundation-6` плюс переписанные сценарии предела и уборщика | рабочий предел `3 → 4` → **4 failed \| 7 passed** (первым красным «expected 4 to be 3»); восстановлено **11 passed** |

**Почему страж пропустил дефект и что изменено.** Он читал вызов журналирования и запрещал в нём
`request.url`. Обход занимал ОДНУ СТРОКУ выше вызова — и ровно так дефект вернулся во второй раз.
Теперь страж собирает имена переменных, ПРОИЗВЕДЁННЫХ от строки запроса (включая цепочки
присваиваний), и запрещает их в вызовах журналирования. Страж, ловящий только прямую форму, учит
писать непрямую.

**Ворота попытки 4**

| Проверка | Код | Результат |
|---|---|---|
| `npm test` · `npm run test:integration` | 0 · 0 | **30** · **51** (было 48) |
| `npm run lint` · `npm run typecheck` · `npm run build` | 0 · 0 · 0 | — |
| `node ../../.claude/hooks/check-ports.cjs .` | 0 | — |
| `bash scripts/check-env-wiring.sh .` | 0 | — |
| `check-pipeline-gaps.sh --traceability` | 0 | `VERDICT traceability=PASS features=4 gaps=0 inconclusive=0` |
| `check-pipeline-gaps.sh --completion` | 2 | `GAP contour=foundation` — **ноль**; код 2 по прежним внешним причинам |

**Стенд не поднимался — по указанию координатора** (на диске 1,4–1,7 ГБ). Дверные пороги проверены
прогоном попытки 3 и с тех пор не менялись; выдавать тот прогон за сегодняшний было бы подлогом,
поэтому он назван своей датой.

**Коммиты попытки 4 (3):** `49df24a` шаблон маршрута вместо пути и событие `request_not_found` ·
`63b370b` страж обхода через переменную и литералы спецификации в тестах предела · `eeabcd9`
документы. Итого по фиче **23 коммита**, push не выполнялся.

Status: completed
