# Оплата · Валидация плана

Вердикт: **🟢 READY с одной находкой, внесённой в план.**

Проверено парами (метод PR-033): ключ идемпотентности ↔ канон Architecture-DATA ✅ ·
PK/уникальные ограничения ↔ схема 004 ✅ · RLS-модель вебхука ↔ 007 (subscriptions под RLS,
checkout_sessions намеренно нет) ✅ · канал инвалидации ↔ код гостя ✅ · цена вне клиента ✅.

НАХОДКА: истечение подписки живёт в notifier, а канал инвалидации кэша был только у web.
Без него AC-6 нарушался бы НЕВИДИМО (бренд возвращается с опозданием до TTL). Внесено в
03_architecture: notifier получает GUEST_INTERNAL_URL.

Критерии измеримы (AC-1…AC-8 — каждый с проверяемым исходом), мутации назначены каждому
несущему свойству, «чего тесты не проверяют» названо честно. Блокеров нет.

Открытые решения владельца: DEC-PAY-1 (цена), DEC-PAY-2 (магазин на пилот) — блокируют
выкатку, не реализацию.
