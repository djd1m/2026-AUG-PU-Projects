# Профиль исходного продукта

**Исходный продукт:** NiceJob (nicejob.com) — reputation marketing для локального бизнеса
**Статус съёмки:** СНЯТ
**Статус съёмки (путь):** СНЯТ
**Происхождение:** прокликано
**Дата съёмки:** 2026-08-30

Съёмка выполнена браузерным прогоном: `getComputedStyle` по живым страницам, 85 переменных,
плюс 29 снимков экрана и проход пути гостя и владельца. Полные измерения —
[`discovery/01a-design-tokens.md`](discovery/01a-design-tokens.md) (облик) и
[`discovery/01-original-walkthrough.md`](discovery/01-original-walkthrough.md) (путь).

Из трёх кандидатов (NiceJob, Birdeye, Podium) снят NiceJob: у него единственного дизайн-система
выгружается машинно, то есть облик воспроизводится **копированием измеренных значений**, а не
подбором по снимку экрана. Birdeye — стоковый Bootstrap, Podium ушёл в «AI Employee» и перестал
быть тем же продуктом.

## Что снято

Захватываются **регулярности** — шаг сетки, шкала кегля, число уровней иерархии, порядок
экранов. Не захватываются имена классов (сборщик меняет их при каждой сборке чужого сайта) и
чужие товарные знаки. Логотип, название и фирменные цвета вместе — это знак, а не измерение.

| ID | Требование | Ось | Измерено | Наш ответ | Статус |
|---|---|---|---|---|---|
| FR-LOOK-001 | Первичное действие — сплошная заливка одного синего, без градиента | облик | `#025bde`, все `button--primary` и `--secondary` | тот же токен `--brand` | ЧЕРНОВИК |
| FR-LOOK-002 | Заголовки темнее основного текста на один шаг, не чёрные | облик | h2/h3 `#00132e` при тексте `#364459` | `--ink` / `--text` | ЧЕРНОВИК |
| FR-LOOK-003 | Фон светлых секций — не белый, а холодный оттенок | облик | `#f5f9ff`, второй по площади | `--tint`, фон гостевой страницы | ЧЕРНОВИК |
| FR-LOOK-004 | Карточка: радиус 16px, рамка 1px, без собственной тени | облик | `rounded-2xl`, `1px solid #c1c1c1` | `--r-card: 16px`, `--line` | ЧЕРНОВИК |
| FR-LOOK-005 | Кнопки — полная пилюля у ВСЕХ типов без исключения | облик | радиус 100px, высота ~50px | `--r-pill: 100px` | ЧЕРНОВИК |
| FR-LOOK-006 | Тень в системе ровно одна, синеватая и слабая | облик | `1px 1px 19px 0 #1874fd26` | `--shadow`, тот же вид | ЧЕРНОВИК |
| FR-LOOK-007 | Гарнитура заголовков отличается от гарнитуры текста | облик | Bogart (serif) против Inter | ОТКЛОНЕНО — системный стек в обеих ролях | ЧЕРНОВИК |
| FR-LOOK-008 | Звёзды рейтинга жёлтые, 156 узлов на странице | облик | `#ffcc00` | ОТКЛОНЕНО — оценок в продукте нет вовсе | ЧЕРНОВИК |
| FR-LOOK-009 | Выбор площадки открывается модальным окном | путь | модалка поверх страницы | ОТКЛОНЕНО — страница без скриптов | ЧЕРНОВИК |
| FR-LOOK-010 | Часть площадок скрыта за «Other options» | путь | сворачивающийся список | ОТКЛОНЕНО — видимых дверей ровно столько, сколько настроено | ЧЕРНОВИК |
| FR-LOOK-011 | Гостевой экран — один вопрос на экран, без формы-простыни | путь | один шаг, крупная цель нажатия | перенесено: выбор двери и форма разнесены | ЧЕРНОВИК |
| FR-LOOK-012 | Гостевой экран не грузит веб-шрифт | путь | Roboto (системный), хотя сайт на Bogart/Inter | перенесено: системный стек | ЧЕРНОВИК |

## Смена источника облика для ВЛАДЕЛЬЧЕСКИХ поверхностей (решение владельца, 02.09.2026)

**Что произошло.** Снятые выше строки FR-LOOK-001…006 сняты с NiceJob, и причина выбора записана
в этом же документе дословно: «у него единственного дизайн-система выгружается машинно». То есть
источник выбран по **удобству измерения**, а не по тому, что копируем. Владелец увидел готовый
интерфейс, прислал снимки Birdeye и назвал это прямо: на фазе прокликивания снимать надо сайт
оригинала, а не аналог из продуктового ресерча.

**Отказ коварен тем, что все ворота были зелёные.** `check-look-trace` проверяет, что снятое
доехало до `Specification.md`, и ничего не знает о том, ТО ЛИ снимали. Съёмка не того сайта
проходит все проверки происхождения: статусы, даты и происхождение на месте, документ выглядит
честным. Обнаружить подмену мог только человек, посмотревший на результат.

**Граница решения.** Меняется облик **владельческих** поверхностей — кабинет и публичная
страница продукта. **Гостевая поверхность НЕ меняется**: её облик связан инвариантами —
побайтовая чистота ответа (T4) и запрет веб-шрифта (ADR-010), — и переодевание сломало бы ровно
то, чем доказывается её чистота. Поэтому FR-LOOK-001…004, 011, 012 остаются в силе для гостя;
FR-LOOK-005 и FR-LOOK-006 отменяются для владельческих поверхностей строками 017 и 020 ниже.

**Происхождение новых строк:** `прокликано` · **Источник:** birdeye.com · **Дата съёмки:**
2026-08-30 (снимки экрана лежат в `discovery/screenshots/birdeye-*.png`, сняты тем же прогоном).
Измерение по снимку слабее браузерного `getComputedStyle`: снимаются **пропорции и структура**,
не точные значения. Это записано, а не замаскировано.

| ID | Требование | Ось | Измерено по снимку | Наш ответ | Статус |
|---|---|---|---|---|---|
| FR-LOOK-013 | Страница — чередование полос во всю ширину: белая, светло-серая; содержимое в общей колонке | облик | 5 полос, колонка ≈1120px | `.band`, `.band--tint`, `--wrap:1120px` | ЧЕРНОВИК |
| FR-LOOK-014 | Первый экран — две колонки: текст слева, наглядное справа, радиус наглядного заметно больше карточного | облик | ≈50/50, радиус ≈24px против ≈16px у карточки | `.split`, `--r-media:24px` | ЧЕРНОВИК |
| FR-LOOK-015 | Заголовок первого экрана крупный, выключка тесная, часть фразы выделена цветом акцента | облик | ≈54px при интерлиньяже ≈1.06; выделено «every location» | `.h-hero`, `em` цветом `--brand` | ЧЕРНОВИК |
| FR-LOOK-016 | Пара действий рядом: сплошная заливка и такая же по размеру в рамке на белом | облик | две кнопки одной высоты, вторая с рамкой | `.btn` и `.btn--ghost` в `.row` | ЧЕРНОВИК |
| FR-LOOK-017 | Элемент управления — прямоугольник с МАЛЫМ радиусом, не пилюля | облик | радиус ≈8px при высоте ≈48px | `--r:10px`; **ОТМЕНЯЕТ FR-LOOK-005** для владельческих поверхностей | ЧЕРНОВИК |
| FR-LOOK-018 | Перечень выгод — строка с круглой отметкой, а не маркером списка | облик | зелёный круг с галочкой, ≈20px | `.checks li::before` | ЧЕРНОВИК |
| FR-LOOK-019 | Полоса чисел: крупное число приглушённым, подпись жирная, волосяная линия, пояснение мелким | облик | 3 колонки, число ≈56px, линия 1px | `.stats`, `.stat` | ЧЕРНОВИК |
| FR-LOOK-020 | Карточка — тонкая рамка и крупный радиус, БЕЗ тени | облик | рамка 1px, тени нет ни у одной карточки | `.card` без `box-shadow`; **ОТМЕНЯЕТ FR-LOOK-006** для владельческих поверхностей | ЧЕРНОВИК |

**Чего эти строки НЕ переносят.** Ни фирменный синий образца, ни его знак и название: цвет
вместе со знаком и именем — товарный знак, а измерение не лицензия. Акцент остался нашим
токеном `--brand`. Ни числа из полосы образца («128% больше отзывов», «4.8 рейтинг»): это чужие
результаты, и скопировать их вместе с формой значило бы выдать чужое измерение за своё. В нашей
полосе стоят замер доставки со стенда, свойство продукта и цена из DEC-PAY-1.

### Почему статус строк — ЧЕРНОВИК, а не ПОДТВЕРЖДЕНО

`ПОДТВЕРЖДЕНО` в этой разметке означает **две независимые съёмки, которые сошлись**: наш живой
прогон и сторонний разбор с датой. У нас съёмка одна — собственная, браузерная, 2026-08-30.
Она сильнее пересказа, но вторым источником сама себе не является, и ставить статус, которого
не заслужили, значило бы завысить уверенность ровно в том документе, который заведён ради
честности происхождения.

Практическое следствие: если облик NiceJob изменится, у нас нет ничего, что об этом сообщит.
Строки стареют молча, и повторная съёмка перед следующей работой над интерфейсом — не
формальность.

## Четыре отклонения — каждое с причиной, не по вкусу

**FR-LOOK-007, гарнитуры.** Веб-шрифт — это запрос к чужому хосту с телефона гостя в заведении
со слабой связью, и он же вернул бы изменчивость в тело ответа, сломав побайтовое равенство,
которым доказывается чистота гостевой страницы (T4). Bogart вдобавок платный и без гарантий
кириллицы. Показательно, что сам NiceJob поступает так же: его **гостевой** экран живёт на
системном Roboto — это и записано строкой FR-LOOK-012.

**FR-LOOK-008, звёзды.** Оценка — это и есть механизм сортировки по тональности, запрещённый
владельцем как несущее требование. Цвет тут ни при чём: отклонён сам элемент.

**FR-LOOK-009 и FR-LOOK-010, модалка и свёрнутый список.** Модалка требует скриптов, которых на
гостевой странице нет намеренно. Свёрнутый список — записанный анти-паттерн: число видимых
дверей обязано равняться числу настроенных, иначе «двойная дверь» перестаёт быть двойной
незаметно для владельца.

## Чего съёмка НЕ даёт

Она отвечает на вопрос «как выглядит и в каком порядке идёт исходный продукт», и только.
Ни его выручка, ни удержание, ни причины его решений из неё не следуют: совпадение облика не
переносит результат. Это ограничение метода, а не пробел данных.
