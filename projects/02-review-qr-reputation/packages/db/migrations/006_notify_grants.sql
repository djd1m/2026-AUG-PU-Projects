-- 006_notify_grants.sql — права роли доставки, пропущенные в исходной матрице.
--
-- ТРЕТЬЕ ПОВТОРЕНИЕ ОДНОГО КЛАССА за один прогон, и стоит назвать его прямо.
-- Матрица прав выписывалась по таблицам, ВОКРУГ КОТОРЫХ ШЁЛ СПОР: гейтинг, счётчики,
-- аналитика. Таблицы, о которых спора не было, в неё не попали — у app_render не хватило
-- archived_at, у app_intake того же, у app_notify не хватает places и channel_bindings.
--
-- Внимание идёт за конфликтом, и там, где конфликта не было, его не хватило. Лечится не
-- «не забыть», а правилом: у КАЖДОЙ роли выписаны права на КАЖДУЮ таблицу, которую
-- трогает её код. Страж check-db-grants.sh расширен соответственно.
--
-- Отдельным файлом, а не правкой 005: та применена, а раннер запрещает менять
-- применённое. Запрет сработал ровно там, ради чего написан.

-- Имя точки нужно в заголовке сообщения владельцу. Только name и id — адрес, тариф и
-- признак брендинга доставке не нужны и не выдаются.
GRANT SELECT (id, name) ON places TO app_notify;

-- Куда слать: привязка мессенджера. bind_token_hash НЕ выдаётся — он нужен только
-- владельческому тракту при подключении канала.
GRANT SELECT (place_id, channel, chat_id, bound_at) ON channel_bindings TO app_notify;
