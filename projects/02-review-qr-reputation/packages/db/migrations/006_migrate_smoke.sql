-- Проверочная миграция раннера. Ничего не меняет в продукте: создаёт и удаляет таблицу.
-- Существует затем, чтобы «раннер применяет новое» было проверено, а не заявлено.
CREATE TABLE _migrate_smoke (id int);
DROP TABLE _migrate_smoke;
