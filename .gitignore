# Ruflo local secrets and runtime data
.env
.env.local
.env.*.local
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/

# Runtime databases / local state (added manually)
ruvector.db
ruvector.db-*
.swarm/
*.db
*.db-wal
*.db-shm
node_modules/

# Временные файлы агентов
*.tmp
*.tmp[0-9]
*.bak

# Разбор инцидента 2026-08-26 и связанные материалы. НЕ КОММИТИТЬ.
# Репозиторий публичный. Внутри: публичные адреса машины и чужих проектов, карта
# опубликованных наружу портов, признаки учётных данных, и — главное —
# 23 адреса выходных узлов VPN владельца за 34 дня, то есть профиль перемещений.
# Каталог создан осознанно как локальный: материалы нужны, публикация их — нет.
docs/security-local/
