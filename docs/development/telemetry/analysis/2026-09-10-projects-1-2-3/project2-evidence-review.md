# Аудит evidence проекта 02

- `RUN_ID`: `telemetry-analysis-20260910`
- `WORK_UNIT_ID`: `p2`
- Режим: read-only вклад проекта 02 в межпроектное сравнение; runtime-тесты, сеть и изменение проекта не выполнялись.
- Область: `projects/02-review-qr-reputation`; `harness-forge` использован только как исторический контекст, явно связанный с ReviewQR.
- Снимок: `2026-09-10T12:53:14Z`; репозиторий `d491ff4b4ccfadccde52e8b23076b201ccc6474d`; релевантное дерево проекта 02 чистое.

## Полнота данных

Структурированная телеметрия проекта 02 — **`not_found`**. В проекте нет `docs/telemetry`, `run.json`, `events.jsonl`, `rates.json`, telemetry/usage/model-паспортов или отдельных evidence/receipt-файлов. Из альтернативных сериализованных форматов найдены только package/TypeScript JSON-файлы, не телеметрия. Поэтому анализатор `analyze.py` не запускался: передавать ему несуществующий каталог означало бы лишь подтвердить ошибку входа. `not_found` означает пробел измерений, а не отсутствие работы, повторов или расхода. Project-level insights также не найдены; заявки `harness-forge` — исторические постфактум-разборы, а не независимые новые прогоны проекта 02.

## Evidence-linked выводы

1. **Наблюдение — главный зафиксированный bottleneck Phase 0–1: параллельная работа над зависимыми документами без заранее закреплённого канона.** Исторический каталог сообщает 12 расхождений, 4 раунда сверки, 4 пересекающихся сообщения по одному гранту, 3 отчёта по устаревшему чтению, 2 ложных результата grep и один файл с двумя писателями (`harness-forge/RAKES-2026-09-01-coordinator.md:6-8`). Причина названа там же: три параллельных исполнителя работали над взаимозависимыми Specification/Architecture/Pseudocode, а канон имён, enum и чисел до старта не фиксировался (`:12-26`); состояние передавалось сообщениями без единого момента истинности (`:30-41`). Это конкретное свидетельство переделки, но не тайминг: `sha256 31b99035a168ea796080cca5a8e1077888903417f1d7b4215d91a689632b1d87`.

2. **Наблюдение — проверки сами создавали повторные итерации и ложные диагнозы.** Общая валидация зафиксировала третье за день ложное красное из-за проверки формы записи вместо смысла и рекомендовала подтверждать совпадение чтением, а не только exit code (`projects/02-review-qr-reputation/docs/validation-report.md:60-71`; `sha256 99c1c1b9cfca13955af26ae045a10a9a24a842c25d1646b5f11679ca288b8c86`). В оплате мутация не применилась из-за несовпавшего `str.replace`, затем восстановление оставило три мутации и затёрло соседние незакоммиченные изменения; разбор занял три итерации (`harness-forge/PR-035-mutation-may-not-land.md:23-53`; `sha256 c87a8943b354e030cc7989ec0d57296ad124fffd66d2be3bb3c595b8807f79ea`). Причина bottleneck — отсутствие проверки, что вход мета-теста действительно изменён, плюс небезопасный способ восстановления.

3. **Вычисленный verdict — завершение оплаты подтверждено только с ограничениями и слабо связано с версией.** Completion заявляет миграцию, 90 зелёных тестов, шесть красных мутаций и живой тестовый платёж с проверкой страницы (`projects/02-review-qr-reputation/docs/features/payment/05_completion.md:3-9`), но прямо оставляет непроверенным ручной повтор вебхука и нерешённым боевой магазин (`:10-14`). В каталоге нет сырого test log/receipt, request/build/image ID или SHA проверенной версии; сам файл вошёл в commit `d58f5cb…` в `2026-09-02T08:11:53Z`, а критичная IPv6-починка вебхука появилась позже в `dfb6497…` в `09:00:59Z`. Поэтому корректная классификация — `completed_with_scope_limits` / историческое self-report evidence, не безусловно воспроизводимая приёмка текущего HEAD. Хеш completion: `014371914a0f66d3b1ac80f630ec66d49c62cd943b3271e011ce3b9d8a49d399`.

4. **Наблюдение — `client-account-handover` нельзя учитывать как завершённую фичу.** Все пункты DoD, включая миграцию, тесты, прямой RLS-запрос и живой круг, остаются `[ ]` (`projects/02-review-qr-reputation/docs/features/client-account-handover/05_completion.md:3-10`; `sha256 0717dd0f80942c307bbe02c617f4815bb698d8885a009fc539a39c5c5037a7e8`). Git-история прямо говорит «план готов, реализация не запущена» (`ae38e454…`, `2026-09-02T09:25:57Z`). Более того, архитектурный план резервирует «Миграцию 012» для handover (`03_architecture.md:31-53`), а существующая `012_bind_token_burn.sql:1-24` реализует другую фичу; поиск runtime-кода не нашёл `handover_invitations`/`attach_child`. Это доказательство незавершённости и устаревания плана, а не дефект уже реализованного handover.

5. **Наблюдение — зелёные ворота не обеспечили соответствие исходному продукту и живому пути.** `PR-038` сообщает, что все проверки прошли, хотя дизайн снимался с удобного для измерения NiceJob вместо названного оригинала Birdeye; несоответствие обнаружил человек уже на готовом продукте, после чего потребовался отдельный пересмотр профиля, спецификации и системы стилей (`harness-forge/PR-038-capture-picks-the-measurable-not-the-named-source.md:21-42,85-90`; `sha256 bdb08cb7007d283949e9bf0cd7efa9dbbb6e8301cf43069aebb6f70d2c92a834`). Отдельный поздний живой дефект виден в миграции `012_bind_token_burn.sql:3-19`: открытие страницы уведомлений уничтожало рабочую Telegram-привязку, и отказ проявлялся только после гостевого сообщения. Эти два escape показывают, почему заявленное завершение документа/теста нельзя приравнивать к надёжности полного пользовательского пути.

## Ограничения сравнения и прогноза

- `elapsed_wall_ms`, active time и интервалы ожидания отсутствуют. Commit timestamps показывают только историю фиксации изменений; их нельзя считать длительностью работы или критическим путём. «20 минут» в `docs/demo-script.md:3-4` — длительность сценария демонстрации, не pipeline run.
- Requested/actual model, effort, fallback и attempt IDs отсутствуют. Упоминания «роя» или числа агентов не подтверждают фактическую модель исполнения.
- Usage и billing samples отсутствуют; токены и стоимость — `unavailable`, не ноль. Полный расход координатора, неуспешных попыток и повторов неизвестен.
- Численный forecast для сопоставимой будущей задачи недопустим: нет измеренного baseline, scope-normalized acceptance и непересекающегося usage. Допустим лишь качественный риск: зависимые документы без канона и проверки по литеральному тексту повышают вероятность повторных раундов; live-provider/UI путь следует измерять отдельно от unit/mutation gates.
- Для межпроектного вывода проект 02 можно использовать как качественный case study причин переделки и escape-дефектов, но не как числовую A/B-точку по скорости, стоимости или эффективности моделей.

Status: completed
